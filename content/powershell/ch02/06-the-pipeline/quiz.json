{
  "lesson": "the-pipeline",
  "questions": [
    {
      "q": "What does the pipe operator | actually pass from the command on its left to the command on its right?",
      "options": [
        "The full object output, with all its properties — not just the text visible on screen",
        "Only the plain text that would print to the console",
        "Nothing — | is purely a visual separator with no functional effect",
        "A screenshot of the previous command's output"
      ],
      "answer": 0,
      "explain": "This is the concrete delivery of Lesson 1's claim: PowerShell's pipeline passes real objects, not text."
    },
    {
      "q": "Why does Get-Process | Sort-Object CPU work correctly?",
      "options": [
        "CPU is a real property on the object being piped, not a column of text Sort-Object has to parse",
        "Sort-Object secretly re-runs Get-Process internally",
        "CPU is a special reserved keyword unrelated to the process object",
        "It doesn't actually work — Sort-Object cannot sort piped objects"
      ],
      "answer": 0,
      "explain": "Because full objects flow through the pipe, Sort-Object can sort directly on the real CPU property."
    },
    {
      "q": "In Where-Object {$_.CPU -gt 100}, what does $_ refer to?",
      "options": [
        "The current object flowing through the pipeline at that point, one at a time",
        "A fixed global variable that never changes value",
        "The name of the cmdlet being called",
        "The total count of objects in the pipeline"
      ],
      "answer": 0,
      "explain": "$_ represents each object in turn as Where-Object evaluates the condition against it."
    },
    {
      "q": "When a pipeline filters objects with Where-Object, what happens to the properties on the objects that pass through?",
      "options": [
        "They're untouched — filtering narrows which objects continue, it doesn't strip properties",
        "All properties except the one being filtered on are deleted",
        "The objects are converted to plain text before continuing",
        "Every property is renamed automatically"
      ],
      "answer": 0,
      "explain": "Filtering only narrows which objects continue down the pipeline; each surviving object keeps its full set of properties."
    },
    {
      "q": "What is the recommended way to read a long, multi-stage pipeline?",
      "options": [
        "Left to right, one stage at a time, asking what each stage receives and what it hands forward",
        "Right to left, since PowerShell evaluates pipelines backwards",
        "All at once, without breaking it into stages",
        "It cannot be read without running it first"
      ],
      "answer": 0,
      "explain": "Reading left to right, stage by stage, turns even a long pipeline into a simple sequence of ordinary filters."
    }
  ]
}
