{
  "lesson": "networking-basics",
  "questions": [
    {
      "q": "What is a VPC?",
      "options": [
        "An isolated, private network you define inside an AWS Region, with its own address space",
        "A single physical server dedicated to one AWS customer",
        "A synonym for an Availability Zone",
        "A billing construct with no actual networking function"
      ],
      "answer": 0,
      "explain": "A VPC is the isolated private network AWS customers define inside a Region — the foundation almost everything else runs inside."
    },
    {
      "q": "How does a subnet relate to a VPC and to Availability Zones?",
      "options": [
        "A subnet is a slice of the VPC's address range, and each subnet lives in exactly one Availability Zone",
        "A subnet spans every Availability Zone in the Region simultaneously",
        "A VPC can only ever contain exactly one subnet",
        "Subnets and VPCs are two names for the same thing"
      ],
      "answer": 0,
      "explain": "Unlike the VPC (which spans a Region), each individual subnet is confined to one Availability Zone."
    },
    {
      "q": "What actually determines whether a subnet is 'public' or 'private'?",
      "options": [
        "Its route table — a public subnet's route table sends internet-bound traffic to an Internet Gateway, a private subnet's doesn't",
        "A checkbox labeled 'public' set when the subnet is created, with no other effect",
        "Whether the subnet contains an EC2 instance or not",
        "The subnet's CIDR block size"
      ],
      "answer": 0,
      "explain": "The lesson is explicit that it's the route table's route to an Internet Gateway that makes a subnet public, not a simple label."
    },
    {
      "q": "What is the key difference between a security group and a network ACL (NACL)?",
      "options": [
        "A security group is a stateful firewall attached to a resource; a NACL is a stateless firewall applied at the subnet level",
        "A NACL is stateful and a security group is stateless",
        "Security groups and NACLs are two names for exactly the same feature",
        "NACLs can only be used inside a public subnet"
      ],
      "answer": 0,
      "explain": "Security groups are stateful and resource-level; NACLs are stateless and subnet-level — a coarser backstop."
    },
    {
      "q": "What does 'stateful' mean in the context of a security group?",
      "options": [
        "If inbound traffic on a port is allowed, the matching outbound response is automatically allowed too, without a separate rule",
        "The security group remembers every IP address that has ever connected, permanently",
        "It requires a database to store its rules externally",
        "It only applies to traffic within a single Availability Zone"
      ],
      "answer": 0,
      "explain": "Statefulness means the return traffic for an allowed request doesn't need its own explicit rule — unlike a stateless NACL."
    }
  ]
}
