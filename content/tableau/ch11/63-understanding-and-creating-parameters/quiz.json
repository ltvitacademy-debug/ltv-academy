{
  "lesson": "understanding-and-creating-parameters",
  "questions": [
    {
      "q": "What makes a parameter fundamentally different from a dimension or measure?",
      "options": [
        "A parameter is a single, workbook-level value you define yourself, completely independent of any connected data source",
        "A parameter can only ever hold the number 1",
        "A parameter is just another name for a filter",
        "A parameter always comes from the data source, exactly like a dimension"
      ],
      "answer": 0,
      "explain": "Dimensions and measures come from your connected data source; a parameter is a value you define in the workbook itself, with no dependency on any data source."
    },
    {
      "q": "What does a parameter do by itself, with nothing else referencing it?",
      "options": [
        "Nothing to the view — its power comes from being referenced in a calculated field, filter, reference line, or title",
        "It automatically filters every worksheet in the workbook",
        "It creates a new calculated field automatically",
        "It changes the underlying data source's values"
      ],
      "answer": 0,
      "explain": "A parameter alone is just a value sitting in the workbook. It has no effect until something else — a calculated field, filter, reference line, or title — references it, which is what the rest of this chapter covers."
    },
    {
      "q": "What's the difference between the List and Range allowable-value settings?",
      "options": [
        "List gives a fixed dropdown of specific values you define; Range gives a slider between a min and max with a step size",
        "List only works with numbers; Range only works with text",
        "They are the exact same setting with two different names",
        "Range only allows exactly two possible values"
      ],
      "answer": 0,
      "explain": "List is the right choice for a clean set of named choices (like a measure selector); Range is the right choice for a continuous, slider-driven value (like a target margin)."
    },
    {
      "q": "How do you make a parameter's control visible on a worksheet?",
      "options": [
        "Right-click the parameter in the Data pane and choose Show Parameter",
        "Parameters are always visible automatically the moment they're created",
        "Drag the parameter onto the Marks card",
        "Parameters cannot have a visible control — they can only be set through calculated fields"
      ],
      "answer": 0,
      "explain": "Show Parameter adds a visible control — a dropdown for a List parameter, a slider for a Range parameter — directly onto the worksheet."
    },
    {
      "q": "Where does a newly created parameter appear in Tableau Desktop?",
      "options": [
        "Under its own \"Parameters\" heading near the bottom of the Data pane",
        "It replaces the field it was created from",
        "Only inside the Format menu",
        "It does not appear anywhere in the Data pane"
      ],
      "answer": 0,
      "explain": "Parameters get their own dedicated section in the Data pane, separate from Dimensions, Measures, and Sets."
    }
  ]
}
