{
  "lesson": "string-calculations",
  "questions": [
    {
      "q": "What does the + operator do when used between two text fields in a Tableau calculation?",
      "options": [
        "Concatenates them into one combined string",
        "Adds their character counts together as a number",
        "Causes a syntax error, since + only works on numbers",
        "Compares them for equality"
      ],
      "answer": 0,
      "explain": "When both sides are text, + performs string concatenation — joining the two strings together, as in [First Name] + \" \" + [Last Name]."
    },
    {
      "q": "What does LEFT([Order ID], 4) return?",
      "options": [
        "The first 4 characters of the Order ID string",
        "The last 4 characters of the Order ID string",
        "Everything except the first 4 characters",
        "The 4th character only"
      ],
      "answer": 0,
      "explain": "LEFT(string, num_chars) returns the first num_chars characters — here, the first 4 characters of Order ID."
    },
    {
      "q": "What does CONTAINS([Category], \"Furniture\") return?",
      "options": [
        "A TRUE/FALSE value depending on whether \"Furniture\" appears anywhere in Category",
        "The position where \"Furniture\" starts inside Category",
        "A new string with \"Furniture\" removed from Category",
        "The number of times \"Furniture\" appears in Category"
      ],
      "answer": 0,
      "explain": "CONTAINS returns a Boolean — TRUE if the substring appears anywhere in the string, FALSE otherwise — which is why it's commonly used inside IF statements."
    },
    {
      "q": "What is the difference between CONTAINS and REPLACE?",
      "options": [
        "CONTAINS checks whether a substring exists and returns TRUE/FALSE; REPLACE actually swaps a substring for another string",
        "They do the same thing, just with different argument order",
        "CONTAINS only works on numbers, REPLACE only works on text",
        "REPLACE checks existence, CONTAINS performs the substitution"
      ],
      "answer": 0,
      "explain": "CONTAINS is a yes/no test for whether a substring is present; REPLACE actually performs the substitution, swapping the found substring for a replacement."
    },
    {
      "q": "Why might you write TRIM(UPPER([Category])) instead of just [Category]?",
      "options": [
        "To normalize inconsistent real-world text data — forcing consistent capitalization and removing stray leading/trailing whitespace",
        "Because TRIM and UPPER are required syntax for every string field",
        "To convert the field from a Dimension into a Measure",
        "Because [Category] alone would cause a syntax error"
      ],
      "answer": 0,
      "explain": "Real data is often inconsistent — the same category might appear as \"Furniture\", \"furniture\", or \"FURNITURE \" with trailing spaces — and wrapping UPPER inside TRIM normalizes both capitalization and whitespace."
    }
  ]
}
