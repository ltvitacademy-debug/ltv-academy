{
  "lesson": "advanced-reference-lines-bands-dynamic-targets",
  "questions": [
    {
      "q": "What is the difference between a reference line and a reference band?",
      "options": [
        "A reference line draws a single line at one value; a reference band shades the area between two values",
        "A reference line only works on discrete axes; a reference band only works on continuous axes",
        "They are the same feature under two different names",
        "A reference band can only be added to maps"
      ],
      "answer": 0,
      "explain": "A reference line marks one constant or computed value with a single line. A reference band shades the area between two constant or computed values."
    },
    {
      "q": "What do the Table, Pane, and Cell scope options control for a reference line or band?",
      "options": [
        "Whether it applies once across the whole view, once per pane (e.g., each row of a small multiple), or once per individual mark grouping",
        "The color of the reference line only",
        "Whether the reference line is visible on export vs. on screen",
        "The font size used for the reference line's label"
      ],
      "answer": 0,
      "explain": "Entire Table applies one reference item across the whole view; Per Pane applies a separate one to each pane of a trellis/small-multiple view; Per Cell applies one to every individual mark grouping."
    },
    {
      "q": "How do you turn a fixed reference line into a dynamic target that a user can move?",
      "options": [
        "Set the reference line's Value field to a parameter instead of a constant number",
        "Reference lines can never be made dynamic in Tableau",
        "Convert the reference line into a table calculation",
        "Rename the reference line to include the word 'dynamic'"
      ],
      "answer": 0,
      "explain": "Pointing a reference line's Value field at a parameter (instead of typing a constant) makes it dynamic — its position now follows whatever value the parameter holds."
    },
    {
      "q": "What makes a dynamic reference line interactive (e.g., moving as a user hovers over marks), rather than just parameter-driven?",
      "options": [
        "A parameter action (Dashboard → Actions → Add Action → Change Parameter) that updates the parameter's value based on user interaction like hover",
        "Simply creating the parameter is enough — no further action is needed",
        "Setting the reference line's scope to Per Cell",
        "Publishing the workbook to Tableau Server"
      ],
      "answer": 0,
      "explain": "A parameter alone just gives the reference line a variable Value field. A parameter action is what actually changes that parameter's value in response to user interaction, like hovering over a mark."
    },
    {
      "q": "In the reference band example shown in this lesson (a small multiple of Consumer, Corporate, and Home Office sales), what scope was used?",
      "options": [
        "Per Pane — a separate reference band on each of the three panes",
        "Entire Table — one single band spanning all three panes combined",
        "Per Cell — a separate band for every single mark",
        "No scope was applied; the band appears everywhere by default"
      ],
      "answer": 0,
      "explain": "The band was applied per pane, giving each of the three small-multiple panes (Consumer, Corporate, Home Office) its own shaded reference range to compare its trend against."
    }
  ]
}
