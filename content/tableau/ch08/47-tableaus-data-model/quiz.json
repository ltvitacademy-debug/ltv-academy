{
  "lesson": "tableaus-data-model",
  "questions": [
    {
      "q": "What are the two layers of Tableau's data model?",
      "options": [
        "The logical layer (logical tables connected by relationships) and the physical layer (physical tables combined by joins/unions inside a logical table)",
        "The reporting layer and the storage layer",
        "The dashboard layer and the worksheet layer",
        "The extract layer and the live-connection layer"
      ],
      "answer": 0,
      "explain": "Every Tableau data source is built from a logical layer (relationships between logical tables) sitting on top of a physical layer (joins/unions inside each logical table)."
    },
    {
      "q": "Where do relationships live, and where do joins live?",
      "options": [
        "Relationships connect logical tables to each other on the logical layer; joins combine physical tables inside one logical table on the physical layer",
        "Relationships and joins are the same thing, just different names",
        "Relationships live in the physical layer; joins live in the logical layer",
        "Joins only exist in Tableau Prep, never in Tableau Desktop"
      ],
      "answer": 0,
      "explain": "The logical layer holds relationships between logical tables; double-clicking into a logical table opens its physical layer, where you build joins or unions."
    },
    {
      "q": "How do you open the physical layer for a given logical table?",
      "options": [
        "Double-click the logical table on the data source canvas",
        "Right-click the worksheet tab and select Physical Layer",
        "It opens automatically the first time you add a filter",
        "You can only view it from Tableau Server, not Desktop"
      ],
      "answer": 0,
      "explain": "Double-clicking a logical table on the logical layer canvas opens its physical layer — the traditional join canvas underneath it."
    },
    {
      "q": "According to this lesson, what problem did the old single-layer canvas cause that relationships were built to fix?",
      "options": [
        "It joined every table to every other table immediately, which could duplicate rows and generate wasteful queries even for worksheets that didn't need most of the tables",
        "It couldn't connect to Excel files at all",
        "It required writing SQL by hand for every join",
        "It didn't support any join types besides inner join"
      ],
      "answer": 0,
      "explain": "Before the two-layer model, every table was joined at query time regardless of what a given worksheet needed, which could multiply rows and waste query performance — relationships are context-aware and avoid both problems."
    },
    {
      "q": "What is a 'logical table', precisely?",
      "options": [
        "A box on the logical layer canvas that may represent exactly one physical table, or several physical tables already joined or unioned together",
        "A table that only exists conceptually and can never be queried",
        "Another name for a Tableau worksheet",
        "A table that has no fields, only calculated fields"
      ],
      "answer": 0,
      "explain": "A logical table is what you see on the main data source canvas — it can bundle one or many physical tables underneath it via the physical layer."
    }
  ]
}
