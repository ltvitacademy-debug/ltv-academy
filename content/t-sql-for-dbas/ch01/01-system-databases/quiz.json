{
  "lesson": "system-databases",
  "questions": [
    {
      "q": "Why does tempdb never get backed up, while master, msdb, and model do?",
      "options": [
        "Tempdb is rebuilt from scratch every time the SQL Server service restarts, so it never holds anything that survives a restart",
        "Tempdb backups are technically impossible in SQL Server",
        "Tempdb is the least important database, so it's simply skipped",
        "Only user databases like AdventureWorks ever need backups"
      ],
      "answer": 0,
      "explain": "Tempdb is scratch space rebuilt on every restart — there's nothing persistent in it to back up."
    },
    {
      "q": "What does the model system database do?",
      "options": [
        "It's the template every new user database is created from",
        "It stores SQL Server Agent job history",
        "It holds all server-level login information",
        "It's a reporting database used only by SSRS"
      ],
      "answer": 0,
      "explain": "Whatever settings (like collation or recovery model) are configured on model become the default for every database created afterward."
    },
    {
      "q": "A DBA wants to confirm last night's backup job actually succeeded. Which system database holds that history?",
      "options": [
        "msdb",
        "master",
        "model",
        "tempdb"
      ],
      "answer": 0,
      "explain": "msdb stores SQL Server Agent jobs, schedules, and backup/restore history."
    },
    {
      "q": "Which system database holds instance-level metadata like logins and linked servers, and where every other database's files live?",
      "options": [
        "master",
        "msdb",
        "model",
        "tempdb"
      ],
      "answer": 0,
      "explain": "master is the instance's own metadata store — the server won't even start without it."
    },
    {
      "q": "What is the central shift this course makes compared to T-SQL Development, per this lesson?",
      "options": [
        "From writing SQL to work with data, to using T-SQL to diagnose and fix problems with SQL Server itself",
        "From SQL Server to a completely different database platform",
        "From T-SQL to Python exclusively",
        "There is no shift — this course just repeats T-SQL Development"
      ],
      "answer": 0,
      "explain": "This course explicitly assumes the T-SQL language foundation and redirects it at server diagnostics instead of business data."
    }
  ]
}
