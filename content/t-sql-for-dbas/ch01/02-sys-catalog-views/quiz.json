{
  "lesson": "sys-catalog-views",
  "questions": [
    {
      "q": "Why does sys.databases show every database on the instance, while sys.tables only shows tables in the current database?",
      "options": [
        "sys.databases is instance-wide metadata; sys.tables, sys.columns, and sys.indexes are scoped to whichever database you're connected to",
        "sys.tables is deprecated and only works in older SQL Server versions",
        "sys.databases requires sysadmin permissions that sys.tables does not",
        "There is no real difference — both return the same rows"
      ],
      "answer": 0,
      "explain": "sys.databases reflects the whole instance; sys.tables, sys.columns, and sys.indexes are database-scoped catalog views."
    },
    {
      "q": "What column in sys.databases tells you whether a database is ONLINE, RESTORING, or SUSPECT?",
      "options": [
        "state_desc",
        "recovery_model_desc",
        "collation_name",
        "is_auto_close_on"
      ],
      "answer": 0,
      "explain": "state_desc reports the database's current state — a key first check when an app can't connect."
    },
    {
      "q": "What key do you join sys.tables and sys.columns on to list every column in a table?",
      "options": [
        "object_id",
        "database_id",
        "column_id",
        "type_desc"
      ],
      "answer": 0,
      "explain": "Every object in a database has an object_id, which sys.columns references to identify which table each column belongs to."
    },
    {
      "q": "Which catalog view lists the indexes (and heap, if present) defined on a table?",
      "options": [
        "sys.indexes",
        "sys.databases",
        "sys.types",
        "sys.columns"
      ],
      "answer": 0,
      "explain": "sys.indexes returns one row per index (plus a HEAP row if the table has no clustered index) for a given object_id."
    },
    {
      "q": "What is a catalog view, in SQL Server terms?",
      "options": [
        "A built-in system view that exposes SQL Server metadata as regular, queryable rows",
        "A user-created view that summarizes business data for reporting",
        "A temporary view that exists only for the life of a session",
        "A deprecated feature replaced entirely by Dynamic Management Views"
      ],
      "answer": 0,
      "explain": "Catalog views are built-in views over SQL Server's internal metadata tables, queried with ordinary SELECT statements."
    }
  ]
}
