{
  "lesson": "consistency-errors",
  "questions": [
    {
      "q": "In DBCC CHECKDB output, what does an allocation error mean, as distinct from a consistency error?",
      "options": [
        "A page's ownership bookkeeping is wrong — claimed twice, or not claimed by any object",
        "The database backup file is corrupted",
        "A login lacks permission to run CHECKDB",
        "The transaction log is full"
      ],
      "answer": 0,
      "explain": "Allocation errors are about page ownership bookkeeping; consistency errors are about the actual data or structure on a page contradicting expectations."
    },
    {
      "q": "CHECKDB reports errors on Sales.SalesOrderDetail, and last night's full backup predates the corruption. What's the preferred fix?",
      "options": [
        "Restore the database (or the affected pages) from that clean backup",
        "Immediately run REPAIR_ALLOW_DATA_LOSS",
        "Drop and recreate the table",
        "Ignore it since CHECKDB will fix itself on the next run"
      ],
      "answer": 0,
      "explain": "A clean backup gives a zero-data-loss fix; REPAIR_ALLOW_DATA_LOSS is reached for only when no usable backup exists."
    },
    {
      "q": "What does REPAIR_ALLOW_DATA_LOSS actually do when it fixes a consistency error?",
      "options": [
        "It can delete the corrupted rows or deallocate the damaged pages to restore structural consistency",
        "It silently recovers all lost data with no downside",
        "It only fixes allocation errors, never consistency errors",
        "It rebuilds the entire database from the transaction log automatically"
      ],
      "answer": 0,
      "explain": "The option name is literal — it can delete data to bring the database back to a consistent state, so it's a last resort, not a first response."
    },
    {
      "q": "What line in a dirty CHECKDB run's output is the one to grep for in an automated job's log?",
      "options": [
        "The summary line reporting 'CHECKDB found N allocation errors and N consistency errors'",
        "The line showing the SQL Server version number",
        "The first informational message in the output",
        "The line showing the current database recovery model"
      ],
      "answer": 0,
      "explain": "That summary line tells you immediately whether anything is actually wrong — anything other than all-zeros means real corruption."
    },
    {
      "q": "Before running any repair option on a corrupted database, what should a DBA do first?",
      "options": [
        "Note the object ID, page numbers, and error numbers, and check for a clean recent backup",
        "Restart the SQL Server service immediately",
        "Shrink the database to reclaim space",
        "Disable DBCC CHECKDB so the error stops appearing"
      ],
      "answer": 0,
      "explain": "That information is needed regardless of which fix path is taken, and checking for a usable backup determines whether a zero-loss restore is possible."
    }
  ]
}
