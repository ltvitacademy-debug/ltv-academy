[
  {
    "type": "title",
    "eyebrow": "T-SQL for Database Administrators · Chapter Four",
    "titlePlain": "Locks & ",
    "titleEmphasis": "Blocking",
    "titleSuffix": "",
    "subtitle": "Lesson 16 · Transactions, Locking & Deadlocks"
  },
  {
    "type": "code",
    "eyebrow": "Every lock, held or waiting",
    "code": "SELECT resource_type, resource_associated_entity_id,\n  request_mode, request_status, request_session_id\nFROM sys.dm_tran_locks\nWHERE resource_database_id = DB_ID('AdventureWorks2012');\n\n-- request_status: GRANT = holding, WAIT = queued",
    "caption": "sys.dm_tran_locks — the full lock picture."
  },
  {
    "type": "steps",
    "eyebrow": "Modes you'll actually see",
    "titlePlain": "S, X, U, and ",
    "titleEmphasis": "intent",
    "titleSuffix": "",
    "steps": [
      { "label": "S / X", "sub": "shared read vs exclusive write, incompatible" },
      { "label": "U", "sub": "update lock, held before escalating to X" },
      { "label": "IS / IX", "sub": "intent locks flagged at the table level" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Tracing the chain",
    "code": "SELECT r.session_id AS blocked_session,\n  r.blocking_session_id AS blocked_by,\n  r.wait_type, r.wait_resource, t.text AS blocked_sql\nFROM sys.dm_exec_requests r\nCROSS APPLY sys.dm_exec_sql_text(r.sql_handle) t\nWHERE r.blocking_session_id <> 0;",
    "caption": "Find the session blocking others but not itself blocked."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 17",
    "titlePlain": "Isolation Levels ",
    "titleEmphasis": "in Practice",
    "titleSuffix": "",
    "subtitle": "Why the same query locks differently depending on isolation level."
  }
]
