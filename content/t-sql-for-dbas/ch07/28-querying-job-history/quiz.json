{
  "lesson": "querying-job-history",
  "questions": [
    {
      "q": "Which msdb table holds one row per job step execution, plus a summary row per job run?",
      "options": [
        "sysjobhistory",
        "sysjobs",
        "sysjobsteps",
        "sysjobschedules"
      ],
      "answer": 0,
      "explain": "sysjobhistory logs every step execution; the step_id = 0 row summarizes the whole run."
    },
    {
      "q": "In sysjobhistory, why do most monitoring queries filter WHERE step_id = 0?",
      "options": [
        "That row is the summary for the entire job run, not just one step",
        "step_id 0 means the job hasn't started yet",
        "It's the only row that includes the job name",
        "step_id 0 rows are the only ones msdb retains long-term"
      ],
      "answer": 0,
      "explain": "Agent writes a step_id = 0 row summarizing the overall outcome of the job run, in addition to a row for each individual step."
    },
    {
      "q": "What does run_status = 0 mean in sysjobhistory?",
      "options": [
        "Failed",
        "Succeeded",
        "Retry",
        "In Progress"
      ],
      "answer": 0,
      "explain": "0 is failed, 1 is succeeded, 2 is retry, 3 is canceled, and 4 is in progress."
    },
    {
      "q": "Why can't you just compare run_date and run_time directly as if they were a datetime?",
      "options": [
        "They're stored as packed, unpadded integers (like 20260919 and 91530), not a datetime type",
        "They're stored as text strings in a locale-specific format",
        "SQL Server encrypts these columns by default",
        "They're actually stored in UTC and need timezone conversion first"
      ],
      "answer": 0,
      "explain": "run_date and run_time are plain integers packed as YYYYMMDD and HHMMSS (unpadded), which is why msdb provides agent_datetime() to convert them."
    },
    {
      "q": "What does msdb.dbo.agent_datetime() do?",
      "options": [
        "Converts a run_date/run_time integer pair into a proper datetime value",
        "Returns the current server datetime",
        "Schedules a job to run at a given datetime",
        "Converts a datetime into the packed integer format Agent uses internally"
      ],
      "answer": 0,
      "explain": "agent_datetime(run_date, run_time) takes the two packed integers sysjobhistory stores and returns a single datetime you can filter and sort on normally."
    }
  ]
}
