[
  {
    "type": "title",
    "eyebrow": "T-SQL for Database Administrators · Chapter Three",
    "titlePlain": "Missing, Unused & ",
    "titleEmphasis": "Duplicate Indexes",
    "titleSuffix": "",
    "subtitle": "Lesson 12 · Index & Statistics Administration"
  },
  {
    "type": "code",
    "eyebrow": "What the optimizer wishes existed",
    "code": "SELECT mid.statement, mid.equality_columns,\n       mid.included_columns,\n       migs.avg_user_impact\nFROM sys.dm_db_missing_index_details AS mid\nJOIN sys.dm_db_missing_index_groups AS mig\n  ON mid.index_handle = mig.index_handle\nJOIN sys.dm_db_missing_index_group_stats AS migs\n  ON mig.index_group_handle = migs.group_handle\nORDER BY migs.avg_user_impact DESC;",
    "caption": "A lead to investigate, not a script to run unattended."
  },
  {
    "type": "steps",
    "eyebrow": "Read with skepticism",
    "titlePlain": "Real caveats on the ",
    "titleEmphasis": "missing-index",
    "titleSuffix": " DMV",
    "steps": [
      { "label": "Since last restart", "sub": "one run can produce a weak suggestion" },
      { "label": "No write cost", "sub": "ignores INSERT/UPDATE/DELETE overhead" },
      { "label": "Overlapping suggestions", "sub": "column order never optimized" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Indexes nobody reads",
    "code": "SELECT OBJECT_NAME(ius.object_id) AS table_name,\n       i.name AS index_name,\n       ius.user_seeks, ius.user_scans,\n       ius.user_updates\nFROM sys.dm_db_index_usage_stats AS ius\nJOIN sys.indexes AS i\n  ON ius.object_id=i.object_id AND ius.index_id=i.index_id\nWHERE ius.database_id = DB_ID()\nORDER BY ius.user_seeks + ius.user_scans ASC;",
    "caption": "High user_updates with zero reads is pure write overhead."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 13",
    "titlePlain": "Statistics: Inspecting & ",
    "titleEmphasis": "Maintaining",
    "titleSuffix": "",
    "subtitle": "The optimizer's row-count estimates, and how they go stale."
  }
]
