{
  "lesson": "top-resource-consuming-queries",
  "questions": [
    {
      "q": "Which column in sys.dm_exec_query_stats represents cumulative CPU time across all executions of a cached plan?",
      "options": [
        "total_worker_time",
        "total_logical_reads",
        "execution_count",
        "avg_duration"
      ],
      "answer": 0,
      "explain": "total_worker_time is CPU time in microseconds, summed across every execution the plan has seen since being cached."
    },
    {
      "q": "Why might a query that runs in 5 milliseconds be a bigger overall problem than a report that takes 30 seconds?",
      "options": [
        "If it runs 100,000 times a day, its total aggregate cost can far exceed the report's one-time cost",
        "Fast queries always use more memory than slow ones",
        "SQL Server always prioritizes long-running queries for eviction",
        "Short queries can never appear in the plan cache"
      ],
      "answer": 0,
      "explain": "Total resource consumption multiplies per-execution cost by execution count, so high-frequency cheap queries can dominate aggregate cost."
    },
    {
      "q": "When should a DBA sort by average rather than total resource consumption?",
      "options": [
        "When investigating one specific complaint about one specific report's worst execution",
        "Always — average is the correct first-pass triage metric",
        "Only when Query Store is disabled",
        "Never — average is meaningless in SQL Server"
      ],
      "answer": 0,
      "explain": "Average finds the single worst individual execution, which is useful for a targeted complaint but misleading for first-pass, server-wide triage."
    },
    {
      "q": "Which column would you sort by to find the queries doing the most I/O in aggregate?",
      "options": [
        "total_logical_reads",
        "total_worker_time",
        "is_percent_growth",
        "plan_generation_num"
      ],
      "answer": 0,
      "explain": "total_logical_reads is the cumulative pages read from buffer cache across all executions — the I/O-bound counterpart to total_worker_time's CPU focus."
    },
    {
      "q": "What is standard practice when triaging 'the server is slow' using sys.dm_exec_query_stats?",
      "options": [
        "Run the query sorted by total CPU and separately by total logical reads, and compare the two lists",
        "Only ever check total_worker_time and ignore I/O entirely",
        "Restart the SQL Server service before running any DMV query",
        "Immediately run DBCC CHECKDB before checking resource consumption"
      ],
      "answer": 0,
      "explain": "Comparing the CPU-sorted and I/O-sorted lists highlights queries that are expensive on both axes — the ones worth tuning first."
    }
  ]
}
