[
  {"slide": "s1", "text": "\"Who has access to this\" and \"who actually did this\" are two different questions. The first is a point-in-time query against catalog views. The second needs something actually recording events as they happen — that's what SQL Server Audit is for."},
  {"slide": "s2", "text": "Sys.database_permissions lists every explicit grant, deny, or revoke at the object, schema, or database level. Join it to database principals and you get a straight answer to \"who can do what\" as of right now — but it says nothing about who actually did anything."},
  {"slide": "s3", "text": "A server audit defines where events get written — a file, the Windows security log, or the application log. A database audit specification defines what actually gets captured, like select and update on a specific table. You need both pieces — an audit with no specification records nothing."},
  {"slide": "s4", "text": "Access is a snapshot: what permissions exist right now. Actions need a trail: what actually happened, and when. Sys.fn_get_audit_file reads the binary sqlaudit files the audit writes and returns them as a normal queryable result set, so you can actually answer \"who ran this update last Tuesday.\""},
  {"slide": "s5", "text": "That closes out security administration. Next up: Chapter Seven, SQL Server Agent and Automation, starting with querying job history."}
]
