{
  "lesson": "monitoring-cpu-memory-io",
  "questions": [
    {
      "q": "What does sys.dm_os_performance_counters expose?",
      "options": [
        "SQL Server's internal perfmon-style counters, queryable directly with T-SQL",
        "Only the login history for the last 30 days",
        "The full text of every stored procedure on the instance",
        "A list of every index and its fragmentation percentage"
      ],
      "answer": 0,
      "explain": "sys.dm_os_performance_counters exposes the same counters Performance Monitor (perfmon) graphs, as queryable rows."
    },
    {
      "q": "What does a sustained drop in Page life expectancy, relative to this server's own baseline, generally signal?",
      "options": [
        "Memory pressure — data pages are being pushed out of the buffer pool faster than normal",
        "A successful index rebuild",
        "That tempdb has run out of disk space",
        "That the recovery model was changed to SIMPLE"
      ],
      "answer": 0,
      "explain": "Page life expectancy estimates how long a page stays in memory; a sustained drop versus baseline is a classic memory pressure signal."
    },
    {
      "q": "What does sys.dm_io_virtual_file_stats(NULL, NULL) return?",
      "options": [
        "Read/write I/O stats, including stall time, for every database file on the instance",
        "Only the files belonging to tempdb",
        "The list of logins with file-level permissions",
        "The fragmentation level of every index"
      ],
      "answer": 0,
      "explain": "Passing NULL, NULL for database_id and file_id returns I/O stats for every file across every database on the instance."
    },
    {
      "q": "What does a high average value of io_stall_read_ms / num_of_reads for a data file generally indicate?",
      "options": [
        "The storage subsystem is slow to respond to reads on that file, not necessarily a problem with the query itself",
        "The query optimizer chose a bad execution plan",
        "The database is in the wrong recovery model",
        "Statistics on that file's table are out of date"
      ],
      "answer": 0,
      "explain": "A high average read stall points at storage responsiveness, distinct from query design or plan choice issues."
    },
    {
      "q": "Why does the guide emphasize reading page life expectancy and I/O stalls together, rather than in isolation?",
      "options": [
        "Because a dropping page life expectancy combined with high I/O stalls on the same file suggests a compounding problem — memory pressure forcing rereads from slow storage",
        "Because SQL Server only reports one counter at a time",
        "Because only one of the two counters is ever accurate",
        "Because they measure the exact same thing and are redundant"
      ],
      "answer": 0,
      "explain": "The two signals reinforce each other: memory pressure evicts pages, which then have to be reread from storage that is itself slow, compounding the problem."
    }
  ]
}
