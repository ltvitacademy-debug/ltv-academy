[
  {
    "type": "title",
    "eyebrow": "Snowflake · Chapter 2, Lesson 8",
    "titlePlain": "Views, Temporary & ",
    "titleEmphasis": "Transient",
    "titleSuffix": " Tables",
    "subtitle": "A persistence concept SQL Server doesn't have"
  },
  {
    "type": "code",
    "eyebrow": "Three table types, one syntax change",
    "code": "CREATE TABLE           orders (id NUMBER, total NUMBER);   -- permanent\nCREATE TRANSIENT TABLE staging_orders (id NUMBER, total NUMBER);\nCREATE TEMPORARY TABLE session_scratch (id NUMBER, total NUMBER);\n\nCREATE OR REPLACE VIEW reporting.high_value_customers AS\nSELECT customer_id, SUM(order_total) AS lifetime_value\nFROM raw.orders\nGROUP BY customer_id\nHAVING SUM(order_total) > 10000;",
    "caption": "Views need no adjustment from T-SQL; table persistence is the new concept."
  },
  {
    "type": "steps",
    "eyebrow": "Recovery windows compared",
    "titlePlain": "Permanent vs. ",
    "titleEmphasis": "transient",
    "titleSuffix": " vs. temporary",
    "steps": [
      { "label": "Permanent", "sub": "7-day Fail-safe + Time Travel" },
      { "label": "Transient", "sub": "No Fail-safe, cheaper storage" },
      { "label": "Temporary", "sub": "Session-scoped, auto-dropped" }
    ]
  },
  {
    "type": "steps",
    "eyebrow": "Why it matters",
    "titlePlain": "Staging tables should be ",
    "titleEmphasis": "transient",
    "titleSuffix": "",
    "steps": [
      { "label": "Reproducible data", "sub": "Reload from source if it fails" },
      { "label": "No Fail-safe needed", "sub": "Saves storage cost" },
      { "label": "No SQL Server equivalent", "sub": "A Snowflake-specific decision" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "MERGE in ",
    "titleEmphasis": "Snowflake",
    "titleSuffix": "",
    "subtitle": "Mostly familiar, with real differences"
  }
]
