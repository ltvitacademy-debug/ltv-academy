{
  "lesson": "automated-file-loading-and-monitoring-loads",
  "questions": [
    {
      "q": "Where does the SQS ARN needed to wire up S3 event notifications come from?",
      "options": [
        "Snowflake's SHOW PIPES output, in the notification_channel column, since Snowflake already created and is listening to that queue",
        "A value you invent yourself in the AWS console",
        "The CREATE STAGE statement's URL field",
        "The output of SYSTEM$PIPE_STATUS"
      ],
      "answer": 0,
      "explain": "SHOW PIPES reveals the notification_channel ARN, which must be copied exactly into the S3 bucket's event notification configuration in AWS."
    },
    {
      "q": "What happens if the ARN copied into the S3 event notification doesn't exactly match the pipe's notification_channel?",
      "options": [
        "Files land in the bucket but nothing triggers the pipe — the automation silently doesn't work",
        "Snowflake automatically corrects the typo",
        "The pipe deletes itself",
        "The load runs but with a warning logged"
      ],
      "answer": 0,
      "explain": "The lesson explicitly calls out that a typo in the ARN means files land and nothing happens — there's no automatic correction."
    },
    {
      "q": "What does SYSTEM$PIPE_STATUS tell you that SHOW PIPES alone does not?",
      "options": [
        "Live execution state, pending file count, and the last time the pipe received a file",
        "The SQL text of the pipe's COPY INTO statement",
        "The pipe's creation date",
        "The warehouse the pipe is assigned to"
      ],
      "answer": 0,
      "explain": "SYSTEM$PIPE_STATUS returns real-time operational JSON (executionState, pendingFileCount, etc.), while SHOW PIPES lists pipe definitions and their notification channel."
    },
    {
      "q": "Why should a pipe run under its own narrowly-scoped role instead of the admin role used to set it up?",
      "options": [
        "Least-privilege: an unattended automation should only have the specific grants it needs, not broad admin access",
        "Because admin roles cannot own pipes in Snowflake",
        "Because pipes require a role named exactly 'snowpipe_role'",
        "Because it makes the pipe run faster"
      ],
      "answer": 0,
      "explain": "The lesson frames this as the same instinct as scoping any service account tightly — insert/select on one table, usage on one stage, nothing more."
    },
    {
      "q": "According to this lesson, what are the two real UI moments shown in its screenshots?",
      "options": [
        "Configuring the AWS S3 event notification, and checking the pipe's status/ARN via SHOW PIPES in Snowsight",
        "Creating a warehouse and creating a database",
        "Writing a COPY INTO statement and running a SELECT query",
        "Signing up for a Snowflake trial and logging in"
      ],
      "answer": 0,
      "explain": "This lesson uses two real screenshots from the Getting Started with Snowpipe quickstart: the AWS-side event notification setup, and the Snowflake-side SHOW PIPES result."
    }
  ]
}
