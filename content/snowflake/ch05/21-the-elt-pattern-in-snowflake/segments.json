[
  { "n": 1, "text": "Traditional ETL transforms data in a separate engine before it ever reaches the warehouse. Snowflake pipelines flip that: extract, load the raw data as-is, then transform it afterward with SQL running inside the warehouse itself. That's ELT." },
  { "n": 2, "text": "This works because storage and compute are billed separately. Loading raw data is cheap storage, not compute. Transformation is ordinary SQL against a warehouse you size for the job and suspend when it's done — no separate transformation engine to install or keep in sync." },
  { "n": 3, "text": "Every lesson from here through Chapter 6 moves data through three layers: raw, an unmodified landing zone; staging or cleaned, typed and deduplicated and validated; and business-ready, modeled as star schemas for a BI tool to query directly." },
  { "n": 4, "text": "Raw and staging tables are good candidates for transient tables — no fail-safe period, lower storage cost, because that data is always reproducible by re-running the load or the transform. Business-ready tables, what reports depend on directly, are usually worth making permanent." },
  { "n": 5, "text": "Next lesson: CREATE TABLE AS SELECT, the single SQL statement that does most of the transforming work in this chapter." }
]
