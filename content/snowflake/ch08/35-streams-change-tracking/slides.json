[
  {
    "type": "title",
    "eyebrow": "Snowflake · Chapter 8, Lesson 35",
    "titlePlain": "Streams: Change Tracking in ",
    "titleEmphasis": "Snowflake",
    "titleSuffix": "",
    "subtitle": "Native CDC — no homemade watermark column required"
  },
  {
    "type": "code",
    "eyebrow": "Creating & querying a stream",
    "code": "CREATE OR REPLACE STREAM orders_stream\n    ON TABLE orders;\n\n-- Query it like a table — returns only\n-- rows changed since last consumed\nSELECT * FROM orders_stream;",
    "caption": "One object, tracking every insert, update, and delete against the table."
  },
  {
    "type": "steps",
    "eyebrow": "The metadata columns",
    "titlePlain": "Every stream adds ",
    "titleEmphasis": "three",
    "titleSuffix": " columns",
    "steps": [
      { "label": "METADATA$ACTION", "sub": "INSERT or DELETE" },
      { "label": "METADATA$ISUPDATE", "sub": "TRUE when a DELETE+INSERT pair is really an UPDATE" },
      { "label": "METADATA$ROW_ID", "sub": "Tracks row version history internally" }
    ]
  },
  {
    "type": "steps",
    "eyebrow": "Consumed, not just read",
    "titlePlain": "A plain SELECT ",
    "titleEmphasis": "doesn't",
    "titleSuffix": " consume it",
    "steps": [
      { "label": "SELECT to peek", "sub": "Safe — doesn't advance the stream" },
      { "label": "DML that reads + commits", "sub": "INSERT/MERGE from the stream advances it" },
      { "label": "Result", "sub": "Process once; already-seen changes stop appearing" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Tasks: Scheduled ",
    "titleEmphasis": "Processing",
    "titleSuffix": "",
    "subtitle": "Running the work that consumes a stream, automatically"
  }
]
