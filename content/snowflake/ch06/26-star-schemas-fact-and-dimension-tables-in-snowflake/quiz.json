{
  "lesson": "star-schemas-fact-and-dimension-tables-in-snowflake",
  "questions": [
    {
      "q": "What changes about the star schema shape (fact table plus surrounding dimension tables) when implementing it in Snowflake?",
      "options": [
        "Nothing about the shape itself — what changes is the exact SQL/DDL used to build it",
        "Snowflake requires fact and dimension tables to live in different databases",
        "Snowflake does not support foreign key relationships between tables at all",
        "Fact tables must be temporary tables in Snowflake"
      ],
      "answer": 0,
      "explain": "The conceptual shape — one fact table with foreign keys to surrounding dimension tables — is assumed known and unchanged; only the DDL syntax used to implement it in Snowflake is new."
    },
    {
      "q": "What does Snowflake's NUMBER type correspond to in T-SQL terms?",
      "options": [
        "A general-purpose numeric type covering what INT, BIGINT, and DECIMAL each handled separately",
        "Only floating-point values, with no support for integers",
        "A text type used for storing numeric-looking strings",
        "A type that can only store dates"
      ],
      "answer": 0,
      "explain": "NUMBER (with optional precision/scale, e.g. NUMBER(12,2)) is Snowflake's single general-purpose numeric type, replacing the need for separate INT/BIGINT/DECIMAL types."
    },
    {
      "q": "By default, does Snowflake enforce PRIMARY KEY and NOT NULL constraints the way SQL Server does?",
      "options": [
        "No — they are declarable as informational metadata but not enforced against bad inserts by default",
        "Yes, identically to SQL Server, blocking any violating insert",
        "Snowflake does not allow declaring PRIMARY KEY or NOT NULL at all",
        "Only NOT NULL is enforced; PRIMARY KEY is never allowed"
      ],
      "answer": 0,
      "explain": "Snowflake lets you declare these constraints for metadata/optimizer purposes, but by default it does not enforce them the way SQL Server blocks violating inserts — data quality checks (Lesson 25) are what actually protect the data."
    },
    {
      "q": "What does CLUSTER BY do on a Snowflake table?",
      "options": [
        "Organizes the table's micro-partitions by a chosen column so filtered queries can prune scans, without guaranteeing physical row order",
        "Creates a traditional SQL Server-style clustered index with guaranteed physical row ordering",
        "Automatically partitions the table across multiple databases",
        "Enforces uniqueness on the clustered column"
      ],
      "answer": 0,
      "explain": "CLUSTER BY organizes micro-partitions to help query pruning, but it is not the same mechanism as a SQL Server clustered index — there's no guaranteed physical row ordering."
    },
    {
      "q": "In the fact table example, what do columns like customer_key, product_key, and date_key represent?",
      "options": [
        "Foreign keys pointing at the surrogate keys of the surrounding dimension tables",
        "Randomly generated values with no relationship to any other table",
        "Columns that must always be VARCHAR type",
        "Clustering keys that replace the need for any other column"
      ],
      "answer": 0,
      "explain": "These are the fact table's foreign keys, referencing each dimension table's surrogate key — the standard star schema relationship, now expressed in Snowflake DDL."
    }
  ]
}
