{
  "lesson": "secure-views-and-row-access-policies",
  "questions": [
    {
      "q": "What does a secure view hide that a regular view does not?",
      "options": [
        "Its underlying SQL definition, from anyone except its owner",
        "The row count returned by a query",
        "The name of the role querying it",
        "All numeric column values automatically"
      ],
      "answer": 0,
      "explain": "A regular view's SQL is visible via GET_DDL or SHOW VIEWS. A secure view hides that definition from everyone but its owner, useful when the query logic itself is sensitive."
    },
    {
      "q": "What does a row access policy control?",
      "options": [
        "Which rows a query returns, evaluated fresh based on the querying role",
        "Which columns exist in a table",
        "How long a query is allowed to run",
        "Whether a table can be dropped"
      ],
      "answer": 0,
      "explain": "A row access policy is a function that returns TRUE for rows a given role should see — it filters results per-query based on who's asking, not what columns exist."
    },
    {
      "q": "Which statement attaches an already-defined row access policy to a table?",
      "options": [
        "ALTER TABLE ... ADD ROW ACCESS POLICY ... ON (column)",
        "CREATE VIEW ... WITH ROW POLICY",
        "GRANT ROW POLICY TO TABLE",
        "SELECT ... APPLY POLICY"
      ],
      "answer": 0,
      "explain": "CREATE ROW ACCESS POLICY defines the filtering function; ALTER TABLE ... ADD ROW ACCESS POLICY ... ON (column) is what attaches it to a specific table's column."
    },
    {
      "q": "In this lesson's example, what happens when the SALES_ADMIN role and the WEST role both run SELECT * FROM transactions on the same table?",
      "options": [
        "SALES_ADMIN sees every row; WEST only sees rows where region = 'WEST'",
        "Both roles see identical results because the table has no policy",
        "WEST sees every row and SALES_ADMIN sees none",
        "The query fails for any role except the table owner"
      ],
      "answer": 0,
      "explain": "The row access policy function returns TRUE for SALES_ADMIN on any row, but only for rows where region_col matches CURRENT_ROLE() otherwise — so the same query returns different rows per role."
    },
    {
      "q": "According to this lesson, when should you reach for a secure view instead of a regular one?",
      "options": [
        "When the view's own query logic (a formula, a filter) is sensitive enough that it shouldn't be visible to everyone who can query it",
        "Every time, since secure views always perform better",
        "Only when the view has zero columns",
        "Never — secure views are deprecated in favor of row access policies"
      ],
      "answer": 0,
      "explain": "Secure views trade away some query-optimization shortcuts, so this lesson recommends using them specifically where hiding the definition matters, not as a default on every view."
    }
  ]
}
