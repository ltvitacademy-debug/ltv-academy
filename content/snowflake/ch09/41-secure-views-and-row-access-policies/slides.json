[
  {
    "type": "title",
    "eyebrow": "Snowflake · Chapter 9, Lesson 41",
    "titlePlain": "Secure Views & ",
    "titleEmphasis": "Row Access Policies",
    "titleSuffix": "",
    "subtitle": "Controlling what a role can see, once it's already allowed in"
  },
  {
    "type": "code",
    "eyebrow": "Hiding a view's definition",
    "code": "CREATE SECURE VIEW sales.public.regional_summary AS\nSELECT region, SUM(amount) AS total_sales\nFROM sales.public.transactions\nWHERE amount > 0\nGROUP BY region;",
    "caption": "SELECT works normally — but GET_DDL no longer reveals this view's logic."
  },
  {
    "type": "code",
    "eyebrow": "Defining a row access policy",
    "code": "CREATE ROW ACCESS POLICY sales.public.region_filter\n  AS (region_col VARCHAR) RETURNS BOOLEAN ->\n  CURRENT_ROLE() IN ('SALES_ADMIN')\n  OR region_col = CURRENT_ROLE();\n\nALTER TABLE sales.public.transactions\n  ADD ROW ACCESS POLICY sales.public.region_filter\n  ON (region);",
    "caption": "The policy function decides row-by-row who sees what."
  },
  {
    "type": "steps",
    "eyebrow": "The same query, different rows",
    "titlePlain": "Filtered by ",
    "titleEmphasis": "who's asking",
    "titleSuffix": "",
    "steps": [
      { "label": "SALES_ADMIN queries", "sub": "Sees every row in the table" },
      { "label": "WEST role queries", "sub": "Sees only region = 'WEST' rows" },
      { "label": "Same SQL, same table", "sub": "No per-region views needed" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Dynamic Data Masking in ",
    "titleEmphasis": "Snowflake",
    "titleSuffix": "",
    "subtitle": "Hiding sensitive column values instead of filtering rows"
  }
]
