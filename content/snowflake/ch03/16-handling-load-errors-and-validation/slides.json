[
  {
    "type": "title",
    "eyebrow": "Snowflake · Chapter 3, Lesson 16",
    "titlePlain": "Handling Load Errors & ",
    "titleEmphasis": "Validation",
    "titleSuffix": "",
    "subtitle": "What happens when a row doesn't parse cleanly"
  },
  {
    "type": "code",
    "eyebrow": "ON_ERROR options",
    "code": "COPY INTO target_table\nFROM @my_stage\nFILE_FORMAT = (FORMAT_NAME = 'csv_ff')\nON_ERROR = 'CONTINUE';  -- default is ABORT_STATEMENT",
    "caption": "ABORT_STATEMENT fails loud by default; CONTINUE skips just the bad row."
  },
  {
    "type": "code",
    "eyebrow": "Dry run before committing",
    "code": "COPY INTO target_table\nFROM @my_stage\nFILE_FORMAT = (FORMAT_NAME = 'csv_ff')\nVALIDATION_MODE = 'RETURN_ERRORS';",
    "caption": "Reports every row that would fail — loads nothing into the table."
  },
  {
    "type": "code",
    "eyebrow": "Checking history after the fact",
    "code": "SELECT file_name, status, error_count, first_error_message\nFROM TABLE(INFORMATION_SCHEMA.COPY_HISTORY(\n  TABLE_NAME => 'target_table',\n  START_TIME => DATEADD(hours, -24, CURRENT_TIMESTAMP())\n))\nWHERE error_count > 0;",
    "caption": "COPY_HISTORY needs a table name and time window by design."
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Lab: Loading a Real ",
    "titleEmphasis": "Dataset",
    "titleSuffix": " Into Snowflake",
    "subtitle": "Stages, file formats, COPY INTO, and error handling together"
  }
]
