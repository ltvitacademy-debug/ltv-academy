[
  {
    "type": "title",
    "eyebrow": "The T-SQL Development Course · Chapter Ten",
    "titlePlain": "Temp Tables vs. Table Variables vs. ",
    "titleEmphasis": "CTEs",
    "titleSuffix": "",
    "subtitle": "Lesson 93 · Performance Tuning"
  },
  {
    "type": "code",
    "eyebrow": "#Temp table — a real table in tempdb",
    "code": "CREATE TABLE #ActiveProducts (\n  ProductID INT, Name NVARCHAR(50), ListPrice MONEY\n);\n\nINSERT INTO #ActiveProducts\nSELECT ProductID, Name, ListPrice\nFROM Production.Product\nWHERE SellEndDate IS NULL;",
    "caption": "Physically stored, statistics maintained, can be indexed — best for large, reused sets."
  },
  {
    "type": "code",
    "eyebrow": "@Table variable — batch-scoped",
    "code": "DECLARE @ActiveProducts TABLE (\n  ProductID INT, Name NVARCHAR(50), ListPrice MONEY\n);\n\nINSERT INTO @ActiveProducts\nSELECT ProductID, Name, ListPrice\nFROM Production.Product\nWHERE SellEndDate IS NULL;",
    "caption": "Also in tempdb, but historically no statistics — the optimizer guesses. Best for small sets."
  },
  {
    "type": "code",
    "eyebrow": "CTE — nothing is stored",
    "code": "WITH ActiveProducts AS (\n  SELECT ProductID, Name, ListPrice\n  FROM Production.Product\n  WHERE SellEndDate IS NULL\n)\nSELECT * FROM ActiveProducts;",
    "caption": "A named subquery, inlined at each reference — re-run every time it's used, unless SQL Server spools it."
  },
  {
    "type": "steps",
    "eyebrow": "The decision rule",
    "titlePlain": "Pick by ",
    "titleEmphasis": "size and reuse",
    "titleSuffix": "",
    "steps": [
      { "label": "#TEMP TABLE", "sub": "large, reused, might need an index" },
      { "label": "@TABLE VARIABLE", "sub": "small, inside a procedure, avoid recompiles" },
      { "label": "CTE", "sub": "one-time readability, or recursion" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 94",
    "titlePlain": "Common T-SQL Performance ",
    "titleEmphasis": "Anti-Patterns",
    "titleSuffix": "",
    "subtitle": "Wrapping up Performance Tuning with the mistakes to avoid."
  }
]
