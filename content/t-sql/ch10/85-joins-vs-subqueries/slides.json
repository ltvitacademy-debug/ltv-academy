[
  {
    "type": "title",
    "eyebrow": "The T-SQL Development Course · Chapter Ten",
    "titlePlain": "Joins vs. ",
    "titleEmphasis": "Subqueries",
    "titleSuffix": "",
    "subtitle": "Lesson 85 · Performance Tuning"
  },
  {
    "type": "code",
    "eyebrow": "Two queries, IDENTICAL results",
    "code": "-- Correlated subquery (Lesson 54):\nWHERE p1.ListPrice > (\n  SELECT AVG(p2.ListPrice) FROM Production.Product AS p2\n  WHERE p2.ProductSubcategoryID = p1.ProductSubcategoryID\n);\n\n-- JOIN rewrite, using a CTE:\nWITH CategoryAverages AS (...)\n... INNER JOIN CategoryAverages AS ca ...\nWHERE p.ListPrice > ca.AvgPrice;",
    "caption": "Both return the exact same rows — the difference is entirely in HOW they get there."
  },
  {
    "type": "steps",
    "eyebrow": "Why the JOIN usually wins",
    "titlePlain": "N calculations vs. ",
    "titleEmphasis": "M",
    "titleSuffix": "",
    "steps": [
      { "label": "SUBQUERY", "sub": "re-runs once per outer row (~N times)" },
      { "label": "JOIN + CTE", "sub": "computes each average ONCE (~M times)" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "When a subquery IS the right choice",
    "code": "-- Scalar subquery: already runs once — no per-row cost\n-- EXISTS/NOT EXISTS: often no faster join equivalent\n-- The optimizer may rewrite simple cases internally anyway",
    "caption": "\"Which is faster\" sometimes means: check the execution plan (Lesson 90)."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 86",
    "titlePlain": "EXISTS vs. IN vs. ",
    "titleEmphasis": "JOIN",
    "titleSuffix": "",
    "subtitle": "For existence checks specifically."
  }
]
