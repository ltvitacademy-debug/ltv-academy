{
  "lesson": "sum-avg",
  "questions": [
    {
      "q": "What does SUM(ListPrice) return?",
      "options": [
        "The total of every non-NULL ListPrice value across all matching rows",
        "The number of rows with a ListPrice",
        "The highest ListPrice value",
        "The average of all ListPrice values"
      ],
      "answer": 0,
      "explain": "SUM adds every value in the column together into one total."
    },
    {
      "q": "What does AVG() compute?",
      "options": [
        "The mean — the sum divided by the count of non-NULL values",
        "The middle value when sorted (median)",
        "The most frequently occurring value",
        "The difference between the highest and lowest value"
      ],
      "answer": 0,
      "explain": "AVG is the built-in way to compute the mean, equivalent to SUM divided by COUNT of non-NULL values."
    },
    {
      "q": "Do SUM() and AVG() treat NULL values as zero?",
      "options": [
        "No — they skip NULL values entirely, the same way COUNT(column) does",
        "Yes, NULL is always treated as zero in aggregate functions",
        "SUM treats NULL as zero, but AVG does not",
        "AVG treats NULL as zero, but SUM does not"
      ],
      "answer": 0,
      "explain": "Both functions ignore NULLs rather than counting them as zero — consistent with the NULL-skipping behavior from Lesson 32."
    },
    {
      "q": "Why does NULL-skipping matter specifically for AVG()?",
      "options": [
        "If NULL were treated as zero, it would artificially pull the average down, misrepresenting the true mean of existing values",
        "It doesn't matter for AVG specifically — only for SUM",
        "AVG cannot be computed at all if any NULLs exist",
        "NULL-skipping only affects the column's data type"
      ],
      "answer": 0,
      "explain": "Treating missing data as zero would distort the average; skipping it keeps AVG() reflecting only the values that actually exist."
    },
    {
      "q": "In 'SELECT AVG(ListPrice) FROM Production.Product WHERE Color = 'Red';', what does the WHERE clause affect?",
      "options": [
        "It filters rows before AVG runs, so only red products are averaged",
        "It has no effect on the aggregate calculation",
        "It filters the result AFTER the average is computed",
        "It changes AVG into SUM automatically"
      ],
      "answer": 0,
      "explain": "This follows the logical processing order from Lesson 11 — WHERE filters rows first, and the aggregate operates only on what's left."
    }
  ]
}
