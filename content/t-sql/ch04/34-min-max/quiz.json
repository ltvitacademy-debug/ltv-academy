{
  "lesson": "min-max",
  "questions": [
    {
      "q": "What does MIN(ListPrice) return?",
      "options": [
        "The smallest non-NULL ListPrice value across all matching rows",
        "The number of distinct ListPrice values",
        "The average ListPrice",
        "The largest ListPrice value"
      ],
      "answer": 0,
      "explain": "MIN() finds the smallest value; MAX() finds the largest."
    },
    {
      "q": "Do MIN() and MAX() work on text columns?",
      "options": [
        "Yes — they follow alphabetical order, returning the earliest or latest sorting value",
        "No, MIN/MAX only work on numeric columns",
        "Only MAX works on text; MIN does not",
        "Text columns must be converted to numbers first"
      ],
      "answer": 0,
      "explain": "MIN(LastName) returns whichever last name sorts earliest alphabetically; MAX(LastName) returns the latest."
    },
    {
      "q": "How do MIN() and MAX() treat date columns?",
      "options": [
        "The same way as numbers — earlier dates are 'smaller,' later dates are 'larger'",
        "Dates cannot be used with MIN or MAX",
        "Only the year portion of the date is considered",
        "Dates are converted to text and sorted alphabetically instead"
      ],
      "answer": 0,
      "explain": "This mirrors the comparison operator behavior from Lesson 12 — dates compare just like numbers."
    },
    {
      "q": "Can a NULL value ever be returned as the result of MIN() or MAX()?",
      "options": [
        "No — NULL is skipped, since it isn't a comparable value at all",
        "Yes, NULL always wins as the minimum",
        "Yes, NULL always wins as the maximum",
        "Only if every value in the column is NULL is this a special exception with a different rule"
      ],
      "answer": 0,
      "explain": "Consistent with COUNT, SUM, and AVG — NULL is excluded from consideration when finding the extreme value."
    },
    {
      "q": "Can MIN and MAX both appear in the same SELECT statement?",
      "options": [
        "Yes — e.g. SELECT MIN(ListPrice), MAX(ListPrice) FROM Production.Product;",
        "No, only one aggregate function is allowed per query",
        "Only if they reference different tables",
        "Only inside a CASE expression"
      ],
      "answer": 0,
      "explain": "Multiple aggregate functions can appear together in one SELECT list, each computing its own summary value."
    }
  ]
}
