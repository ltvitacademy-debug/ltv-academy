{
  "lesson": "union-vs-union-all",
  "questions": [
    {
      "q": "How does UNION differ from JOIN?",
      "options": [
        "UNION stacks query results top to bottom (more rows); JOIN combines tables side by side (more columns)",
        "UNION and JOIN are functionally identical",
        "UNION only works within a single table; JOIN requires two tables",
        "JOIN removes duplicates; UNION never does"
      ],
      "answer": 0,
      "explain": "This is the fundamental distinction — UNION appends result sets; JOIN widens rows with matched columns."
    },
    {
      "q": "What must be true of the queries combined with UNION?",
      "options": [
        "They must return the same number of columns, with compatible types in the same position",
        "They must query the exact same table",
        "They must both include a WHERE clause",
        "They must both use GROUP BY"
      ],
      "answer": 0,
      "explain": "Column count and type compatibility are the hard requirements — column names come from the first query."
    },
    {
      "q": "What is the one functional difference between UNION and UNION ALL?",
      "options": [
        "UNION removes duplicate rows across the combined result; UNION ALL keeps every row",
        "UNION ALL requires more columns than UNION",
        "UNION can combine more than two queries; UNION ALL cannot",
        "UNION ALL only works with numeric columns"
      ],
      "answer": 0,
      "explain": "That's the entire distinction — UNION behaves like an implicit DISTINCT applied across both result sets."
    },
    {
      "q": "Why is UNION ALL generally faster than UNION?",
      "options": [
        "UNION has to compare every row against every other row to find and remove duplicates; UNION ALL skips that step",
        "UNION ALL uses a different indexing strategy that UNION cannot access",
        "UNION ALL returns fewer rows, which is inherently faster",
        "There is no performance difference between the two"
      ],
      "answer": 0,
      "explain": "Deduplication is real computational work — skipping it when you don't need it is a meaningful performance win."
    },
    {
      "q": "What is the recommended default between UNION and UNION ALL, according to this lesson?",
      "options": [
        "Default to UNION ALL unless you specifically need duplicates removed",
        "Default to UNION always, for safety",
        "They should never be used interchangeably in any circumstance",
        "Always use UNION ALL, since UNION is deprecated"
      ],
      "answer": 0,
      "explain": "Don't pay for deduplication you don't actually need — UNION ALL is at least as fast, often significantly faster."
    }
  ]
}
