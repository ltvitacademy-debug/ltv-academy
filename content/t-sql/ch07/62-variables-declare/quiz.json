{
  "lesson": "variables-declare",
  "questions": [
    {
      "q": "What does DECLARE do?",
      "options": [
        "Creates a variable with a specified data type",
        "Deletes a variable",
        "Runs a stored procedure",
        "Creates a new table"
      ],
      "answer": 0,
      "explain": "DECLARE @VariableName DataType; is the syntax to create a variable."
    },
    {
      "q": "What must every T-SQL variable name start with?",
      "options": [
        "@",
        "#",
        "$",
        "&"
      ],
      "answer": 0,
      "explain": "The @ prefix distinguishes a variable from a column or table name at a glance."
    },
    {
      "q": "What does SET do?",
      "options": [
        "Assigns a single value to a variable",
        "Creates a new variable",
        "Deletes a table",
        "Declares a new data type"
      ],
      "answer": 0,
      "explain": "SET @VariableName = value; assigns a value after DECLARE has created the variable."
    },
    {
      "q": "What is a common use of assigning a variable with SELECT instead of SET?",
      "options": [
        "Capturing the result of a query (like an aggregate) into a variable for reuse later",
        "SELECT cannot be used to assign variables in T-SQL",
        "SELECT can only assign string values, never numbers",
        "SELECT assignment is identical to DECLARE"
      ],
      "answer": 0,
      "explain": "SELECT @Var = AVG(ListPrice) FROM Product captures a computed value once, avoiding recalculating it multiple times."
    },
    {
      "q": "Can a variable be given an initial value directly in its DECLARE statement?",
      "options": [
        "Yes — e.g. DECLARE @MinPrice MONEY = 100;",
        "No, a separate SET statement is always required",
        "Only for INT variables",
        "Only when declaring multiple variables at once"
      ],
      "answer": 0,
      "explain": "DECLARE can include an initial assignment inline, as an alternative to a separate SET statement."
    }
  ]
}
