{
  "lesson": "if-else",
  "questions": [
    {
      "q": "What is the fundamental difference between CASE and IF/ELSE?",
      "options": [
        "CASE is an expression that evaluates to a value; IF/ELSE is a statement that controls which block of code runs",
        "They are functionally identical, just different syntax",
        "IF/ELSE can be used inside a SELECT list; CASE cannot",
        "CASE can only be used in stored procedures; IF/ELSE cannot"
      ],
      "answer": 0,
      "explain": "This distinction was first introduced in Lesson 7 and is central to understanding control flow vs. expressions."
    },
    {
      "q": "Can IF be used inside a SELECT list the way CASE can?",
      "options": [
        "No — IF decides between running one chunk of T-SQL or another, it doesn't produce a value inline",
        "Yes, IF works identically to CASE inside SELECT",
        "Only in SQL Server 2019 and later",
        "Only when combined with a subquery"
      ],
      "answer": 0,
      "explain": "This is exactly why CASE and IF/ELSE serve different purposes despite superficial similarity."
    },
    {
      "q": "What does PRINT do?",
      "options": [
        "Outputs a message to the Messages tab in SSMS",
        "Inserts a row into a table",
        "Displays a value in the Results grid",
        "Creates a new variable"
      ],
      "answer": 0,
      "explain": "PRINT is useful for demonstration and debugging, distinct from returning query results."
    },
    {
      "q": "In a chain of ELSE IF conditions, when is each one evaluated?",
      "options": [
        "Only if every condition above it was false, evaluated top to bottom",
        "All conditions are evaluated simultaneously",
        "Only the last ELSE IF is ever checked",
        "In random order"
      ],
      "answer": 0,
      "explain": "This mirrors the top-to-bottom evaluation order from searched CASE (Lesson 8), just applied to control flow."
    },
    {
      "q": "What kind of expression does an IF condition evaluate?",
      "options": [
        "A predicate, exactly like a WHERE condition",
        "Only a column name, with no comparison allowed",
        "A CASE expression specifically",
        "IF cannot evaluate expressions, only literal TRUE/FALSE values"
      ],
      "answer": 0,
      "explain": "IF's condition is evaluated the same way a WHERE clause's predicate is (Lesson 20's concept), just used for branching instead of filtering."
    }
  ]
}
