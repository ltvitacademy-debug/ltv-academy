[
  {
    "type": "title",
    "eyebrow": "The T-SQL Development Course · Chapter Seven",
    "titlePlain": "",
    "titleEmphasis": "IF/ELSE",
    "titleSuffix": "",
    "subtitle": "Lesson 64 · Programming with T-SQL"
  },
  {
    "type": "code",
    "eyebrow": "A real statement, not a value",
    "code": "DECLARE @ProductCount INT;\nSELECT @ProductCount = COUNT(*) FROM Production.Product;\n\nIF @ProductCount > 500\n    PRINT 'Large catalog';\nELSE\n    PRINT 'Small catalog';",
    "caption": "IF controls which CODE runs — unlike CASE, it can't sit inside a SELECT list."
  },
  {
    "type": "code",
    "eyebrow": "Chaining with ELSE IF",
    "code": "IF @ProductCount > 1000\n    PRINT 'Very large catalog';\nELSE IF @ProductCount > 500\n    PRINT 'Large catalog';\nELSE IF @ProductCount > 100\n    PRINT 'Medium catalog';\nELSE\n    PRINT 'Small catalog';",
    "caption": "Same top-to-bottom order as searched CASE (Lesson 8) — applied to control flow instead."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 65",
    "titlePlain": "BEGIN/END ",
    "titleEmphasis": "Blocks",
    "titleSuffix": "",
    "subtitle": "Running more than one statement per branch."
  }
]
