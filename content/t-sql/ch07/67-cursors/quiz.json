{
  "lesson": "cursors",
  "questions": [
    {
      "q": "What does a cursor let you do?",
      "options": [
        "Step through a query's results one row at a time, in code",
        "Return a query's entire result set instantly, like a normal SELECT",
        "Create a new table from a query",
        "Combine multiple queries with UNION"
      ],
      "answer": 0,
      "explain": "Cursors provide row-by-row processing, unlike the set-based nature of a regular query."
    },
    {
      "q": "What does OPEN do in the cursor pattern?",
      "options": [
        "Executes the cursor's query and positions it before the first row",
        "Retrieves the first row's values into variables",
        "Releases the cursor's result set",
        "Removes the cursor definition entirely"
      ],
      "answer": 0,
      "explain": "OPEN runs the underlying SELECT and readies the cursor for FETCH operations."
    },
    {
      "q": "What does @@FETCH_STATUS indicate?",
      "options": [
        "0 if the last FETCH succeeded, nonzero once there are no more rows",
        "The total number of rows in the cursor",
        "The current row number being processed",
        "Whether the cursor has been deallocated"
      ],
      "answer": 0,
      "explain": "This system function is what drives the WHILE loop that processes each row, stopping automatically when rows are exhausted."
    },
    {
      "q": "Why are both CLOSE and DEALLOCATE required?",
      "options": [
        "CLOSE releases the result set; DEALLOCATE removes the cursor definition — skipping either leaves resources tied up",
        "They do the exact same thing, so only one is technically needed",
        "DEALLOCATE must be called before OPEN",
        "CLOSE is optional, but DEALLOCATE is required"
      ],
      "answer": 0,
      "explain": "Both are necessary cleanup steps to avoid tying up server resources unnecessarily."
    },
    {
      "q": "Why should cursors be a last resort rather than a first choice?",
      "options": [
        "They are the most expensive row-by-row option in T-SQL, and a set-based query is almost always faster",
        "Cursors are deprecated and no longer supported",
        "Cursors cannot be combined with WHILE loops",
        "Cursors can only process a maximum of 10 rows"
      ],
      "answer": 0,
      "explain": "Legitimate use cases exist (like certain administrative tasks), but processing business data row by row with a cursor usually has a faster set-based alternative."
    }
  ]
}
