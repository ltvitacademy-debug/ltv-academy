[
  {
    "type": "title",
    "eyebrow": "The T-SQL Development Course · Chapter Seven",
    "titlePlain": "The ",
    "titleEmphasis": "MERGE",
    "titleSuffix": " Statement",
    "subtitle": "Lesson 71 · Programming with T-SQL"
  },
  {
    "type": "code",
    "eyebrow": "Insert, update, delete — one statement",
    "code": "MERGE Production.Product AS target\nUSING (SELECT ProductID, Name, ListPrice\n       FROM Production.Product\n       WHERE ProductSubcategoryID = 1) AS source\n    ON target.ProductID = source.ProductID\nWHEN MATCHED THEN\n    UPDATE SET target.ListPrice = source.ListPrice * 1.05\nWHEN NOT MATCHED BY TARGET THEN\n    INSERT (ProductID, Name, ListPrice)\n    VALUES (source.ProductID, source.Name, source.ListPrice)\nWHEN NOT MATCHED BY SOURCE\n    AND target.ProductSubcategoryID = 1 THEN\n    DELETE;",
    "caption": "In practice, \"source\" is almost always a different table — like a staging table."
  },
  {
    "type": "steps",
    "eyebrow": "The three WHEN clauses = join logic",
    "titlePlain": "Same idea as ",
    "titleEmphasis": "JOIN",
    "titleSuffix": ", different action",
    "steps": [
      { "label": "MATCHED", "sub": "in both (= INNER JOIN) → UPDATE" },
      { "label": "NOT MATCHED BY TARGET", "sub": "only in source → INSERT" },
      { "label": "NOT MATCHED BY SOURCE", "sub": "only in target → DELETE" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Chapter 8",
    "titlePlain": "Transactions and Error ",
    "titleEmphasis": "Handling",
    "titleSuffix": "",
    "subtitle": "Making sure scripts behave correctly when something goes wrong."
  }
]
