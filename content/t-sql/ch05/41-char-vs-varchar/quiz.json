{
  "lesson": "char-vs-varchar",
  "questions": [
    {
      "q": "What does CHAR(5) do when storing the value 'OK'?",
      "options": [
        "Pads it with trailing spaces to store exactly 5 characters: 'OK   '",
        "Truncates it to 2 characters and shrinks the column",
        "Rejects the value with an error",
        "Stores only 'OK' using 2 characters of space, same as VARCHAR"
      ],
      "answer": 0,
      "explain": "CHAR always allocates and stores the full declared length, padding shorter values with spaces."
    },
    {
      "q": "How does VARCHAR(50) store a 3-character name?",
      "options": [
        "Using roughly 3 characters of storage, not the full 50",
        "Using exactly 50 characters, padded like CHAR",
        "It rejects values shorter than 50 characters",
        "It rounds up to the nearest 10 characters"
      ],
      "answer": 0,
      "explain": "VARCHAR only uses the space actually needed for the stored value, up to its declared maximum."
    },
    {
      "q": "When should you prefer CHAR over VARCHAR?",
      "options": [
        "For values that are genuinely fixed-length in the real world, like country codes or state abbreviations",
        "For all text columns, always",
        "Only for numeric data",
        "CHAR should never be used in modern SQL Server"
      ],
      "answer": 0,
      "explain": "CHAR fits data with a truly consistent length; using it for variable-length text wastes space."
    },
    {
      "q": "What happens if a value exceeds a column's declared CHAR/VARCHAR length?",
      "options": [
        "The value is truncated (or an error is raised, depending on settings) rather than the column silently growing",
        "The column automatically resizes itself to fit the longer value",
        "SQL Server converts the column to a different data type automatically",
        "Nothing — there is no length enforcement at all"
      ],
      "answer": 0,
      "explain": "A declared length is a hard limit — oversized input causes truncation or an error, a common source of unexpected data loss."
    },
    {
      "q": "Why is VARCHAR the overwhelmingly common default for real-world text?",
      "options": [
        "Most real text (names, addresses, descriptions) genuinely varies in length, and VARCHAR avoids wasting space on padding",
        "VARCHAR is always faster than CHAR in every scenario",
        "CHAR cannot store more than 10 characters",
        "VARCHAR is required for any column that might contain a NULL"
      ],
      "answer": 0,
      "explain": "Using CHAR for variable-length data wastes storage and can introduce subtle trailing-space comparison bugs."
    }
  ]
}
