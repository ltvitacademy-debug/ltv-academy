{
  "lesson": "len-upper-lower",
  "questions": [
    {
      "q": "What does LEN('Hello   ') (with three trailing spaces) return?",
      "options": [
        "5 — LEN ignores trailing spaces",
        "8 — LEN counts every character including trailing spaces",
        "0",
        "An error, since trailing spaces are invalid"
      ],
      "answer": 0,
      "explain": "LEN() specifically ignores trailing spaces when counting, which can be surprising."
    },
    {
      "q": "Does LEN() ignore leading spaces the same way it ignores trailing ones?",
      "options": [
        "No — leading spaces DO count toward the length",
        "Yes, both leading and trailing spaces are ignored",
        "LEN() ignores all spaces anywhere in the string",
        "LEN() cannot process strings containing spaces at all"
      ],
      "answer": 0,
      "explain": "Only trailing spaces are excluded from the count — leading spaces are counted normally."
    },
    {
      "q": "Why does the trailing-space behavior of LEN() matter more for CHAR columns than VARCHAR?",
      "options": [
        "CHAR pads values with trailing spaces to reach its declared length, and LEN() won't count that padding",
        "VARCHAR always has trailing spaces; CHAR never does",
        "LEN() cannot be used on CHAR columns at all",
        "There's no actual difference between how LEN() treats CHAR vs. VARCHAR"
      ],
      "answer": 0,
      "explain": "This directly ties back to Lesson 41 — CHAR's padding behavior interacts with LEN()'s trailing-space exclusion."
    },
    {
      "q": "Do UPPER() and LOWER() change the underlying stored data?",
      "options": [
        "No — like every function used so far, they only affect the query's output",
        "Yes, they permanently modify the table's data",
        "Only UPPER() modifies the data; LOWER() does not",
        "They modify the data only when combined with GROUP BY"
      ],
      "answer": 0,
      "explain": "Consistent with every other function in this course — these are output transformations, not data modifications."
    },
    {
      "q": "Why might you use UPPER() on both sides of a WHERE comparison, like WHERE UPPER(LastName) = UPPER('smith')?",
      "options": [
        "To guarantee a case-insensitive comparison regardless of the server's default collation settings",
        "To make the query run faster in all cases",
        "Because WHERE cannot compare lowercase text otherwise",
        "It has no practical effect and is purely stylistic"
      ],
      "answer": 0,
      "explain": "Most SQL Server setups already compare case-insensitively by default, but explicit UPPER()/UPPER() removes any doubt."
    }
  ]
}
