{
  "lesson": "date-functions",
  "questions": [
    {
      "q": "What does GETDATE() return?",
      "options": [
        "The current date and time from the server",
        "The date a specific row was created, from a system table",
        "Only the current time, with no date",
        "The earliest date in a specified column"
      ],
      "answer": 0,
      "explain": "GETDATE() is commonly used for timestamping and for date-math referencing 'today.'"
    },
    {
      "q": "How do you make DATEADD go backward instead of forward?",
      "options": [
        "Pass a negative number for the amount to add",
        "Swap the order of the interval and date arguments",
        "DATEADD can only move forward, never backward",
        "Use DATESUB instead of DATEADD"
      ],
      "answer": 0,
      "explain": "DATEADD(MONTH, -3, GETDATE()) computes three months ago — negative numbers subtract."
    },
    {
      "q": "What does DATEDIFF(DAY, OrderDate, DueDate) return?",
      "options": [
        "The number of day boundaries crossed between OrderDate and DueDate",
        "The exact time of day DueDate occurs",
        "A boolean TRUE/FALSE indicating whether the dates match",
        "The sum of OrderDate and DueDate"
      ],
      "answer": 0,
      "explain": "DATEDIFF measures the distance between two dates in the specified interval unit."
    },
    {
      "q": "What happens if you swap DATEDIFF's start and end date arguments?",
      "options": [
        "The sign of the result flips (positive becomes negative, or vice versa)",
        "The function throws an error",
        "The result is unaffected — order doesn't matter",
        "It automatically corrects the order for you"
      ],
      "answer": 0,
      "explain": "Order matters in DATEDIFF — reversing startDate and endDate negates the result."
    },
    {
      "q": "In 'WHERE OrderDate > DATEADD(YEAR, -1, GETDATE())', what is being computed?",
      "options": [
        "Orders placed within the last year, using DATEADD to compute the date one year before now",
        "Orders placed exactly one year from today",
        "The total count of orders from any date",
        "This filters out all orders entirely"
      ],
      "answer": 0,
      "explain": "DATEADD(YEAR, -1, GETDATE()) computes 'one year ago from right now,' and WHERE keeps only more recent orders."
    }
  ]
}
