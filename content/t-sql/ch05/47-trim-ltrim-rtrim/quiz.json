{
  "lesson": "trim-ltrim-rtrim",
  "questions": [
    {
      "q": "What does LTRIM() remove?",
      "options": [
        "Leading (left-side) spaces only",
        "Trailing (right-side) spaces only",
        "Both leading and trailing spaces",
        "All spaces anywhere in the string, including the middle"
      ],
      "answer": 0,
      "explain": "LTRIM specifically targets the left/leading side of the string."
    },
    {
      "q": "What is TRIM() shorthand for?",
      "options": [
        "LTRIM(RTRIM(...)) — removing both leading and trailing whitespace in one call",
        "A function that removes all whitespace, including in the middle of the string",
        "A synonym for REPLACE",
        "A function exclusive to CHAR columns"
      ],
      "answer": 0,
      "explain": "TRIM() is the modern, cleaner way to do what previously required nesting LTRIM and RTRIM together."
    },
    {
      "q": "Do LTRIM, RTRIM, or TRIM affect spaces in the middle of a string?",
      "options": [
        "No — none of them touch internal whitespace, only leading/trailing",
        "Yes, all three remove every space in the string",
        "Only TRIM affects internal spaces",
        "Only RTRIM affects internal spaces"
      ],
      "answer": 0,
      "explain": "These functions are strictly about the edges of a string, not its interior."
    },
    {
      "q": "Why can a trailing space cause 'WHERE FirstName = \\'John \\'' to fail matching 'John'?",
      "options": [
        "Exact-match comparison treats 'John ' and 'John' as different strings, even though they look identical",
        "WHERE automatically trims all string literals before comparing",
        "Trailing spaces are stripped automatically by SQL Server",
        "This comparison would always succeed regardless of trailing spaces"
      ],
      "answer": 0,
      "explain": "Untrimmed whitespace is a common, invisible cause of comparisons silently failing to match expected rows."
    },
    {
      "q": "Which of these is a realistic real-world source of messy whitespace in data?",
      "options": [
        "Copy-pasted data from spreadsheets or web forms, or data imported from external systems",
        "Whitespace never occurs in real databases",
        "Only numeric columns can contain unwanted whitespace",
        "TRIM() itself introduces the whitespace it later has to remove"
      ],
      "answer": 0,
      "explain": "User input, copy-paste, and imports from other systems are common, realistic sources of stray whitespace."
    }
  ]
}
