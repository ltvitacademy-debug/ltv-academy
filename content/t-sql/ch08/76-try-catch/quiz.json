{
  "lesson": "try-catch",
  "questions": [
    {
      "q": "Without TRY/CATCH, what happens when a runtime error occurs mid-script?",
      "options": [
        "The batch stops at that point, and the error propagates up to whatever called it",
        "SQL Server automatically retries the statement",
        "The error is silently ignored",
        "The rest of the script runs normally, skipping only the failed line"
      ],
      "answer": 0,
      "explain": "This is exactly the problem TRY/CATCH exists to let you handle gracefully."
    },
    {
      "q": "What happens the instant an error occurs inside a TRY block?",
      "options": [
        "Execution jumps immediately to the CATCH block, skipping whatever remained in TRY",
        "The whole script terminates immediately with no further processing",
        "The error is logged but execution continues in TRY as normal",
        "CATCH runs first, then TRY resumes"
      ],
      "answer": 0,
      "explain": "The remainder of the TRY block never executes once an error is caught."
    },
    {
      "q": "What does ERROR_MESSAGE() return when called outside a CATCH block?",
      "options": [
        "NULL",
        "The most recent error from any prior script",
        "An empty string",
        "It causes a syntax error"
      ],
      "answer": 0,
      "explain": "ERROR_MESSAGE(), ERROR_NUMBER(), and ERROR_LINE() are only meaningful inside a CATCH block."
    },
    {
      "q": "Why should ROLLBACK live inside the CATCH block when combining TRY/CATCH with a transaction?",
      "options": [
        "It guarantees atomicity even in the face of an unexpected error, rather than leaving a partial change behind",
        "ROLLBACK cannot be used anywhere except inside CATCH",
        "It has no functional purpose there — it's purely a style choice",
        "It prevents the CATCH block from running at all"
      ],
      "answer": 0,
      "explain": "This ties Lessons 72, 73, and 76 together — CATCH is exactly where you'd handle an unexpected failure mid-transaction."
    },
    {
      "q": "If no error occurs inside a TRY block, does the matching CATCH block ever run?",
      "options": [
        "No — CATCH only runs when an error actually occurs inside TRY",
        "Yes, CATCH always runs regardless of whether an error occurred",
        "Only if the TRY block contains a transaction",
        "Only on the first execution of the script"
      ],
      "answer": 0,
      "explain": "CATCH is purely error-triggered — a successful TRY block skips it entirely."
    }
  ]
}
