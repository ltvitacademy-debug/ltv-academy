{
  "lesson": "transactions",
  "questions": [
    {
      "q": "What problem does a transaction solve?",
      "options": [
        "A multi-step change failing halfway through, leaving data in an inconsistent state",
        "Slow query performance",
        "Duplicate rows in a table",
        "Incorrect column data types"
      ],
      "answer": 0,
      "explain": "Without a transaction, one statement succeeding and the next failing can leave data corrupted (like stock vanishing between locations)."
    },
    {
      "q": "What does BEGIN TRANSACTION do?",
      "options": [
        "Marks the start of a group of statements that must all succeed together",
        "Immediately saves all pending changes permanently",
        "Deletes all uncommitted changes",
        "Creates a new database"
      ],
      "answer": 0,
      "explain": "It opens a transaction boundary — nothing inside it is permanent until COMMIT."
    },
    {
      "q": "What does COMMIT TRANSACTION do?",
      "options": [
        "Makes every change made since BEGIN TRANSACTION permanent, all at once",
        "Undoes every change since BEGIN TRANSACTION",
        "Starts a new transaction",
        "Only saves the first statement in the transaction"
      ],
      "answer": 0,
      "explain": "COMMIT is what finalizes the transaction's changes as permanent."
    },
    {
      "q": "What does ROLLBACK TRANSACTION do?",
      "options": [
        "Undoes every change made since the matching BEGIN TRANSACTION, as if none of it happened",
        "Makes the changes permanent, same as COMMIT",
        "Only undoes the most recent statement, not the whole transaction",
        "Restarts the transaction from the beginning"
      ],
      "answer": 0,
      "explain": "ROLLBACK is the safety net that prevents a half-finished multi-step change from corrupting data."
    },
    {
      "q": "Before COMMIT runs, are the transaction's changes visible outside the current session?",
      "options": [
        "No — they are provisional, visible within the session but not yet permanently saved",
        "Yes, they are immediately visible and permanent to everyone",
        "Changes are never visible until the server restarts",
        "This depends entirely on the table's data type"
      ],
      "answer": 0,
      "explain": "Changes inside an open transaction remain provisional until COMMIT finalizes them."
    }
  ]
}
