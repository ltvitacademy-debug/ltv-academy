{
  "lesson": "auth-modes",
  "questions": [
    {
      "q": "What does Windows Authentication Mode accept?",
      "options": [
        "Only Windows logins — SQL Server logins are rejected outright, even if one exists",
        "Only SQL Server logins",
        "Both Windows and SQL Server logins",
        "Neither — it requires a third-party authentication provider"
      ],
      "answer": 0,
      "explain": "This mode is exclusively Windows-based, rejecting SQL Server Authentication attempts entirely."
    },
    {
      "q": "What does Mixed Mode accept?",
      "options": [
        "Both Windows logins and SQL Server logins",
        "Only SQL Server logins",
        "Only Windows logins",
        "Only logins from a specific domain"
      ],
      "answer": 0,
      "explain": "Mixed Mode's formal name is 'SQL Server and Windows Authentication Mode' — both are accepted."
    },
    {
      "q": "Is there a mode that accepts only SQL Server logins and rejects Windows ones?",
      "options": [
        "No — Windows Authentication is always available in both modes",
        "Yes, it's called SQL Server Authentication Mode",
        "Yes, but only in cloud-hosted SQL Server instances",
        "This depends on the SQL Server edition"
      ],
      "answer": 0,
      "explain": "The only two modes are Windows Authentication Mode and Mixed Mode — there's no SQL-Server-only mode."
    },
    {
      "q": "Why might an organization choose Windows Authentication Mode alone?",
      "options": [
        "It's generally more secure for an all-Windows environment, centralizing credentials in Active Directory",
        "It's the only mode that supports stored procedures",
        "It's required for using transactions",
        "It's the only mode that allows more than one user to connect"
      ],
      "answer": 0,
      "explain": "This avoids managing a second, separate set of credentials outside Active Directory."
    },
    {
      "q": "What happens if a server is in Windows Authentication Mode only and someone tries to connect with a SQL Server login?",
      "options": [
        "The connection fails outright, regardless of whether the username/password are correct",
        "The connection succeeds, but with limited permissions",
        "SQL Server automatically converts it to a Windows login",
        "It works fine, since Mixed Mode is always enabled by default"
      ],
      "answer": 0,
      "explain": "The mode itself blocks the attempt before credentials are even checked."
    }
  ]
}
