{
  "lesson": "unpivot",
  "questions": [
    {
      "q": "What does UNPIVOT do relative to PIVOT?",
      "options": [
        "The exact opposite — turns several columns back into rows",
        "The identical operation, just with different syntax",
        "Removes duplicate rows, unrelated to PIVOT",
        "Adds an aggregate function to a PIVOT result"
      ],
      "answer": 0,
      "explain": "PIVOT goes rows-to-columns; UNPIVOT goes columns-to-rows."
    },
    {
      "q": "In the QuarterlySales example, what do the four Q1Sales-Q4Sales columns become after UNPIVOT?",
      "options": [
        "Two columns — Quarter (which column it came from) and SalesAmount (the value) — with one row per original column",
        "A single combined column with all four values concatenated together",
        "Four separate tables",
        "They remain unchanged — UNPIVOT only affects row count, not columns"
      ],
      "answer": 0,
      "explain": "This is the tall-shape result: one row per SalesID per quarter, instead of one row per SalesID with four quarter columns."
    },
    {
      "q": "Does UNPIVOT require an aggregate function, like PIVOT does?",
      "options": [
        "No — UNPIVOT is just reshaping, with no aggregation involved",
        "Yes, UNPIVOT requires the same aggregate function as PIVOT",
        "Only when combined with GROUP BY",
        "UNPIVOT requires two aggregate functions"
      ],
      "answer": 0,
      "explain": "This is one of the syntax differences between the two — PIVOT summarizes with an aggregate, UNPIVOT just restructures."
    },
    {
      "q": "What kind of real-world data typically needs UNPIVOT applied to it?",
      "options": [
        "Data that arrives already 'wide' (like a spreadsheet import) and needs to be normalized into a tall shape for proper querying",
        "Data that is already normalized and in a tall shape",
        "Only data stored in VARCHAR(MAX) columns",
        "Data with no NULL values anywhere"
      ],
      "answer": 0,
      "explain": "Spreadsheet-style exports or legacy system data with one column per category are the classic UNPIVOT candidates."
    },
    {
      "q": "In UNPIVOT's syntax, what does FOR name?",
      "options": [
        "The new column that holds which original column each row came from",
        "The aggregate function used to summarize values",
        "The table being unpivoted",
        "The WHERE condition applied afterward"
      ],
      "answer": 0,
      "explain": "This mirrors PIVOT's FOR clause, but in the reverse direction — it names the column, not creates new columns."
    }
  ]
}
