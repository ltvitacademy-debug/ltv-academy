{
  "lesson": "views",
  "questions": [
    {
      "q": "What is a view?",
      "options": [
        "A SELECT statement saved in the database under a name, queryable like a table",
        "A physical copy of a table's data",
        "A temporary result set scoped to one statement",
        "A synonym for a stored procedure"
      ],
      "answer": 0,
      "explain": "Unlike a CTE, a view persists in the database and is available to any query with permission."
    },
    {
      "q": "Does a view store a copy of the underlying data?",
      "options": [
        "No — it stores only the SELECT statement itself, run fresh against live data each time it's queried",
        "Yes, a view is a full snapshot copy of the data at creation time",
        "Only the first 1000 rows are stored",
        "It depends on whether ALTER VIEW has been run"
      ],
      "answer": 0,
      "explain": "This is why a view is always automatically up to date — there's no refresh step needed."
    },
    {
      "q": "How does a view differ from a CTE in terms of persistence?",
      "options": [
        "A view persists in the database until explicitly dropped; a CTE only exists for one statement",
        "A CTE persists permanently; a view is temporary",
        "They are functionally identical in every respect",
        "Views can only be used once, like CTEs"
      ],
      "answer": 0,
      "explain": "This is the key distinction previewed at the end of Lesson 56."
    },
    {
      "q": "What does ALTER VIEW do?",
      "options": [
        "Redefines an existing view's saved query",
        "Permanently deletes a view",
        "Creates a brand new view with a different name",
        "Copies data from the view into a new table"
      ],
      "answer": 0,
      "explain": "ALTER VIEW changes what SELECT statement the view runs, without needing to drop and recreate it."
    },
    {
      "q": "Why would a team use a view instead of having everyone write the same complex query repeatedly?",
      "options": [
        "It packages a well-tested query once, under a friendly name, so others can reuse it without re-deriving the logic",
        "Views run faster than any equivalent SELECT statement in all cases",
        "Views are required before any JOIN can be used",
        "Views automatically fix data quality issues in the underlying tables"
      ],
      "answer": 0,
      "explain": "This reusability across developers, reports, and tools is the primary practical value of views."
    }
  ]
}
