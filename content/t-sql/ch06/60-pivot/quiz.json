{
  "lesson": "pivot",
  "questions": [
    {
      "q": "What does PIVOT do?",
      "options": [
        "Turns distinct row values into column headers, aggregating as it goes",
        "Removes duplicate rows from a result set",
        "Sorts a table by multiple columns",
        "Joins two unrelated tables together"
      ],
      "answer": 0,
      "explain": "PIVOT rotates data from a 'tall' shape (many rows) into a 'wide' shape (fewer rows, more columns)."
    },
    {
      "q": "What are the three things PIVOT requires?",
      "options": [
        "An aggregate function, a spreading column (FOR), and an explicit value list (IN)",
        "A WHERE clause, a GROUP BY, and an ORDER BY",
        "Two tables and a JOIN condition",
        "A primary key and a foreign key"
      ],
      "answer": 0,
      "explain": "AVG(ListPrice), FOR Color, and IN ([Red],[Blue],[Black]) are the three required pieces in the example."
    },
    {
      "q": "Why must the values in PIVOT's IN (...) list be named explicitly?",
      "options": [
        "PIVOT does not automatically discover distinct values — you must specify which ones become columns",
        "IN (...) is optional and can be omitted entirely",
        "PIVOT can only handle numeric values in that list",
        "The values are generated automatically from GROUP BY"
      ],
      "answer": 0,
      "explain": "Unlike DISTINCT-based grouping, PIVOT needs the exact set of values you want turned into columns spelled out."
    },
    {
      "q": "Why does the PIVOT example use an inner SELECT as its source instead of the raw table?",
      "options": [
        "PIVOT needs a source narrowed to the relevant columns — pivoting directly on a table with unrelated columns causes confusing results",
        "Inner SELECTs are required syntax for any PIVOT statement, regardless of columns",
        "The inner SELECT removes NULL values automatically",
        "PIVOT cannot reference a table directly under any circumstances"
      ],
      "answer": 0,
      "explain": "Narrowing to just ProductSubcategoryID, Color, and ListPrice avoids unrelated columns interfering with the pivot."
    },
    {
      "q": "Why are square brackets used around [Red], [Blue], [Black] in the PIVOT syntax?",
      "options": [
        "Ordinary T-SQL identifier-quoting, letting a value like Red be used as if it were a column name",
        "They mark the values as string literals instead of identifiers",
        "They are required only for numeric values",
        "They indicate the values should be excluded from the result"
      ],
      "answer": 0,
      "explain": "Since PIVOT turns these values into actual column headers, they need to be quoted as identifiers, same convention seen with reserved-word column names."
    }
  ]
}
