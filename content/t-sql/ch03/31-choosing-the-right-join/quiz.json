{
  "lesson": "choosing-the-right-join",
  "questions": [
    {
      "q": "What is the key question to ask before choosing a join type?",
      "options": [
        "Do I need rows that have no match on the other side?",
        "How many columns does each table have?",
        "Which table was created first?",
        "Is the data numeric or text?"
      ],
      "answer": 0,
      "explain": "This single question routes most decisions to INNER, LEFT, FULL OUTER, or CROSS JOIN."
    },
    {
      "q": "For 'every customer, including those who've never ordered, with $0 shown for their spending,' which join is correct?",
      "options": [
        "LEFT JOIN, keeping every customer even without a matching order",
        "INNER JOIN, since it's simpler",
        "CROSS JOIN, to get every possible combination",
        "RIGHT JOIN, since orders come second in the query"
      ],
      "answer": 0,
      "explain": "\"Including customers who've never ordered\" is the signal that unmatched left-side rows must be kept — that's LEFT JOIN."
    },
    {
      "q": "Which join type would you use when you genuinely want every combination of two sets, not a match at all?",
      "options": [
        "CROSS JOIN",
        "INNER JOIN",
        "SELF JOIN",
        "OUTER APPLY"
      ],
      "answer": 0,
      "explain": "CROSS JOIN is rare but correct when the goal really is 'every possible pairing,' like a full size/color variant matrix."
    },
    {
      "q": "According to this course's convention, which should you write when you need to keep unmatched rows from one specific table?",
      "options": [
        "LEFT JOIN — even though RIGHT JOIN could technically achieve the same result",
        "RIGHT JOIN, always",
        "FULL OUTER JOIN, to be safe",
        "CROSS JOIN"
      ],
      "answer": 0,
      "explain": "This course consistently prefers LEFT JOIN (Lesson 24) for readability, even though RIGHT JOIN is a valid alternative."
    },
    {
      "q": "When does CROSS APPLY / OUTER APPLY come into play instead of a regular JOIN?",
      "options": [
        "When joining to a table-valued function rather than a table, needing per-row re-evaluation",
        "Whenever more than two tables are involved",
        "Only when using SELF JOIN",
        "Whenever WHERE is also present in the query"
      ],
      "answer": 0,
      "explain": "APPLY exists specifically for joining to functions that need each row's own value as input, which regular JOIN...ON cannot do."
    }
  ]
}
