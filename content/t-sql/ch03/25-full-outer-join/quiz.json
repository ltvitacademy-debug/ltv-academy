{
  "lesson": "full-outer-join",
  "questions": [
    {
      "q": "What does FULL OUTER JOIN return?",
      "options": [
        "Every row from both tables, matched or not, NULL-filling wherever a match is missing",
        "Only rows that match on both sides",
        "Only rows from the left table",
        "Only rows from the right table"
      ],
      "answer": 0,
      "explain": "FULL OUTER JOIN combines LEFT JOIN and RIGHT JOIN behavior at once."
    },
    {
      "q": "Which join type keeps only rows matching on both sides?",
      "options": [
        "INNER JOIN",
        "LEFT JOIN",
        "RIGHT JOIN",
        "FULL OUTER JOIN"
      ],
      "answer": 0,
      "explain": "INNER JOIN is the most restrictive of the four — only true matches survive."
    },
    {
      "q": "How do you find every row unmatched on EITHER side using FULL OUTER JOIN?",
      "options": [
        "Add WHERE checking IS NULL on the join column from both tables, combined with OR",
        "FULL OUTER JOIN cannot be used to find unmatched rows",
        "Use INNER JOIN instead and filter with NOT IN",
        "Add WHERE checking IS NOT NULL on both join columns"
      ],
      "answer": 0,
      "explain": "WHERE p.ProductID IS NULL OR sod.ProductID IS NULL catches gaps from either direction in one query."
    },
    {
      "q": "In a FULL OUTER JOIN result, when would a row have NULL in every column from the left table?",
      "options": [
        "When a right-table row has no matching row on the left",
        "When a left-table row has no matching row on the right",
        "This can never happen in a FULL OUTER JOIN",
        "Only when using INNER JOIN"
      ],
      "answer": 0,
      "explain": "A right-side row with nothing to match on the left gets NULLs filled in for every left-table column — the mirror case of an unmatched left row."
    },
    {
      "q": "Of the four join types covered so far, which is the most inclusive (returns the most rows, all else equal)?",
      "options": [
        "FULL OUTER JOIN",
        "INNER JOIN",
        "LEFT JOIN",
        "RIGHT JOIN"
      ],
      "answer": 0,
      "explain": "FULL OUTER JOIN keeps everything INNER, LEFT, and RIGHT JOIN would each keep individually, plus more."
    }
  ]
}
