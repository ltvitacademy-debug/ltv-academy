{
  "lesson": "left-join",
  "questions": [
    {
      "q": "What does LEFT JOIN guarantee compared to INNER JOIN?",
      "options": [
        "Every row from the left table appears in the results, matched or not",
        "Every row from the right table appears, matched or not",
        "Only rows matching on both sides appear, just like INNER JOIN",
        "Rows are sorted automatically by the join column"
      ],
      "answer": 0,
      "explain": "LEFT JOIN keeps every row from the table named in FROM, regardless of whether a match exists on the right."
    },
    {
      "q": "What value appears in the right table's columns when a LEFT JOIN finds no match?",
      "options": [
        "NULL",
        "Zero",
        "An empty string",
        "The query fails with an error"
      ],
      "answer": 0,
      "explain": "Unmatched right-side columns are filled with NULL, exactly like a missing value from Lesson 16."
    },
    {
      "q": "What determines which table is the 'left' table in a JOIN?",
      "options": [
        "The table named in the FROM clause, written first in the query",
        "Whichever table has more rows",
        "Whichever table's primary key comes first alphabetically",
        "The table that was created first in the database"
      ],
      "answer": 0,
      "explain": "'Left' and 'right' refer purely to position in the query text — FROM names the left table, JOIN names the right."
    },
    {
      "q": "How do you use LEFT JOIN to find rows with no match at all?",
      "options": [
        "Add WHERE on a right-table column IS NULL, since that column can only be NULL if the match failed",
        "LEFT JOIN cannot be used to find unmatched rows",
        "Switch to INNER JOIN instead",
        "Add an ORDER BY on the unmatched column"
      ],
      "answer": 0,
      "explain": "WHERE sod.ProductID IS NULL isolates exactly the rows where the LEFT JOIN found nothing on the right side."
    },
    {
      "q": "In a query finding products that have never sold, why check sod.ProductID IS NULL specifically?",
      "options": [
        "sod.ProductID can only be NULL if no matching SalesOrderDetail row was found for that product",
        "ProductID is always NULL by default in SalesOrderDetail",
        "It's an arbitrary choice — any column from either table would work identically",
        "It filters out products with a NULL name"
      ],
      "answer": 0,
      "explain": "Since ProductID is a foreign key (Lesson 21) that would normally always have a value, its NULL here specifically signals 'no match found.'"
    }
  ]
}
