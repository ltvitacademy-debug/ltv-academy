{
  "lesson": "distinct",
  "questions": [
    {
      "q": "What does DISTINCT do?",
      "options": [
        "Removes duplicate rows from the result set",
        "Sorts the results alphabetically",
        "Removes NULL values only",
        "Limits the number of rows returned"
      ],
      "answer": 0,
      "explain": "DISTINCT filters the result set down to unique rows, based on the selected columns."
    },
    {
      "q": "When SELECT DISTINCT includes two columns, what counts as a duplicate?",
      "options": [
        "Rows must match on the entire combination of both columns to be considered duplicates",
        "Any row matching on just the first column",
        "Any row matching on just the second column",
        "DISTINCT can only be used with a single column"
      ],
      "answer": 0,
      "explain": "DISTINCT operates on the whole selected row, not each column independently — Red/Large and Red/Small count as different rows."
    },
    {
      "q": "Why does adding a primary key column to a DISTINCT query usually defeat its purpose?",
      "options": [
        "A primary key is unique per row, so every row becomes distinct again, returning all rows",
        "Primary keys can't be selected alongside DISTINCT",
        "It causes a syntax error",
        "It only affects sort order, not row count"
      ],
      "answer": 0,
      "explain": "Since DISTINCT compares the full row, a column that's already unique (like a primary key) means no two rows can ever match."
    },
    {
      "q": "Where does the DISTINCT keyword go in a SELECT statement?",
      "options": [
        "Immediately after SELECT",
        "At the very end of the statement",
        "Immediately after FROM",
        "Inside the WHERE clause"
      ],
      "answer": 0,
      "explain": "SELECT DISTINCT column_list FROM table — DISTINCT modifies the SELECT clause itself."
    },
    {
      "q": "How does DISTINCT differ conceptually from GROUP BY?",
      "options": [
        "DISTINCT removes duplicate rows; GROUP BY collapses rows into groups for aggregation",
        "They are functionally identical in every case",
        "GROUP BY can only be used with numeric columns",
        "DISTINCT requires an aggregate function; GROUP BY does not"
      ],
      "answer": 0,
      "explain": "They can sometimes look similar in output, but GROUP BY exists to support aggregation (COUNT, SUM, AVG) within groups — covered in Chapter 4."
    }
  ]
}
