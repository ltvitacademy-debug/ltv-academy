{
  "lesson": "connecting-and-use",
  "questions": [
    {
      "q": "What's the difference between Windows Authentication and SQL Server Authentication?",
      "options": [
        "Windows Authentication uses your current Windows login; SQL Server Authentication uses a username/password stored inside SQL Server",
        "They are two names for the same login method",
        "SQL Server Authentication only works in the cloud",
        "Windows Authentication requires a password stored in SQL Server"
      ],
      "answer": 0,
      "explain": "Windows Authentication trusts your OS login with no separate password; SQL Server Authentication is a credential SQL Server manages itself."
    },
    {
      "q": "What does the USE statement do?",
      "options": [
        "Sets the database context for the statements that follow it",
        "Deletes a database",
        "Creates a new database connection dialog",
        "Grants a user permission to a database"
      ],
      "answer": 0,
      "explain": "USE switches which database subsequent statements in the session run against."
    },
    {
      "q": "What is GO in a T-SQL script?",
      "options": [
        "A batch separator recognized by SSMS, not a T-SQL statement itself",
        "A T-SQL keyword that starts a loop",
        "A synonym for EXECUTE",
        "A comment marker"
      ],
      "answer": 0,
      "explain": "GO tells SSMS (and other T-SQL tools) where one batch ends and the next begins — the database engine itself doesn't recognize GO."
    },
    {
      "q": "Which function returns the name of the database your session is currently connected to?",
      "options": [
        "DB_NAME()",
        "CURRENT_DATABASE",
        "GET_DB()",
        "SESSION_DB()"
      ],
      "answer": 0,
      "explain": "DB_NAME() is the built-in T-SQL function for this — commonly used as SELECT DB_NAME();"
    },
    {
      "q": "Why does every lesson script in this course open with a USE statement?",
      "options": [
        "So the script is unambiguous about which database it targets, regardless of what the toolbar dropdown shows",
        "Because SQL Server requires USE before every SELECT",
        "To improve query performance",
        "Because AdventureWorks2012 cannot be queried without it"
      ],
      "answer": 0,
      "explain": "Relying on the dropdown alone risks running a script against the wrong database; USE makes the target explicit in the script itself."
    }
  ]
}
