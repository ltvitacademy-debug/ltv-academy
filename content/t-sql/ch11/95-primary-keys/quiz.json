{
  "lesson": "primary-keys",
  "questions": [
    {
      "q": "What two things does a primary key guarantee?",
      "options": [
        "Every value is unique, and no value is ever NULL",
        "Every value is sorted alphabetically",
        "The column must be an INT data type",
        "The table can have no foreign keys"
      ],
      "answer": 0,
      "explain": "Uniqueness plus NOT NULL together mean a primary key value always identifies exactly one row."
    },
    {
      "q": "What is a composite primary key?",
      "options": [
        "A primary key made of two or more columns together, where the combination is unique even though no single column is",
        "A primary key that changes value over time",
        "A key that spans multiple databases",
        "A primary key with no data type"
      ],
      "answer": 0,
      "explain": "AdventureWorks2012's SalesOrderDetail is keyed on SalesOrderID plus SalesOrderDetailID together, not either column alone."
    },
    {
      "q": "By default, what kind of index does creating a primary key build?",
      "options": [
        "A clustered index, unless you explicitly specify NONCLUSTERED",
        "A nonclustered index, unless you explicitly specify CLUSTERED",
        "No index at all",
        "A filtered index"
      ],
      "answer": 0,
      "explain": "Ties back to Lesson 88 — a primary key defaults to clustered, physically sorting the table by that key."
    },
    {
      "q": "Why name a primary key constraint explicitly, like PK_Department?",
      "options": [
        "It gives you a predictable name to reference later if you need to drop or alter it",
        "SQL Server requires an explicit name or the CREATE TABLE fails",
        "It makes the column NOT NULL automatically",
        "It's required for composite keys only"
      ],
      "answer": 0,
      "explain": "Without an explicit name, SQL Server auto-generates one that's harder to predict and reference in future scripts."
    },
    {
      "q": "How do you add a primary key to a table that already exists?",
      "options": [
        "ALTER TABLE ... ADD CONSTRAINT ... PRIMARY KEY (...)",
        "You cannot add a primary key after CREATE TABLE",
        "DROP TABLE and recreate it with the same name",
        "UPDATE the table's schema property directly"
      ],
      "answer": 0,
      "explain": "ALTER TABLE ADD CONSTRAINT is the standard way to add a primary key to an existing table."
    }
  ]
}
