{
  "lesson": "comparison-operators",
  "questions": [
    {
      "q": "Which operator does this course use for 'not equal to', and why?",
      "options": [
        "<> — it's the ANSI SQL standard form",
        "!= — it's the only form T-SQL supports",
        "NOT= — the correct T-SQL syntax",
        "Neither works in T-SQL"
      ],
      "answer": 0,
      "explain": "T-SQL supports both <> and !=, but <> is the ANSI standard, which is why this course uses it consistently."
    },
    {
      "q": "What does >= mean?",
      "options": [
        "Greater than or equal to",
        "Greater than only",
        "Equal to, ignoring case",
        "Approximately equal to"
      ],
      "answer": 0,
      "explain": "WHERE ListPrice >= 500 includes products priced at exactly 500 as well as above it."
    },
    {
      "q": "How do dates compare using these operators?",
      "options": [
        "The same way numbers do — earlier dates are 'less than' later ones",
        "Dates cannot be compared with these operators",
        "Only equality (=) works with dates",
        "Dates must be converted to strings first"
      ],
      "answer": 0,
      "explain": "WHERE OrderDate > '2013-01-01' returns rows with a date later than January 1, 2013 — same logic as numeric comparison."
    },
    {
      "q": "Can a comparison operator be used against an expression, not just a bare column?",
      "options": [
        "Yes — e.g. WHERE ListPrice - StandardCost > 100",
        "No, only a single column name is allowed",
        "Only with the = operator",
        "Only inside a CASE expression"
      ],
      "answer": 0,
      "explain": "Comparisons evaluate against any valid expression, including calculations combining multiple columns."
    },
    {
      "q": "How many comparison operators does this lesson cover?",
      "options": [
        "Six: =, <>, >, <, >=, <=",
        "Three: =, >, <",
        "Four, since <> and != count as separate operators",
        "Eight, including AND and OR"
      ],
      "answer": 0,
      "explain": "AND/OR are logical operators covered in Lesson 13, not comparison operators."
    }
  ]
}
