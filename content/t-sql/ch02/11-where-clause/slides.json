[
  {
    "type": "title",
    "eyebrow": "The T-SQL Development Course · Chapter Two",
    "titlePlain": "The ",
    "titleEmphasis": "WHERE",
    "titleSuffix": " Clause",
    "subtitle": "Lesson 11 · Filtering and Sorting"
  },
  {
    "type": "code",
    "eyebrow": "Cutting rows down",
    "code": "SELECT Name, ListPrice\nFROM Production.Product\nWHERE ListPrice > 100;",
    "caption": "WHERE goes after FROM — evaluated once per row, keeping only matches."
  },
  {
    "type": "code",
    "eyebrow": "Filtering text",
    "code": "SELECT Name, Color\nFROM Production.Product\nWHERE Color = 'Red';",
    "caption": "Text values need single quotes; comparisons are case-insensitive by default."
  },
  {
    "type": "code",
    "eyebrow": "This FAILS",
    "code": "SELECT ListPrice * 0.9 AS DiscountedPrice\nFROM Production.Product\nWHERE DiscountedPrice < 50;",
    "caption": "WHERE runs before SELECT — the alias DiscountedPrice doesn't exist yet."
  },
  {
    "type": "code",
    "eyebrow": "The fix",
    "code": "SELECT ListPrice * 0.9 AS DiscountedPrice\nFROM Production.Product\nWHERE ListPrice * 0.9 < 50;",
    "caption": "Repeat the expression — WHERE can't see SELECT's aliases."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 12",
    "titlePlain": "Relational/Comparison ",
    "titleEmphasis": "Operators",
    "titleSuffix": "",
    "subtitle": "Every way to compare values inside a WHERE clause."
  }
]
