{
  "lesson": "predicates",
  "questions": [
    {
      "q": "What is a predicate?",
      "options": [
        "An expression that evaluates to TRUE, FALSE, or UNKNOWN — the general mechanism SQL Server uses to filter data",
        "A synonym for a stored procedure",
        "A type of index",
        "Another name for a column alias"
      ],
      "answer": 0,
      "explain": "Every condition you write in WHERE (and elsewhere) is a predicate, resolving to one of these three states for each row."
    },
    {
      "q": "Which of these is an example of a predicate from this chapter?",
      "options": [
        "ListPrice > 100",
        "SELECT Name FROM Production.Product",
        "AS TotalPrice",
        "ORDER BY Name"
      ],
      "answer": 0,
      "explain": "ListPrice > 100 evaluates to TRUE or FALSE per row — a classic comparison predicate."
    },
    {
      "q": "Besides WHERE, where else have you already used predicates in this course?",
      "options": [
        "Inside a searched CASE expression's WHEN clauses",
        "Inside a column alias",
        "Inside a semicolon",
        "Predicates only ever appear in WHERE"
      ],
      "answer": 0,
      "explain": "Chapter 1's searched CASE (Lesson 8) used full boolean conditions in each WHEN — those are predicates too."
    },
    {
      "q": "Which value can a predicate resolve to, in addition to TRUE and FALSE?",
      "options": [
        "UNKNOWN — the special result NULL comparisons produce",
        "ZERO",
        "EMPTY",
        "VOID"
      ],
      "answer": 0,
      "explain": "This ties back to Lesson 16 — a comparison involving NULL evaluates to UNKNOWN, not TRUE or FALSE."
    },
    {
      "q": "Which upcoming clause (covered starting in Chapter 3) also expects a predicate?",
      "options": [
        "The condition after JOIN ... ON",
        "The FROM clause's table name",
        "The SELECT column list",
        "The semicolon at the end of a statement"
      ],
      "answer": 0,
      "explain": "A JOIN's ON condition works exactly like a WHERE condition — it's a predicate that determines which row combinations match."
    }
  ]
}
