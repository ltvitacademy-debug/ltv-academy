{
  "lesson": "null-handling",
  "questions": [
    {
      "q": "What does NULL actually represent?",
      "options": [
        "An unknown or not-applicable value — the absence of a value entirely",
        "The number zero",
        "An empty string",
        "The text 'NULL'"
      ],
      "answer": 0,
      "explain": "NULL is not zero, not empty text — it means the value is unknown or doesn't apply."
    },
    {
      "q": "Why does 'WHERE MiddleName = NULL' return zero rows, even when NULLs exist?",
      "options": [
        "An unknown value can't be said to equal anything, so = NULL always evaluates to unknown/false",
        "It's a syntax error that SQL Server silently ignores",
        "MiddleName can never actually be NULL in SQL Server",
        "= NULL only works with numeric columns"
      ],
      "answer": 0,
      "explain": "This is a classic beginner trap — equality comparisons against NULL never evaluate to true, regardless of the data."
    },
    {
      "q": "What is the correct way to test for NULL?",
      "options": [
        "IS NULL / IS NOT NULL",
        "= NULL / <> NULL",
        "== NULL",
        "LIKE NULL"
      ],
      "answer": 0,
      "explain": "IS NULL and IS NOT NULL are special syntax built specifically for this purpose, not regular comparison operators."
    },
    {
      "q": "What does ISNULL(MiddleName, '(none)') do?",
      "options": [
        "Returns MiddleName's value if it's not NULL, otherwise returns '(none)'",
        "Deletes rows where MiddleName is NULL",
        "Converts every value to '(none)' regardless of MiddleName",
        "Tests whether MiddleName is NULL, returning true or false"
      ],
      "answer": 0,
      "explain": "ISNULL substitutes a default value only when the first argument is NULL."
    },
    {
      "q": "How does COALESCE() differ from ISNULL()?",
      "options": [
        "COALESCE accepts any number of arguments and returns the first non-NULL one; ISNULL takes exactly two",
        "COALESCE only works with text columns",
        "They are functionally identical in every respect",
        "ISNULL is the ANSI-standard version; COALESCE is SQL Server-specific"
      ],
      "answer": 0,
      "explain": "COALESCE(a, b, c, ...) can chain multiple fallbacks; ISNULL is limited to exactly one replacement value. COALESCE is also the ANSI-standard function."
    }
  ]
}
