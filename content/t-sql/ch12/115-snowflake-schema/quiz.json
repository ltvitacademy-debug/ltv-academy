{
  "lesson": "snowflake-schema",
  "questions": [
    {
      "q": "What is a snowflake schema?",
      "options": [
        "A star schema whose dimension tables are further normalized into smaller, related sub-dimension tables",
        "A schema with no fact table at all",
        "A synonym for a star schema with no real difference",
        "A schema used only for storing weather data"
      ],
      "answer": 0,
      "explain": "Snowflaking splits a dimension into related tables, adding join hops instead of keeping everything one hop from the fact table."
    },
    {
      "q": "In AdventureWorksDW2014, how many joins does it take to get from FactInternetSales to a product's category name?",
      "options": [
        "Three joins — fact to DimProduct, to DimProductSubcategory, to DimProductCategory",
        "One join, directly to a category table",
        "Zero joins — the category name is stored on the fact table itself",
        "It's not possible to retrieve category name at all"
      ],
      "answer": 0,
      "explain": "The product family is genuinely snowflaked into a three-table chain, unlike a pure one-hop star dimension."
    },
    {
      "q": "Why is AdventureWorksDW2014 considered a hybrid schema?",
      "options": [
        "DimDate and DimCustomer sit one hop from the fact table (star-shaped), while the product family chains two hops deep (snowflaked)",
        "It has no dimension tables at all",
        "Every dimension in it is snowflaked with no exceptions",
        "It switches between star and snowflake shape randomly at query time"
      ],
      "answer": 0,
      "explain": "Most real warehouses mix shapes depending on each dimension's needs, rather than applying one dogmatic choice everywhere."
    },
    {
      "q": "What is the main tradeoff of snowflaking a dimension?",
      "options": [
        "Category/attribute names live in exactly one normalized place, but every query touching them now pays for extra joins",
        "Snowflaking always makes queries faster with no downside",
        "Snowflaking removes the need for a fact table",
        "Snowflaking eliminates the need for foreign keys"
      ],
      "answer": 0,
      "explain": "Fewer duplicated values but more joins per query — the classic normalization-versus-speed tradeoff, applied to dimensions."
    },
    {
      "q": "When does snowflaking a dimension make the most sense?",
      "options": [
        "When a dimension attribute changes often, so duplication would mean updating thousands of rows across a denormalized star dimension",
        "Only when the warehouse has fewer than 10 total rows",
        "Snowflaking should never be used under any circumstances",
        "Only for fact tables, never for dimension tables"
      ],
      "answer": 0,
      "explain": "Normalizing an often-changing attribute avoids the update cost that a fully denormalized star dimension would otherwise incur."
    }
  ]
}
