[
  {
    "type": "title",
    "eyebrow": "The T-SQL Development Course · Chapter Twelve · Course Finale",
    "titlePlain": "Where T-SQL Fits in the BI/Warehousing ",
    "titleEmphasis": "Pipeline",
    "titleSuffix": "",
    "subtitle": "Lesson 118 · The Final Lesson"
  },
  {
    "type": "steps",
    "eyebrow": "The pipeline, stage by stage",
    "titlePlain": "T-SQL, at ",
    "titleEmphasis": "every",
    "titleSuffix": " stage",
    "steps": [
      { "label": "SOURCE (OLTP)", "sub": "every SELECT and JOIN you wrote in Chapters 1-9" },
      { "label": "TRANSFORM (ELT)", "sub": "INSERT...SELECT IS the transform engine (Lesson 112)" },
      { "label": "WAREHOUSE (OLAP)", "sub": "querying the star schema (this chapter)" },
      { "label": "BI TOOLS", "sub": "every chart is backed by a T-SQL query underneath" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "One last query, touching several chapters",
    "code": "SELECT d.CalendarYear, d.EnglishMonthName,\n  SUM(f.SalesAmount) AS MonthlySales,\n  SUM(SUM(f.SalesAmount)) OVER (\n    PARTITION BY d.CalendarYear ORDER BY d.MonthNumberOfYear\n  ) AS YearToDateSales\nFROM dbo.FactInternetSales AS f\nJOIN dbo.DimDate AS d ON d.DateKey = f.OrderDateKey\nWHERE d.CalendarYear >= 2011\nGROUP BY d.CalendarYear, d.EnglishMonthName, d.MonthNumberOfYear;",
    "caption": "JOIN (Ch3) + GROUP BY (Ch4) + SARGable WHERE (Ch10) + a window function (Ch9) — on a star schema (Ch12)."
  },
  {
    "type": "code",
    "eyebrow": "Performance tuning applies everywhere",
    "code": "-- The SAME diagnostic tools from Chapter 10 —\n-- execution plans, STATISTICS IO —\n-- work at EVERY stage: source, transform, warehouse",
    "caption": "A slow query is a slow query, wherever in the pipeline it lives."
  },
  {
    "type": "outro",
    "eyebrow": "Course Complete · 118 of 118 Lessons",
    "titlePlain": "From your first SELECT to a real ",
    "titleEmphasis": "warehouse",
    "titleSuffix": "",
    "subtitle": "Congratulations on finishing T-SQL Development."
  }
]
