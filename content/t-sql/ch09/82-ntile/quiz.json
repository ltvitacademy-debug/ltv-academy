{
  "lesson": "ntile",
  "questions": [
    {
      "q": "What does NTILE(4) do?",
      "options": [
        "Divides the ordered rows into 4 roughly equal groups, numbered 1 through 4",
        "Returns only the top 4 rows",
        "Ranks rows individually, same as RANK()",
        "Removes duplicate rows, keeping only 4 per group"
      ],
      "answer": 0,
      "explain": "NTILE splits an ordered result set into a fixed number of buckets, unlike RANK/DENSE_RANK which rank rows individually."
    },
    {
      "q": "In 'NTILE(4) OVER (ORDER BY ListPrice DESC)', which group holds the most expensive products?",
      "options": [
        "Group 1",
        "Group 4",
        "It's unpredictable",
        "The group is determined by PARTITION BY only"
      ],
      "answer": 0,
      "explain": "Because of the DESC order, group 1 is filled first with the highest-priced rows."
    },
    {
      "q": "What happens when the total row count isn't evenly divisible by NTILE's bucket count?",
      "options": [
        "SQL Server distributes the extra rows to the earliest groups, one extra row each",
        "SQL Server raises an error",
        "The extra rows are dropped from the result set entirely",
        "All extra rows go into the last group"
      ],
      "answer": 0,
      "explain": "For example, 22 rows into NTILE(4) produces groups of 6, 6, 5, 5 — no group differs by more than one row."
    },
    {
      "q": "Can NTILE be combined with PARTITION BY?",
      "options": [
        "Yes — it computes the buckets separately within each partition, just like other window functions this chapter",
        "No, NTILE cannot be partitioned",
        "Only when using RANK() in the same query",
        "Only for numeric partition columns"
      ],
      "answer": 0,
      "explain": "NTILE(4) OVER (PARTITION BY Color ORDER BY ListPrice DESC) computes quartiles separately per color."
    },
    {
      "q": "What real-world question is NTILE well suited for?",
      "options": [
        "\"Which price quartile does this product fall into?\" — splitting data into a fixed number of equal-sized groups",
        "\"What is the exact rank of this specific row?\"",
        "\"How many rows share this exact value?\"",
        "\"What is the running total up to this row?\""
      ],
      "answer": 0,
      "explain": "NTILE is coarser than RANK/DENSE_RANK — it groups rows into buckets rather than ranking them individually."
    }
  ]
}
