{
  "lesson": "row-number",
  "questions": [
    {
      "q": "What does ROW_NUMBER() assign to each row?",
      "options": [
        "A unique, sequential integer based on the order specified inside OVER()",
        "The row's total count of NULL values",
        "A random number",
        "The same number to every row in the result set"
      ],
      "answer": 0,
      "explain": "1, 2, 3, and so on, following the ORDER BY inside OVER()."
    },
    {
      "q": "Why does ROW_NUMBER() require an ORDER BY inside OVER()?",
      "options": [
        "Without an order, 'which row is first' is undefined",
        "ORDER BY is optional and rarely used with ROW_NUMBER",
        "ROW_NUMBER() ignores ORDER BY entirely",
        "It's only required when PARTITION BY is also present"
      ],
      "answer": 0,
      "explain": "Unlike AVG()/COUNT() from Lesson 79, ROW_NUMBER() needs a defined sequence to assign numbers meaningfully."
    },
    {
      "q": "What happens when PARTITION BY is combined with ROW_NUMBER()'s ORDER BY?",
      "options": [
        "The numbering restarts at 1 for each partition/group",
        "PARTITION BY is ignored when ROW_NUMBER() is used",
        "The numbering continues across all partitions without restarting",
        "It causes a syntax error"
      ],
      "answer": 0,
      "explain": "This is exactly the mechanism Lesson 51 used — each duplicate group gets numbered 1, 2, 3 independently."
    },
    {
      "q": "Does ROW_NUMBER() ever assign the same number to two tied rows?",
      "options": [
        "No — it always produces unique numbers, even for rows tied on the ORDER BY column",
        "Yes, tied rows always get the same number",
        "Only when PARTITION BY is omitted",
        "ROW_NUMBER() cannot be used with columns that might tie"
      ],
      "answer": 0,
      "explain": "One of two tied rows arbitrarily gets a lower number — RANK() and DENSE_RANK() (Lesson 81) handle ties differently."
    },
    {
      "q": "In Lesson 51's deduplication query, what does a RowNum greater than 1 indicate?",
      "options": [
        "The row is a duplicate of an earlier row within the same PARTITION BY group",
        "The row has a NULL value in it",
        "The row is the original, unique copy",
        "Nothing meaningful — RowNum values above 1 are ignored"
      ],
      "answer": 0,
      "explain": "This is the exact technique explained fully now that ROW_NUMBER() and OVER()/PARTITION BY have both been covered properly."
    }
  ]
}
