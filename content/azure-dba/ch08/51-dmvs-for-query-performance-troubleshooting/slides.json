[
  {
    "type": "title",
    "eyebrow": "Azure Database Administrator · Chapter Eight",
    "titlePlain": "DMVs for Query Performance ",
    "titleEmphasis": "Troubleshooting",
    "titleSuffix": "",
    "subtitle": "Lesson 51 · Query Performance Tuning"
  },
  {
    "type": "code",
    "eyebrow": "Cumulative stats per cached plan",
    "code": "SELECT TOP 10\n    qs.total_worker_time / qs.execution_count AS avg_cpu_time,\n    qs.total_logical_reads / qs.execution_count AS avg_logical_reads,\n    qs.execution_count, qs.sql_handle\nFROM sys.dm_exec_query_stats qs\nORDER BY qs.total_worker_time DESC;",
    "caption": "Live from the plan cache -- no ALTER DATABASE, no setup required."
  },
  {
    "type": "code",
    "eyebrow": "Turning a handle into text",
    "code": "SELECT qs.total_worker_time / qs.execution_count AS avg_cpu_time,\n       st.text AS query_text\nFROM sys.dm_exec_query_stats qs\nCROSS APPLY sys.dm_exec_sql_text(qs.sql_handle) st\nORDER BY qs.total_worker_time DESC;",
    "caption": "CROSS APPLY calls dm_exec_sql_text once per row, with that row's own handle."
  },
  {
    "type": "code",
    "eyebrow": "Getting the plan itself",
    "code": "SELECT qs.total_worker_time / qs.execution_count AS avg_cpu_time,\n       qp.query_plan\nFROM sys.dm_exec_query_stats qs\nCROSS APPLY sys.dm_exec_query_plan(qs.plan_handle) qp\nORDER BY qs.total_worker_time DESC;",
    "caption": "The exact plan that already ran -- click the XML to render it graphically."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 52",
    "titlePlain": "Intelligent Query Processing & ",
    "titleEmphasis": "Automatic Tuning",
    "titleSuffix": "",
    "subtitle": "Closing this chapter with SQL Server tuning itself."
  }
]
