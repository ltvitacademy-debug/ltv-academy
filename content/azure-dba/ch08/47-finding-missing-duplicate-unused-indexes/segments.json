[
  {"slide": "s1", "text": "You learned to create indexes correctly in T-SQL Development. The more common real job is the opposite direction: auditing indexes that already exist -- what's missing, what's duplicated, what's dead weight."},
  {"slide": "s2", "text": "sys.dm_db_missing_index_details tracks every time the optimizer would have benefited from an index that doesn't exist. Equality columns lead the new index, included columns make it covering. Treat the impact estimate as a hint, verify with a real plan."},
  {"slide": "s3", "text": "Two indexes sharing the same leading column are almost always redundant, even with different included columns. Group your nonclustered indexes by table and leading column -- more than one group per column is a duplicate candidate."},
  {"slide": "s4", "text": "sys.dm_db_index_usage_stats tracks seeks, scans, lookups, and updates per index. Zero seeks, zero scans, zero lookups, but real write activity, means that index is pure overhead. It resets on every restart, so check over a real window."},
  {"slide": "s5", "text": "Add what's missing, remove what's duplicated, drop what's genuinely unused -- always cross-checked against a real plan first. Next up: Query Store, SQL Server's own flight data recorder for every plan a query has ever used."}
]
