{
  "lesson": "query-store-fundamentals",
  "questions": [
    {
      "q": "What does Query Store persist that DMVs like sys.dm_exec_query_stats do not survive?",
      "options": [
        "Query text, every plan a query has used, and runtime statistics per plan, persisted to disk across service restarts",
        "Nothing -- Query Store and sys.dm_exec_query_stats store the exact same information",
        "Only the current execution plan, discarded on the next query run",
        "Backup file locations"
      ],
      "answer": 0,
      "explain": "This persistence across restarts is exactly what lets Query Store answer 'was this always slow, or did something change.'"
    },
    {
      "q": "Is Query Store on by default on Azure SQL Database, or does it need to be turned on?",
      "options": [
        "It is on by default on Azure SQL Database; on SQL Server on-prem or a VM, it's turned on per database with ALTER DATABASE",
        "It is never available on Azure SQL Database",
        "It must always be turned on manually, on every platform, with no default",
        "It can only be enabled by Microsoft support"
      ],
      "answer": 0,
      "explain": "This platform difference is worth knowing directly -- Azure SQL Database defaults it on."
    },
    {
      "q": "In a query's Plan Summary view, what does seeing two separate clusters of dots at very different average-duration heights usually mean?",
      "options": [
        "The query ran under two meaningfully different execution plans during those time periods",
        "Query Store has stopped recording data",
        "The query returned different columns each time it ran",
        "The database was in single-user mode during that period"
      ],
      "answer": 0,
      "explain": "Each dot color/plan id in the legend corresponds to a distinct plan -- a height jump between clusters is a visible plan change."
    },
    {
      "q": "What does the Top Resource Consumers report let you do?",
      "options": [
        "Rank the top queries on a database by a chosen metric (duration, CPU time, logical reads, etc.) over a time window, instead of guessing which query to tune first",
        "Automatically rewrite the worst-performing queries",
        "Delete queries that use too many resources",
        "Show only queries written by a specific login"
      ],
      "answer": 0,
      "explain": "Selecting a bar in the report drills into that specific query's own Plan Summary view and text."
    },
    {
      "q": "What does QUERY_CAPTURE_MODE = AUTO do?",
      "options": [
        "Skips capturing one-off ad hoc queries that ran once and never again, reducing noise in Query Store",
        "Forces Query Store to capture every single query with no filtering at all",
        "Automatically forces the best plan for every query",
        "Disables Query Store entirely"
      ],
      "answer": 0,
      "explain": "Capturing only 'relevant' recurring queries keeps the store from filling with one-time noise."
    }
  ]
}
