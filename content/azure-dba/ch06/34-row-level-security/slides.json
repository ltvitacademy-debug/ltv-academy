[
  {
    "type": "title",
    "eyebrow": "Azure Database Administrator · Chapter Six",
    "titlePlain": "Row-Level ",
    "titleEmphasis": "Security",
    "titleSuffix": "",
    "subtitle": "Lesson 34 · Data Security & Compliance"
  },
  {
    "type": "code",
    "eyebrow": "Same query, different rows",
    "code": "SalesRep A: SELECT * FROM Orders  -> only rows where SalesRepID = A's ID\nSalesRep B: SELECT * FROM Orders  -> only rows where SalesRepID = B's ID\n\nBoth have IDENTICAL table-level SELECT permission.",
    "caption": "The security policy filters below the object-permission layer entirely."
  },
  {
    "type": "code",
    "eyebrow": "The two objects that do it",
    "code": "CREATE FUNCTION dbo.fn_SalesRepPredicate(@SalesRepID AS INT)\nRETURNS TABLE WITH SCHEMABINDING AS RETURN\n  SELECT 1 AS fn_result\n  WHERE @SalesRepID = CAST(SESSION_CONTEXT(N'CurrentSalesRepID') AS INT)\n\nCREATE SECURITY POLICY SalesRepFilter\nADD FILTER PREDICATE dbo.fn_SalesRepPredicate(SalesRepID) ON dbo.Orders\nWITH (STATE = ON);",
    "caption": "A predicate function, bound to a table by a security policy."
  },
  {
    "type": "steps",
    "eyebrow": "Two predicate types, two behaviors",
    "titlePlain": "Silent filter, or ",
    "titleEmphasis": "hard block",
    "titleSuffix": "",
    "steps": [
      { "label": "FILTER Predicate", "sub": "SELECT/UPDATE/DELETE — row silently isn't there" },
      { "label": "BLOCK Predicate", "sub": "INSERT/UPDATE/DELETE — throws an error instead" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 35",
    "titlePlain": "Data Discovery, Classification & ",
    "titleEmphasis": "Sensitive Data",
    "titleSuffix": "",
    "subtitle": "Finding which columns need protecting like this in the first place."
  }
]
