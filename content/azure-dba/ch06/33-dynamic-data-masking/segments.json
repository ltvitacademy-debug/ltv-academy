[
  {"slide": "s1", "text": "TDE and Always Encrypted protect against someone who shouldn't have access at all. Dynamic Data Masking solves a different problem — a user with legitimate query access who doesn't need to see every sensitive column's real value."},
  {"slide": "s2", "text": "The stored data never changes. A privileged user's query sees the real email address. A masked user running the exact same query sees a masked version — the rewrite happens in the result set, not on disk."},
  {"slide": "s3", "text": "Random masking replaces a numeric value with a plausible but fake number inside a defined range — configured right here, on the column's masking rule."},
  {"slide": "s4", "text": "Default masks fully by data type. Email keeps the first letter and a fixed domain. Random substitutes a fake number in range. Partial shows real characters at the start or end and masks the middle."},
  {"slide": "s5", "text": "DDM is a least-exposure convenience, not a security boundary — a WHERE clause can still filter on the real value. Next up: Row-Level Security, controlling which rows a user sees at all."}
]
