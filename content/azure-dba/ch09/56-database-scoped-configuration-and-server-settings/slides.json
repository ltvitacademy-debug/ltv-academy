[
  {
    "type": "title",
    "eyebrow": "Azure Database Administrator · Chapter Nine",
    "titlePlain": "Database-Scoped Configuration & Server ",
    "titleEmphasis": "Settings",
    "titleSuffix": "",
    "subtitle": "Lesson 56 · Database Performance & Maintenance"
  },
  {
    "type": "code",
    "eyebrow": "Per-database, not server-wide",
    "code": "-- Check the effective value:\nSELECT * FROM sys.database_scoped_configurations\nWHERE name = 'MAXDOP';\n\n-- Override for this database only:\nALTER DATABASE SCOPED CONFIGURATION SET MAXDOP = 4;\nALTER DATABASE SalesDB SET COMPATIBILITY_LEVEL = 150;",
    "caption": "MAXDOP caps cores per query. Compatibility level pins optimizer behavior, not engine version."
  },
  {
    "type": "steps",
    "eyebrow": "Why this is real on multi-database instances",
    "titlePlain": "One instance, ",
    "titleEmphasis": "different needs",
    "titleSuffix": "",
    "steps": [
      { "label": "OLTP Database", "sub": "Low MAXDOP — many small fast queries" },
      { "label": "Reporting Database", "sub": "Higher MAXDOP — big aggregations use more cores" },
      { "label": "Managed Instance / VM", "sub": "Both live on the same instance — scoped config makes it work" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 57",
    "titlePlain": "Resource ",
    "titleEmphasis": "Governor",
    "titleSuffix": "",
    "subtitle": "Stopping one noisy workload from starving everything else."
  }
]
