[
  {"slide": "s1", "text": "Fragmentation and stale statistics are performance problems — the database still returns correct answers. Corruption is different: a page on disk that doesn't match what SQL Server expects, and it can propagate to a wrong result with no error at all."},
  {"slide": "s2", "text": "DBCC CHECKDB validates page checksums, allocation consistency, and b-tree structure across the whole database. If it finds corruption, restore from a known-good backup first — REPAIR_ALLOW_DATA_LOSS can permanently delete corrupted data to restore consistency."},
  {"slide": "s3", "text": "On Azure SQL Database and Managed Instance, Microsoft runs integrity checks as part of the managed service. SQL Server on an Azure VM is IaaS, so scheduling CHECKDB yourself is still your job there."},
  {"slide": "s4", "text": "A backup gives your data back; REPAIR_ALLOW_DATA_LOSS gives you a database that stops erroring with data quietly missing. Next up: database-scoped configuration, tuning one database differently from server defaults."}
]
