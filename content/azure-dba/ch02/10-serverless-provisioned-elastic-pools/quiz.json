{
  "lesson": "serverless-provisioned-elastic-pools",
  "questions": [
    {
      "q": "What defines provisioned compute, per this lesson?",
      "options": [
        "A fixed number of vCores or DTUs that's billed whether the database is busy or completely idle",
        "Compute that automatically pauses and resumes based on load",
        "Compute shared across many databases in a pool",
        "Compute that is always free of charge"
      ],
      "answer": 0,
      "explain": "Provisioned compute is the right default for steady, predictable usage because fixed capacity prices predictability correctly."
    },
    {
      "q": "What tradeoff does serverless compute accept in exchange for auto-pausing and paying only for storage during inactivity?",
      "options": [
        "A cold-start delay of typically a handful of seconds when a paused database receives its first connection again",
        "Permanent data loss whenever the database pauses",
        "A requirement to manually resume the database via the Azure CLI",
        "Loss of all configured firewall rules on resume"
      ],
      "answer": 0,
      "explain": "That cold-start delay is a reasonable price for dev/test or spiky workloads that don't need to be always-on."
    },
    {
      "q": "What problem do elastic pools solve that provisioned compute on individual databases does not?",
      "options": [
        "Many databases each spiking at different, unpredictable times, without provisioning peak capacity for every one individually",
        "A single database's own steady, predictable load",
        "The need for automatic OS patching",
        "Applying Azure Hybrid Benefit to a DTU-based database"
      ],
      "answer": 0,
      "explain": "An elastic pool gives a shared compute/storage budget that all member databases draw from, avoiding the expense of sizing every database for its own worst case."
    },
    {
      "q": "In most regions, which service tier supports serverless compute?",
      "options": [
        "General Purpose",
        "Business Critical",
        "Hyperscale",
        "All vCore tiers support serverless equally"
      ],
      "answer": 0,
      "explain": "Business Critical and Hyperscale are provisioned-only as of this course's writing, per Lesson 8."
    },
    {
      "q": "Why is a SaaS vendor running one database per tenant a textbook elastic pool use case?",
      "options": [
        "Because a handful of tenants spike during their own business hours while most sit quiet, and the pool absorbs that variance instead of sizing every tenant's database for its own peak",
        "Because elastic pools guarantee zero cost regardless of usage",
        "Because elastic pools require every tenant to have identical usage patterns",
        "Because SaaS vendors cannot use provisioned or serverless compute at all"
      ],
      "answer": 0,
      "explain": "This is exactly the uncorrelated-spike shape elastic pools are built for, as shown in the real Portal screenshot of the pool creation flow."
    }
  ]
}
