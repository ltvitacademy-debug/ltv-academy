[
  {"slide": "s1", "text": "Storage isn't fully separate from compute even under the vCore model. Business Critical's storage rides on local SSD while General Purpose's is remote — why Business Critical has lower latency but higher cost per GB."},
  {"slide": "s2", "text": "General Purpose uses remote storage — higher latency, lower cost. Business Critical uses local SSD — lower latency, higher cost. Hyperscale uses distributed storage designed for multi-terabyte databases."},
  {"slide": "s3", "text": "Changing compute or tier cuts over in minutes with a brief reconnect window — near-zero downtime, not zero. Read scale-out routes reads to an HA secondary that Business Critical and Hyperscale already maintain; General Purpose has none."},
  {"slide": "s4", "text": "Chapter 2's decisions so far stack into one real configuration: vCore with Hybrid Benefit, Business Critical for read scale-out and latency, provisioned compute for steady load, storage sized to the workload, and read scale-out enabled for reporting."},
  {"slide": "s5", "text": "Next up: Managed Instance's own deployment, including its real deployment time — often hours, not minutes."}
]
