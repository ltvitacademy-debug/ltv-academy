{
  "lesson": "sql-authentication-vs-entra-authentication",
  "questions": [
    {
      "q": "Where does SQL authentication store the credential a client connects with?",
      "options": [
        "A username and a hash of the password, stored inside the SQL Server/Azure SQL engine itself",
        "In Microsoft Entra ID exclusively",
        "It doesn't store a credential at all",
        "In the client application's source code only"
      ],
      "answer": 0,
      "explain": "SQL authentication is the model where the login's credential lives and is checked inside the database engine, which is exactly what creates the password-management and no-MFA problems."
    },
    {
      "q": "What does FROM EXTERNAL PROVIDER mean in CREATE LOGIN [alex@ltvacademy.com] FROM EXTERNAL PROVIDER?",
      "options": [
        "The identity is not stored in SQL Server at all — it's a reference to an identity that already exists in Microsoft Entra ID",
        "It creates a brand-new password stored in SQL Server",
        "It disables authentication for that login entirely",
        "It only works for on-premises SQL Server, never Azure SQL"
      ],
      "answer": 0,
      "explain": "Entra-authenticated logins reference an external identity; authentication happens against Entra ID rather than against a credential stored in the database."
    },
    {
      "q": "Which capability does Entra authentication provide that SQL authentication does not?",
      "options": [
        "Built-in multi-factor authentication enforced through Entra's conditional access",
        "Faster query execution",
        "Automatic table partitioning",
        "Automatic data compression"
      ],
      "answer": 0,
      "explain": "MFA is enforced by Entra ID itself for Entra-authenticated logins, with no extra configuration on the SQL side — SQL authentication has no equivalent built-in mechanism."
    },
    {
      "q": "Why is disabling access centralized under Entra authentication but not under SQL authentication?",
      "options": [
        "Disabling a person in Entra ID immediately stops every Entra-authenticated login they had, while SQL logins must each be found and disabled individually per system",
        "SQL authentication automatically disables all logins the moment one is disabled",
        "There is no difference between the two models for disabling access",
        "Entra authentication cannot be disabled once granted"
      ],
      "answer": 0,
      "explain": "Each system with its own SQL logins is its own identity silo, so removing access means hunting down every login separately — Entra's centralized identity avoids that entirely."
    },
    {
      "q": "What does this lesson say authentication answers, versus what authorization (covered in later lessons) answers?",
      "options": [
        "Authentication proves who is connecting; authorization determines what that identity is allowed to do once connected",
        "Authentication and authorization are the same concept",
        "Authorization happens before authentication in every case",
        "Authentication determines table permissions directly"
      ],
      "answer": 0,
      "explain": "A successful login only proves identity — GRANT, DENY, REVOKE, and role membership (Lessons 21-23) are what determine what that identity can actually do."
    }
  ]
}
