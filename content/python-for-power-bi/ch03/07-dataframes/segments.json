[
  {"slide": "s1", "text": "Every DataFrame you'll ever build or import has exactly four parts, and once you can name all four from memory, DataFrames stop feeling mysterious and start feeling like something you actually understand."},
  {"slide": "s2", "text": "Rows are records — one product, one customer, one transaction. Columns are named fields, shared across every row. The index is the row labels down the left, numbered by default starting at zero. And values are just the actual data sitting at each intersection."},
  {"slide": "s3", "text": "Pull a single column out with square brackets, and it stops being a DataFrame — it becomes a Series, Pandas' name for one labeled column on its own. A DataFrame is really just several Series lined up side by side, sharing the same index."},
  {"slide": "s4", "text": "Here's what trips people up: those zero, one, two labels down the left are not a column. They don't count toward your column total, and by default they're just row position, not meaningful data — though later lessons will replace them with something that actually means something."},
  {"slide": "s5", "text": "Three quick checks before touching a real dataset. Shape gives you row and column counts. Columns lists every column name. And dtypes shows each column's actual data type — which matters enormously once Lesson 13 covers what happens when that type is wrong."},
  {"slide": "s6", "text": "Rows, columns, index, values, and a Series as one column pulled out — that's the complete anatomy. Lesson 8 finally brings in real data, importing an actual CSV file with Python for the first time in this course."}
]
