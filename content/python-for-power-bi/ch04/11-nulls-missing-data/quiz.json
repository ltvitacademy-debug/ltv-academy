{
  "lesson": "nulls-missing-data",
  "questions": [
    {
      "q": "What does a missing value look like inside a Pandas DataFrame?",
      "options": [
        "NaN (Not a Number) — Pandas' explicit marker for 'no value was here,' regardless of column type",
        "An empty string, always",
        "The number zero",
        "The text 'NULL' as a literal string"
      ],
      "answer": 0,
      "explain": "NaN is Pandas' universal marker for missing data, distinct from an empty string or a zero — it explicitly means no value was recorded."
    },
    {
      "q": "What does .isna() return?",
      "options": [
        "True/False for every cell, with True marking exactly where values are missing",
        "A count of total rows in the DataFrame",
        "The actual missing values themselves",
        "A new DataFrame with missing values already removed"
      ],
      "answer": 0,
      "explain": ".isna() produces a same-shaped DataFrame of True/False values, letting you see exactly which cells are missing before deciding what to do."
    },
    {
      "q": "What does .isna().sum() give you that .isna() alone doesn't?",
      "options": [
        "A per-column count of how many missing values exist, rather than a True/False grid",
        "The exact row numbers of missing values",
        "The average of all non-missing values",
        "Nothing different — they return identical output"
      ],
      "answer": 0,
      "explain": "Summing the True/False grid (True counts as 1) collapses it into a quick per-column total, a fast way to gauge how bad the gaps are."
    },
    {
      "q": "What is the key consideration before using .fillna(0) on a revenue column?",
      "options": [
        "Whether a missing value actually means 'zero sales' or means 'we don't know' — those are very different things to report on",
        "Whether the column contains any negative numbers",
        ".fillna(0) should always be used on every numeric column automatically",
        "Whether the DataFrame has more than 1000 rows"
      ],
      "answer": 0,
      "explain": "Filling missing data requires understanding what the gap actually represents — treating 'unknown' as 'zero' can silently distort real analysis."
    },
    {
      "q": "When is .dropna() the right choice instead of .fillna()?",
      "options": [
        "When a missing value makes the row genuinely unusable, rather than having a sensible default to fill it with",
        "Always — dropna() is universally more correct than fillna()",
        "Only when the DataFrame has no missing values at all",
        "Never — dropna() should be avoided in all cases"
      ],
      "answer": 0,
      "explain": "The right tool depends on what the missing value means: dropna() when the row can't be sensibly completed, fillna() when a reasonable default genuinely exists."
    }
  ]
}
