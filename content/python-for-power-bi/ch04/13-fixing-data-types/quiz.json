{
  "lesson": "fixing-data-types",
  "questions": [
    {
      "q": "Why might a column of numbers load as 'object' (text) instead of a numeric type?",
      "options": [
        "Source issues like a stray currency symbol, inconsistent formatting, or a source system quoting every value as a string",
        "This can never actually happen with numeric data",
        "Only if the DataFrame has more than 1000 rows",
        "Pandas always converts every column to numeric automatically"
      ],
      "answer": 0,
      "explain": "Real-world source data quirks — quoting, formatting, stray symbols — can cause a genuinely numeric column to load as text, showing as 'object' in .dtypes."
    },
    {
      "q": "What happens if you try to .sum() a numeric-looking column that's actually stored as text?",
      "options": [
        "It silently produces string concatenation instead of numeric addition, rather than throwing an obvious error",
        "Pandas automatically converts it to numeric first, so the sum is always correct",
        "It always throws a clear error immediately",
        "The column is automatically dropped from the DataFrame"
      ],
      "answer": 0,
      "explain": "This is the dangerous part — text 'addition' concatenates characters rather than adding values, which can silently produce nonsense that looks plausible at a glance."
    },
    {
      "q": "What does .astype(int) do?",
      "options": [
        "Converts a column to the integer data type",
        "Converts a column to text",
        "Removes all non-numeric characters from a column without converting its type",
        "Only works on columns that are already numeric"
      ],
      "answer": 0,
      "explain": "astype(int) explicitly converts a column's data type — turning the string '120' into the actual integer 120, so mathematical operations work correctly."
    },
    {
      "q": "What habit does this lesson recommend for any freshly loaded dataset?",
      "options": [
        "Check .dtypes first, before filtering, grouping, or calculating anything",
        "Immediately delete any column that contains numbers",
        "Convert every column to text regardless of its actual content",
        "Skip type checking entirely and only check for missing values"
      ],
      "answer": 0,
      "explain": "Checking .dtypes as a first step — before any other operation — catches type mismatches early, before they cause quiet miscalculations downstream."
    },
    {
      "q": "In the lesson's example, what does Units Sold summing to 205 confirm?",
      "options": [
        "That the column was successfully converted to a real numeric type using .astype(int), since 120 + 85 = 205",
        "That the original text values were correct all along",
        "That .astype() failed and fell back to string concatenation",
        "That the DataFrame has exactly 205 rows"
      ],
      "answer": 0,
      "explain": "205 is the correct numeric sum of 120 and 85 — confirming the conversion worked, since summing the original string values would have concatenated them instead."
    }
  ]
}
