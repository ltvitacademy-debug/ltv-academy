{
  "lesson": "tools-overview",
  "questions": [
    {
      "q": "In SSMS, what's the difference between the estimated and actual execution plan?",
      "options": [
        "The estimated plan is compiled without running the query; the actual plan is shown after execution and includes real row counts",
        "They are two names for the exact same information",
        "The estimated plan only works on Azure SQL Database",
        "The actual plan never includes cost percentages"
      ],
      "answer": 0,
      "explain": "Ctrl+L shows the estimated (compile-time) plan; toggling \"Include Actual Execution Plan\" (Ctrl+M) and running the query shows the actual plan with real row counts."
    },
    {
      "q": "What does this lesson say Query Store is useful for beyond baselining, from Lesson 3?",
      "options": [
        "Spotting plan regressions over time (a query that had a good plan last week and a bad one today) and forcing a known-good plan without touching code",
        "Replacing the need for any execution plan viewer",
        "It can only be used on read-only replicas",
        "Capturing wait statistics at the operating-system level"
      ],
      "answer": 0,
      "explain": "The lesson describes Query Store's \"Regressed Queries\" report and sp_query_store_force_plan as tools for catching and fixing plan regressions without code changes."
    },
    {
      "q": "Per this lesson, when is Extended Events the right tool to reach for over DMVs or Query Store?",
      "options": [
        "When you need more fine-grained detail than DMVs/Query Store provide, such as capturing every execution of a specific query with its exact parameter values",
        "Only when the SQL Server instance has no DMVs available",
        "Extended Events cannot be used in production environments",
        "Only for capturing CPU-level hardware counters"
      ],
      "answer": 0,
      "explain": "Extended Events is the lightweight, low-overhead way to capture detailed event-level data, like exact parameter values per execution, that DMVs alone don't expose."
    },
    {
      "q": "What are DMVs, per this lesson?",
      "options": [
        "Dynamic Management Views — an always-on, T-SQL-queryable window into SQL Server's internal state, at no extra cost to enable",
        "A third-party monitoring product",
        "A deprecated feature replaced entirely by Query Store",
        "Views that only administrators with sysadmin role can create manually"
      ],
      "answer": 0,
      "explain": "DMVs like sys.dm_exec_query_stats or sys.dm_os_wait_stats are built into SQL Server and queryable directly with T-SQL — no separate tool needed."
    },
    {
      "q": "What is the honest limit of third-party monitoring tools, per this lesson?",
      "options": [
        "They surface the same underlying DMV/Extended Events signals in a friendlier, dashboarded UI, but they don't tune anything for you or replace understanding why a query is slow",
        "They can automatically rewrite slow queries for you",
        "They eliminate the need to ever read an execution plan",
        "They are strictly worse than DMVs in every situation"
      ],
      "answer": 0,
      "explain": "The lesson is explicit: these tools add convenience (dashboards, alerting, UI) over the same data, not the tuning itself or the underlying diagnostic understanding."
    }
  ]
}
