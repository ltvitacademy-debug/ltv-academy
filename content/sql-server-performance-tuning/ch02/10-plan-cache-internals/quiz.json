{
  "lesson": "plan-cache-internals",
  "questions": [
    {
      "q": "What does sys.dm_exec_cached_plans track?",
      "options": [
        "One row per cached compiled plan, including usecounts, object type, and size",
        "Only currently executing queries, not cached plans",
        "Backup history, not query plans",
        "Index fragmentation levels"
      ],
      "answer": 0,
      "explain": "sys.dm_exec_cached_plans exposes the plan cache directly, with usecounts showing how often each plan has been reused."
    },
    {
      "q": "What causes ad hoc plan bloat?",
      "options": [
        "An application building SQL as a literal string with values baked in, so every distinct literal produces a cache miss and a brand-new plan",
        "Using stored procedures too often",
        "Setting max server memory too high",
        "Rebuilding indexes too frequently"
      ],
      "answer": 0,
      "explain": "Unparameterized literal SQL means each call has different query text, forcing a fresh compile and cache entry every time."
    },
    {
      "q": "What is the real fix for ad hoc plan bloat, versus what optimize for ad hoc workloads does?",
      "options": [
        "The real fix is parameterizing the application (e.g. sp_executesql); optimize for ad hoc workloads only mitigates the damage by caching a lightweight stub first",
        "optimize for ad hoc workloads is itself the complete fix",
        "There is no fix — ad hoc bloat is unavoidable",
        "Rebooting the SQL Server service is the fix"
      ],
      "answer": 0,
      "explain": "The setting reduces wasted cache memory but doesn't stop the bloat — proper parameterization is the actual fix."
    },
    {
      "q": "What does a large number of rows with usecounts = 1 and near-identical text differing only by a literal value indicate?",
      "options": [
        "The signature of ad hoc plan bloat",
        "A healthy, well-parameterized workload",
        "A backup failure",
        "An index fragmentation problem"
      ],
      "answer": 0,
      "explain": "This exact pattern — one-off plans differing only by a literal — is how you spot ad hoc bloat in the cache."
    },
    {
      "q": "In a healthy OLTP workload's plan cache, what should usecounts typically look like?",
      "options": [
        "Well above 1 for most plans, since parameterized queries and stored procedures reuse the same compiled plan repeatedly",
        "Always exactly 1 for every plan",
        "Usecounts has no relationship to workload health",
        "Usecounts should always be 0"
      ],
      "answer": 0,
      "explain": "Plan reuse — the point of the cache — shows up as usecounts well above 1 for the queries actually running repeatedly."
    }
  ]
}
