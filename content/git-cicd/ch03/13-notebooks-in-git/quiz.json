{
  "lesson": "notebooks-in-git",
  "questions": [
    {
      "q": "Why is a Jupyter notebook's diff unreadable by default, according to this lesson?",
      "options": [
        "A .ipynb file is JSON containing code, metadata, and base64-encoded image outputs, not plain text",
        "Git doesn't support the .ipynb file extension at all",
        "Jupyter notebooks can't be committed to Git",
        "It's a bug in Git that will eventually be fixed"
      ],
      "answer": 0,
      "explain": "A notebook is a structured JSON document, not plain source — its diffs get buried in metadata and embedded binary output."
    },
    {
      "q": "What does nbdime do differently from a plain diff on a notebook?",
      "options": [
        "It parses the notebook's actual structure, showing a readable code diff and rendered before/after plot comparisons",
        "It deletes all notebook outputs automatically",
        "It converts the notebook into a .py file permanently",
        "It prevents notebooks from being committed at all"
      ],
      "answer": 0,
      "explain": "nbdime understands the notebook's JSON structure, so it can separate the real code change from embedded output noise."
    },
    {
      "q": "How does nbdime integrate with your day-to-day Git workflow after setup?",
      "options": [
        "It wires directly into git diff, so the same command shows readable output automatically",
        "It requires replacing git diff with an entirely different command every time",
        "It only works inside a special nbdime-only terminal",
        "It requires manually converting every notebook before each diff"
      ],
      "answer": 0,
      "explain": "Running `nbdime config-git --enable --global` routes git diff through nbdime automatically — no new commands to remember."
    },
    {
      "q": "Which of these is a habit this lesson recommends to reduce notebook diff noise, even without extra tooling?",
      "options": [
        "Clearing outputs before committing exploratory work",
        "Never running the notebook before committing it",
        "Always committing every cell's output regardless of relevance",
        "Renaming the file to .py without changing its contents"
      ],
      "answer": 0,
      "explain": "Clearing saved outputs strips embedded images and execution counts, cutting diff noise significantly."
    },
    {
      "q": "What does this lesson suggest doing once notebook logic becomes a real, reusable pipeline step?",
      "options": [
        "Move it out of the notebook into an ordinary .py source file",
        "Duplicate it across multiple notebooks for redundancy",
        "Convert the entire notebook to a PDF",
        "Leave it in the notebook permanently, since notebooks are meant for production code"
      ],
      "answer": 0,
      "explain": "A notebook that's become a real pipeline step is a strong signal that logic belongs in ordinary source code instead."
    }
  ]
}
