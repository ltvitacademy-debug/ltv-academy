{
  "lesson": "repos-and-remotes",
  "questions": [
    {
      "q": "What is a Git \"remote,\" according to this lesson?",
      "options": [
        "A named pointer to another copy of your repository, usually hosted on GitHub",
        "A special file that must exist in every Git repository",
        "A backup that Git creates automatically every hour",
        "A branch that only administrators can access"
      ],
      "answer": 0,
      "explain": "A remote is simply a name Git gives to another copy of your repository that your local repository knows how to talk to."
    },
    {
      "q": "Is \"origin\" a required name that Git enforces for your first remote?",
      "options": [
        "No — it's just a convention; Git would work the same if you named it something else",
        "Yes — Git will refuse to push if the remote isn't named origin",
        "Yes — but only for private repositories",
        "No — GitHub renames it automatically after the first push"
      ],
      "answer": 0,
      "explain": "origin is a naming convention, not a Git rule. The name only matters because push/pull commands reference it."
    },
    {
      "q": "What two things does `git remote add` require?",
      "options": [
        "A name for the remote (e.g. origin) and a URL",
        "A username and a password",
        "A branch name and a commit hash",
        "A file path and a file size"
      ],
      "answer": 0,
      "explain": "git remote add takes a name and a URL, registering that remote on your local repository."
    },
    {
      "q": "What happens if you run `git remote add origin <url>` on a repository that already has an origin?",
      "options": [
        "Git fails with \"fatal: remote origin already exists\"",
        "Git silently overwrites the old URL with the new one",
        "Git creates a second remote automatically named origin2",
        "Git deletes the local repository's commit history"
      ],
      "answer": 0,
      "explain": "Git refuses to add a second remote under a name that's already taken. Use git remote set-url to change an existing one, or pick a new name (like upstream) for a genuinely different remote."
    },
    {
      "q": "What is the conventional name for a second remote pointing at the original repository you forked from?",
      "options": [
        "upstream",
        "origin2",
        "master",
        "backup"
      ],
      "answer": 0,
      "explain": "upstream is the common convention for the original repo you forked, kept alongside your own fork's origin remote."
    }
  ]
}
