{
  "lesson": "github-actions-basics",
  "questions": [
    {
      "q": "Where must a GitHub Actions workflow file live for GitHub to recognize it automatically?",
      "options": [
        ".github/workflows/ at the root of the repository",
        "Anywhere in the repository, as long as it ends in .yml",
        "A separate settings page must register the file's path first",
        "It must be uploaded through a special GitHub Actions website"
      ],
      "answer": 0,
      "explain": "GitHub scans .github/workflows/ automatically — committing a file there is the entire setup, no separate registration needed."
    },
    {
      "q": "Where do you find every workflow run for a repository, past and present?",
      "options": [
        "The Actions tab",
        "The Issues tab",
        "The repository's Settings page",
        "A separate GitHub Actions dashboard on a different domain"
      ],
      "answer": 0,
      "explain": "The Actions tab lists every run of every workflow file in the repository."
    },
    {
      "q": "What does a workflow run's summary page show?",
      "options": [
        "What triggered it, how long it took, and whether each job succeeded",
        "Only the exact line of code that changed",
        "The billing information for the entire GitHub account",
        "Nothing useful — you must read raw logs to know anything"
      ],
      "answer": 0,
      "explain": "The summary gives an at-a-glance view of the trigger, duration, and pass/fail status."
    },
    {
      "q": "According to this lesson, where do you actually go to debug a failing pipeline step?",
      "options": [
        "Into the job's expanded step-by-step logs, to find the specific step that failed",
        "The repository's README file",
        "The commit message of the triggering push",
        "You cannot debug a failure — you must re-run the whole workflow blindly"
      ],
      "answer": 0,
      "explain": "Expanded step logs show exactly which step failed and its output, rather than just an overall red X on the job."
    },
    {
      "q": "What triggers a workflow to run, based on the example in this lesson?",
      "options": [
        "The event specified under `on:`, such as a push",
        "A human manually clicking a 'Run Now' button every time",
        "A scheduled email reminder",
        "Nothing — workflows run continuously with no trigger"
      ],
      "answer": 0,
      "explain": "The `on:` key defines the triggering event — in the example, `on: [push]` runs the workflow on every push."
    }
  ]
}
