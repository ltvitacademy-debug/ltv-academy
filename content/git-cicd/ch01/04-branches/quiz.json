{
  "lesson": "branches",
  "questions": [
    {
      "q": "Why would you branch before reworking the grain of a dbt model that feeds production dashboards?",
      "options": [
        "Committing directly on main would expose your half-finished, possibly broken work to everyone using the project immediately",
        "Branching is required by Git before any commit can happen",
        "Branches make your SQL queries run faster",
        "There is no reason — branching is purely optional busywork"
      ],
      "answer": 0,
      "explain": "A branch keeps risky, in-progress work isolated from main until it's ready, so production dashboards aren't affected mid-change."
    },
    {
      "q": "What does git switch -c rework-ltv-grain do?",
      "options": [
        "Creates a new branch called rework-ltv-grain and switches to it in one step",
        "Deletes the current branch",
        "Merges rework-ltv-grain into main immediately",
        "Renames the main branch"
      ],
      "answer": 0,
      "explain": "-c tells git switch to create the named branch and check it out immediately, equivalent to git checkout -b."
    },
    {
      "q": "According to this lesson, what happens to main while you commit on a separate branch?",
      "options": [
        "main doesn't move — your branch's commits only extend that branch's own line of history",
        "main is automatically updated with every commit you make",
        "main is deleted",
        "main and your branch merge automatically after every commit"
      ],
      "answer": 0,
      "explain": "Commits on a branch only extend that branch; main stays exactly where it was until you explicitly merge or rebase."
    },
    {
      "q": "What happens to your in-progress schema-rework commits when you run git switch main?",
      "options": [
        "Nothing is lost — they're safely committed on the other branch, just not currently checked out",
        "They are permanently deleted",
        "They are automatically merged into main",
        "Git refuses to switch branches if there are any commits"
      ],
      "answer": 0,
      "explain": "Switching branches changes which commits are checked out on disk; committed work on the other branch remains intact and waiting."
    },
    {
      "q": "What does running git branch with no arguments do?",
      "options": [
        "Lists every branch you have and marks the current one with an asterisk",
        "Creates a new branch with a random name",
        "Deletes all branches except main",
        "Pushes all branches to GitHub"
      ],
      "answer": 0,
      "explain": "git branch with no arguments simply lists local branches, marking the currently checked-out one with an asterisk."
    }
  ]
}
