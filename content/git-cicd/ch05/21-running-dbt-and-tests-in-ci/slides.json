[
  {
    "type": "title",
    "eyebrow": "Git, GitHub & CI/CD for Data · Chapter 5, Lesson 21",
    "titlePlain": "Running dbt Models & Tests in ",
    "titleEmphasis": "CI",
    "titleSuffix": "",
    "subtitle": "Picking up right after dbt deps in Lesson 20's workflow"
  },
  {
    "type": "code",
    "eyebrow": "A profiles.yml generated at CI time",
    "code": "retail_orders_analytics:\n  target: ci\n  outputs:\n    ci:\n      type: postgres\n      host: \"{{ env_var('DB_HOST') }}\"\n      password: \"{{ env_var('DB_PASSWORD') }}\"",
    "caption": "Every real value comes from env_var — nothing sensitive is written as literal text."
  },
  {
    "type": "code",
    "eyebrow": "The step that runs dbt",
    "code": "- name: Run dbt build\n  env:\n    DB_HOST: ${{ secrets.DB_HOST }}\n    DB_PASSWORD: ${{ secrets.DB_PASSWORD }}\n  run: dbt build --profiles-dir .",
    "caption": "dbt build, not run + test separately — it stops on upstream test failures."
  },
  {
    "type": "steps",
    "eyebrow": "Reading a real failure",
    "titlePlain": "The same output, ",
    "titleEmphasis": "captured",
    "titleSuffix": " and turned red",
    "steps": [
      { "label": "Which test failed", "sub": "e.g. not_null_stg_orders_order_id" },
      { "label": "How many rows violated it", "sub": "Same output dbt prints locally" },
      { "label": "PR shows a failing check", "sub": "Before a reviewer opens the diff" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Deploying on Merge to ",
    "titleEmphasis": "Main",
    "titleSuffix": "",
    "subtitle": "What happens after a pull request with a passing pipeline merges"
  }
]
