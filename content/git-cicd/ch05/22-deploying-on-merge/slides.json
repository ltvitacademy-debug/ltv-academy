[
  {
    "type": "title",
    "eyebrow": "Git, GitHub & CI/CD for Data · Chapter 5, Lesson 22",
    "titlePlain": "Deploying on Merge to ",
    "titleEmphasis": "Main",
    "titleSuffix": "",
    "subtitle": "A PR and a merge are different moments — they need different jobs"
  },
  {
    "type": "code",
    "eyebrow": "Two jobs, two conditions",
    "code": "build-and-test:\n  if: github.event_name == 'pull_request'\n\ndeploy:\n  if: github.event_name == 'push' &&\n      github.ref == 'refs/heads/main'",
    "caption": "Every PR gets checked. Only a real merge to main deploys."
  },
  {
    "type": "code",
    "eyebrow": "One profiles.yml, two targets",
    "code": "outputs:\n  ci:\n    schema: ci\n  prod:\n    schema: analytics",
    "caption": "--target prod switches the schema — same models, same tests, real data."
  },
  {
    "type": "steps",
    "eyebrow": "What \"deploy\" means here",
    "titlePlain": "No server ",
    "titleEmphasis": "to restart",
    "titleSuffix": "",
    "steps": [
      { "label": "Web app deploy", "sub": "Restart a running process" },
      { "label": "dbt build --target prod", "sub": "Running it successfully IS the deploy" },
      { "label": "Command finishes", "sub": "Production tables already reflect the change" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Environment Variables & ",
    "titleEmphasis": "Secrets",
    "titleSuffix": "",
    "subtitle": "Where production credentials come from, and how they stay out of the repo"
  }
]
