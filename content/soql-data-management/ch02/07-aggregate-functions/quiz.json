{
  "lesson": "aggregate-functions",
  "questions": [
    {
      "q": "Which set lists the aggregate functions covered in this lesson?",
      "options": [
        "COUNT, SUM, AVG, MIN, MAX",
        "COUNT, TOTAL, MEAN, LOW, HIGH",
        "SUM, ROUND, TOP, BOTTOM, FIRST",
        "COUNT, SUM, MEDIAN, MODE, RANGE"
      ],
      "answer": 0,
      "explain": "SOQL supports COUNT, SUM, AVG, MIN, and MAX, plus COUNT_DISTINCT."
    },
    {
      "q": "What is the difference between COUNT() and COUNT(Email)?",
      "options": [
        "COUNT() returns the number of matching rows, while COUNT(Email) counts only rows where Email is not null",
        "They always return the same number",
        "COUNT() counts only null values",
        "COUNT(Email) counts the characters in the Email field"
      ],
      "answer": 0,
      "explain": "COUNT() counts matching rows, like COUNT(*) in T-SQL. COUNT(field) skips rows where the field is null."
    },
    {
      "q": "How do you alias an aggregate in SOQL?",
      "options": [
        "Place the alias directly after the function, as in SUM(Amount) totalAmount, with no AS keyword",
        "SUM(Amount) AS totalAmount is required",
        "Aliases are not supported in SOQL",
        "Use square brackets, as in SUM(Amount)[totalAmount]"
      ],
      "answer": 0,
      "explain": "SOQL aliases follow the function directly with no AS. Without an alias, columns are named expr0, expr1, and so on."
    },
    {
      "q": "What kind of records does an aggregate query with GROUP BY or aggregate fields return, and how does Apex read the values?",
      "options": [
        "AggregateResult records, read by alias, such as ar.get('totalAmount')",
        "Standard Opportunity records with extra fields",
        "A single string with comma-separated values",
        "The query returns nothing until it is saved"
      ],
      "answer": 0,
      "explain": "Aggregate queries return AggregateResult records. Values are read by alias, or by expr0, expr1 if there is no alias."
    },
    {
      "q": "Why does SELECT StageName, COUNT(Id) FROM Opportunity fail without a GROUP BY?",
      "options": [
        "A plain field mixed with an aggregate needs GROUP BY so SOQL knows how to group the rows",
        "StageName is not a valid Opportunity field",
        "COUNT cannot be used on the Id field",
        "Aggregates can only run on custom objects"
      ],
      "answer": 0,
      "explain": "Mixing a plain field with an aggregate requires GROUP BY, which is covered in the next lesson."
    }
  ]
}
