{
  "lesson": "capstone-load-it-back-safely",
  "questions": [
    {
      "q": "Why do the 61 Accounts load before the 1,240 Contacts?",
      "options": [
        "Contacts reference their parent Account, so the parents must exist first for the link to resolve",
        "Data Loader can only process Accounts first alphabetically",
        "Accounts load faster than Contacts",
        "Contacts cannot be loaded while any Accounts exist"
      ],
      "answer": 0,
      "explain": "Parents before children: a Contact can only be linked to an Account that already exists in the org."
    },
    {
      "q": "What does an upsert on Legacy_Id__c do when a row's key already matches an existing record?",
      "options": [
        "Updates that record instead of inserting a duplicate",
        "Always inserts a new record anyway",
        "Deletes the existing record and recreates it",
        "Skips the row silently"
      ],
      "answer": 0,
      "explain": "Upsert updates the record with the matching External ID and inserts only when no match exists, so a repeated run won't duplicate records."
    },
    {
      "q": "In the Contact upsert mapping, how should the Parent_Legacy_Id__c column be mapped?",
      "options": [
        "To the Account relationship's Legacy_Id__c, so Salesforce looks up the parent by External ID",
        "To Contact.Id",
        "To the Contact's LastName field",
        "It should not be mapped at all"
      ],
      "answer": 0,
      "explain": "Mapping to the parent relationship's External ID lets Salesforce resolve the parent, with no Salesforce record Ids needed in the file."
    },
    {
      "q": "Why is the surplus-Lead delete the last load step?",
      "options": [
        "It is the least reversible step, so everything else is verified before it runs",
        "Delete must always run after inserts for technical reasons",
        "Leads cannot be deleted until Contacts are relinked",
        "Deleting earlier would change the Account count"
      ],
      "answer": 0,
      "explain": "Order the riskiest change last. Deleted records do go to the Recycle Bin, but validating everything else first keeps mistakes small."
    },
    {
      "q": "A Data Loader run finishes with some rows in the error file. What is the right next move?",
      "options": [
        "Read the error column, fix the cause, and re-run only the failed rows before continuing",
        "Ignore the error file and proceed to the next load file",
        "Re-run the entire file from the beginning",
        "Delete the error file and check again tomorrow"
      ],
      "answer": 0,
      "explain": "Confirm success plus errors equals input rows, understand each error, and re-run only failures. Don't move on with unresolved errors."
    }
  ]
}
