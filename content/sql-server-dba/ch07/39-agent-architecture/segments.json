[
  {"slide": "s1", "text": "SQL Server Agent runs the scheduled and event-driven work a production instance depends on — backups, index maintenance, ETL, custom T-SQL — as its own separate Windows service."},
  {"slide": "s2", "text": "Agent is independent of the Database Engine. If the Agent service stops, the Engine keeps answering queries just fine — which means Agent can silently be down while nothing else looks wrong, until a backup job that never ran gets noticed."},
  {"slide": "s3", "text": "Everything Agent manages lives in msdb: sysjobs, sysjobsteps, sysschedules, sysoperators, sysalerts, and sysjobhistory. The SSMS Agent interface is really just reading and writing these tables."},
  {"slide": "s4", "text": "Five pieces work together: a job made of steps, run in sequence, a reusable schedule for when it runs, and an alert tied to an operator who actually gets notified."},
  {"slide": "s5", "text": "Next up: creating jobs and steps directly with T-SQL, using sp_add_job and sp_add_jobstep."}
]
