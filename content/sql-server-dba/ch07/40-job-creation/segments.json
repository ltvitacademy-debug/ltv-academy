[
  {"slide": "s1", "text": "This lesson gets hands-on with the first piece of Agent's object model — creating a job and its steps directly with T-SQL, the same system stored procedures the SSMS New Job dialog calls behind the scenes."},
  {"slide": "s2", "text": "sp_add_job creates the job row in msdb.dbo.sysjobs — but at this point the job has no steps and isn't targeted at any server, so it can't run anything yet."},
  {"slide": "s3", "text": "sp_add_jobstep adds a step, executed by a specific subsystem — TSQL, CmdExec, PowerShell, or SSIS. On_success_action and on_fail_action control whether the job moves to the next step or quits, reporting success or failure."},
  {"slide": "s4", "text": "Three calls assemble a runnable job: sp_add_job creates it, sp_add_jobstep adds each action in order, and sp_add_jobserver targets it at a server so it can actually execute."},
  {"slide": "s5", "text": "Next up: attaching recurring schedules to a job with sp_add_schedule."}
]
