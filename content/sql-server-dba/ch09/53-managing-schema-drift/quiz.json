{
  "lesson": "managing-schema-drift",
  "questions": [
    {
      "q": "What is schema drift?",
      "options": [
        "The gap between what source control says a schema should be and what the live database actually looks like",
        "A SQL Server feature for automatically syncing environments",
        "The natural growth of a transaction log over time",
        "A type of index fragmentation"
      ],
      "answer": 0,
      "explain": "Drift is the mismatch between the source-of-truth (source control) and the live, actual state of the database."
    },
    {
      "q": "Which scenario, from this lesson, is a typical, mundane cause of schema drift?",
      "options": [
        "An index added directly in production during an incident that never gets backported to source control",
        "A DACPAC deployment completing successfully",
        "A scheduled backup job running on time",
        "A stored procedure being called by an application"
      ],
      "answer": 0,
      "explain": "Drift usually starts with an ordinary out-of-process change — like an emergency production fix — that never makes it back into source control."
    },
    {
      "q": "Why is drift particularly dangerous during a DACPAC-based deployment?",
      "options": [
        "The deployment tool trusts that the live database matches what it expects, so drift can cause the diff to fail or silently undo an undocumented fix",
        "DACPAC deployments are immune to drift by design",
        "Drift only affects manual scripted deployments, never DACPACs",
        "DACPACs cannot be used once drift exists, permanently"
      ],
      "answer": 0,
      "explain": "A DACPAC's generated diff assumes the live database matches its expected starting state; drift breaks that assumption and can cause failures or unintended reversions."
    },
    {
      "q": "What is the practical way to detect schema drift before it causes a deployment failure, per this lesson?",
      "options": [
        "Run a recurring schema-compare between a source-control snapshot (e.g., a DACPAC) and the live database, not just at deployment time",
        "Wait for a deployment to fail and investigate afterward",
        "Disable all indexes to prevent drift from occurring",
        "Restore from backup weekly regardless of need"
      ],
      "answer": 0,
      "explain": "SSDT's schema-compare tooling, run as a recurring check, catches drift while it's still small instead of at deployment time."
    },
    {
      "q": "According to this lesson, is schema drift primarily a technical problem or a process problem?",
      "options": [
        "A process problem — the real fix is closing the gap that let out-of-process changes happen in the first place",
        "Purely a technical problem solved entirely by better deployment tooling",
        "An inevitability that cannot be reduced by any process change",
        "A hardware issue related to disk latency"
      ],
      "answer": 0,
      "explain": "Detecting drift with schema-compare helps, but the real fix is a process rule that no live change ships without a matching source-control update."
    }
  ]
}
