{
  "lesson": "service-accounts-and-configuration-manager",
  "questions": [
    {
      "q": "Why is Local System considered a poor choice for a production SQL Server service account?",
      "options": [
        "It's the most privileged built-in account and a shared identity with no accountability to a specific service",
        "It doesn't allow SQL Server to start at all",
        "It's only available on Linux installations",
        "It requires a domain to function"
      ],
      "answer": 0,
      "explain": "Local System grants far more machine-level privilege than the service needs, with no way to audit which service used it."
    },
    {
      "q": "What makes a group Managed Service Account (gMSA) the current best practice on a domain?",
      "options": [
        "It behaves as a least-privilege, individually-auditable identity while Active Directory rotates its password automatically",
        "It has no password at all and requires no domain",
        "It is identical in privilege to Local System but easier to type",
        "It only works with SQL Server Express"
      ],
      "answer": 0,
      "explain": "A gMSA removes the manual password-rotation burden a plain dedicated domain account carries, without giving up least-privilege benefits."
    },
    {
      "q": "Why should you change a SQL Server service account through Configuration Manager rather than services.msc?",
      "options": [
        "Configuration Manager also grants the account the right to act as a service and adjusts registry/file permissions the change depends on",
        "services.msc is not able to change any service's account at all",
        "Configuration Manager is required by Windows for all services, not just SQL Server",
        "There is no real difference; either approach works identically"
      ],
      "answer": 0,
      "explain": "Changing the account directly in services.msc can leave the instance in a broken state because the necessary permission grants weren't applied."
    },
    {
      "q": "What is SQL Server Configuration Manager's actual job?",
      "options": [
        "Managing SQL Server services and network protocol configuration for every installed instance and component on a machine",
        "Writing and executing T-SQL queries against a database",
        "Managing SQL Server Agent job schedules",
        "Managing Windows Update settings"
      ],
      "answer": 0,
      "explain": "It's a dedicated MMC snap-in for service and network management, separate from SQL Server Management Studio, which is for T-SQL and object management."
    },
    {
      "q": "Which network protocol is the standard for real client-server SQL Server connectivity and must be enabled for anything beyond local connections?",
      "options": [
        "TCP/IP",
        "Shared Memory",
        "Named Pipes",
        "HTTP"
      ],
      "answer": 0,
      "explain": "Shared Memory only works for connections originating on the same machine, and Named Pipes is mostly used for local/same-machine scenarios; TCP/IP is needed for real network client-server connectivity."
    }
  ]
}
