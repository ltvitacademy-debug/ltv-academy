{
  "lesson": "installing-sql-server",
  "questions": [
    {
      "q": "What is the difference between a default instance and a named instance during Setup's Instance Configuration screen?",
      "options": [
        "A default instance is unnamed, one per machine; a named instance has its own name and multiple can coexist on one machine",
        "A named instance can only be installed on Linux, never Windows",
        "There is no difference — the terms are interchangeable",
        "A default instance requires SQL Server Browser; a named instance does not"
      ],
      "answer": 0,
      "explain": "Named instances require SQL Server Browser running so clients can resolve the dynamic port; a machine can have one default instance plus any number of named instances."
    },
    {
      "q": "What must you set during the Database Engine Configuration screen if you choose Mixed Mode authentication?",
      "options": [
        "The sa password, right there in the wizard",
        "The server's IP address",
        "The Windows domain administrator's password",
        "Nothing extra — Mixed Mode requires no additional configuration"
      ],
      "answer": 0,
      "explain": "Mixed Mode allows both Windows logins and SQL Server logins like sa, and the wizard requires you to set the sa password when you choose it."
    },
    {
      "q": "Why does the Data Directories screen matter, per this lesson?",
      "options": [
        "It's where the file-layout decision from planning actually gets applied, setting default paths for data, log, and tempdb files",
        "It only affects where log files for the setup process itself are written",
        "It has no real effect since paths can be freely changed after install with no downside",
        "It only appears when installing Analysis Services"
      ],
      "answer": 0,
      "explain": "This screen is exactly why the file-layout decision needed to happen during planning, before this moment in the wizard."
    },
    {
      "q": "Which of the following does the Setup wizard NOT configure, according to this lesson?",
      "options": [
        "Max server memory and MAXDOP",
        "Feature selection",
        "Authentication mode",
        "Default data and log file paths"
      ],
      "answer": 0,
      "explain": "Max server memory, MAXDOP, cost threshold for parallelism, tempdb file count, and backup compression default are all post-install configuration steps, not part of Setup."
    },
    {
      "q": "Why is Windows Authentication mode described as more secure than Mixed Mode when every client can authenticate via Active Directory?",
      "options": [
        "It trusts only Windows/AD logins, eliminating the need for a separate SQL Server login like sa with its own password to manage",
        "It disables all network connectivity to the instance",
        "It is only available in Enterprise edition",
        "It automatically encrypts all data at rest"
      ],
      "answer": 0,
      "explain": "Windows-only mode removes SQL Server logins (including sa) from the picture entirely when AD authentication already covers every client."
    }
  ]
}
