{
  "lesson": "testing-upgrades",
  "questions": [
    {
      "q": "What is the recommended core setup for a trustworthy upgrade test?",
      "options": [
        "Restore an actual, current production backup to a staging instance running the target version, then run the real application workload against it",
        "Create a fresh empty database on the new version and run a few sample queries",
        "Skip testing entirely if the upgrade is only a Cumulative Update",
        "Test only the system databases, since user databases are unaffected by upgrades"
      ],
      "answer": 0,
      "explain": "Real production data and a real workload surface data-skew, fragmentation, and scale issues that a fresh test database with sample rows would never reveal."
    },
    {
      "q": "What does Query Store provide that makes upgrade regression testing concrete rather than vague?",
      "options": [
        "A Regressed Queries report ranking queries by performance change, with the old and new execution plans shown side by side",
        "Automatic rollback of any query that runs slower after an upgrade",
        "A guarantee that no query will ever regress after an upgrade",
        "A replacement for taking backups before an upgrade"
      ],
      "answer": 0,
      "explain": "Query Store records historical plans and runtime stats, letting you directly compare before and after an upgrade via its Regressed Queries report."
    },
    {
      "q": "If Query Store reveals a specific query regressed after a test upgrade, what stopgap procedure can force it back to its prior plan?",
      "options": [
        "sp_query_store_force_plan",
        "DBCC CHECKDB",
        "ALTER DATABASE SET COMPATIBILITY_LEVEL",
        "sp_helpdb"
      ],
      "answer": 0,
      "explain": "sp_query_store_force_plan can force a specific historical plan for a query as a temporary fix while the underlying cause is investigated."
    },
    {
      "q": "According to this lesson, what does 'passing' an upgrade test actually require?",
      "options": [
        "The application functions correctly end-to-end, no query shows an unacceptable regression in Query Store, and flagged breaking changes are addressed",
        "The installer completing without throwing an error",
        "The new version number appearing correctly in @@VERSION",
        "Successfully restoring the backup, regardless of application behavior afterward"
      ],
      "answer": 0,
      "explain": "The lesson explicitly states that 'it installed without errors' is the bare minimum, not a passing test — passing requires measured application and query behavior."
    },
    {
      "q": "Why is a fresh, empty database with a handful of sample queries an inadequate upgrade test?",
      "options": [
        "It won't surface data-skew, fragmentation, or scale/concurrency issues that only appear with real production data and workload",
        "Empty databases cannot be restored on a new SQL Server version",
        "Sample queries always run faster than real production queries, hiding no issues",
        "SQL Server refuses to run on a database with fewer than a minimum row count"
      ],
      "answer": 0,
      "explain": "Realistic data distributions, index fragmentation, and production-scale volume and concurrency are exactly what surface optimizer and plan-related upgrade problems."
    }
  ]
}
