[
  {
    "type": "title",
    "eyebrow": "Data Modeling & Data Warehousing · Chapter 8, Lesson 38",
    "titlePlain": "Building the ",
    "titleEmphasis": "Physical Schema",
    "titleSuffix": "",
    "subtitle": "Turning the design into real CREATE TABLE statements"
  },
  {
    "type": "code",
    "eyebrow": "SCD Type 2 in real T-SQL",
    "code": "CREATE TABLE dw.DimVendor (\n  VendorKey INT IDENTITY(1,1) PRIMARY KEY,\n  VendorAlternateKey INT NOT NULL,\n  CreditRating TINYINT NOT NULL,\n  EffectiveDate DATE NOT NULL,\n  ExpirationDate DATE NULL,\n  IsCurrent BIT NOT NULL DEFAULT 1\n);",
    "caption": "DimVendor — the only Type 2 dimension in this design"
  },
  {
    "type": "code",
    "eyebrow": "The accumulating snapshot fact table",
    "code": "CREATE TABLE dw.FactPurchaseOrderLine (\n  PurchaseOrderLineKey INT IDENTITY(1,1) PK,\n  VendorKey INT NOT NULL,\n  OrderDateKey INT NOT NULL,\n  PurchaseOrderID INT NOT NULL,\n  ReceivedQty DECIMAL(8,2) NOT NULL\n);",
    "caption": "Surrogate FKs, a milestone date, a degenerate dimension, and a measure"
  },
  {
    "type": "steps",
    "eyebrow": "Making the fact table perform",
    "titlePlain": "Indexing ",
    "titleEmphasis": "and",
    "titleSuffix": " partitioning",
    "steps": [
      { "label": "Nonclustered index", "sub": "Filter by vendor + date, join to dims" },
      { "label": "Clustered columnstore", "sub": "Standard choice for a large fact table" },
      { "label": "Partition by OrderDateKey", "sub": "Matches how it's queried and loaded" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Wrap-Up & ",
    "titleEmphasis": "Portfolio",
    "titleSuffix": " Presentation",
    "subtitle": "Checking the design and making it resume-ready"
  }
]
