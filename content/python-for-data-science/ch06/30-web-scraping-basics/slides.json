[
  {
    "type": "title",
    "eyebrow": "Python for Data Science · Chapter 6",
    "titlePlain": "Web Scraping ",
    "titleEmphasis": "Basics",
    "titleSuffix": "",
    "subtitle": "Lesson 30 · Files, Databases & APIs"
  },
  {
    "type": "steps",
    "eyebrow": "Before you write any code",
    "titlePlain": "Scrape ",
    "titleEmphasis": "responsibly",
    "titleSuffix": "",
    "steps": [
      {
        "label": "Is there an API?",
        "sub": "An official feed beats scraping"
      },
      {
        "label": "Read robots.txt",
        "sub": "What may automated tools fetch?"
      },
      {
        "label": "Read the terms of use",
        "sub": "Some sites forbid scraping outright"
      },
      {
        "label": "Be gentle",
        "sub": "Identify yourself; pause between calls"
      }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Check robots.txt, then fetch",
    "code": "rp = RobotFileParser()\nrp.set_url(base + \"robots.txt\")\nrp.read()\nrp.can_fetch(\"*\", base)   # True\n\nhdr = {\"User-Agent\": \"class-demo\"}\nresp = requests.get(base, headers=hdr,\n                    timeout=10)",
    "caption": "Practice site: books.toscrape.com (a scraping sandbox)."
  },
  {
    "type": "code",
    "eyebrow": "Parse the HTML",
    "code": "soup = BeautifulSoup(\n    resp.text, \"html.parser\")\ncards = soup.select(\n    \"article.product_pod\")\nlen(cards)                # 20\n\nc = cards[0]\nc.h3.a[\"title\"]  # A Light in the Attic",
    "caption": "select() takes a CSS selector."
  },
  {
    "type": "code",
    "eyebrow": "Rows to a DataFrame",
    "code": "rows = []\nfor card in cards:\n    rows.append({\n        \"title\": card.h3.a[\"title\"],\n        \"price\": card.select_one(\n            \"p.price_color\"\n        ).get_text(strip=True),\n    })\ndf = pd.DataFrame(rows)",
    "caption": "Then clean: strip the £ sign, to_numeric()."
  },
  {
    "type": "steps",
    "eyebrow": "Why scrapers break",
    "titlePlain": "Expect ",
    "titleEmphasis": "fragility",
    "titleSuffix": "",
    "steps": [
      {
        "label": "Layout changes",
        "sub": "Your selectors stop matching"
      },
      {
        "label": "JavaScript content",
        "sub": "requests sees only the empty shell"
      },
      {
        "label": "Blocked or asked to stop",
        "sub": "Slow down, or stop"
      }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 31",
    "titlePlain": "Clean Code ",
    "titleEmphasis": "for Notebooks",
    "titleSuffix": "",
    "subtitle": "Chapter 7 begins: writing reliable data code."
  }
]