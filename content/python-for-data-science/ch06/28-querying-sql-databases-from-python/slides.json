[
  {
    "type": "title",
    "eyebrow": "Python for Data Science · Chapter 6",
    "titlePlain": "Querying SQL Databases ",
    "titleEmphasis": "From Python",
    "titleSuffix": "",
    "subtitle": "Lesson 28 · Files, Databases & APIs"
  },
  {
    "type": "code",
    "eyebrow": "Connection + query = DataFrame",
    "code": "import sqlite3\nconn = sqlite3.connect(\":memory:\")\norders.to_sql(\"orders\", conn,\n              index=False)\n\ndf = pd.read_sql_query(\n    \"SELECT * FROM orders\", conn)",
    "caption": "SQLite ships with Python, so no server is needed."
  },
  {
    "type": "code",
    "eyebrow": "SQL Server · illustrative, not run",
    "code": "from sqlalchemy import create_engine\n\nengine = create_engine(\n  \"mssql+pyodbc://USER:PWD@myhost/\"\n  \"AdventureWorks2012?driver=\"\n  \"ODBC+Driver+17+for+SQL+Server\")\n\npd.read_sql_query(sql, engine)",
    "caption": "Check your driver version; keep PWD in env vars."
  },
  {
    "type": "code",
    "eyebrow": "The T-SQL you already know",
    "code": "q = \"\"\"\nSELECT c.city, COUNT(*) AS n_orders,\n       SUM(o.amount) AS revenue\nFROM orders o\nJOIN customers c\n  ON c.customer_id = o.customer_id\nGROUP BY c.city\nORDER BY revenue DESC\"\"\"\npd.read_sql_query(q, conn)",
    "caption": "Illustrative result: Atlanta 3 orders, 490.0 revenue."
  },
  {
    "type": "code",
    "eyebrow": "Parameters, never string pasting",
    "code": "pd.read_sql_query(\n    \"SELECT * FROM orders\"\n    \" WHERE status = ?\",\n    conn, params=(bad,))\n\n# bad = \"shipped' OR '1'='1\"\n# f-string  -> 6 rows (all!)\n# params    -> 0 rows",
    "caption": "Parameters stop SQL injection."
  },
  {
    "type": "steps",
    "eyebrow": "SQL or pandas?",
    "titlePlain": "Push down, then ",
    "titleEmphasis": "pull once",
    "titleSuffix": "",
    "steps": [
      {"label": "Filter in SQL", "sub": "The database is built for it"},
      {"label": "Explore in pandas", "sub": "Pull only the rows you need"},
      {"label": "Mind the dialect", "sub": "T-SQL TOP vs SQLite LIMIT"},
      {"label": "Write back", "sub": "to_sql(..., if_exists=\"replace\")"}
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 29",
    "titlePlain": "Pulling Data ",
    "titleEmphasis": "From APIs",
    "titleSuffix": "",
    "subtitle": "Requests, JSON, and turning responses into DataFrames."
  }
]
