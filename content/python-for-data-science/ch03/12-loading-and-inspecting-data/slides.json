[
  {
    "type": "title",
    "eyebrow": "Python for Data Science · Chapter 3",
    "titlePlain": "Loading & ",
    "titleEmphasis": "Inspecting Data",
    "titleSuffix": "",
    "subtitle": "Lesson 12 · pandas Fundamentals"
  },
  {
    "type": "code",
    "eyebrow": "Load a CSV",
    "code": "import io\nimport pandas as pd\n\n# csv holds the file text\norders = pd.read_csv(io.StringIO(csv))\n\n# with a real file:\n# pd.read_csv(\"orders.csv\")",
    "caption": "Order 5 has an empty amount, on purpose."
  },
  {
    "type": "steps",
    "eyebrow": "The inspection routine",
    "titlePlain": "Four checks, ",
    "titleEmphasis": "every time",
    "titleSuffix": "",
    "steps": [
      {"label": "head / tail", "sub": "Check both ends of the file"},
      {"label": "info()", "sub": "Types and non-null counts"},
      {"label": "describe()", "sub": "Numeric ranges at a glance"},
      {"label": "isna().sum()", "sub": "Missing values per column"}
    ]
  },
  {
    "type": "code",
    "eyebrow": "Reading info() like a checklist",
    "code": "orders.info()\n\n# order_id    8 non-null   int64\n# order_date  8 non-null   object\n# amount      7 non-null   float64\n#\n# object = dates loaded as text\n# 7 non-null = one missing amount",
    "caption": "Two problems spotted in one call."
  },
  {
    "type": "code",
    "eyebrow": "Fix it while loading",
    "code": "o2 = pd.read_csv(\n    io.StringIO(csv),\n    parse_dates=[\"order_date\"],\n    usecols=[\"order_id\", \"order_date\",\n             \"amount\"])\n\no2[\"order_date\"].dtype\n# datetime64[ns]",
    "caption": "nrows=5 would read only the first 5 lines."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 13",
    "titlePlain": "Selecting & ",
    "titleEmphasis": "Filtering",
    "titleSuffix": "",
    "subtitle": "Pull out exactly the rows and columns you need."
  }
]
