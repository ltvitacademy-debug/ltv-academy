[
  {
    "type": "title",
    "eyebrow": "Python for Data Science · Chapter 3",
    "titlePlain": "Creating & Transforming ",
    "titleEmphasis": "Columns",
    "titleSuffix": "",
    "subtitle": "Lesson 14 · pandas Fundamentals"
  },
  {
    "type": "steps",
    "eyebrow": "Four tools cover most of it",
    "titlePlain": "New columns, ",
    "titleEmphasis": "four ways",
    "titleSuffix": "",
    "steps": [
      {"label": "Arithmetic", "sub": "New numbers from old ones"},
      {"label": "np.where", "sub": "SQL's CASE WHEN"},
      {"label": "map / pd.cut", "sub": "Recode and bucket values"},
      {"label": "astype", "sub": "Fix a column's type"}
    ]
  },
  {
    "type": "code",
    "eyebrow": "Arithmetic creates a column",
    "code": "o = orders.copy()\n\no[\"amount_with_tax\"] = (\n    o[\"amount\"] * 1.08).round(2)\n\n# 120.50 -> 130.14\n# NaN    -> NaN (missing stays missing)",
    "caption": "Illustrative 8% tax rate."
  },
  {
    "type": "code",
    "eyebrow": "np.where, and its trap",
    "code": "import numpy as np\n\no[\"size\"] = np.where(\n    o[\"amount\"] >= 100,\n    \"large\", \"small\")\n\n# order 5 (amount NaN) -> \"small\"",
    "caption": "NaN >= 100 is False: a silent wrong answer."
  },
  {
    "type": "code",
    "eyebrow": "Recode and bucket",
    "code": "o[\"status\"].map({\n    \"shipped\": \"done\",\n    \"returned\": \"issue\",\n    \"pending\": \"open\"})\n\npd.cut(o[\"amount\"],\n       bins=[0, 75, 150, 1000],\n       labels=[\"low\", \"mid\", \"high\"])",
    "caption": "Unmapped values in map() become NaN."
  },
  {
    "type": "code",
    "eyebrow": "Housekeeping",
    "code": "o[\"order_id\"] = (\n    o[\"order_id\"].astype(str))\n\no.rename(columns={\"amount\": \"total\"})\no.drop(columns=[\"customer_id\"])\n\n# rename/drop return a NEW DataFrame",
    "caption": "Prefer vectorized code over apply."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 15",
    "titlePlain": "Sorting, Ranking & ",
    "titleEmphasis": "Value Counts",
    "titleSuffix": "",
    "subtitle": "ORDER BY and quick frequency tables in pandas."
  }
]
