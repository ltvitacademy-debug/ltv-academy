[
  {"slide": "s1", "text": "Every data scientist has shipped a number that turned out to be wrong. Usually it wasn't a dramatic crash. A join quietly duplicated rows, or a small tweak to a cleaning function changed a total, and nobody re-checked. Tests are how you catch those mistakes before your stakeholders do."},
  {"slide": "s2", "text": "A test is just a small function that checks one behavior and fails loudly if it's wrong. Arrange a tiny input you can verify in your head, act by calling your function, then assert the result matches. Here, two rows with quantities two and three and prices five and four should give revenue of ten and twelve. Name the test after the behavior, and a runner like pytest will find every function whose name starts with test, and report which passed and failed."},
  {"slide": "s3", "text": "Now imagine someone refactors add revenue and types a plus sign where a multiply sign belonged. On our sample the result becomes seven and seven, instead of ten and twelve. The assertion fails immediately. Without that test, the wrong revenue would flow quietly into every chart downstream."},
  {"slide": "s4", "text": "For whole tables, pandas gives you assert frame equal, which compares columns, order, values, and data types. And tests check your code, while data checks examine the real data each time it arrives. Assert that order IDs are unique and revenue is never negative, and a pipeline stops before bad numbers spread."},
  {"slide": "s5", "text": "What should you test first? Business rules, like revenue and discounts. Edge cases, like empty input, missing values, and duplicates. And every bug you've ever fixed, so it never returns."},
  {"slide": "s6", "text": "Tests protect your code today. Next up, reproducibility, so your results come out the same tomorrow."}
]
