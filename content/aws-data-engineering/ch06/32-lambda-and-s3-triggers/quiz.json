{
  "lesson": "lambda-and-s3-triggers",
  "questions": [
    {
      "q": "What event type fires when a new object is uploaded to S3?",
      "options": [
        "s3:ObjectCreated:Put",
        "s3:ObjectDeleted",
        "s3:BucketCreated",
        "lambda:Invoke"
      ],
      "answer": 0,
      "explain": "s3:ObjectCreated:Put (and related ObjectCreated variants) is the event type an S3 notification matches to invoke a target like Lambda."
    },
    {
      "q": "What information does S3 pass to the Lambda function when it invokes it via a trigger?",
      "options": [
        "The bucket name and the object key",
        "The full contents of the object, embedded in the event",
        "Nothing — the function must poll S3 to find out what changed",
        "Only the object's file size"
      ],
      "answer": 0,
      "explain": "The event payload contains the bucket and key; the function typically fetches the object itself using that information."
    },
    {
      "q": "What do prefix and suffix filters on an S3 event notification let you do?",
      "options": [
        "Restrict which objects trigger the notification, e.g. only .csv files under a raw/ prefix",
        "Encrypt objects before they reach Lambda",
        "Automatically convert file formats before invocation",
        "Limit how many times a Lambda function can be invoked per day"
      ],
      "answer": 0,
      "explain": "Filters let a bucket with several file types route only matching objects to the Lambda function meant to handle them."
    },
    {
      "q": "What does a resource-based policy on a Lambda function do in the S3-trigger pattern?",
      "options": [
        "Grants the S3 service principal permission to invoke the function",
        "Grants the function permission to read from S3",
        "Grants IAM users permission to edit the function's code",
        "Encrypts the function's environment variables"
      ],
      "answer": 0,
      "explain": "The resource-based policy is what allows S3 (s3.amazonaws.com) to call into Lambda; without it, S3 can't invoke the function at all."
    },
    {
      "q": "A Lambda function is invoked successfully on every S3 upload but fails with access-denied when it tries to read the object. What's most likely missing?",
      "options": [
        "The execution role lacks S3 read permissions",
        "The resource-based policy is missing",
        "The event notification filter is misconfigured",
        "The function has exceeded its memory limit"
      ],
      "answer": 0,
      "explain": "Successful invocation means the resource-based policy is fine; an access-denied error when reading S3 points to a missing permission on the function's own execution role."
    }
  ]
}
