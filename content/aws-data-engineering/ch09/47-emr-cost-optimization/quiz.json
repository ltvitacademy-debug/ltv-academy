{
  "lesson": "emr-cost-optimization",
  "questions": [
    {
      "q": "A nightly batch job runs for about 40 minutes on an EMR cluster that otherwise sits idle all day. What two changes from this lesson would cut its cost the most?",
      "options": [
        "Make it a transient cluster with auto-termination, and run task nodes on Spot",
        "Increase the core node count and disable Spot entirely",
        "Switch the master node to Spot and leave the cluster always-on",
        "Nothing can be done — EMR clusters must run continuously"
      ],
      "answer": 0,
      "explain": "A transient, auto-terminating cluster eliminates the idle-time cost entirely, and Spot task nodes cut the cost of the compute that does run — the two biggest levers for a short, bursty job like this."
    },
    {
      "q": "Why are task nodes, specifically, the safe place to use Spot Instances?",
      "options": [
        "They hold no HDFS data, so a Spot reclamation only costs recomputation, not data loss",
        "Spot pricing is only available for task nodes by AWS policy",
        "Task nodes are always more expensive on-demand than core nodes",
        "Master nodes cannot run on EC2 at all"
      ],
      "answer": 0,
      "explain": "Because task nodes store no HDFS data, losing one to Spot reclamation is a compute setback, not a data-loss risk — unlike core nodes."
    },
    {
      "q": "What does EMR auto-termination do?",
      "options": [
        "Shuts a cluster down automatically once its steps complete, or after a configured idle period",
        "Automatically deletes all data stored in S3 after a job finishes",
        "Terminates the AWS account after repeated idle clusters",
        "Prevents a cluster from ever being auto-scaled"
      ],
      "answer": 0,
      "explain": "Auto-termination stops a cluster from billing indefinitely by shutting it down once its work is done or it's been idle for a configured period."
    },
    {
      "q": "How does EMR Serverless bill for usage, compared to a traditional managed EMR cluster?",
      "options": [
        "It bills for the resources a submitted job actually consumes, rather than for a fixed-size provisioned cluster",
        "It requires a flat monthly subscription regardless of usage",
        "It is always more expensive than a comparable managed cluster",
        "It bills identically to a managed cluster, with no difference at all"
      ],
      "answer": 0,
      "explain": "EMR Serverless removes the fixed-cluster-sizing cost model — AWS provisions and scales compute per job, and billing follows actual consumption."
    },
    {
      "q": "What does \"right-sizing\" an EMR cluster mean, according to this lesson?",
      "options": [
        "Matching instance family and count to the workload's actual utilization from real runs, instead of oversizing as a guess",
        "Always choosing the smallest instance type available regardless of workload",
        "Running every workload on EMR Serverless instead of managed clusters",
        "Setting the maximum task node count to zero"
      ],
      "answer": 0,
      "explain": "Right-sizing means checking actual CPU/memory/YARN utilization from real runs and adjusting instance type and count accordingly, rather than defaulting to an oversized \"safe\" guess."
    }
  ]
}
