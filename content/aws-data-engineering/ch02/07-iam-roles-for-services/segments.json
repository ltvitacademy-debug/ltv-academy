[
  {"slide": "s1", "text": "Every service in this course — Glue, Lambda, EMR, Redshift — needs permission to talk to S3 and to each other. None of them should do that with a long-lived access key. They do it by assuming an IAM role."},
  {"slide": "s2", "text": "A trust policy names a principal — for a service, that's a service principal like glue dot amazonaws dot com. This one says only the Glue service may assume the role. Lambda, naming a different principal, gets rejected outright."},
  {"slide": "s3", "text": "Roles beat access keys three ways: there are no standing credentials to leak, since temporary credentials expire on their own within about an hour by default; a role carries two separate policies, trust for who can assume it and permissions for what it can do once assumed; and the sts AssumeRole call happens automatically behind the scenes — you never make that call yourself."},
  {"slide": "s4", "text": "IAM roles for services, down. Next up: resource-based policies — bucket policies versus identity policies, and how they combine."}
]
