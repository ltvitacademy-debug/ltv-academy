[
  {"slide": "s1", "text": "Let's put everything from this chapter together: a state machine that starts a Glue crawler, runs a Glue ETL job, and finishes with a Lambda notification — with retries and a failure path built in."},
  {"slide": "s2", "text": "StartCrawler refreshes the Data Catalog schema for newly-landed data. RunETLJob transforms that data now that the catalog is current. NotifyComplete invokes a Lambda that posts a completion message, only once the ETL job has actually succeeded."},
  {"slide": "s3", "text": "Both StartCrawler and RunETLJob catch their failures into the same NotifyFailure state. One fallback path handles a failure at either stage, instead of duplicating notification logic for each one — a genuinely common production pattern."},
  {"slide": "s4", "text": "You could have one Lambda invoke the next to chain these steps, but then retry logic and failure branching live as scattered code, invisible from outside. Step Functions gives you one console view showing exactly where last night's run stopped, with retry and catch rules declared, not buried."},
  {"slide": "s5", "text": "Orchestration chapter complete. Next up: AWS Database Migration Service — moving a real database into AWS, starting with DMS fundamentals."}
]
