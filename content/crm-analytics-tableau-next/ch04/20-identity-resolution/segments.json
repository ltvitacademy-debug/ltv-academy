[
  {"slide": "s1", "text": "The same person can show up as a Lead in Sales Cloud, a subscriber in Marketing Cloud, and a guest checkout online, each with a slightly different name and email. Identity resolution decides which records are the same person, and builds one unified profile from them."},
  {"slide": "s2", "text": "This is a real published ruleset. Match rules sit at the top: each rule combines criteria, like a fuzzy name plus a normalized email, and the rules are joined with OR. Above them, the counts: source, matched, and unified. Comparing source to unified tells you whether the rules are doing anything."},
  {"slide": "s3", "text": "Matching isn't enough, because matched records can still disagree. Reconciliation rules pick one value per field. Here the default is most frequent, while loyalty fields use source sequence, meaning one source is treated as authoritative."},
  {"slide": "s4", "text": "In T-SQL terms, it's a fuzzy dedupe join plus a rule for which value wins. Match on a name and a normalized email, or match on a shared identifier, then reconcile the disagreements."},
  {"slide": "s5", "text": "Three stages: match rules find the same person, reconciliation rules choose surviving values, and a unified individual is created. Your source records stay untouched underneath. Rules too strict leave duplicates; rules too loose merge strangers."},
  {"slide": "s6", "text": "Next up: calculated insights, reusable metrics computed on top of the unified data."}
]
