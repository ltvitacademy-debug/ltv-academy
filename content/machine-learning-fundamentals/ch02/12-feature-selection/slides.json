[
  {
    "type": "title",
    "eyebrow": "Machine Learning Fundamentals · Chapter 2",
    "titlePlain": "Feature ",
    "titleEmphasis": "Selection",
    "titleSuffix": "",
    "subtitle": "Lesson 12 · Data Preparation & Feature Engineering"
  },
  {
    "type": "steps",
    "eyebrow": "Three families of methods",
    "titlePlain": "Keep what ",
    "titleEmphasis": "earns its place",
    "titleSuffix": "",
    "steps": [
      {
        "label": "FILTER",
        "sub": "score each feature alone"
      },
      {
        "label": "WRAPPER",
        "sub": "try subsets, keep the best"
      },
      {
        "label": "EMBEDDED",
        "sub": "select while training"
      }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Filter · scikit-learn diabetes data",
    "code": "kb = SelectKBest(f_regression, k=4)\nkb.fit(X, y)\nmask = kb.get_support()\nprint(list(X.columns[mask]))\n\n# ['bmi', 'bp', 's4', 's5']\n# scores: bmi 230.7, s5 207.3,\n#         sex 0.8",
    "caption": "Each feature is scored on its own against the target."
  },
  {
    "type": "code",
    "eyebrow": "Wrapper · recursive elimination",
    "code": "rfe = RFE(LinearRegression(),\n          n_features_to_select=4)\nrfe.fit(X, y)\nkeep = X.columns[rfe.support_]\nprint(list(keep))\n\n# ['bmi', 's1', 's2', 's5']",
    "caption": "A different set than the filter: features are judged together."
  },
  {
    "type": "code",
    "eyebrow": "Select inside the pipeline",
    "code": "pipe = make_pipeline(\n    SelectKBest(f_regression, k=4),\n    LinearRegression())\ncross_val_score(pipe, X, y, cv=5,\n    scoring=\"r2\").mean()\n\n# 4 features: 0.460\n# all 10:     0.482",
    "caption": "Refit on each training fold, so nothing leaks."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 13",
    "titlePlain": "Linear ",
    "titleEmphasis": "Regression",
    "titleSuffix": "",
    "subtitle": "Fitting a line to predict a number."
  }
]