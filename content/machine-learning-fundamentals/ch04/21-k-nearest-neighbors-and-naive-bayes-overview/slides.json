[
  {
    "type": "title",
    "eyebrow": "Machine Learning Fundamentals · Chapter 4",
    "titlePlain": "k-Nearest Neighbors & ",
    "titleEmphasis": "Naive Bayes",
    "titleSuffix": "",
    "subtitle": "Lesson 21 · Trees & Ensembles"
  },
  {
    "type": "steps",
    "eyebrow": "k-NN: predict by similarity",
    "titlePlain": "Three moves, ",
    "titleEmphasis": "no training",
    "titleSuffix": "",
    "steps": [
      {
        "label": "1. Store",
        "sub": "Memorize every training row"
      },
      {
        "label": "2. Measure",
        "sub": "Distance to the new row"
      },
      {
        "label": "3. Vote",
        "sub": "The k closest decide the class"
      }
    ]
  },
  {
    "type": "code",
    "eyebrow": "The scaling trap (illustrative data)",
    "code": "knn = KNeighborsClassifier(\n    n_neighbors=5)\nknn.fit(X_tr, y_tr).score(X_te, y_te)\n# 0.453  <- coin flip\n\nknn_s = make_pipeline(StandardScaler(),\n    KNeighborsClassifier(\n        n_neighbors=5))\n# ...same fit/score -> 0.9",
    "caption": "Same data, same k. Only the scale changed."
  },
  {
    "type": "code",
    "eyebrow": "Choosing k (scaled): train vs. test",
    "code": "k= 1   train 1.000   test 0.900\nk= 5   train 0.929   test 0.900\nk=15   train 0.913   test 0.927\nk=51   train 0.893   test 0.900",
    "caption": "Small k overfits; large k blurs. Compare validation scores."
  },
  {
    "type": "code",
    "eyebrow": "Naive Bayes: fast and probabilistic",
    "code": "# sklearn.naive_bayes.GaussianNB\nnb = GaussianNB().fit(X_tr, y_tr)\nnb.score(X_te, y_te)          # 0.913\nnb.predict_proba(X_te[:2])\n# [[0.131 0.869]\n#  [0.942 0.058]]",
    "caption": "Assumes features are independent given the class."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 22",
    "titlePlain": "Clustering ",
    "titleEmphasis": "Overview",
    "titleSuffix": "",
    "subtitle": "Finding groups in data that has no labels."
  }
]