[
  {
    "type": "title",
    "eyebrow": "AI & Generative AI Fundamentals for Data Scientists · Chapter 3",
    "titlePlain": "Creating & Comparing ",
    "titleEmphasis": "Embeddings",
    "titleSuffix": "",
    "subtitle": "Lesson 12 · Embeddings & Vector Databases"
  },
  {
    "type": "steps",
    "eyebrow": "Every embedding application",
    "titlePlain": "Create, compare, ",
    "titleEmphasis": "rank",
    "titleSuffix": "",
    "steps": [
      {"label": "Create", "sub": "Fit or load one embedding model"},
      {"label": "Embed the query", "sub": "Same model: transform, don't refit"},
      {"label": "Compare", "sub": "Cosine to every stored vector"},
      {"label": "Rank", "sub": "Sort scores, keep the top few"}
    ]
  },
  {
    "type": "code",
    "eyebrow": "Fit once, then reuse the same objects",
    "code": "vec = TfidfVectorizer()\nsvd = TruncatedSVD(n_components=3,\n                   random_state=0)\nE = svd.fit_transform(vec.fit_transform(docs))\n\ndef embed(texts):\n    return svd.transform(vec.transform(texts))",
    "caption": "At query time use transform, never fit_transform."
  },
  {
    "type": "code",
    "eyebrow": "Rank tickets for a new query",
    "code": "q = embed([\"my parcel is late\"])\nscores = cosine_similarity(q, E)[0]\nfor i in np.argsort(-scores)[:3]:\n    print(i, round(scores[i], 2), docs[i])\n# 7 0.94 shipment delivery delayed again\n# 9 0.91 late package delivery again\n# 5 0.91 package delivery is late",
    "caption": "All three top hits are shipping tickets."
  },
  {
    "type": "screenshot",
    "file": "chart.png",
    "eyebrow": "Query scores against all 15 tickets (illustrative)",
    "caption": "Output of the code above: tickets 5 to 9 are the shipping ones."
  },
  {
    "type": "steps",
    "eyebrow": "Three ways to compare vectors",
    "titlePlain": "Cosine, dot, ",
    "titleEmphasis": "Euclidean",
    "titleSuffix": "",
    "steps": [
      {"label": "Cosine", "sub": "Angle only, ignores length"},
      {"label": "Dot product", "sub": "Equals cosine on unit vectors"},
      {"label": "Euclidean distance", "sub": "Squared = 2 - 2 x cosine (unit)"}
    ]
  },
  {
    "type": "code",
    "eyebrow": "Pretrained model: shown, NOT run here",
    "code": "from sentence_transformers import (\n    SentenceTransformer)\nmodel = SentenceTransformer(\n  \"sentence-transformers/all-MiniLM-L6-v2\")\nemb = model.encode(sentences)\nprint(emb.shape)  # docs show [3, 384]\nsims = model.similarity(emb, emb)",
    "caption": "From the sentence-transformers quickstart; check current docs."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 13",
    "titlePlain": "Vector ",
    "titleEmphasis": "Databases",
    "titleSuffix": "",
    "subtitle": "Storing and searching millions of embeddings fast."
  }
]
