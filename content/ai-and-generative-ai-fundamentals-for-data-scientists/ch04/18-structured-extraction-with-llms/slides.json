[
  {
    "type": "title",
    "eyebrow": "AI & Generative AI Fundamentals · Chapter 4",
    "titlePlain": "Structured Extraction ",
    "titleEmphasis": "With LLMs",
    "titleSuffix": "",
    "subtitle": "Lesson 18 · RAG & AI APIs"
  },
  {
    "type": "steps",
    "eyebrow": "From free text to a table",
    "titlePlain": "Trust, but ",
    "titleEmphasis": "validate",
    "titleSuffix": "",
    "steps": [
      { "label": "DEFINE", "sub": "fields, types, allowed values" },
      { "label": "ASK", "sub": "request JSON with those keys" },
      { "label": "VALIDATE", "sub": "parse and check in your own code" },
      { "label": "RETRY OR ROUTE", "sub": "feed the error back, cap tries" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Validate with the json module",
    "code": "def validate(raw):\n    data = json.loads(clean(raw))\n    for f in FIELDS:\n        if f not in data:\n            return None, f\"missing: {f}\"\n    if data[\"category\"] not in ALLOWED:\n        return None, \"bad category\"\n    return data, None",
    "caption": "Condensed; the guide has the full version."
  },
  {
    "type": "code",
    "eyebrow": "Five canned replies, what the run printed",
    "code": "OK    perfect JSON\nOK    JSON inside a code fence\nFAIL  missing field: order_id\nFAIL  category not in allowed list\nFAIL  not valid JSON (chatty prose)",
    "caption": "Replies written by hand; no model was called."
  },
  {
    "type": "code",
    "eyebrow": "Retry with the error included",
    "code": "prompt = f\"Extract JSON from: {ticket}\"\nfor attempt in range(1, 4):\n    data, err = validate(model(prompt))\n    if data:\n        return data, attempt\n    prompt += f\"\\nRejected: {err}\"\nreturn None, 3",
    "caption": "Fake model: failed once, passed on attempt 2."
  },
  {
    "type": "steps",
    "eyebrow": "Habits",
    "titlePlain": "Keep your own ",
    "titleEmphasis": "guardrails",
    "titleSuffix": "",
    "steps": [
      { "label": "TEST WITH BAD OUTPUT", "sub": "write broken replies on purpose" },
      { "label": "USE PROVIDER MODES", "sub": "check current docs, still validate" },
      { "label": "HUMAN FALLBACK", "sub": "log what still fails" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 19",
    "titlePlain": "Cost, Latency & ",
    "titleEmphasis": "Privacy Tradeoffs",
    "titleSuffix": "",
    "subtitle": "What LLM calls really cost, and what never to send."
  }
]
