[
  {
    "type": "title",
    "eyebrow": "AI & Generative AI Fundamentals for Data Scientists · Chapter 4",
    "titlePlain": "Retrieval-Augmented ",
    "titleEmphasis": "Generation",
    "titleSuffix": " Overview",
    "subtitle": "Lesson 15 · RAG & AI APIs"
  },
  {
    "type": "steps",
    "eyebrow": "The RAG loop",
    "titlePlain": "Retrieve, augment, ",
    "titleEmphasis": "generate",
    "titleSuffix": "",
    "steps": [
      {
        "label": "Retrieve",
        "sub": "Search your index for top chunks"
      },
      {
        "label": "Augment",
        "sub": "Put chunks in the prompt"
      },
      {
        "label": "Generate",
        "sub": "LLM answers from that context"
      }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Retrieval with a minimum score",
    "code": "def retrieve(question, k=2, min_score=0.1):\n    s = cosine_similarity(\n        vec.transform([question]), X)[0]\n    top = np.argsort(-s)[:k]\n    return [(int(i), round(float(s[i]), 2))\n            for i in top if s[i] >= min_score]",
    "caption": "Weak matches are dropped, not passed to the model."
  },
  {
    "type": "code",
    "eyebrow": "Augment: build a grounded prompt",
    "code": "def build_prompt(question):\n    hits = retrieve(question)\n    if not hits:\n        return None   # don't ask the LLM\n    # context = numbered chunks, one per line\n    # prompt = \"Answer using only the\n    #   context, else say you don't know\"\n    #   + context + question + \"Answer:\"\n    return prompt",
    "caption": "Instructions, then context, then the question."
  },
  {
    "type": "code",
    "eyebrow": "What the code produced (no LLM called)",
    "code": "# \"How long do refunds take?\" -> [(0, 0.38)]\n# Context:\n# [1] Refunds are issued to the original\n#     payment method within 5 business\n#     days of approval.\n\n# \"Can I pay with bitcoin?\" -> []\n#   build_prompt returns None",
    "caption": "No relevant context: say so, don't let the model guess."
  },
  {
    "type": "steps",
    "eyebrow": "Where RAG breaks",
    "titlePlain": "Plan for ",
    "titleEmphasis": "failure",
    "titleSuffix": " modes",
    "steps": [
      {
        "label": "Retrieval misses",
        "sub": "Measure precision@k, recall@k"
      },
      {
        "label": "Bad chunks",
        "sub": "Test chunk size and boundaries"
      },
      {
        "label": "Stale index",
        "sub": "Re-index when documents change"
      },
      {
        "label": "Ungrounded answers",
        "sub": "Return sources, check faithfulness"
      }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 16",
    "titlePlain": "Building a Simple ",
    "titleEmphasis": "RAG",
    "titleSuffix": " Pipeline",
    "subtitle": "Putting retrieval and generation together."
  }
]