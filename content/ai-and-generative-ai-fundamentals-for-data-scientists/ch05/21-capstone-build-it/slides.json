[
  {
    "type": "title",
    "eyebrow": "AI & Generative AI Fundamentals · Chapter 5",
    "titlePlain": "Capstone: ",
    "titleEmphasis": "Build It",
    "titleSuffix": "",
    "subtitle": "Lesson 21 · Capstone"
  },
  {
    "type": "code",
    "eyebrow": "Step 1 · The stubbed model",
    "code": "def fake_llm(prompt):\n    # STUB: hand-written rules\n    if prompt.startswith(\"ROUTE:\"):\n        q = prompt.lower()\n        if \"revenue\" in q:\n            return DATA_JSON\n        return '{\"kind\": \"docs\"}'\n    return \"...\"",
    "caption": "Condensed. A real model replaces this function."
  },
  {
    "type": "code",
    "eyebrow": "Step 2 · Validate the routing JSON",
    "code": "try:\n    spec = json.loads(raw)\nexcept json.JSONDecodeError:\n    return None, \"not valid JSON\"\nif spec.get(\"metric\") not in METRICS:\n    return None, \"bad metric\"\nfor col, val in spec[\"filters\"].items():\n    if val not in set(df[col]):\n        return None, \"bad filter\"",
    "caption": "Condensed; same checks as lesson 18."
  },
  {
    "type": "code",
    "eyebrow": "Step 3 · The data path, whitelisted",
    "code": "AGG = {\"revenue\": (\"revenue\", \"sum\"),\n       \"orders\": (\"order_id\", \"count\")}\n\ndef run_data(spec, df):\n    for c, v in spec[\"filters\"].items():\n        df = df[df[c] == v]\n    column, func = AGG[spec[\"metric\"]]\n    return df[column].agg(func)",
    "caption": "The model picks keys. pandas does the math."
  },
  {
    "type": "code",
    "eyebrow": "Step 4 · Put it together",
    "code": "def ask(question, df, search, llm):\n    spec = route(question, df, llm)\n    if spec[\"kind\"] == \"data\":\n        return llm(data_prompt(spec))\n    hits = search.top(question)\n    if not hits:\n        return \"I don't know.\"\n    return llm(docs_prompt(hits))",
    "caption": "Condensed; the guide has the full version."
  },
  {
    "type": "code",
    "eyebrow": "What the local tool answered",
    "code": "orders in North     -> 76\nToys in South       -> 1745.99\nreturn window       -> 30 days [0]\nairspeed of swallow -> I don't know",
    "caption": "Model stubbed; retrieval and pandas were real."
  },
  {
    "type": "code",
    "eyebrow": "Try to break it",
    "code": "# router replies with chatty prose first\nrouter calls: 2      # retry recovered\nanswer: 76\n\n# router invents the region \"Mars\"\nSorry, I could not understand\nthat request.",
    "caption": "Bad output fails safely instead of guessing."
  },
  {
    "type": "screenshot",
    "file": "revenue-by-category.png",
    "eyebrow": "Output of the code above",
    "caption": "Revenue by category from run_data. Synthetic, illustrative."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 22",
    "titlePlain": "Wrap-Up & ",
    "titleEmphasis": "Portfolio Presentation",
    "titleSuffix": "",
    "subtitle": "Evaluate the tool and present it honestly."
  }
]