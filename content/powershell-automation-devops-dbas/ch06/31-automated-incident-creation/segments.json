[
  {"slide": "s1", "text": "A chat notification is easy to miss during a busy shift. A real ticket in the system your team already tracks work through is harder to lose. This lesson covers a script that doesn't just notify, it opens an actual incident."},
  {"slide": "s2", "text": "Most modern ticketing systems — ServiceNow and Jira are two widely used examples — expose a REST API for creating a ticket. Structurally it's the same shape as the chat webhook: authenticate, POST a structured payload, get a ticket ID back. The exact field names differ by product, so this is genuinely a read-your-own-docs step."},
  {"slide": "s3", "text": "A ticket that just says \"something is wrong\" isn't much better than the chat notification alone. A useful incident names the specific instance and check, the actual metric value against the threshold, and a timestamp — enough for a responder to triage without re-running the check themselves."},
  {"slide": "s4", "text": "Next up: reducing alert fatigue through automation — tuning thresholds against real baseline data so the alerts that do fire actually matter."}
]
