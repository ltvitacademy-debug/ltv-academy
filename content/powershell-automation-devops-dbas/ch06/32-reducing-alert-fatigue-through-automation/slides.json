[
  {
    "type": "title",
    "eyebrow": "PowerShell, Automation & DevOps for DBAs · Chapter Six",
    "titlePlain": "Reducing Alert Fatigue ",
    "titleEmphasis": "Through Automation",
    "titleSuffix": "",
    "subtitle": "Lesson 32 · Monitoring & Alerting Automation"
  },
  {
    "type": "code",
    "eyebrow": "Thresholds from real baselines, not guesses",
    "code": "# Guessed, generic, probably wrong for this specific server:\nif ($cpuPercent -gt 80) { Send-AlertNotification -Message \"High CPU\" }\n\n# Grounded in this server's own real baseline data:\n$baselineCpu = Get-Content '\\\\fileshare\\baselines\\sqlprd01-cpu-baseline.json' |\n    ConvertFrom-Json\nif ($cpuPercent -gt ($baselineCpu.P95 * 1.25)) {\n    Send-AlertNotification -Message \"CPU 25% above this server's own P95 baseline\"\n}",
    "caption": "A server that runs at 85% CPU nightly doesn't need an alert at 80%."
  },
  {
    "type": "code",
    "eyebrow": "Suppressing duplicate alerts",
    "code": "$alertKey = \"SQLPRD01-LowDiskSpace-D\"\n$recentAlert = Get-Content $stateFilePath -ErrorAction SilentlyContinue |\n    ConvertFrom-Json |\n    Where-Object { $_.Key -eq $alertKey -and $_.FiredAt -gt (Get-Date).AddHours(-1) }\n\nif (-not $recentAlert) {\n    Send-AlertNotification -Message \"Low disk space on SQLPRD01 D:\"\n}",
    "caption": "A condition that's still true five minutes later doesn't need five more pings."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 33",
    "titlePlain": "What DevOps Actually Changes ",
    "titleEmphasis": "for a DBA",
    "titleSuffix": "",
    "subtitle": "Chapter Seven · DevOps Culture for DBAs"
  }
]
