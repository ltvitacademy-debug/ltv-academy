[
  {
    "type": "title",
    "eyebrow": "PowerShell, Automation & DevOps for DBAs · Chapter Six",
    "titlePlain": "Building Automated ",
    "titleEmphasis": "Alerting",
    "titleSuffix": "",
    "subtitle": "Lesson 28 · Monitoring & Alerting Automation"
  },
  {
    "type": "code",
    "eyebrow": "A real threshold-check-and-notify script",
    "code": "$results = Test-DbaLastBackup -SqlInstance SQLPRD01\n\nforeach ($r in $results) {\n    if (-not $r.RestoreResult -eq 'Success') {\n        Send-AlertNotification -Message \"Backup verification failed for $($r.Database)\"\n    }\n}\n\n$diskSpace = Get-DbaDiskSpace -SqlInstance SQLPRD01 |\n    Where-Object { $_.PercentFree -lt 10 }\nif ($diskSpace) {\n    Send-AlertNotification -Message \"Low disk space: $($diskSpace.PercentFree)% free\"\n}",
    "caption": "Real dbatools health checks, wrapped in a threshold and a notify step."
  },
  {
    "type": "code",
    "eyebrow": "The threshold lives in the script, not in someone's head",
    "code": "# Scheduled the same way any PowerShell script is scheduled\npowershell.exe -File \"C:\\Scripts\\Check-BackupHealth.ps1\"",
    "caption": "Version-controlled, reviewable, changeable — not an informal habit."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 29",
    "titlePlain": "Integrating With Teams, ",
    "titleEmphasis": "Slack & Email",
    "titleSuffix": "",
    "subtitle": "Actually delivering the alert once the threshold is breached."
  }
]
