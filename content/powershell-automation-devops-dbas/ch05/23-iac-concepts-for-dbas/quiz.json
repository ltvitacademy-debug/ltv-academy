{
  "lesson": "iac-concepts-for-dbas",
  "questions": [
    {
      "q": "What is the core idea of infrastructure as code, unchanged whether the target is an app server or a SQL Server instance?",
      "options": [
        "A declarative file describes the desired end state, is kept in version control, and a tool reconciles reality to match it",
        "You write an imperative step-by-step script that must be run manually every time",
        "Infrastructure is described only in a wiki page, never in a file a tool can read",
        "IaC only applies to cloud resources, never to on-premises servers"
      ],
      "answer": 0,
      "explain": "That declarative, version-controlled, tool-applied shape is the same whether you're standing up an app server or a SQL Server instance."
    },
    {
      "q": "In this lesson's terms, what does 'provisioning' mean?",
      "options": [
        "Bringing a compute/storage/networking resource into existence, such as the VM or the Azure SQL Database resource itself",
        "Setting sp_configure values on an already-running instance",
        "Creating SQL logins and permissions",
        "Writing a maintenance job schedule"
      ],
      "answer": 0,
      "explain": "Provisioning is about the resource existing at all — the compute, storage, and networking it needs."
    },
    {
      "q": "In this lesson's terms, what does 'configuration' mean?",
      "options": [
        "Setting the state of what's running on a resource once it exists — memory limits, sp_configure values, logins, jobs",
        "Deleting a resource that is no longer needed",
        "Naming a resource group in the cloud provider's console",
        "Choosing which region a VM will be deployed to"
      ],
      "answer": 0,
      "explain": "Configuration is what happens after the resource exists — the SQL-Server-specific internal state."
    },
    {
      "q": "Why does the lesson say IaC tools model SQL Server configuration poorly compared to provisioning?",
      "options": [
        "IaC tools are built primarily to describe infrastructure resources, not the fine-grained internal state of a running SQL Server instance like specific logins or sp_configure values",
        "SQL Server configuration cannot be automated by any tool, ever",
        "IaC tools are only compatible with Linux servers",
        "Configuration is against Microsoft's licensing terms to automate"
      ],
      "answer": 0,
      "explain": "IaC tools like Terraform and Bicep excel at declaring resources to exist; SQL Server internals are typically handled by a separate configuration step."
    },
    {
      "q": "Per this lesson, what real benefit does an IaC definition give you that a written runbook does not, in a disaster-recovery scenario?",
      "options": [
        "The IaC definition is the actual, automatically-applied recipe to rebuild the server, rather than a document that may be stale or never re-validated",
        "IaC definitions automatically back up all data inside the database",
        "IaC eliminates the need for backups entirely",
        "IaC guarantees zero downtime during any rebuild"
      ],
      "answer": 0,
      "explain": "A runbook can silently go stale; an IaC template that's actually applied by a tool reflects what the infrastructure is meant to be right now."
    }
  ]
}
