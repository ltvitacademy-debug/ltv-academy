{
  "lesson": "versioning-stored-procedures-and-views",
  "questions": [
    {
      "q": "What is the one-object-per-file convention?",
      "options": [
        "Each stored procedure, view, function, or table gets its own SQL file containing its full CREATE script",
        "Every object in the database is stored in exactly one shared file",
        "Only stored procedures get individual files; views and tables share a file",
        "Each file contains one line of SQL, regardless of the object"
      ],
      "answer": 0,
      "explain": "One file per object means a change to any single object only ever touches that object's own file."
    },
    {
      "q": "Why does a single giant schema-dump file make Git diffs hard to review?",
      "options": [
        "A one-line change can be buried thousands of lines into a huge file, forcing reviewers to hunt for the actual change",
        "Git refuses to diff files larger than 100 lines",
        "SQL Server won't allow schema exports larger than one file",
        "Large files can't be committed to Git at all"
      ],
      "answer": 0,
      "explain": "The core problem is a diff-readability one: the real change gets lost in a huge file, making review far harder."
    },
    {
      "q": "Why do two developers changing different, unrelated stored procedures on the same day avoid merge conflicts with one-file-per-object?",
      "options": [
        "They're editing entirely different files, so Git has nothing to reconcile between their changes",
        "Git automatically detects unrelated SQL objects and merges them intelligently",
        "They must coordinate manually before committing, every time",
        "SQL Server locks the repository during a commit"
      ],
      "answer": 0,
      "explain": "Since each object lives in its own file, unrelated changes to different objects never touch the same file's lines."
    },
    {
      "q": "In a real diff on a single object's file, what does a reviewer see?",
      "options": [
        "Exactly what changed in that one object, and nothing else",
        "A complete re-listing of the entire database schema",
        "Only the object's name, with no detail on what changed",
        "A binary diff that can't be read as text"
      ],
      "answer": 0,
      "explain": "Because the file only contains that one object, the diff is precisely scoped to what actually changed."
    },
    {
      "q": "What convention do database projects (from Lesson 13) formalize regarding file structure?",
      "options": [
        "One file per object, organized into folders like Tables/, Views/, and StoredProcedures/",
        "A single .sql file containing the entire database",
        "One file per developer, regardless of which objects they touch",
        "Storing objects as compiled binaries instead of readable SQL"
      ],
      "answer": 0,
      "explain": "A .sqlproj structures the schema into per-object files inside type-based folders, matching the practice this lesson describes."
    }
  ]
}
