{
  "lesson": "database-projects-and-schema-comparison",
  "questions": [
    {
      "q": "What is a SQL Server Database Project (.sqlproj)?",
      "options": [
        "A structured, buildable collection of files representing a database schema, part of SQL Server Data Tools",
        "A single giant .sql file containing the entire schema",
        "A cloud-only Azure resource with no local file representation",
        "A proprietary binary format that can't be viewed as text"
      ],
      "answer": 0,
      "explain": ".sqlproj organizes each object into its own file inside a structured project that SSDT can build and validate."
    },
    {
      "q": "What does 'building' a database project actually check?",
      "options": [
        "That the schema is internally consistent — e.g. no view references a nonexistent column, no foreign key points at an undefined table",
        "That the database has enough disk space allocated",
        "That every stored procedure runs in under one second",
        "That all table names are in alphabetical order"
      ],
      "answer": 0,
      "explain": "The build step parses and validates every script in the project, catching broken references before deployment."
    },
    {
      "q": "What does a schema comparison tool like SSDT's Schema Compare do?",
      "options": [
        "Diffs two schemas (e.g. a project and a live server) and can generate a script to reconcile the differences",
        "Automatically merges two databases into one without review",
        "Deletes any objects that exist in only one of the two schemas being compared",
        "Only works when comparing two live SQL Server instances, never a project file"
      ],
      "answer": 0,
      "explain": "Schema Compare produces an itemized diff between a source and target schema, plus an optional script to bring them in line."
    },
    {
      "q": "What is 'schema drift'?",
      "options": [
        "The gap between what source control says a schema should be and what a live database actually contains",
        "The natural slowdown of query performance over time",
        "A SQL Server feature that automatically renames columns",
        "The difference between two branches in Git before they're merged"
      ],
      "answer": 0,
      "explain": "Drift happens when a live database diverges from the reviewed, versioned definition — often from an out-of-process change."
    },
    {
      "q": "Where can you find schema comparison functionality, per this lesson?",
      "options": [
        "SSDT's Schema Compare feature, and a comparable extension in Azure Data Studio",
        "Only inside SQL Server Management Studio, nowhere else",
        "It requires a third-party paid product with no Microsoft equivalent",
        "It's a command only available through raw T-SQL, with no tooling"
      ],
      "answer": 0,
      "explain": "Both SSDT (Visual Studio) and Azure Data Studio provide schema comparison tooling for exactly this task."
    }
  ]
}
