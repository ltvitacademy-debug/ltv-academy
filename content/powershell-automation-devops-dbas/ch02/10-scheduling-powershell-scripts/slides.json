[
  {
    "type": "title",
    "eyebrow": "PowerShell, Automation & DevOps for DBAs · Chapter Two",
    "titlePlain": "Scheduling PowerShell ",
    "titleEmphasis": "Scripts",
    "titleSuffix": "",
    "subtitle": "Lesson 10 · Automating Routine DBA Tasks"
  },
  {
    "type": "code",
    "eyebrow": "Register-ScheduledTask",
    "code": "$action = New-ScheduledTaskAction -Execute \"powershell.exe\" `\n    -Argument \"-NoProfile -File C:\\Scripts\\Backup-Sales.ps1\"\n$trigger = New-ScheduledTaskTrigger -Daily -At 2am\n\nRegister-ScheduledTask -TaskName \"Nightly Sales Backup\" `\n    -Action $action -Trigger $trigger `\n    -User \"DOMAIN\\svc-dbascripts\" -RunLevel Highest",
    "caption": "Build the Task Scheduler entry entirely from PowerShell — repeatable across servers."
  },
  {
    "type": "code",
    "eyebrow": "schtasks.exe, the classic equivalent",
    "code": "schtasks /Create /TN \"Nightly Sales Backup\" `\n    /TR \"powershell.exe -NoProfile -File C:\\Scripts\\Backup-Sales.ps1\" `\n    /SC Daily /ST 02:00 /RU \"DOMAIN\\svc-dbascripts\"",
    "caption": "Same underlying Task Scheduler entry, predates the ScheduledTasks module."
  },
  {
    "type": "code",
    "eyebrow": "Why not just SQL Server Agent?",
    "code": "# Agent's PowerShell step type runs under ONE account:\n# the SQL Server Agent service account — for every job.\n#\n# Task Scheduler lets you pick a dedicated account\n# PER scheduled task, scoped to what it actually needs.",
    "caption": "Agent's fixed account is either too little access or dangerously too much."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 11",
    "titlePlain": "Error Handling in ",
    "titleEmphasis": "Automation",
    "titleSuffix": "",
    "subtitle": "Logging patterns for scripts nobody's watching run."
  }
]
