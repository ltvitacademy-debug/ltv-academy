[
  {
    "type": "title",
    "eyebrow": "PowerShell, Automation & DevOps for DBAs · Chapter Two",
    "titlePlain": "Automating Backups With ",
    "titleEmphasis": "PowerShell",
    "titleSuffix": "",
    "subtitle": "Lesson 6 · Automating Routine DBA Tasks"
  },
  {
    "type": "code",
    "eyebrow": "Backup-DbaDatabase, for real",
    "code": "Backup-DbaDatabase -SqlInstance SQLPRD01 `\n    -Database Sales `\n    -Path \\\\backups\\sales `\n    -Type Full `\n    -CompressBackup `\n    -Checksum",
    "caption": "Same Full/Differential/Log types as T-SQL BACKUP DATABASE — different engine."
  },
  {
    "type": "code",
    "eyebrow": "The same strategy, in a script",
    "code": "if ((Get-Date).DayOfWeek -eq 'Sunday') {\n    Backup-DbaDatabase -SqlInstance $instance -Database Sales -Path $backupRoot -Type Full\n}\nelse {\n    Backup-DbaDatabase -SqlInstance $instance -Database Sales -Path $backupRoot -Type Differential\n}",
    "caption": "The branching logic lives in PowerShell, not three separate Agent job schedules."
  },
  {
    "type": "code",
    "eyebrow": "Retention cleanup, in the same routine",
    "code": "$retentionDays = 14\n\nGet-ChildItem $backupRoot -Filter *.bak |\n    Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-$retentionDays) } |\n    Remove-Item",
    "caption": "Closes a gap pure T-SQL/Agent jobs often leave to a separate maintenance plan step."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 7",
    "titlePlain": "Automating ",
    "titleEmphasis": "Health Checks",
    "titleSuffix": "",
    "subtitle": "Chaining dbatools health-check cmdlets into one report."
  }
]
