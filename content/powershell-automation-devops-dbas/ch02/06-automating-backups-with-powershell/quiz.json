{
  "lesson": "automating-backups-with-powershell",
  "questions": [
    {
      "q": "What dbatools cmdlet performs a SQL Server backup?",
      "options": [
        "Backup-DbaDatabase",
        "New-DbaBackup",
        "Save-DbaDatabase",
        "Export-DbaDatabase"
      ],
      "answer": 0,
      "explain": "Backup-DbaDatabase follows the Verb-DbaNoun convention and performs the same operation as T-SQL's BACKUP DATABASE statement."
    },
    {
      "q": "What values does the -Type parameter of Backup-DbaDatabase accept?",
      "options": [
        "Full, Differential, or Log — the same three backup types taught in the SQL Server DBA course",
        "Only Full",
        "Small, Medium, Large",
        "Sync and Async"
      ],
      "answer": 0,
      "explain": "These are the identical backup types from T-SQL BACKUP DATABASE, just invoked through a dbatools cmdlet instead."
    },
    {
      "q": "What happens if you call Backup-DbaDatabase against an instance without specifying -Database?",
      "options": [
        "It backs up every user database on the instance, avoiding the need to write an explicit loop",
        "It throws an error and refuses to run",
        "It only backs up the master database",
        "It backs up a randomly chosen database"
      ],
      "answer": 0,
      "explain": "Omitting -Database backs up all user databases on the instance — a convenience that would otherwise require hand-written looping logic."
    },
    {
      "q": "In the lesson's script example, what determines whether a full or differential backup runs on a given day?",
      "options": [
        "An if/else branch checking (Get-Date).DayOfWeek, replacing what would otherwise be three separately maintained SQL Server Agent job schedules",
        "A random number generator",
        "The size of the database",
        "It cannot be automated and must be chosen manually each time"
      ],
      "answer": 0,
      "explain": "PowerShell's real conditional logic lets one script express the same full/differential/log cadence that used to require separate Agent job schedules."
    },
    {
      "q": "Per this lesson, what actually changes between a T-SQL/Agent-based backup strategy and a Backup-DbaDatabase-based one?",
      "options": [
        "The execution engine — real conditionals, error handling, and multi-server scripting — while the underlying backup strategy (RPO/RTO, backup types, cadence) stays the same",
        "The RPO and RTO targets change completely",
        "PowerShell backups are not real SQL Server backups",
        "Retention policy concepts no longer apply"
      ],
      "answer": 0,
      "explain": "The lesson frames this as the same strategy running on a more capable execution engine, not a new backup strategy."
    }
  ]
}
