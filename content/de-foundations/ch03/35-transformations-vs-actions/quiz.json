{
  "lesson": "transformations-vs-actions",
  "questions": [
    {
      "q": "What does a transformation do?",
      "options": [
        "Returns a new DataFrame describing one more step in the plan, without actually running anything",
        "Immediately runs and prints a result",
        "Always writes data to storage",
        "Deletes the original DataFrame permanently"
      ],
      "answer": 0,
      "explain": ".filter(), .select(), .withColumn(), .groupBy(), and .join() are all transformations."
    },
    {
      "q": "What does an action do?",
      "options": [
        "Triggers execution of the entire plan built up so far, and returns a result or writes output",
        "Only builds a new step in the plan, like a transformation",
        "Deletes all transformations from memory without running them",
        "Actions never actually execute any code"
      ],
      "answer": 0,
      "explain": ".show(), .count(), .collect(), and .write.parquet() are all actions."
    },
    {
      "q": "Why does chaining several transformations before one action run more efficiently than running each step separately?",
      "options": [
        "Because none of the transformations ran individually, Spark can combine all of them into a single, optimized pass over the data",
        "Chaining transformations has no performance benefit at all",
        "Each transformation in a chain still runs immediately and separately",
        "Actions always run slower when preceded by transformations"
      ],
      "answer": 0,
      "explain": "This is Lesson 34's lazy evaluation paying off directly — the whole plan is optimized together."
    },
    {
      "q": "What is the simple test for telling a transformation apart from an action?",
      "options": [
        "Does it return a new DataFrame you could keep chaining onto (transformation), or does it return a plain value, print something, or write to storage (action)?",
        "Transformations always start with the letter T",
        "Actions can only be called once per script",
        "There is no reliable way to tell them apart"
      ],
      "answer": 0,
      "explain": "If you can keep chaining methods onto the result, it was a transformation; if it 'finishes' something, it was an action."
    },
    {
      "q": "In the chaining example, when does any real computation actually happen?",
      "options": [
        "Only when .show() is called at the end — all four preceding transformations run together in one pass at that point",
        "Each transformation runs immediately as it's written, one at a time",
        "Computation happens the moment the DataFrame variable is first created",
        "Nothing ever actually runs, even after calling .show()"
      ],
      "answer": 0,
      "explain": "This is the direct payoff of Lesson 34's lazy evaluation, applied to a real multi-step chain."
    }
  ]
}
