{
  "lesson": "sparksession",
  "questions": [
    {
      "q": "What is SparkSession?",
      "options": [
        "The unified entry point for reading data, running SQL, and configuring Spark",
        "A type of file format",
        "A synonym for a Worker Node",
        "Only used for writing files, never reading them"
      ],
      "answer": 0,
      "explain": "Every PySpark line in Chapter 4 hangs off of a SparkSession object."
    },
    {
      "q": "What does .getOrCreate() do?",
      "options": [
        "Creates a new session, or safely reuses one that already exists",
        "Always creates a brand new session, even if one already exists",
        "Deletes any existing session",
        "Only works the very first time it's called"
      ],
      "answer": 0,
      "explain": "This makes it safe to call more than once without accidentally creating duplicate sessions."
    },
    {
      "q": "What two things does the spark object give direct access to, per this lesson?",
      "options": [
        "spark.read for loading files as DataFrames, and spark.sql() for running SQL against registered views",
        "Only the ability to shut down the cluster",
        "Direct access to another user's files on the same cluster",
        "Only the ability to change the Cluster Manager"
      ],
      "answer": 0,
      "explain": "Both come from the same SparkSession object, which is why it's the central entry point."
    },
    {
      "q": "What did SparkSession unify, historically?",
      "options": [
        "Three separate early-Spark entry points — SparkContext, SQLContext, and HiveContext — into one",
        "It unified Python and Java into a single language",
        "It unified all Cluster Managers into one universal manager",
        "There was never any unification — SparkSession has always been the only option"
      ],
      "answer": 0,
      "explain": "Modern Spark (2.0 onward) uses SparkSession as the single entry point instead of three separate ones."
    },
    {
      "q": "Can the older SparkContext still be accessed if genuinely needed?",
      "options": [
        "Yes, via spark.sparkContext, for low-level needs, though SparkSession is the only entry point most work requires",
        "No, SparkContext has been completely removed from all versions of Spark",
        "Only by restarting the entire cluster",
        "SparkContext can only be accessed before SparkSession is created"
      ],
      "answer": 0,
      "explain": "This is why Lesson 30's diagram showing SparkContext inside the Driver is still accurate, just slightly lower-level than what you'll typically use."
    }
  ]
}
