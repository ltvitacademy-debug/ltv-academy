[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Three",
    "titlePlain": "",
    "titleEmphasis": "SparkSession",
    "titleSuffix": "",
    "subtitle": "Lesson 36 · Apache Spark Fundamentals"
  },
  {
    "type": "code",
    "eyebrow": "Creating one",
    "code": "from pyspark.sql import SparkSession\n\nspark = (\n  SparkSession.builder\n  .appName(\"NYCTaxiCleaning\")\n  .getOrCreate()\n)",
    "caption": "getOrCreate() reuses an existing session safely — no accidental duplicates."
  },
  {
    "type": "code",
    "eyebrow": "What it actually gives you",
    "code": "df = spark.read.parquet(\"cleansed/yellow_tripdata_2024-01.parquet\")\n\ndf.createOrReplaceTempView(\"trips\")\nresult = spark.sql(\"SELECT VendorID, COUNT(*) FROM trips GROUP BY VendorID\")",
    "caption": "spark.read (Lessons 40-42) and spark.sql() (Lesson 38) — both from the same spark object."
  },
  {
    "type": "code",
    "eyebrow": "A brief history — what it unified",
    "code": "-- Early Spark: THREE separate entry points\n-- SparkContext (core) + SQLContext (SQL) + HiveContext (Hive)\n--\n-- Spark 2.0+: unified into ONE -- SparkSession\n-- (spark.sparkContext still works, if you ever need it)",
    "caption": "Lesson 30's diagram showed SparkContext inside the Driver — accurate, just one layer below what you'll actually use."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 37",
    "titlePlain": "Spark ",
    "titleEmphasis": "DataFrames",
    "titleSuffix": "",
    "subtitle": "How this differs from Chapter 2's Pandas DataFrame."
  }
]
