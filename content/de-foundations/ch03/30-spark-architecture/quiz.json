{
  "lesson": "spark-architecture",
  "questions": [
    {
      "q": "What runs inside the Driver Program?",
      "options": [
        "Your actual code, along with the SparkContext/SparkSession that plans and coordinates the job",
        "Only the Cluster Manager's resource allocation logic",
        "The physical data files themselves",
        "Nothing — the Driver Program is just a placeholder with no real function"
      ],
      "answer": 0,
      "explain": "The driver plans the work but doesn't do the heavy lifting itself — that's the executors' job."
    },
    {
      "q": "What does the Cluster Manager do?",
      "options": [
        "Allocates machines and resources to a Spark job — this could be Spark's own standalone manager, Kubernetes, or YARN",
        "Runs the actual data transformations",
        "Stores the final output files",
        "Writes the Python code for the job"
      ],
      "answer": 0,
      "explain": "It's the middleman between the driver and the actual worker machines."
    },
    {
      "q": "What is an Executor?",
      "options": [
        "A process on a worker node that runs Tasks and can cache data in memory for reuse",
        "A synonym for the Driver Program",
        "A type of file format",
        "The name of Spark's cluster manager"
      ],
      "answer": 0,
      "explain": "Executors are where the actual computation happens, on the worker nodes."
    },
    {
      "q": "How does Spark's architecture implement data locality (from Lesson 29)?",
      "options": [
        "The Cluster Manager tries to place Executors on machines that already hold the relevant data, rather than moving data elsewhere",
        "Data locality has no connection to Spark's actual architecture",
        "The Driver Program physically copies all data to itself first",
        "Executors always run on a single, fixed machine regardless of data location"
      ],
      "answer": 0,
      "explain": "This is exactly the general idea from Lesson 29, implemented by a real architectural component."
    },
    {
      "q": "How does Spark's architecture implement fault tolerance (from Lesson 29)?",
      "options": [
        "If a Worker Node fails mid-job, the Driver reschedules that Executor's unfinished Tasks onto a different Worker Node",
        "If any Worker Node fails, the entire Spark job is permanently lost with no recovery",
        "Fault tolerance is not actually possible in Spark's architecture",
        "The Cluster Manager prevents any machine from ever failing"
      ],
      "answer": 0,
      "explain": "The Driver's awareness of task completion status is what allows this kind of automatic recovery."
    }
  ]
}
