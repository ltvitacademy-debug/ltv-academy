{
  "lesson": "spark-sql",
  "questions": [
    {
      "q": "What does createOrReplaceTempView() do?",
      "options": [
        "Registers a DataFrame under a name that spark.sql() can then reference in a query",
        "Permanently writes a DataFrame to disk",
        "Deletes a DataFrame from memory",
        "Converts a DataFrame into a Pandas DataFrame"
      ],
      "answer": 0,
      "explain": "This is the step that makes a DataFrame queryable with real SQL syntax."
    },
    {
      "q": "Why do the SQL API and DataFrame API produce identical results for the same logic?",
      "options": [
        "Both compile down to the exact same underlying execution plan through Spark's Catalyst optimizer",
        "They actually produce different results, and SQL is always slower",
        "The DataFrame API cannot express GROUP BY or WHERE logic at all",
        "SQL only works on small DataFrames, never large ones"
      ],
      "answer": 0,
      "explain": "Catalyst doesn't care which syntax you started from — you can pick whichever is more readable."
    },
    {
      "q": "Does spark.sql() run a query immediately when called?",
      "options": [
        "No — it returns a DataFrame describing a plan, following the same lazy evaluation as every transformation",
        "Yes, it always runs immediately regardless of lazy evaluation rules",
        "Only SELECT * queries run immediately; others are lazy",
        "spark.sql() cannot be used with actions at all"
      ],
      "answer": 0,
      "explain": "An action like .show() is still required to actually trigger execution — Lessons 34-35's rules apply unchanged."
    },
    {
      "q": "Why might this lesson's SQL syntax look familiar to someone who took this site's T-SQL course?",
      "options": [
        "Spark SQL uses real, genuine SQL syntax — SELECT, WHERE, GROUP BY — similar to standard T-SQL",
        "Spark SQL and T-SQL share no syntax in common at all",
        "Spark SQL only works with Microsoft SQL Server specifically",
        "T-SQL and Spark SQL are actually the exact same product"
      ],
      "answer": 0,
      "explain": "The underlying SQL vocabulary genuinely overlaps, even though the engines running it are completely different."
    },
    {
      "q": "What does Chapter 4 add, now that Chapter 3 is complete?",
      "options": [
        "The actual PySpark syntax for every concept covered in Chapter 3, applied hands-on to real NYC Taxi data",
        "A completely different set of concepts unrelated to anything in Chapter 3",
        "Chapter 4 only covers SQL, with no Python involved at all",
        "Chapter 4 replaces the need to understand Chapter 3's concepts entirely"
      ],
      "answer": 0,
      "explain": "Chapter 4 doesn't introduce new ideas — it teaches the hands-on syntax for ideas Chapter 3 already explained conceptually."
    }
  ]
}
