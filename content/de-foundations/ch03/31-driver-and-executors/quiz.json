{
  "lesson": "driver-and-executors",
  "questions": [
    {
      "q": "What does the Driver actually send to Executors?",
      "options": [
        "Serialized instructions describing the computation to run — not the raw data itself",
        "The entire dataset, copied to every Executor",
        "A full copy of the Driver's memory",
        "Nothing — Executors work independently with no communication from the Driver"
      ],
      "answer": 0,
      "explain": "This pairs with Lesson 29's data locality — the instructions travel; the data mostly stays where it already is."
    },
    {
      "q": "Why don't Executors send their full results back to the Driver by default?",
      "options": [
        "Doing so for a large dataset would recreate the exact memory problem Spark exists to avoid",
        "Executors are physically incapable of sending any data back",
        "The Driver never needs any information about job completion",
        "Sending results back is always required for every single operation"
      ],
      "answer": 0,
      "explain": "Instead, most operations write results to storage directly and just report completion back to the Driver."
    },
    {
      "q": "When do Executors send actual values back to the Driver?",
      "options": [
        "For a genuinely small result the Driver explicitly asked for, like count() or collect() on a small DataFrame",
        "For every single operation, regardless of size",
        "Never, under any circumstances",
        "Only when the Cluster Manager specifically requests it"
      ],
      "answer": 0,
      "explain": "This distinction previews Lesson 35's transformations (stay distributed) versus actions (return something to the Driver)."
    },
    {
      "q": "Why is the fact that the Driver builds an entire plan before execution significant?",
      "options": [
        "It lets Spark look at everything at once and optimize the whole plan, rather than running each operation immediately like Pandas does",
        "It has no real benefit and only adds unnecessary delay",
        "It means the plan can never be changed once built",
        "It prevents Spark from ever using multiple Executors"
      ],
      "answer": 0,
      "explain": "This is exactly why Lesson 34's lazy evaluation is possible — the plan-first approach enables whole-plan optimization."
    },
    {
      "q": "In the lesson's example, what would calling .count() on a 500-million-row DataFrame actually send back to the Driver?",
      "options": [
        "A single small number (the count), not the 500 million rows themselves",
        "All 500 million rows, copied in full",
        "Nothing at all — count() cannot be executed",
        "Exactly half of the rows, chosen randomly"
      ],
      "answer": 0,
      "explain": "count() is an action requesting a small, specific result — not the full dataset."
    }
  ]
}
