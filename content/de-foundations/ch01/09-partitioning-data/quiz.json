{
  "lesson": "partitioning-data",
  "questions": [
    {
      "q": "What does partitioning mean at the storage level?",
      "options": [
        "Deliberately organizing files into a directory structure that mirrors a column value, like year/month/day",
        "Splitting a single file into random chunks with no structure",
        "Encrypting data before writing it",
        "The same thing as an in-memory Spark partition"
      ],
      "answer": 0,
      "explain": "Storage partitioning is about the directory layout on disk, matching columns you filter on often."
    },
    {
      "q": "What is the Hive-style partitioning convention?",
      "options": [
        "Directory names in the form key=value, like year=2024/month=01/",
        "A naming convention exclusive to Apache Hive with no other tool support",
        "Storing all partitions in a single flat file",
        "A convention only used for CSV files"
      ],
      "answer": 0,
      "explain": "This key=value directory naming is widely recognized by Spark, Databricks, Synapse, and most big-data tools."
    },
    {
      "q": "What is partition pruning?",
      "options": [
        "Skipping entire directories of files that a query's filter can't possibly match, without opening any of them",
        "Deleting old partitions automatically after a fixed time period",
        "The same thing as column pruning within a single file",
        "A method for compressing partition directory names"
      ],
      "answer": 0,
      "explain": "Partition pruning operates one level above column pruning — it skips whole files and directories, not just parts of one file."
    },
    {
      "q": "How does a 'Spark partition' (Lesson 33) differ from the storage partitioning covered in this lesson?",
      "options": [
        "A Spark partition is a unit of in-memory parallelism while processing data, not a directory structure on disk",
        "They are exactly the same concept with no difference at all",
        "Spark partitions only apply to CSV files, never Parquet",
        "Storage partitioning only exists inside Spark, not on disk"
      ],
      "answer": 0,
      "explain": "Same word, two different layers of the stack — this lesson covers the on-disk directory kind."
    },
    {
      "q": "Why is partitioning on a column with millions of distinct values usually a mistake?",
      "options": [
        "It creates millions of tiny directories instead of a few useful, prunable ones — a problem called over-partitioning",
        "Azure physically forbids more than 100 partition directories",
        "It has no downside and is always the best choice",
        "It only affects CSV files, not Parquet"
      ],
      "answer": 0,
      "explain": "Good partition columns have a manageable number of distinct values, like year/month/day, not something like a raw timestamp."
    }
  ]
}
