{
  "lesson": "lists-and-dictionaries",
  "questions": [
    {
      "q": "How are elements in a list accessed?",
      "options": [
        "By position, using a zero-based index",
        "By a named key, like a dictionary",
        "Only by iterating from the beginning every time",
        "Lists cannot be accessed at all once created"
      ],
      "answer": 0,
      "explain": "fare_amounts[0] gets the first element; fare_amounts[-1] gets the last."
    },
    {
      "q": "How are values in a dictionary accessed?",
      "options": [
        "By a named key, not by position",
        "Only by numeric index, like a list",
        "Dictionaries cannot store more than one value",
        "By alphabetical order automatically"
      ],
      "answer": 0,
      "explain": "trip['fare_amount'] retrieves the value associated with that specific key."
    },
    {
      "q": "What does it mean that lists are mutable?",
      "options": [
        "Elements can be added, removed, or changed after the list is created",
        "A list's contents can never change once created",
        "Lists can only hold one data type at a time",
        "Mutable means the list is stored on disk instead of memory"
      ],
      "answer": 0,
      "explain": ".append() is a direct example of mutating a list after creation."
    },
    {
      "q": "Why does a Python dictionary look structurally identical to a JSON object?",
      "options": [
        "Because Lesson 24's JSON processing reads a JSON object and hands back exactly this kind of Python dictionary automatically",
        "It's a coincidence with no real connection",
        "JSON objects are actually a completely different data structure",
        "Dictionaries can only be used with XML, not JSON"
      ],
      "answer": 0,
      "explain": "This structural match is exactly why JSON parses so naturally into Python's native data types."
    },
    {
      "q": "What does a list of dictionaries represent, conceptually?",
      "options": [
        "Something that looks like rows in a table, each dictionary representing one row",
        "A single value with no internal structure",
        "A type of error in Python",
        "A dictionary that has been converted into a string"
      ],
      "answer": 0,
      "explain": "This is exactly the shape Lesson 22 gets from reading a JSON file, and what a Pandas DataFrame formalizes further."
    }
  ]
}
