{
  "lesson": "connecting-python-to-sql",
  "questions": [
    {
      "q": "What does pyodbc do?",
      "options": [
        "Connects Python to SQL Server over ODBC",
        "Reads CSV files directly",
        "Only works with JSON APIs, not databases",
        "Converts a DataFrame into a SQL Server database automatically"
      ],
      "answer": 0,
      "explain": "It's Python's standard library for talking to SQL Server."
    },
    {
      "q": "What does pd.read_sql() do?",
      "options": [
        "Runs a SQL query and returns the result directly as a DataFrame, with column types inferred",
        "Only works with SELECT * queries, no filtering allowed",
        "Requires manually parsing the query results row by row",
        "Deletes rows from the database"
      ],
      "answer": 0,
      "explain": "It combines a real SQL query with the convenience of getting a ready-to-use DataFrame back."
    },
    {
      "q": "Why might you connect directly to a database instead of exporting to a file first?",
      "options": [
        "Direct connection always sees current data and lets the database engine handle filtering and sorting efficiently, rather than pulling everything into Python first",
        "Exporting to a file is always faster and should always be preferred",
        "Databases cannot be queried directly under any circumstances",
        "There is no real difference between the two approaches"
      ],
      "answer": 0,
      "explain": "An exported file can go stale the moment the source table is updated."
    },
    {
      "q": "Why does closing a database connection belong in a finally block?",
      "options": [
        "The connection closes whether the query succeeded or failed, avoiding a leaked open connection if something goes wrong mid-query",
        "finally blocks are required by pyodbc and have no other purpose",
        "Connections close themselves automatically with no code needed",
        "finally only matters for file operations, never database connections"
      ],
      "answer": 0,
      "explain": "This is Lesson 19's exception-handling pattern applied directly to a real database connection."
    },
    {
      "q": "What real database does this lesson's example query run against?",
      "options": [
        "AdventureWorks2012 — the same database used in this site's T-SQL Development course",
        "A fictional database invented just for this lesson",
        "AdventureWorksDW2014",
        "A generic placeholder database with no real name"
      ],
      "answer": 0,
      "explain": "This ties directly back to the T-SQL Development course's real lab database, if the student has taken it."
    }
  ]
}
