[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Two",
    "titlePlain": "Exception ",
    "titleEmphasis": "Handling",
    "titleSuffix": "",
    "subtitle": "Lesson 19 · Python for Data Engineers"
  },
  {
    "type": "code",
    "eyebrow": "The problem: real data has bad rows",
    "code": "fare_amount_str = \"N/A\"\nfare_amount = float(fare_amount_str)\n# ValueError: could not convert string to float: 'N/A'\n#\n# Unhandled, this crashes EVERYTHING after it in the loop.",
    "caption": "One bad row, ten thousand rows in the file — without handling, all ten thousand stop."
  },
  {
    "type": "code",
    "eyebrow": "try / except — catch it instead",
    "code": "try:\n    fare_amount = float(fare_amount_str)\nexcept ValueError:\n    print(\"Could not parse fare amount:\", fare_amount_str)\n    fare_amount = None",
    "caption": "Catch the SPECIFIC exception type — a bare except: can hide real bugs you needed to see."
  },
  {
    "type": "code",
    "eyebrow": "The real pattern — one bad row, job keeps going",
    "code": "raw_fares = [\"14.50\", \"9.75\", \"N/A\", \"22.00\", \"bad-data\"]\nclean_fares = []\n\nfor raw in raw_fares:\n    try:\n        clean_fares.append(float(raw))\n    except ValueError:\n        print(\"Skipping unparseable value:\", raw)",
    "caption": "Exactly the resilience Lesson 27's ETL — and every real production pipeline — actually needs."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 20",
    "titlePlain": "Working With ",
    "titleEmphasis": "Files",
    "titleSuffix": "",
    "subtitle": "Opening, reading, and writing real files on disk."
  }
]
