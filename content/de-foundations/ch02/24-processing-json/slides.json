[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Two",
    "titlePlain": "Processing ",
    "titleEmphasis": "JSON",
    "titleSuffix": "",
    "subtitle": "Lesson 24 · Python for Data Engineers"
  },
  {
    "type": "code",
    "eyebrow": "When JSON isn't flat",
    "code": "{\n  \"trip_id\": \"T1001\",\n  \"fare\": {\"amount\": 14.50, \"currency\": \"USD\"},\n  \"locations\": {\n    \"pickup\": {\"borough\": \"Manhattan\"},\n    \"dropoff\": {\"borough\": \"Brooklyn\"}\n  }\n}",
    "caption": "pd.DataFrame() directly on this puts whole dictionaries INSIDE single cells. Not what you want."
  },
  {
    "type": "code",
    "eyebrow": "json.loads() / json.dumps()",
    "code": "import json\n\ndata = json.loads(json_text)       # JSON text -> Python dict\nprint(data[\"fare\"][\"amount\"])      # 14.50\n\nback_to_text = json.dumps(data)    # Python dict -> JSON text",
    "caption": "requests' .json() (Lesson 23) is calling loads for you underneath — this is what's happening."
  },
  {
    "type": "code",
    "eyebrow": "pd.json_normalize() — flattening it properly",
    "code": "df = pd.json_normalize(data)\nprint(df.columns)\n# trip_id, fare.amount, fare.currency,\n# locations.pickup.borough, locations.dropoff.borough",
    "caption": "Dot-separated column names — a real, filterable table instead of dictionaries stuck in cells."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 25",
    "titlePlain": "Connecting Python to ",
    "titleEmphasis": "SQL",
    "titleSuffix": "",
    "subtitle": "Pulling data straight from a database instead."
  }
]
