{
  "lesson": "building-a-simple-python-etl-process",
  "questions": [
    {
      "q": "What does the extract() function in this lesson's script do?",
      "options": [
        "Reads the raw file exactly as it arrived, corresponding to Chapter 1's raw zone",
        "Writes the final cleaned output file",
        "Deletes invalid rows",
        "Connects to a REST API"
      ],
      "answer": 0,
      "explain": "Extract is the 'E' in ETL — pulling raw source data in, unmodified."
    },
    {
      "q": "What does the transform() function do?",
      "options": [
        "Cleans and validates the data — dropping nulls, filtering invalid rows, removing duplicates, adding a computed column",
        "Only reads the file with no other logic",
        "Writes the file to its final destination",
        "Connects to a SQL Server database"
      ],
      "answer": 0,
      "explain": "This corresponds to Chapter 1's cleansed zone, combining several of this chapter's cleaning techniques from Lesson 26."
    },
    {
      "q": "Why does run_etl() wrap the extract/transform/load calls in try/except?",
      "options": [
        "So a real failure (like a missing source file) is caught and reported instead of crashing the whole script ungracefully",
        "try/except has no real purpose in this script",
        "It's required syntax with no functional benefit",
        "It only catches errors during the load step, never extract or transform"
      ],
      "answer": 0,
      "explain": "This is Lesson 19's exception handling applied to the entire pipeline, not just one operation."
    },
    {
      "q": "Why does load() write the output as Parquet rather than CSV?",
      "options": [
        "This reflects Lesson 8's guidance — Parquet is the analytics-ready format for the cleansed zone, ready for Chapter 4's Spark to read",
        "Parquet is required by Python and CSV cannot be written",
        "There is no meaningful difference between the two formats",
        "CSV files cannot be read by Pandas at all"
      ],
      "answer": 0,
      "explain": "The raw file arrived as CSV; the cleansed output is deliberately converted to Parquet."
    },
    {
      "q": "What is the relationship between this three-function ETL shape and real production data engineering?",
      "options": [
        "This is close to how real, small-scale ETL scripts actually look, before a team graduates to orchestrating them with tools like Data Factory or Databricks Jobs",
        "This is purely a toy example with no resemblance to real practice",
        "Real ETL scripts never use functions or error handling",
        "Production ETL always requires a distributed cluster, never a single script"
      ],
      "answer": 0,
      "explain": "The extract/transform/load pattern, wrapped in error handling, is genuinely representative of real-world practice at this scale."
    }
  ]
}
