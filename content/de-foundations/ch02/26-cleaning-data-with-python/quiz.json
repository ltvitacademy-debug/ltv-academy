{
  "lesson": "cleaning-data-with-python",
  "questions": [
    {
      "q": "What does .isna() return?",
      "options": [
        "A True/False value per element, marking which values are missing",
        "The total count of rows in the DataFrame",
        "A list of column names",
        "The average of a numeric column"
      ],
      "answer": 0,
      "explain": "Calling .sum() on the result counts how many True values (missing values) there are."
    },
    {
      "q": "What is the difference between .fillna() and .dropna()?",
      "options": [
        "fillna replaces missing values with something specific; dropna removes entire rows missing a value",
        "They do exactly the same thing with different names",
        "fillna only works on strings; dropna only works on numbers",
        "dropna fills missing values; fillna deletes rows"
      ],
      "answer": 0,
      "explain": "Which one is correct depends on the business rule — some missing values should become a default, others should be dropped."
    },
    {
      "q": "Why must you use & and | instead of Python's own 'and'/'or' when combining conditions on a DataFrame?",
      "options": [
        "Python's own and/or don't work correctly across entire DataFrame columns — & and | are required for element-wise comparison",
        "& and | are just stylistic preferences with no functional difference",
        "and/or only work with strings, never numbers",
        "DataFrames cannot have combined conditions at all"
      ],
      "answer": 0,
      "explain": "This is a common early mistake — & and | are DataFrame-specific operators for this exact purpose."
    },
    {
      "q": "What does .drop_duplicates() do?",
      "options": [
        "Removes rows that are exact duplicates of an earlier row",
        "Removes all rows with any missing value",
        "Converts a column's type",
        "Sorts the DataFrame alphabetically"
      ],
      "answer": 0,
      "explain": "This is a common real issue when combining data from multiple sources or re-processing a file ingested twice."
    },
    {
      "q": "What does .astype() do?",
      "options": [
        "Converts an existing column to a different type after the file has already loaded",
        "Deletes a column entirely",
        "Only works during pd.read_csv(), never afterward",
        "Renames a column"
      ],
      "answer": 0,
      "explain": "This is useful when a type needs fixing after loading, distinct from the dtype parameter used at read_csv() time in Lesson 22."
    }
  ]
}
