[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Two",
    "titlePlain": "",
    "titleEmphasis": "Functions",
    "titleSuffix": "",
    "subtitle": "Lesson 18 · Python for Data Engineers"
  },
  {
    "type": "code",
    "eyebrow": "Defining a function",
    "code": "def is_valid_fare(fare_amount):\n    if fare_amount <= 0:\n        return False\n    return True\n\nprint(is_valid_fare(14.50))   # True\nprint(is_valid_fare(-5.00))   # False",
    "caption": "Define once, call as many times as you want — never rewrite the logic inside it."
  },
  {
    "type": "code",
    "eyebrow": "Multiple parameters, default values",
    "code": "def is_valid_trip(fare_amount, passenger_count, min_fare=0.01):\n    if fare_amount < min_fare:\n        return False\n    if passenger_count <= 0:\n        return False\n    return True\n\nis_valid_trip(14.50, 1)              # min_fare defaults to 0.01\nis_valid_trip(0.00, 1, min_fare=0)    # explicit override",
    "caption": "A default lets callers skip a parameter entirely, or override it when they need to."
  },
  {
    "type": "code",
    "eyebrow": "Refactoring an inline loop into a function",
    "code": "def is_valid_trip(trip):\n    if trip[\"fare_amount\"] <= 0:\n        return False\n    if trip[\"passenger_count\"] <= 0:\n        return False\n    return True\n\nvalid_trips = [trip for trip in trips if is_valid_trip(trip)]",
    "caption": "Same rule as Lesson 17's loop — now named, reusable, and testable on its own."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 19",
    "titlePlain": "Exception ",
    "titleEmphasis": "Handling",
    "titleSuffix": "",
    "subtitle": "What happens when something goes wrong."
  }
]
