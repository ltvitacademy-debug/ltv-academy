{
  "lesson": "functions",
  "questions": [
    {
      "q": "What does 'def' do in Python?",
      "options": [
        "Defines a new, named, reusable function",
        "Deletes a variable",
        "Declares a loop",
        "Imports a library"
      ],
      "answer": 0,
      "explain": "Once defined, a function can be called as many times as needed without rewriting its logic."
    },
    {
      "q": "What is a parameter?",
      "options": [
        "A named input a function expects to be given when it's called",
        "The function's name itself",
        "A type of loop",
        "The value a function returns"
      ],
      "answer": 0,
      "explain": "fare_amount in def is_valid_fare(fare_amount) is a parameter — the input the function operates on."
    },
    {
      "q": "What does 'return' do inside a function?",
      "options": [
        "Sends a value back to whoever called the function",
        "Prints a value to the screen permanently",
        "Deletes the function after it runs once",
        "Stops the entire program"
      ],
      "answer": 0,
      "explain": "return is how a function hands its result back to the calling code."
    },
    {
      "q": "What is a default parameter value, like min_fare=0.01?",
      "options": [
        "A value a parameter takes on automatically if the caller doesn't explicitly provide one",
        "A value that can never be changed",
        "A value that only applies to the first function call",
        "A required parameter with no flexibility"
      ],
      "answer": 0,
      "explain": "Callers can omit the argument entirely and get the default, or override it explicitly."
    },
    {
      "q": "Why does turning Lesson 17's inline validation logic into a function help?",
      "options": [
        "It becomes named, reusable, and independently testable — callable from anywhere without duplicating the rule",
        "It makes the code run slower on purpose",
        "It removes the need for any conditions at all",
        "Functions cannot be reused, so there's no actual benefit"
      ],
      "answer": 0,
      "explain": "This is exactly why every PySpark transformation in Chapter 4 is built around calling functions rather than repeating logic inline."
    }
  ]
}
