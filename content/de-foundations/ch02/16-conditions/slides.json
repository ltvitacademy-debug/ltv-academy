[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Two",
    "titlePlain": "",
    "titleEmphasis": "Conditions",
    "titleSuffix": "",
    "subtitle": "Lesson 16 · Python for Data Engineers"
  },
  {
    "type": "code",
    "eyebrow": "if / elif / else",
    "code": "fare_amount = -5.00\n\nif fare_amount < 0:\n    print(\"Invalid: negative fare\")\nelif fare_amount == 0:\n    print(\"Warning: zero fare\")\nelse:\n    print(\"Valid fare\")",
    "caption": "Top to bottom, first True condition wins — everything else is skipped."
  },
  {
    "type": "code",
    "eyebrow": "and / or / not — combining conditions",
    "code": "if passenger_count == 0 and fare_amount > 0:\n    print(\"Suspicious: fare with no passengers\")\n\nif fare_amount < 0 or passenger_count < 0:\n    print(\"Invalid: negative value somewhere\")",
    "caption": "and needs BOTH sides true. or needs at least ONE. not flips a condition entirely."
  },
  {
    "type": "code",
    "eyebrow": "A real validation pattern",
    "code": "def is_valid_trip(fare_amount, passenger_count, trip_distance):\n    if fare_amount <= 0:\n        return False\n    if passenger_count <= 0:\n        return False\n    if trip_distance <= 0:\n        return False\n    return True",
    "caption": "One condition per business rule — exactly Lesson 27's ETL and Chapter 4's PySpark cleaning."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 17",
    "titlePlain": "",
    "titleEmphasis": "Loops",
    "titleSuffix": "",
    "subtitle": "Doing something to every row, automatically."
  }
]
