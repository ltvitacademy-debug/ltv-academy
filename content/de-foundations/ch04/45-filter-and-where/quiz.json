{
  "lesson": "filter-and-where",
  "questions": [
    {
      "q": "What is the relationship between df.filter() and df.where() in PySpark?",
      "options": [
        "They're genuinely interchangeable — where() exists as a SQL-flavored alias for filter()",
        "filter() only works on numbers, where() only works on strings",
        "where() is deprecated and should never be used",
        "filter() removes columns, where() removes rows"
      ],
      "answer": 0,
      "explain": "where() exists purely so SQL-background readers feel at home, since WHERE does the same job in Spark SQL (Lesson 59)."
    },
    {
      "q": "Why does trips.filter(\"trip_distance\" > 10) silently do the wrong thing?",
      "options": [
        "It compares the Python string \"trip_distance\" to the integer 10 using Python's own rules, with no connection to the actual DataFrame column",
        "It raises a clear error immediately",
        "It works correctly and is the recommended syntax",
        "It filters every row in the DataFrame regardless of the condition"
      ],
      "answer": 0,
      "explain": "This is a real trap — col() is what actually builds a Spark condition that gets evaluated against the DataFrame's rows."
    },
    {
      "q": "Why must you use & and | instead of Python's and/or when combining filter conditions?",
      "options": [
        "Spark's Column objects overload the bitwise operators (& and |) for this purpose — and/or don't work the same way on them",
        "Python's and/or are faster, so Spark forbids them by convention only",
        "& and | only work when there's exactly one condition",
        "There's no real difference between the two forms"
      ],
      "answer": 0,
      "explain": "This is why each individual condition also needs its own parentheses, due to Python's operator precedence rules."
    },
    {
      "q": "What is the Pandas equivalent of trips.filter(col(\"trip_distance\") > 10)?",
      "options": [
        "df[df[\"trip_distance\"] > 10], the boolean indexing pattern from Lesson 24",
        "df.drop(\"trip_distance\")",
        "df.sort_values(\"trip_distance\")",
        "There is no equivalent operation in Pandas"
      ],
      "answer": 0,
      "explain": "Same underlying concept — keep rows where a condition is true — just a method call instead of bracket indexing."
    },
    {
      "q": "Why do you need col() rather than a plain string when writing a filter condition?",
      "options": [
        "col() builds an actual Spark condition object that can be evaluated against every row, rather than a plain Python comparison",
        "Plain strings work exactly the same as col() in every case",
        "col() is only needed for numeric columns, never for text columns",
        "Filter conditions never need col() in PySpark"
      ],
      "answer": 0,
      "explain": "Without col(), a comparison like \"trip_distance\" > 10 just evaluates in plain Python, disconnected from the DataFrame."
    }
  ]
}
