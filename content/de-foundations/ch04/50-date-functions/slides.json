[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Four",
    "titlePlain": "Date ",
    "titleEmphasis": "Functions",
    "titleSuffix": "",
    "subtitle": "Lesson 50 · PySpark"
  },
  {
    "type": "code",
    "eyebrow": "Pulling parts out of a timestamp",
    "code": "from pyspark.sql.functions import year, month, dayofweek\n\ntrips = trips.withColumn(\"pickup_year\", year(col(\"tpep_pickup_datetime\")))\ntrips = trips.withColumn(\"pickup_month\", month(col(\"tpep_pickup_datetime\")))\ntrips = trips.withColumn(\"pickup_dow\", dayofweek(col(\"tpep_pickup_datetime\")))",
    "caption": "Needs a real timestamp column (Lesson 48's .cast()). dayofweek: 1=Sunday."
  },
  {
    "type": "code",
    "eyebrow": "datediff() — real duration",
    "code": "from pyspark.sql.functions import datediff, to_date\n\ntrips = trips.withColumn(\n  \"days_between\",\n  datediff(to_date(col(\"tpep_dropoff_datetime\")), to_date(col(\"tpep_pickup_datetime\"))),\n)",
    "caption": "Whole days between two dates — to_date() strips the time portion first."
  },
  {
    "type": "code",
    "eyebrow": "date_format() — for display only",
    "code": "from pyspark.sql.functions import date_format\n\ntrips = trips.withColumn(\n  \"pickup_display\",\n  date_format(col(\"tpep_pickup_datetime\"), \"MMM d, yyyy\"),\n)",
    "caption": "Renders like \"Jan 15, 2024\" — keep the real timestamp for actual date math."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 51",
    "titlePlain": "Null ",
    "titleEmphasis": "Handling",
    "titleSuffix": "",
    "subtitle": "Catching what earlier lessons left as null."
  }
]
