[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Four",
    "titlePlain": "Defining ",
    "titleEmphasis": "Schemas",
    "titleSuffix": "",
    "subtitle": "Lesson 43 · PySpark"
  },
  {
    "type": "code",
    "eyebrow": "Building a StructType by hand",
    "code": "from pyspark.sql.types import (\n  StructType, StructField,\n  StringType, IntegerType, DoubleType, TimestampType,\n)\n\ntaxi_schema = StructType([\n  StructField(\"VendorID\", StringType(), True),\n  StructField(\"tpep_pickup_datetime\", TimestampType(), True),\n  StructField(\"passenger_count\", IntegerType(), True),\n  StructField(\"fare_amount\", DoubleType(), True),\n])",
    "caption": "Each StructField: column name, type class, and a nullable flag."
  },
  {
    "type": "code",
    "eyebrow": "Using it — no inferSchema needed",
    "code": "df = spark.read.csv(\n  \"/data/nyc_taxi/yellow_tripdata_2024-01.csv\",\n  header=True,\n  schema=taxi_schema,\n)\n\ndf.printSchema()   # matches instantly, no guessing pass",
    "caption": "schema= and inferSchema=True are mutually exclusive in practice."
  },
  {
    "type": "code",
    "eyebrow": "What nullable=False actually does",
    "code": "StructField(\"passenger_count\", IntegerType(), False)\n\n# NOT a hard guarantee on read --\n# mostly a hint for Spark's optimizer.\n# Real null validation: Lesson 51.",
    "caption": "A signal, not a guardrail — Spark generally still loads an unexpected null."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 44",
    "titlePlain": "select",
    "titleEmphasis": "()",
    "titleSuffix": "",
    "subtitle": "Choosing exactly the columns you need."
  }
]
