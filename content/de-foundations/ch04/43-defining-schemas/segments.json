[
  {"slide": "s1", "text": "Now let's write a schema ourselves, by hand, instead of asking Spark to guess."},
  {"slide": "s2", "text": "Struct type takes a list of struct fields, and each one names three things: the column name, the type — called, not just named, note the parentheses — and whether nulls are allowed."},
  {"slide": "s3", "text": "Pass it to spark dot read dot csv as schema, and there's no infer schema needed at all. In practice these two are mutually exclusive — once you hand Spark a schema, it trusts it completely instead of inspecting the actual data."},
  {"slide": "s4", "text": "But here's the catch: setting nullable to false doesn't actually make Spark reject a null on read. It's mostly a hint for Spark's optimizer. If the real data has an unexpected null anyway, Spark will generally still load it — the mismatch shows up later as a bug. Real null validation is Lesson 51's job, not this flag's."},
  {"slide": "s5", "text": "Name, type, and a nullable hint — not a guarantee. Next lesson: select, choosing exactly the columns you actually need."}
]
