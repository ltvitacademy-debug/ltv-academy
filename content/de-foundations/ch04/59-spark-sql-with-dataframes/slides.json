[
  {
    "type": "title",
    "eyebrow": "Data Engineering Foundations · Chapter Four",
    "titlePlain": "Spark SQL With ",
    "titleEmphasis": "DataFrames",
    "titleSuffix": "",
    "subtitle": "Lesson 59 · PySpark"
  },
  {
    "type": "code",
    "eyebrow": "Running SQL against a temp view",
    "code": "trips.createOrReplaceTempView(\"trips\")\n\nresult = spark.sql(\"\"\"\n  SELECT VendorID, COUNT(*) AS trip_count, AVG(fare_amount) AS avg_fare\n  FROM trips WHERE trip_distance > 5 GROUP BY VendorID\n\"\"\")",
    "caption": "Returns an ordinary DataFrame -- every method from this chapter still works on it."
  },
  {
    "type": "code",
    "eyebrow": "The same query, both ways",
    "code": "# DataFrame API\ntrips.filter(col(\"trip_distance\") > 5).groupBy(\"VendorID\").agg(\n  count(\"*\").alias(\"trip_count\"), avg(\"fare_amount\").alias(\"avg_fare\")\n)\n\n# Spark SQL -- the exact same result, same Catalyst plan",
    "caption": "Straight back to Lesson 38 -- both compile to the same execution plan. Neither is faster."
  },
  {
    "type": "code",
    "eyebrow": "Mixing both freely",
    "code": "cleaned = trips.filter(col(\"fare_amount\") > 0).na.drop(subset=[\"passenger_count\"])\ncleaned.createOrReplaceTempView(\"cleaned_trips\")\n\nspark.sql(\"SELECT VendorID, SUM(fare_amount) AS total FROM cleaned_trips GROUP BY VendorID\")",
    "caption": "Clean with the DataFrame API, then query with SQL -- or the reverse. Nothing stops either."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 60",
    "titlePlain": "Writing ",
    "titleEmphasis": "Data",
    "titleSuffix": "",
    "subtitle": "Saving results back out."
  }
]
