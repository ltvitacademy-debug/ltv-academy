{
  "lesson": "temporary-views",
  "questions": [
    {
      "q": "What does trips.createOrReplaceTempView(\"trips\") actually do?",
      "options": [
        "Registers the name \"trips\" for Spark SQL to reference, pointing at the same underlying DataFrame — it does not copy any data",
        "Copies the DataFrame's data into a new file on disk",
        "Permanently saves the DataFrame as a database table",
        "Converts the DataFrame into a Pandas DataFrame"
      ],
      "answer": 0,
      "explain": "Nothing about the DataFrame itself changes — Spark just gets a name to call it by in SQL."
    },
    {
      "q": "What happens if you call createOrReplaceTempView() again with the same name but a different DataFrame?",
      "options": [
        "It replaces the old registration with no error and no duplicate",
        "It throws an error since the name is already taken",
        "It creates a second, separately-named view automatically",
        "It merges the two DataFrames together under that name"
      ],
      "answer": 0,
      "explain": "This is convenient during iterative development — re-register under the same name and SQL queries pick up the change."
    },
    {
      "q": "How long does a temporary view persist?",
      "options": [
        "Only as long as the SparkSession that created it — it's not saved to disk and disappears when the session ends",
        "Forever, until manually deleted",
        "It's saved to disk permanently, like a real database table",
        "It persists across different Spark applications automatically"
      ],
      "answer": 0,
      "explain": "This is unlike a real database table — a temp view is a querying convenience, not a way to persist data."
    },
    {
      "q": "Why is createOrReplaceTempView() necessary before using spark.sql()?",
      "options": [
        "spark.sql() looks up table names among registered views — without registering the DataFrame first, there's nothing for the SQL query to find",
        "spark.sql() works on any DataFrame automatically without any setup",
        "It's only needed for DataFrames read from CSV, not Parquet",
        "It converts SQL syntax into PySpark method calls internally"
      ],
      "answer": 0,
      "explain": "This is the one-time setup step that Lesson 59's real Spark SQL queries rely on."
    },
    {
      "q": "Is a temporary view visible to a different Spark application running separately?",
      "options": [
        "No — it's tied to the specific SparkSession that created it",
        "Yes, temp views are shared globally across all Spark applications",
        "Only if the view was created using union() first",
        "Only on the same physical machine, regardless of session"
      ],
      "answer": 0,
      "explain": "This is part of why a temp view is described as a convenience rather than a way to persist or share data."
    }
  ]
}
