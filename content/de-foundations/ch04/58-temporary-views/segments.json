[
  {"slide": "s1", "text": "Now let's give a DataFrame a name that SQL can actually reference — temporary views."},
  {"slide": "s2", "text": "Create or replace temp view doesn't copy or move any data — it registers the name trips as something Spark SQL can reference, pointing at the exact same underlying DataFrame. Nothing about trips itself changes."},
  {"slide": "s3", "text": "And that view lives only as long as the Spark session that created it. It's not saved to disk, not visible to another Spark application, and disappears the moment the session ends — completely unlike a real database table."},
  {"slide": "s4", "text": "This is exactly the setup step Lesson 59 relies on. Spark dot sql runs a real SQL query against that registered name — without registering it first, there'd be nothing for that query to find."},
  {"slide": "s5", "text": "A name, not a copy, and it lives only as long as the session. Next lesson: Spark SQL with DataFrames, two APIs, one engine underneath."}
]
