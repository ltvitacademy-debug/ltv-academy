{
  "lesson": "creating-dataframes",
  "questions": [
    {
      "q": "What does spark.createDataFrame() do?",
      "options": [
        "Builds a DataFrame from Python data, like a list of tuples or dictionaries, with or without an explicit schema",
        "Only works with data already stored in a file",
        "Converts a Spark DataFrame into a Pandas DataFrame",
        "Deletes an existing DataFrame"
      ],
      "answer": 0,
      "explain": "This is the real, hands-on entry point Chapter 4 starts with."
    },
    {
      "q": "What are StructType and StructField used for?",
      "options": [
        "Describing an explicit schema — column names and types — instead of letting Spark guess",
        "Reading files from a REST API",
        "Only used with Pandas, never Spark",
        "Deleting columns from a DataFrame"
      ],
      "answer": 0,
      "explain": "Stating the schema explicitly avoids the risk of Spark inferring it incorrectly on a large distributed dataset."
    },
    {
      "q": "Can Spark create a DataFrame directly from a list of dictionaries, like Lesson 15's shape?",
      "options": [
        "Yes — Spark can infer a schema from a list of dictionaries directly, without requiring a StructType",
        "No, dictionaries can never be used to create a Spark DataFrame",
        "Only if every dictionary has exactly one key",
        "Only after first converting to a Pandas DataFrame"
      ],
      "answer": 0,
      "explain": "This mirrors Lesson 21's Pandas behavior, just using PySpark's inference instead."
    },
    {
      "q": "What does spark.range(10) create?",
      "options": [
        "A DataFrame with one column, 'id', containing the integers 0 through 9",
        "A DataFrame with 10 randomly generated columns",
        "An error, since range() requires two arguments",
        "A DataFrame of exactly 10 different data types"
      ],
      "answer": 0,
      "explain": "This is genuinely useful for quickly testing an idea without needing real data on hand."
    },
    {
      "q": "Why does an explicit schema matter more in Spark than in Pandas?",
      "options": [
        "Getting a schema wrong across a genuinely distributed dataset is far more expensive to discover and fix later",
        "Spark cannot infer schemas under any circumstances",
        "Explicit schemas are required by Python syntax rules",
        "There is no real difference in importance between the two"
      ],
      "answer": 0,
      "explain": "This is why Spark code often states types explicitly, even though inference is available as a convenience."
    }
  ]
}
