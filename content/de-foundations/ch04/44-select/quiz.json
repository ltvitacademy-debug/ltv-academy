{
  "lesson": "select",
  "questions": [
    {
      "q": "What does df.select(\"VendorID\", \"fare_amount\") return?",
      "options": [
        "A new DataFrame containing only those two columns",
        "A single Python list of values",
        "The full original DataFrame, unchanged",
        "An error, since select() requires exactly one column"
      ],
      "answer": 0,
      "explain": "This is the PySpark equivalent of Lesson 24's Pandas double-bracket column selection."
    },
    {
      "q": "Which of these are valid ways to reference a column in PySpark?",
      "options": [
        "A plain string, dot notation (df.col_name), and the col() function — all three work",
        "Only a plain string works; the other two are invalid syntax",
        "Only col() works; strings and dot notation always fail",
        "None of these work without importing pandas first"
      ],
      "answer": 0,
      "explain": "All three produce the same result for a plain reference, but col() becomes necessary once you want to transform or rename the column."
    },
    {
      "q": "Why does .alias() require a col()-style reference instead of working on a plain string?",
      "options": [
        "Because .alias() is a method that needs a Column object to call it on, and a plain string doesn't have that method",
        "It doesn't require col() — plain strings support .alias() too",
        "Because alias() only works on numeric columns",
        "Because strings are immutable in Python"
      ],
      "answer": 0,
      "explain": "This is exactly why col() is introduced now — it's the gateway to renaming and transforming columns in later lessons."
    },
    {
      "q": "Is df.select(\"fare_amount\") a transformation or an action?",
      "options": [
        "A transformation — it builds a plan but doesn't execute anything until an action like .show() runs",
        "An action — it immediately loads and prints the data",
        "Neither — select() doesn't exist in PySpark",
        "It depends on whether the DataFrame came from CSV or Parquet"
      ],
      "answer": 0,
      "explain": "This connects directly back to Lesson 34's lazy evaluation: select() builds the plan, .show() or .count() runs it."
    },
    {
      "q": "Why is selecting fewer columns from a Parquet-backed DataFrame especially efficient?",
      "options": [
        "Because Parquet's columnar storage means only the selected columns' bytes get read off disk, per Lesson 42",
        "Because Parquet automatically deletes unselected columns from the file",
        "It isn't more efficient — Parquet always reads the entire file regardless",
        "Because select() converts the file to CSV first"
      ],
      "answer": 0,
      "explain": "This ties directly back to Lesson 42's columnar storage explanation."
    }
  ]
}
