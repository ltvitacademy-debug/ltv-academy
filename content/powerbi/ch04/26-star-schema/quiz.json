{
  "lesson": "star-schema",
  "questions": [
    {
      "q": "What defines a star schema?",
      "options": [
        "One fact table at the center, with dimension tables connected directly to it",
        "Every table connected to every other table",
        "A model with only dimension tables and no fact table",
        "Tables chained together in a single long sequence"
      ],
      "answer": 0,
      "explain": "Star schema places one fact table at the center, with each dimension table connected directly to it — giving the layout its star-like shape."
    },
    {
      "q": "In Power BI, where can you see the shape of your model's relationships as a diagram?",
      "options": [
        "Model view",
        "Data view",
        "The Filters pane",
        "The Format pane"
      ],
      "answer": 0,
      "explain": "Model view shows every table and the relationship lines connecting them, making the star (or non-star) shape of your model visible."
    },
    {
      "q": "Why does a star schema make filtering more predictable than a looped or chained table layout?",
      "options": [
        "Every dimension table has exactly one, direct path to the fact table",
        "Star schema removes the need for any relationships",
        "It reduces the number of columns in the fact table",
        "It automatically sorts every table alphabetically"
      ],
      "answer": 0,
      "explain": "With one direct route from each dimension to the fact table, there's no ambiguity about which path a filter should travel — unlike a loop, where two paths might reach the same table."
    },
    {
      "q": "Where should a hierarchy like Category → Subcategory → Product be built?",
      "options": [
        "From columns within a single dimension table",
        "Split evenly across the fact table and a dimension table",
        "Inside the fact table only",
        "It can't be built in Power BI"
      ],
      "answer": 0,
      "explain": "A hierarchy needs all its levels in one table to work cleanly. If Category and Product are split into separate tables (a snowflake design), you can't build one hierarchy spanning both."
    },
    {
      "q": "What design keeps a dimension split across multiple related tables instead of one?",
      "options": [
        "A snowflake",
        "A star schema",
        "A factless fact table",
        "A junk dimension"
      ],
      "answer": 0,
      "explain": "A snowflake dimension normalizes a single entity, like Product, across multiple related tables — the opposite of consolidating it into one dimension table."
    }
  ]
}
