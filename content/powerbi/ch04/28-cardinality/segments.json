[
  {"slide": "s1", "text": "Every relationship you create has a setting called cardinality. Here's what it actually describes, and when to touch it."},
  {"slide": "s2", "text": "Cardinality answers one question: can a value repeat on each side? Look at a star schema, and you'll see the same pattern on every spoke — a 1 on the dimension side, where each value appears once, and an asterisk on the fact side, where it can repeat many times."},
  {"slide": "s3", "text": "Power BI gives you four cardinality options, but many-to-one is the one you'll use constantly — it's exactly the shape a fact table pointing at a dimension table takes. Power BI usually sets this correctly on its own."},
  {"slide": "s4", "text": "Sometimes two tables happen to line up so a value never repeats on either side — Power BI notices, and defaults to one-to-one instead. Be careful here: if you know a future refresh will add duplicates, override it to many-to-one yourself before that happens."},
  {"slide": "s5", "text": "Cardinality sets how values match up. Next: filter direction — how far a filter is allowed to travel once that match exists."}
]
