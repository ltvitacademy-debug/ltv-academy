[
  {"slide": "s1", "text": "Merge reached sideways. This lesson reaches down — stacking tables on top of each other instead of joining them side by side."},
  {"slide": "s2", "text": "Here's the idea. Two tables, not quite identical columns, stacked into one. Every column from every table survives. Where a table didn't have a given column, those rows just get null. No error, nothing lost."},
  {"slide": "s3", "text": "Append Queries sits right next to Merge Queries, same Combine group, Home ribbon. Append Queries adds rows into your current query. Append Queries As New builds a separate query instead, leaving both originals untouched."},
  {"slide": "s4", "text": "Pick your primary table, choose Append Queries, and select the table to stack underneath it. Two Tables mode is the default."},
  {"slide": "s5", "text": "Power Query matches columns by name, not position, and stacks everything together. This Online Sales data never had a Referrer column, so those rows show null there — exactly as expected."},
  {"slide": "s6", "text": "Need more than two? Switch to Three or More Tables mode and build a list — move tables across with Add. This is the natural choice for combining several similarly-shaped sources at once, like monthly exports or regional files, instead of appending them one pair at a time."},
  {"slide": "s7", "text": "Merge for columns, Append for rows — between the two, you can combine almost any two tables Power BI hands you. Next lesson: Parameters, for making your queries reusable instead of hardcoded. See you there."}
]
