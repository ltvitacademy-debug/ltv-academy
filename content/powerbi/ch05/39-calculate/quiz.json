{
  "lesson": "calculate",
  "questions": [
    {
      "q": "In one sentence, what does CALCULATE do?",
      "options": [
        "Evaluates an expression inside a modified filter context",
        "Sums every numeric column in a table automatically",
        "Converts a calculated column into a measure",
        "Removes all relationships from a model temporarily"
      ],
      "answer": 0,
      "explain": "CALCULATE takes an expression (usually a measure or aggregation) and evaluates it after applying one or more filter arguments that modify the filter context."
    },
    {
      "q": "What happens if you call CALCULATE with only an expression and no filter arguments?",
      "options": [
        "It behaves exactly like the expression on its own",
        "It throws a syntax error — filters are required",
        "It removes all existing filters automatically",
        "It returns blank in every case"
      ],
      "answer": 0,
      "explain": "Filters are optional. With none supplied, CALCULATE simply evaluates the expression under the filter context that already exists."
    },
    {
      "q": "If a column already has a filter applied (e.g. from a slicer) and CALCULATE adds a new filter on that same column, what happens by default?",
      "options": [
        "CALCULATE's filter overwrites the existing one for that expression",
        "The two filters combine with AND logic automatically",
        "CALCULATE's filter is ignored in favor of the existing one",
        "Power BI throws an error for conflicting filters"
      ],
      "answer": 0,
      "explain": "By default, CALCULATE's filter argument replaces any existing filter on the same column — it doesn't combine with it. KEEPFILTERS changes this behavior."
    },
    {
      "q": "Besides a simple Boolean comparison, what else can a CALCULATE filter argument be?",
      "options": [
        "A table expression (like FILTER) or a filter modifier function (like ALL or REMOVEFILTERS)",
        "Only a single column name with no condition",
        "A visual formatting setting",
        "A Power Query step"
      ],
      "answer": 0,
      "explain": "CALCULATE accepts Boolean expressions, table expressions built with functions like FILTER, and filter modifier functions like ALL, ALLEXCEPT, or REMOVEFILTERS."
    },
    {
      "q": "Why is CALCULATE described as the most important function in DAX?",
      "options": [
        "Nearly every advanced DAX pattern — percent of total, year-over-year growth, security filters — is built using it",
        "It's the only function capable of aggregating a column",
        "It's required in every single DAX formula, even the simplest ones",
        "It's the fastest-executing function in the language"
      ],
      "answer": 0,
      "explain": "CALCULATE underlies most sophisticated DAX calculations by giving formulas a way to deliberately modify filter context — which is why it recurs constantly throughout the rest of this chapter."
    }
  ]
}
