{
  "lesson": "context-transition",
  "questions": [
    {
      "q": "What is context transition?",
      "options": [
        "Converting the current row context into an equivalent filter context",
        "Converting a calculated column into a measure",
        "Switching a relationship from active to inactive",
        "Changing a report's slicers programmatically"
      ],
      "answer": 0,
      "explain": "Context transition takes 'the current row' and turns it into 'a filter restricted to that row's values' — the bridge between row context and filter context."
    },
    {
      "q": "What triggers context transition?",
      "options": [
        "CALCULATE evaluating an expression inside row context",
        "Adding a slicer to a report",
        "Refreshing the data model",
        "Creating a new relationship between two tables"
      ],
      "answer": 0,
      "explain": "Whenever CALCULATE runs inside row context — whether written explicitly or triggered implicitly by referencing a measure — it converts that row context into filter context."
    },
    {
      "q": "Why does context transition happen automatically when you reference a measure inside a calculated column?",
      "options": [
        "Every measure is implicitly wrapped in its own CALCULATE behind the scenes",
        "Calculated columns automatically convert into measures when referenced",
        "It doesn't happen automatically — CALCULATE must always be written explicitly",
        "Measures ignore row context entirely and use only filter context"
      ],
      "answer": 0,
      "explain": "Because a measure reference is implicitly CALCULATE-wrapped, referencing one from row context (like inside a calculated column) triggers context transition without any visible CALCULATE in your formula."
    },
    {
      "q": "In the Customer Segment example, what does ALLEXCEPT(Customer, Customer[CustomerKey]) do after context transition occurs?",
      "options": [
        "Removes other filters on the Customer table while keeping the filter that context transition just created on CustomerKey",
        "Removes the CustomerKey filter entirely",
        "Adds a new relationship to the Sales table",
        "Converts the calculated column into a measure"
      ],
      "answer": 0,
      "explain": "ALLEXCEPT clears filters from all columns of the Customer table except CustomerKey — preserving exactly the single-customer filter context transition produced."
    },
    {
      "q": "Why does understanding context transition matter, even for formulas you don't write yourself?",
      "options": [
        "It explains how a calculated column, which only sees row context, can produce results that depend on filtering an entirely different table",
        "It's required knowledge before you can create any measure at all",
        "It only matters for formulas using time intelligence functions",
        "It has no practical effect on how formulas behave"
      ],
      "answer": 0,
      "explain": "Recognizing the pattern — something triggered CALCULATE, converting row context into filter context — demystifies formulas that would otherwise look impossible."
    }
  ]
}
