{
  "lesson": "row-vs-filter-context",
  "questions": [
    {
      "q": "What is \"context\" in DAX?",
      "options": [
        "The surrounding situation — a row, a set of active filters, or both — a formula is evaluated within",
        "A type of DAX function",
        "The table a measure's home is set to",
        "A setting in Power Query"
      ],
      "answer": 0,
      "explain": "Context is the environment a formula runs in — which row, which filters — and it's the concept nearly every advanced DAX function exists to read or modify."
    },
    {
      "q": "What is row context most naturally associated with?",
      "options": [
        "Calculated columns, which are evaluated once per row",
        "Slicers and report-level filters",
        "The CALCULATE function specifically",
        "Only DirectQuery data sources"
      ],
      "answer": 0,
      "explain": "Row context is 'the current row' — it's the default evaluation environment for a calculated column, which computes a value for every row automatically."
    },
    {
      "q": "In 'Store Sales = CALCULATE([Total Sales], Channel[ChannelName] = \"Store\")', what does the filter argument represent?",
      "options": [
        "Filter context, written directly into the formula",
        "Row context extended across a relationship",
        "A calculated column definition",
        "A syntax error, since filters can't be written inside formulas"
      ],
      "answer": 0,
      "explain": "The Channel[ChannelName] = \"Store\" argument narrows down which rows count — that's filter context, set explicitly inside the formula via CALCULATE."
    },
    {
      "q": "Why can the exact same measure formula return different numbers on the same report?",
      "options": [
        "Because the filter context surrounding it changes as slicers, filters, or visual selections change",
        "Because measures recalculate randomly",
        "Because Power BI caches an old value and doesn't always refresh it",
        "This never happens — a measure's formula always returns the same number"
      ],
      "answer": 0,
      "explain": "A measure is re-evaluated within whatever filter context is currently active — the formula stays fixed, but the surrounding filters change what it calculates over."
    },
    {
      "q": "Why doesn't a calculated column's value change when a report's slicers change?",
      "options": [
        "Because it was computed once, at refresh time, using only row context — it has no awareness of report-level filters",
        "Because calculated columns are recalculated on every slicer change, just slower than measures",
        "Because slicers only affect visuals, never the underlying data model",
        "Because calculated columns can't be added to visuals at all"
      ],
      "answer": 0,
      "explain": "A calculated column only ever sees row context at the moment it's computed — it's stored, static data by the time any report filter gets applied."
    }
  ]
}
