{
  "lesson": "rls-roles",
  "questions": [
    {
      "q": "Where do you create a new RLS role in Power BI Desktop?",
      "options": [
        "Modeling tab → Manage Roles → New",
        "File → Publish → Manage Roles",
        "It can only be created in the Power BI service, never Desktop",
        "Home tab → Refresh → Manage Roles"
      ],
      "answer": 0,
      "explain": "Roles are defined in Desktop via the Modeling tab's Manage Roles dialog, where selecting New starts a new role definition."
    },
    {
      "q": "What can you NOT do from inside Desktop's Manage Roles dialog?",
      "options": [
        "Assign users to the role — that only happens in the Power BI service after publishing",
        "Name the role",
        "Select which table to filter",
        "Write a DAX filter expression"
      ],
      "answer": 0,
      "explain": "Desktop lets you define the role and its filter, but user assignment is exclusively a service-side step, covered in Lesson 81."
    },
    {
      "q": "When must you switch from the default editor to the DAX editor when defining a role's filter?",
      "options": [
        "When the filter needs a dynamic DAX function like USERNAME() or USERPRINCIPALNAME(), which the default drop-down editor can't express",
        "Every single time, regardless of the filter",
        "Only when filtering a table with more than 1000 rows",
        "Never — the DAX editor is deprecated"
      ],
      "answer": 0,
      "explain": "The default editor only handles simple static filters. Dynamic expressions using DAX functions require switching to the DAX editor."
    },
    {
      "q": "What does the DAX filter expression [Region] = \"West\" do when applied as an RLS role filter?",
      "options": [
        "It evaluates TRUE/FALSE per row, keeping only rows where Region equals 'West' visible to anyone assigned to that role",
        "It deletes every row where Region does not equal 'West' from the underlying data source",
        "It renames the Region column to 'West' for all users",
        "It applies only to the report's visuals, not the underlying semantic model"
      ],
      "answer": 0,
      "explain": "An RLS filter expression is evaluated per row; only rows returning TRUE remain visible to a user assigned to that role — the underlying data source itself is untouched."
    },
    {
      "q": "Why can't you name an RLS role 'London,Paris'?",
      "options": [
        "Role names can't contain a comma",
        "Role names must be exactly one word",
        "Role names can't contain any letters",
        "There's actually no restriction on role names at all"
      ],
      "answer": 0,
      "explain": "Power BI explicitly disallows commas in role names — a small but real constraint worth knowing before you hit the error."
    }
  ]
}
