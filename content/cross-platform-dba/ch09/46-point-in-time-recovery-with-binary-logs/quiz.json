{
  "lesson": "point-in-time-recovery-with-binary-logs",
  "questions": [
    {
      "q": "What does MySQL's binary log (binlog) actually record, per this lesson?",
      "options": [
        "Every event that changes data, in the order it was committed, across a sequence of binlog files",
        "Only failed queries and error messages",
        "A snapshot of the entire database taken once per day",
        "Only schema changes like CREATE TABLE, never row-level data changes"
      ],
      "answer": 0,
      "explain": "The binlog is an ordered record of data-changing events, used for both replication and point-in-time recovery."
    },
    {
      "q": "What does mysqlbinlog do in a point-in-time recovery scenario, per this lesson?",
      "options": [
        "Reads binlog files and replays their events as SQL against a restored database, up to a chosen point",
        "Takes a fresh physical backup of the database",
        "Compresses binlog files to save disk space",
        "Converts a logical backup into a physical one"
      ],
      "answer": 0,
      "explain": "mysqlbinlog extracts and replays events from binlog files, filterable by timestamp or log position, to roll a restored database forward."
    },
    {
      "q": "What is the correct sequence for MySQL point-in-time recovery, per this lesson?",
      "options": [
        "Restore the most recent full backup, then replay the relevant binlog events up to the desired point",
        "Replay all binlog events first, then take a full backup",
        "Delete the binlog files, then restore the full backup",
        "Point-in-time recovery requires no full backup at all, only binlogs"
      ],
      "answer": 0,
      "explain": "The pattern is: restore a full backup as the starting point, then use mysqlbinlog to apply changes recorded after that backup up to the target time."
    },
    {
      "q": "Why is enabling log_bin (binary logging) before an incident essential, per this lesson?",
      "options": [
        "MySQL cannot log changes retroactively, so binary logging must already be on to enable point-in-time recovery later",
        "log_bin only affects backup file compression",
        "log_bin is required for mysqldump to function at all",
        "log_bin has no effect on recovery, only on replication"
      ],
      "answer": 0,
      "explain": "Point-in-time recovery depends on a binlog chain that already existed before the incident — it can't be enabled after the fact to recover past data."
    },
    {
      "q": "How does MySQL's binlog-based point-in-time recovery compare conceptually to SQL Server, per this lesson?",
      "options": [
        "It solves the same structural problem as a SQL Server transaction log backup chain, through a different underlying mechanism",
        "MySQL has no equivalent concept to point-in-time recovery at all",
        "The binlog is identical in format and mechanism to a SQL Server transaction log",
        "SQL Server has no equivalent capability, making this a MySQL-only feature"
      ],
      "answer": 0,
      "explain": "The lesson frames binlog-based PITR as structurally the same goal as a SQL Server log backup chain, achieved with a genuinely different MySQL mechanism."
    }
  ]
}
