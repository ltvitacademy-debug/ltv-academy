{
  "lesson": "percona-xtrabackup-and-physical-backups",
  "questions": [
    {
      "q": "Why does Percona XtraBackup exist, per this lesson?",
      "options": [
        "MySQL ships no built-in equivalent to RMAN or BACKUP DATABASE for fast, hot, physical backups of InnoDB at scale",
        "mysqldump was discontinued and needed a replacement",
        "XtraBackup is required to install MySQL",
        "MySQL cannot run without XtraBackup installed"
      ],
      "answer": 0,
      "explain": "Unlike Oracle's RMAN or SQL Server's native backup tooling, MySQL has no built-in hot physical backup utility, which XtraBackup fills."
    },
    {
      "q": "What does the xtrabackup --backup step actually do, per this lesson?",
      "options": [
        "Copies InnoDB's data files while streaming redo log changes generated during the copy",
        "Runs a full mysqldump export in the background",
        "Permanently locks all tables until the backup finishes",
        "Deletes the existing data files before copying new ones"
      ],
      "answer": 0,
      "explain": "The --backup step copies files while capturing redo log entries, without needing to stop the server or block writers for most of its runtime."
    },
    {
      "q": "Why is the xtrabackup --prepare step necessary before restoring, per this lesson?",
      "options": [
        "It replays the captured redo log against the copied files to bring them to a consistent, restorable state",
        "It compresses the backup files to save disk space",
        "It uploads the backup to Percona's servers",
        "It converts the backup into a mysqldump-compatible SQL file"
      ],
      "answer": 0,
      "explain": "Without --prepare, the copied files aren't yet consistent — --prepare applies the same crash-recovery logic InnoDB uses after an unclean shutdown."
    },
    {
      "q": "How does an XtraBackup restore work, per this lesson?",
      "options": [
        "Stop MySQL, copy the prepared files into the data directory with --copy-back, then start the server",
        "Replay the SQL file with the mysql client, same as mysqldump",
        "Run RESTORE DATABASE against the XtraBackup files",
        "XtraBackup backups cannot be restored, only inspected"
      ],
      "answer": 0,
      "explain": "Because it's a physical backup, restoring means placing prepared files back into MySQL's data directory and starting the server — no SQL replay."
    },
    {
      "q": "Why is XtraBackup considered the industry-standard choice for large MySQL databases, per this lesson?",
      "options": [
        "It's free, open-source, and large-scale MySQL shops actually run it in production because mysqldump doesn't scale to their restore-time requirements",
        "It is the only backup tool MySQL's license permits",
        "It replaces the need for any backup strategy at all",
        "It is slower than mysqldump but more secure"
      ],
      "answer": 0,
      "explain": "XtraBackup is framed as the honest, real-world standard tool for large databases, filling a genuine gap rather than being an arbitrary add-on."
    }
  ]
}
