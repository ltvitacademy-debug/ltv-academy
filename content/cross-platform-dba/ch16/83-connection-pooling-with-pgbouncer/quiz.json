{
  "lesson": "connection-pooling-with-pgbouncer",
  "questions": [
    {
      "q": "Why does connection pooling matter more operationally for PostgreSQL than for a thread-based engine like SQL Server, per this lesson?",
      "options": [
        "PostgreSQL forks a full OS process per connection, which has real memory/CPU cost and a lower practical connection ceiling",
        "PostgreSQL doesn't support more than 10 connections at all without pooling",
        "SQL Server has no concept of connections",
        "PgBouncer is required for PostgreSQL to accept any client connections whatsoever"
      ],
      "answer": 0,
      "explain": "PostgreSQL's process-per-connection architecture (from Chapter 12) makes unpooled high-connection-count workloads expensive in a way thread-based engines aren't."
    },
    {
      "q": "What does PgBouncer do, architecturally?",
      "options": [
        "It sits between client applications and PostgreSQL, maintaining a small pool of real backend connections reused across many clients",
        "It replaces PostgreSQL's WAL-based replication mechanism",
        "It is a PostgreSQL extension that runs inside the database server process itself",
        "It converts PostgreSQL's process-per-connection model into a thread-per-connection model"
      ],
      "answer": 0,
      "explain": "PgBouncer is a separate lightweight pooler that clients connect to, which hands out a smaller set of real PostgreSQL backend connections."
    },
    {
      "q": "Which PgBouncer pooling mode holds a server connection only for the duration of a single transaction?",
      "options": [
        "Transaction pooling",
        "Session pooling",
        "Statement pooling",
        "Connection pooling"
      ],
      "answer": 0,
      "explain": "Transaction pooling assigns a server connection per transaction and returns it to the pool immediately after, which is why it's the mode most deployments run."
    },
    {
      "q": "What is the main tradeoff of transaction pooling, per this lesson?",
      "options": [
        "Session state like SET statements can't be relied on to persist across transactions, since the next transaction may land on a different backend",
        "It cannot be used with any web application",
        "It provides worse connection reuse than session pooling",
        "It requires disabling replication entirely"
      ],
      "answer": 0,
      "explain": "Because the server connection is returned to the pool after each transaction, session-scoped state isn't guaranteed to survive to the next transaction."
    },
    {
      "q": "Which pooling mode is described as maximizing reuse but being the most restrictive, since it breaks multi-statement transactions?",
      "options": [
        "Statement pooling",
        "Session pooling",
        "Transaction pooling",
        "Hot standby pooling"
      ],
      "answer": 0,
      "explain": "Statement pooling releases the connection after every individual statement, which is incompatible with most real multi-statement application transactions."
    }
  ]
}
