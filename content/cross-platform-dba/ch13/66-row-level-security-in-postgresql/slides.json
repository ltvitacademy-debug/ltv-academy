[
  {
    "type": "title",
    "eyebrow": "Cross-Platform Relational Database Administration · Chapter Thirteen",
    "titlePlain": "Row-Level Security in ",
    "titleEmphasis": "PostgreSQL",
    "titleSuffix": "",
    "subtitle": "Lesson 66 · PostgreSQL Security & User Management"
  },
  {
    "type": "code",
    "eyebrow": "Enable RLS and write a policy",
    "code": "ALTER TABLE sales.orders ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY tenant_isolation ON sales.orders\n  FOR ALL TO app_user\n  USING (tenant_id =\n    current_setting('app.current_tenant')::int);",
    "caption": "The filter is applied automatically — the application never writes WHERE tenant_id = ... itself."
  },
  {
    "type": "code",
    "eyebrow": "USING vs. WITH CHECK",
    "code": "CREATE POLICY read_own_rows ON sales.orders\n  FOR SELECT TO app_user\n  USING (owner_id = current_user_id());\n\nCREATE POLICY insert_own_rows ON sales.orders\n  FOR INSERT TO app_user\n  WITH CHECK (owner_id = current_user_id());",
    "caption": "USING filters visible rows; WITH CHECK validates rows being written."
  },
  {
    "type": "steps",
    "eyebrow": "Who RLS applies to",
    "titlePlain": "Owners Bypass by ",
    "titleEmphasis": "Default",
    "titleSuffix": "",
    "steps": [
      { "label": "Named roles / PUBLIC", "sub": "policies apply as written" },
      { "label": "Table owner", "sub": "bypasses RLS unless forced" },
      { "label": "FORCE ROW LEVEL SECURITY", "sub": "makes RLS apply to the owner too" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 67",
    "titlePlain": "PostgreSQL Auditing & ",
    "titleEmphasis": "pgAudit",
    "titleSuffix": "",
    "subtitle": "Because there's no built-in auditing the way Oracle has it."
  }
]
