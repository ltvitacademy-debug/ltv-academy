{
  "lesson": "oracle-rac-real-application-clusters-overview",
  "questions": [
    {
      "q": "What does an Oracle RAC configuration fundamentally consist of?",
      "options": [
        "Multiple Oracle instances, each with their own memory, mounting and opening the same single database on shared storage concurrently",
        "One primary database and one standby database kept in sync via redo transport",
        "A single instance that automatically clones itself across servers",
        "Multiple independent databases with no shared storage at all"
      ],
      "answer": 0,
      "explain": "RAC is multiple peer instances sharing one database on shared storage (typically ASM), not a primary/standby relationship."
    },
    {
      "q": "What problem does Cache Fusion solve in a RAC cluster?",
      "options": [
        "Keeping every instance's buffer cache coherent by shipping cached data blocks directly between instances' memory over the interconnect",
        "Compressing redo logs before they are archived",
        "Encrypting data in transit between the primary and a standby",
        "Automatically gathering optimizer statistics across all instances"
      ],
      "answer": 0,
      "explain": "Cache Fusion ships modified cached blocks directly between instances' memory so no instance reads a stale copy, enabling safe concurrent access."
    },
    {
      "q": "Why is describing RAC as 'Oracle's version of a SQL Server Failover Cluster Instance' meaningfully inaccurate?",
      "options": [
        "An FCI is active-passive (one node runs the instance at a time), while RAC is active-active (every instance processes work concurrently)",
        "Because RAC and FCI are actually identical in every respect",
        "Because FCI is active-active and RAC is active-passive",
        "Because RAC doesn't use shared storage at all, unlike an FCI"
      ],
      "answer": 0,
      "explain": "The core architectural distinction is active-passive (FCI) versus active-active (RAC) — RAC instances all handle work simultaneously, not just after a failover."
    },
    {
      "q": "What two benefits does RAC deliver together because every instance is active?",
      "options": [
        "Scalability (adding a node adds capacity) and availability (a lost node doesn't stop the rest)",
        "Encryption at rest and encryption in transit",
        "Automatic statistics gathering and automatic indexing",
        "Zero licensing cost and zero administrative complexity"
      ],
      "answer": 0,
      "explain": "Because every RAC instance actively processes work, the cluster gains both scale-out capacity and continuous availability if a node fails."
    },
    {
      "q": "What is Oracle Grid Infrastructure's role relative to RAC?",
      "options": [
        "It's the underlying cluster layer — Clusterware plus ASM, managing membership via the OCR and a voting disk — that RAC runs on top of",
        "It is an optional GUI tool with no functional role in RAC operation",
        "It replaces the need for a database entirely",
        "It is only used for Data Guard, never for RAC"
      ],
      "answer": 0,
      "explain": "Grid Infrastructure (Clusterware + ASM, using the OCR and a voting disk) provides the cluster membership and shared-storage foundation RAC depends on."
    }
  ]
}
