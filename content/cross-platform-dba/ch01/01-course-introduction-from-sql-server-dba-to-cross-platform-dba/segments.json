[
  {"slide": "s1", "text": "This course closes out the SQL Server Database Administrator path, but it isn't another SQL Server course. It asks what happens when the job also includes Oracle, MySQL, or PostgreSQL, because that's what the company actually runs."},
  {"slide": "s2", "text": "Every DBA instinct from this path transfers directly — measure before you change something, a recovery model determines what's restorable, an untested backup isn't a real backup. Those are relational database facts, not SQL-Server-specific facts. What doesn't transfer is syntax, tooling, and architecture detail."},
  {"slide": "s3", "text": "Oracle, MySQL, and PostgreSQL alongside SQL Server cover the large majority of real-world relational deployments. Oracle dominates large enterprise and legacy systems, MySQL is everywhere in web applications, and PostgreSQL is the default for new open-source projects."},
  {"slide": "s4", "text": "This course teaches each platform on its own terms, the same discipline Azure Database Administrator used for the cloud. Next up: why organizations end up running multiple database platforms in the first place."}
]
