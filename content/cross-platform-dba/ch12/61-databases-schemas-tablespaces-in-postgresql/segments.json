[
  {"slide": "s1", "text": "PostgreSQL's object hierarchy looks like SQL Server's on the surface, but one link means something meaningfully different. In PostgreSQL, a schema is a real namespace, functionally closer to what a database feels like day to day."},
  {"slide": "s2", "text": "The hierarchy runs cluster to database to schema to table. A cluster is one server managing one or more databases, each a genuinely isolated namespace — connections are scoped to exactly one database, unlike SQL Server's easy three-part cross-database naming."},
  {"slide": "s3", "text": "You can still join across schemas in the same database with ordinary SQL, just like SQL Server. But PostgreSQL practice leans on schemas more heavily as an organizational tool — one schema per application or tenant inside a shared database is a genuinely common pattern."},
  {"slide": "s4", "text": "A tablespace tells PostgreSQL where on disk a database or object's files physically live, independent of the logical hierarchy. It's a server-wide object, conceptually similar to SQL Server filegroups mapped to separate disks."},
  {"slide": "s5", "text": "That's the honest comparison: PostgreSQL's isolation lives at the database level, and schemas are the flexible organizational layer underneath. Next up: psql, pgAdmin, and the real tools DBAs use day to day."}
]
