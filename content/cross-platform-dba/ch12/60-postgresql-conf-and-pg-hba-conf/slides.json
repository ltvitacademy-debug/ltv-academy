[
  {
    "type": "title",
    "eyebrow": "Cross-Platform Relational Database Administration · Chapter Twelve",
    "titlePlain": "postgresql.conf & ",
    "titleEmphasis": "pg_hba.conf",
    "titleSuffix": "",
    "subtitle": "Lesson 60 · PostgreSQL Architecture & Installation"
  },
  {
    "type": "code",
    "eyebrow": "postgresql.conf — server behavior",
    "code": "shared_buffers = 4GB\nwork_mem = 64MB\nlisten_addresses = '*'\nport = 5432\nmax_connections = 100",
    "caption": "Most settings apply on reload; shared_buffers needs a full restart."
  },
  {
    "type": "code",
    "eyebrow": "pg_hba.conf — who can connect",
    "code": "# TYPE  DATABASE  USER   ADDRESS        METHOD\nlocal   all       all                   peer\nhost    all       all    127.0.0.1/32   scram-sha-256\nhost    all       all    0.0.0.0/0      scram-sha-256",
    "caption": "First matching rule wins. No SQL Server or Oracle equivalent has this per-source-address granularity."
  },
  {
    "type": "steps",
    "eyebrow": "pg_hba.conf columns",
    "titlePlain": "Five Columns, ",
    "titleEmphasis": "One Decision",
    "titleSuffix": "",
    "steps": [
      { "label": "TYPE", "sub": "local (socket) or host (TCP/IP)" },
      { "label": "DATABASE / USER", "sub": "which db and role the rule covers" },
      { "label": "ADDRESS", "sub": "client IP range in CIDR notation" },
      { "label": "METHOD", "sub": "trust, peer, scram-sha-256, cert" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 61",
    "titlePlain": "Databases, Schemas & ",
    "titleEmphasis": "Tablespaces",
    "titleSuffix": "",
    "subtitle": "PostgreSQL's real object hierarchy."
  }
]
