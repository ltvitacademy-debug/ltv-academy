{
  "lesson": "rman-architecture-and-configuration",
  "questions": [
    {
      "q": "What does an RMAN channel actually do?",
      "options": [
        "It's a connection to a specific device (disk or tape) that performs the actual backup/restore I/O",
        "It's the network connection between RMAN and the recovery catalog only",
        "It's a synonym for the target database",
        "It's a scheduled job that triggers backups automatically"
      ],
      "answer": 0,
      "explain": "RMAN allocates one or more channels to perform the read/write I/O against disk or tape."
    },
    {
      "q": "By default, where does RMAN store metadata about the backups it has taken?",
      "options": [
        "In the target database's own control file",
        "In a mandatory separate recovery catalog database",
        "In a flat text file on the OS",
        "It doesn't store any metadata by default"
      ],
      "answer": 0,
      "explain": "The control file is the default metadata store, bounded by CONTROL_FILE_RECORD_KEEP_TIME."
    },
    {
      "q": "What is a real advantage of using a recovery catalog over relying only on the control file?",
      "options": [
        "Metadata survives even if the target database's control file is lost, and one catalog can track many target databases centrally",
        "It's required before RMAN can take any backup at all",
        "It eliminates the need for channels",
        "It automatically encrypts all backups"
      ],
      "answer": 0,
      "explain": "A recovery catalog is a separate, durable metadata store that isn't bounded by control-file retention and can serve multiple databases."
    },
    {
      "q": "What does CONFIGURE BACKUP OPTIMIZATION ON actually change about RMAN's behavior?",
      "options": [
        "RMAN skips re-backing-up files it can tell haven't changed since a previous backup that satisfies the retention policy",
        "It compresses every backup automatically",
        "It forces every backup to run on tape instead of disk",
        "It disables the recovery catalog"
      ],
      "answer": 0,
      "explain": "Backup optimization speeds up repeat backups by skipping unchanged files, rather than changing storage medium or compression."
    },
    {
      "q": "What does CONFIGURE CONTROLFILE AUTOBACKUP ON provide?",
      "options": [
        "Automatic backup of the control file and spfile after every backup and significant structural change, so a lost control file can still be restored",
        "Automatic deletion of obsolete backups",
        "Automatic allocation of channels for every session",
        "Automatic registration with the recovery catalog"
      ],
      "answer": 0,
      "explain": "Controlfile autobackup protects against total control file loss by keeping an automatic, current copy available to restore from."
    }
  ]
}
