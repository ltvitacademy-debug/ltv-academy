[
  {
    "type": "title",
    "eyebrow": "Cross-Platform Relational Database Administration · Chapter Two",
    "titlePlain": "Oracle Net Services & ",
    "titleEmphasis": "Listener Configuration",
    "titleSuffix": "",
    "subtitle": "Lesson 9 · Oracle Architecture & Installation"
  },
  {
    "type": "code",
    "eyebrow": "tnsnames.ora — the client side",
    "code": "ORCLPDB =\n  (DESCRIPTION =\n    (ADDRESS = (PROTOCOL = TCP)\n      (HOST = dbhost)(PORT = 1521))\n    (CONNECT_DATA =\n      (SERVICE_NAME = orclpdb)))",
    "caption": "A net service name alias, so a client can type sqlplus hr@orclpdb."
  },
  {
    "type": "code",
    "eyebrow": "listener.ora — the server side",
    "code": "LISTENER =\n  (DESCRIPTION =\n    (ADDRESS = (PROTOCOL = TCP)\n      (HOST = dbhost)(PORT = 1521)))\n\n# services usually register\n# dynamically via PMON",
    "caption": "The Oracle Net Listener (tnslsnr) listens on port 1521 by default."
  },
  {
    "type": "steps",
    "eyebrow": "The connection flow, end to end",
    "titlePlain": "Client to ",
    "titleEmphasis": "Instance",
    "titleSuffix": "",
    "steps": [
      { "label": "Resolve", "sub": "tnsnames.ora → host, port, service" },
      { "label": "Reach Listener", "sub": "connects on port 1521" },
      { "label": "Hand Off", "sub": "listener routes to the instance" },
      { "label": "Session", "sub": "instance establishes the session" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 10",
    "titlePlain": "Tablespaces, Datafiles & Oracle ",
    "titleEmphasis": "Storage Structures",
    "titleSuffix": "",
    "subtitle": "Where the data actually lives, one layer down."
  }
]
