[
  {
    "type": "title",
    "eyebrow": "Cross-Platform Relational Database Administration · Chapter Ten",
    "titlePlain": "The MySQL Performance Schema & ",
    "titleEmphasis": "sys Schema",
    "titleSuffix": "",
    "subtitle": "Lesson 51 · MySQL Performance Tuning"
  },
  {
    "type": "code",
    "eyebrow": "Performance Schema",
    "code": "performance_schema.*\n  statement execution stats\n  wait events (locks, I/O)\n  memory usage\n\nSELECT digest_text, count_star, sum_timer_wait\nFROM performance_schema.events_statements_summary_by_digest\nORDER BY sum_timer_wait DESC LIMIT 10;",
    "caption": "Configurable, ongoing instrumentation — not a SQL Server DMV, architecturally."
  },
  {
    "type": "code",
    "eyebrow": "sys schema — the readable layer",
    "code": "SELECT query, exec_count, total_latency\nFROM sys.statement_analysis\nORDER BY total_latency DESC LIMIT 10;\n\nSELECT * FROM sys.schema_unused_indexes;",
    "caption": "Views built on top of Performance Schema, ships built in."
  },
  {
    "type": "steps",
    "eyebrow": "Using both together",
    "titlePlain": "Readable first, ",
    "titleEmphasis": "raw when needed",
    "titleSuffix": "",
    "steps": [
      { "label": "sys schema", "sub": "fast, readable answer to common questions" },
      { "label": "Performance Schema", "sub": "drop down for specific raw granularity" },
      { "label": "Together", "sub": "MySQL's equivalent-purpose tooling to DMVs/Query Store" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 52",
    "titlePlain": "Configuration Tuning: Buffer Pool & ",
    "titleEmphasis": "Key Variables",
    "titleSuffix": "",
    "subtitle": "innodb_buffer_pool_size and the settings that matter most."
  }
]
