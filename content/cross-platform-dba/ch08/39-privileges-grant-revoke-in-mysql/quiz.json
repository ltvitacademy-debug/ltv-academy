{
  "lesson": "privileges-grant-revoke-in-mysql",
  "questions": [
    {
      "q": "What does creating a MySQL user@host account with CREATE USER grant by itself?",
      "options": [
        "Nothing beyond the ability to authenticate — privileges must be granted separately",
        "Full SELECT access to every database automatically",
        "Automatic membership in the DBA role",
        "Read access to the database matching the username"
      ],
      "answer": 0,
      "explain": "Authentication and authorization are separate steps in MySQL; a new account can log in but can do nothing until GRANT is used."
    },
    {
      "q": "What does GRANT SELECT, INSERT, UPDATE ON shop.* TO 'app_svc'@'10.0.4.%'; scope the privilege to?",
      "options": [
        "Every table within the shop database",
        "Every database on the server",
        "Only one specific table named 'shop'",
        "Only specific columns of the shop database"
      ],
      "answer": 0,
      "explain": "The dbname.* syntax is a database-level grant, applying to every table inside that one database."
    },
    {
      "q": "What is column-level privilege scope used for, per this lesson's example?",
      "options": [
        "Letting an account like a support agent see specific columns (e.g. email) while other columns in the same table stay inaccessible",
        "Restricting access to an entire database",
        "Granting access to every table at once",
        "Column-level scope does not exist in MySQL"
      ],
      "answer": 0,
      "explain": "GRANT SELECT (col1, col2) ON table restricts the privilege to named columns only, useful when a table mixes sensitive and non-sensitive data."
    },
    {
      "q": "What does the GRANT OPTION privilege specifically allow?",
      "options": [
        "The grantee can grant their own privileges to other accounts",
        "The grantee gains automatic DROP privileges",
        "The grantee's password never expires",
        "The grantee can bypass host restrictions"
      ],
      "answer": 0,
      "explain": "GRANT OPTION lets an account extend its own access to others, which is why it should be handed out sparingly."
    },
    {
      "q": "What does SHOW GRANTS FOR 'app_svc'@'10.0.4.%'; return?",
      "options": [
        "The account's current privileges, expressed as the exact GRANT statements that would reproduce them",
        "A log of every query the account has run",
        "The account's password in plaintext",
        "A list of every account on the server"
      ],
      "answer": 0,
      "explain": "SHOW GRANTS is the standard way to audit exactly what an account currently has without reading raw system tables."
    }
  ]
}
