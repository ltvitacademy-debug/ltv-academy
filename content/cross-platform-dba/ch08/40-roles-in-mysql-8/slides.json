[
  {
    "type": "title",
    "eyebrow": "Cross-Platform Relational Database Administration · Chapter Eight",
    "titlePlain": "Roles in ",
    "titleEmphasis": "MySQL 8",
    "titleSuffix": "",
    "subtitle": "Lesson 40 · MySQL Security & User Management"
  },
  {
    "type": "code",
    "eyebrow": "Creating a role and granting privileges to it",
    "code": "CREATE ROLE 'app_readonly';\nGRANT SELECT ON shop.* TO 'app_readonly';\n\nCREATE ROLE 'app_readwrite';\nGRANT SELECT, INSERT, UPDATE, DELETE ON shop.* TO 'app_readwrite';",
    "caption": "A role is created almost exactly like a user — internally it's implemented as one."
  },
  {
    "type": "code",
    "eyebrow": "Granting a role to real user accounts",
    "code": "CREATE USER 'analyst_jane'@'10.0.5.%' IDENTIFIED BY 'a-strong-password';\nGRANT 'app_readonly' TO 'analyst_jane'@'10.0.5.%';\n\nCREATE USER 'dev_mark'@'10.0.4.%' IDENTIFIED BY 'another-strong-password';\nGRANT 'app_readwrite' TO 'dev_mark'@'10.0.4.%';",
    "caption": "Change the role's privileges once, and every account holding it updates immediately."
  },
  {
    "type": "code",
    "eyebrow": "Making the role actually active",
    "code": "SET DEFAULT ROLE 'app_readonly' TO 'analyst_jane'@'10.0.5.%';\n\n-- without a default role, activate manually per session:\nSET ROLE 'app_readonly';",
    "caption": "Granting a role doesn't make it active on connect — SET DEFAULT ROLE does."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 41",
    "titlePlain": "MySQL Security Best Practices and ",
    "titleEmphasis": "Hardening",
    "titleSuffix": "",
    "subtitle": "The real mysql_secure_installation checklist, in full."
  }
]
