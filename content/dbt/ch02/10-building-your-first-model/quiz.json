{
  "lesson": "building-your-first-model",
  "questions": [
    {
      "q": "What are the three pieces this lesson says you combine to build your first real model?",
      "options": [
        "A declared source, a staging model built on it, and ref() calls connecting them",
        "A Python script, a YAML file, and a Docker container",
        "A dbt Cloud account, a Snowflake trial, and a GitHub repo",
        "Three separate staging models with no other logic"
      ],
      "answer": 0,
      "explain": "This lesson walks through combining a source, its staging model, and ref() calls into one new downstream model."
    },
    {
      "q": "After writing a new model .sql file, what extra step is required to register it with the project?",
      "options": [
        "None — dbt discovers any .sql file under a configured model-path automatically",
        "Running a separate `dbt register` command",
        "Adding its filename manually to dbt_project.yml",
        "Emailing dbt Labs to add it to the catalog"
      ],
      "answer": 0,
      "explain": "There is no registration step — dbt automatically discovers any .sql file located under a configured model-path."
    },
    {
      "q": "In the real terminal screenshot in this lesson, what changed in the `dbt run` output after the user added their new customers model?",
      "options": [
        "It now reports 'Found 3 models' instead of 2, and builds the new model alongside the starter ones",
        "It reported an unrecoverable error and refused to run",
        "The starter models were deleted automatically",
        "Nothing changed — the new model was silently ignored"
      ],
      "answer": 0,
      "explain": "The screenshot shows dbt finding 3 models instead of 2 and successfully building the newly added customers model."
    },
    {
      "q": "If a new model file doesn't show up in a run, what two things does this lesson say to check first?",
      "options": [
        "Whether the file is under a configured model-path and whether it has a .sql extension",
        "Whether the file has been committed to Git",
        "Whether the Snowflake warehouse is large enough",
        "Whether dbt Cloud's subscription has been renewed"
      ],
      "answer": 0,
      "explain": "Only two things make a file 'a model' to dbt: living under a configured model-path, and having a .sql extension."
    },
    {
      "q": "How can you build just one specific model instead of the whole project?",
      "options": [
        "dbt run --select <model_name>",
        "dbt run --only-one",
        "There is no way to run a single model",
        "You must delete every other model file first"
      ],
      "answer": 0,
      "explain": "The --select flag lets you target a single model (or a set of models) instead of running the entire project."
    }
  ]
}
