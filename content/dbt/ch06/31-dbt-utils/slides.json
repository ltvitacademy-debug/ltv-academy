[
  {
    "type": "title",
    "eyebrow": "dbt / Analytics Engineering · Chapter 6, Lesson 31",
    "titlePlain": "dbt_utils: The Macros You'll Actually ",
    "titleEmphasis": "Reuse",
    "titleSuffix": "",
    "subtitle": "The one package nearly every dbt project installs"
  },
  {
    "type": "code",
    "eyebrow": "generate_surrogate_key() — replaces HASH()",
    "code": "select\n    {{ dbt_utils.generate_surrogate_key(\n        ['customer_id', 'order_date']\n    ) }} as order_key,\n    customer_id, order_date, order_total\nfrom {{ ref('stg_orders') }}",
    "caption": "Same model, unchanged, on Snowflake, BigQuery, or Postgres."
  },
  {
    "type": "code",
    "eyebrow": "date_spine() — a calendar with no source table",
    "code": "select *\nfrom {{ dbt_utils.date_spine(\n    datepart=\"day\",\n    start_date=\"cast('2023-01-01' as date)\",\n    end_date=\"cast('2026-12-31' as date)\"\n) }}",
    "caption": "A complete dim_date-style spine in one macro call."
  },
  {
    "type": "steps",
    "eyebrow": "Worth knowing by name",
    "titlePlain": "Three more ",
    "titleEmphasis": "reusable",
    "titleSuffix": " macros",
    "steps": [
      { "label": "star()", "sub": "All columns except the ones listed" },
      { "label": "pivot()", "sub": "Row values become columns" },
      { "label": "union_relations()", "sub": "Combines tables with overlapping columns" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "union_relations() in practice",
    "code": "select * from {{ dbt_utils.union_relations(\n    relations=[ref('orders_us'), ref('orders_eu'), ref('orders_apac')]\n) }}",
    "caption": "A table missing a column gets null filled in — not an error."
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Version-Controlling a dbt ",
    "titleEmphasis": "Project",
    "titleSuffix": "",
    "subtitle": "Chapter 7: tying everything to real Git and CI/CD"
  }
]
