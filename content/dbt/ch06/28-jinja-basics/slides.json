[
  {
    "type": "title",
    "eyebrow": "dbt / Analytics Engineering · Chapter 6, Lesson 28",
    "titlePlain": "Jinja Basics Inside ",
    "titleEmphasis": "dbt",
    "titleSuffix": "",
    "subtitle": "The templating language you've already been writing"
  },
  {
    "type": "code",
    "eyebrow": "The two delimiters",
    "code": "-- {{ }} — an EXPRESSION: produces a value\nselect * from {{ ref('stg_orders') }}\n\n-- {% %} — a STATEMENT: control flow, no value itself\n{% if is_incremental() %}\n    where updated_at > (select max(updated_at) from {{ this }})\n{% endif %}",
    "caption": "{{ }} renders to something; {% %} controls whether SQL appears at all."
  },
  {
    "type": "code",
    "eyebrow": "Variables and loops",
    "code": "{% set payment_methods = ['credit_card', 'paypal', 'bank_transfer'] %}\n\nwhere payment_method in (\n    {% for method in payment_methods %}\n        '{{ method }}'{% if not loop.last %},{% endif %}\n    {% endfor %}\n)",
    "caption": "loop.last skips the trailing comma on the final item."
  },
  {
    "type": "code",
    "eyebrow": "Generating repeated SQL",
    "code": "{% for status in ['pending', 'shipped', 'delivered', 'cancelled'] %}\n    sum(case when order_status = '{{ status }}' then 1 else 0 end)\n        as orders_{{ status }}\n    {% if not loop.last %},{% endif %}\n{% endfor %}",
    "caption": "Four columns, written once — SQL has no loops of its own."
  },
  {
    "type": "steps",
    "eyebrow": "Seeing what it compiles to",
    "titlePlain": "Jinja gone, ",
    "titleEmphasis": "plain SQL",
    "titleSuffix": " only",
    "steps": [
      { "label": "target/compiled/", "sub": "Every run writes the rendered SQL" },
      { "label": "Compiled Code tab", "sub": "Live, side by side, in dbt Cloud" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Writing Custom ",
    "titleEmphasis": "Macros",
    "titleSuffix": "",
    "subtitle": "Packaging a piece of Jinja so you write it once"
  }
]
