[
  {
    "type": "title",
    "eyebrow": "dbt / Analytics Engineering · Chapter 6, Lesson 29",
    "titlePlain": "Writing Custom ",
    "titleEmphasis": "Macros",
    "titleSuffix": "",
    "subtitle": "A function for SQL, defined once, called by name"
  },
  {
    "type": "code",
    "eyebrow": "macros/cents_to_dollars.sql",
    "code": "{% macro cents_to_dollars(column_name, decimal_places=2) %}\n    round({{ column_name }} / 100.0, {{ decimal_places }})\n{% endmacro %}",
    "caption": "decimal_places=2 gives the argument a default — callers can omit it."
  },
  {
    "type": "code",
    "eyebrow": "Calling it from a model",
    "code": "select order_id, customer_id,\n    {{ cents_to_dollars('order_total_cents') }} as order_total,\n    {{ cents_to_dollars('tax_cents', 4) }} as tax_amount\nfrom {{ ref('stg_orders') }}\n\n-- compiles to:\n-- round(order_total_cents / 100.0, 2) as order_total,\n-- round(tax_cents / 100.0, 4) as tax_amount",
    "caption": "One place to fix a rounding bug — not a copy-pasted round() in every model."
  },
  {
    "type": "code",
    "eyebrow": "Returning a real value",
    "code": "{% macro get_status_list() %}\n    {% set statuses = ['pending', 'shipped', 'delivered', 'cancelled'] %}\n    {{ return(statuses) }}\n{% endmacro %}\n\n{% set statuses = get_status_list() %}",
    "caption": "return() hands back a real Python list, reusable by any model or macro."
  },
  {
    "type": "steps",
    "eyebrow": "When it's worth it",
    "titlePlain": "Reuse, ",
    "titleEmphasis": "not",
    "titleSuffix": " obfuscation",
    "steps": [
      { "label": "Worth it", "sub": "Same logic in 2+ models" },
      { "label": "Not worth it", "sub": "Hiding 5 clear lines behind a call" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "dbt ",
    "titleEmphasis": "Packages",
    "titleSuffix": "",
    "subtitle": "Installing someone else's macros into your project"
  }
]
