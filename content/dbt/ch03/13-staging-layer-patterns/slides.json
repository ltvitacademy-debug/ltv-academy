[
  {
    "type": "title",
    "eyebrow": "dbt / Analytics Engineering · Chapter 3, Lesson 13",
    "titlePlain": "Staging Layer ",
    "titleEmphasis": "Patterns",
    "titleSuffix": "",
    "subtitle": "1-to-1 with sources, renamed and typed — nothing more"
  },
  {
    "type": "steps",
    "eyebrow": "What a staging model can do",
    "titlePlain": "Four things it's ",
    "titleEmphasis": "allowed",
    "titleSuffix": " to do",
    "steps": [
      { "label": "Renaming", "sub": "Cryptic source columns to clear names" },
      { "label": "Type casting", "sub": "String timestamp to real timestamp" },
      { "label": "Basic computations", "sub": "Cents to dollars, simple math" },
      { "label": "Categorizing", "sub": "Status codes to readable labels" }
    ]
  },
  {
    "type": "steps",
    "eyebrow": "What a staging model must never do",
    "titlePlain": "Two things that are ",
    "titleEmphasis": "off-limits",
    "titleSuffix": "",
    "steps": [
      { "label": "No joins", "sub": "Duplicates computation, confuses row ownership" },
      { "label": "No aggregations", "sub": "Must preserve the source table's exact grain" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "A real stg_orders.sql (dbt Labs' own example)",
    "code": "with source as (\n    select * from {{ source('ecom', 'raw_orders') }}\n),\nrenamed as (\n    select\n        id as order_id,\n        customer as customer_id,\n        {{ cents_to_dollars('subtotal') }} as subtotal,\n        {{ dbt.date_trunc('day', 'ordered_at') }} as ordered_at\n    from source\n)\nselect * from renamed",
    "caption": "Rename, cast, compute — no join, no group by."
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Intermediate ",
    "titleEmphasis": "Models",
    "titleSuffix": "",
    "subtitle": "The joining staging models are never allowed to do"
  }
]
