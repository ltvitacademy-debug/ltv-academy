[
  {
    "type": "title",
    "eyebrow": "dbt / Analytics Engineering · Chapter 5, Lesson 25",
    "titlePlain": "Incremental Models: ",
    "titleEmphasis": "Fundamentals",
    "titleSuffix": "",
    "subtitle": "Processing only new or changed rows, not the whole history"
  },
  {
    "type": "code",
    "eyebrow": "materialized='incremental'",
    "code": "{{ config(\n    materialized='incremental',\n    unique_key='order_id',\n) }}\n\nselect order_id, customer_id, order_total, order_ts\nfrom {{ source('shop', 'orders') }}\n\n{% if is_incremental() %}\n    where order_ts > (select max(order_ts) from {{ this }})\n{% endif %}",
    "caption": "False on the first run (no table yet); true — and filtering — on every run after."
  },
  {
    "type": "steps",
    "eyebrow": "The watermark parallel",
    "titlePlain": "The same pattern, ",
    "titleEmphasis": "generalized",
    "titleSuffix": "",
    "steps": [
      { "label": "Snowflake", "sub": "A separate load_watermarks table you advance by hand" },
      { "label": "dbt incremental", "sub": "{{ this }} IS the watermark — no separate table" }
    ]
  },
  {
    "type": "steps",
    "eyebrow": "What it needs",
    "titlePlain": "Two things a ",
    "titleEmphasis": "table",
    "titleSuffix": " model doesn't need",
    "steps": [
      { "label": "unique_key", "sub": "Tells an update apart from a new row" },
      { "label": "A reliable watermark column", "sub": "Must actually increase, consistently" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Incremental Strategies: merge, delete+insert, ",
    "titleEmphasis": "append",
    "titleSuffix": "",
    "subtitle": "How dbt actually applies the new rows it finds"
  }
]
