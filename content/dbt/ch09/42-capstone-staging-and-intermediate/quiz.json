{
  "lesson": "capstone-staging-and-intermediate",
  "questions": [
    {
      "q": "What must exist before any dbt model can ref() through source(), according to this lesson?",
      "options": [
        "A sources.yml declaring the raw tables, with a freshness check on at least one",
        "Nothing — source() works automatically with no declaration",
        "A completed marts layer",
        "A generated dbt Docs site"
      ],
      "answer": 0,
      "explain": "sources.yml declares the raw tables dbt is allowed to reference, one entry per raw table, and this lesson requires a freshness check on at least one."
    },
    {
      "q": "According to this lesson, what should a staging model contain?",
      "options": [
        "A thin, one-to-one, typed wrapper over one raw table, with nothing joined in yet",
        "The final joined fact table, ready for BI tools",
        "Every raw source combined into one giant model",
        "Only aggregated totals with no row-level detail"
      ],
      "answer": 0,
      "explain": "Staging models are 1:1 with a single raw source — typed and cleaned, but not yet joined to anything else."
    },
    {
      "q": "Where does the JSON source's flattening logic live in this capstone's dbt project?",
      "options": [
        "Inside the stg_events staging model itself, so dbt owns that transformation step",
        "It happens automatically with no code required",
        "In a one-off SQL script run before dbt, exactly as it was in the Snowflake capstone",
        "It cannot be done inside a dbt model at all"
      ],
      "answer": 0,
      "explain": "The lesson explicitly moves the flattening from a one-off script into the stg_events model, so dbt owns the transformation."
    },
    {
      "q": "What distinguishes int_orders_enriched from a marts-layer model, according to this lesson?",
      "options": [
        "It has no surrogate keys yet and nothing outside the project queries it directly — it exists to make marts simpler",
        "It is materialized as a view while marts models never are",
        "It contains no SQL at all",
        "There is no meaningful difference between intermediate and marts models"
      ],
      "answer": 0,
      "explain": "Intermediate models combine staging models but aren't endpoints themselves — that's what makes them intermediate rather than marts."
    },
    {
      "q": "Which models are allowed to query source() directly in this project's convention?",
      "options": [
        "Only staging models — every other model downstream should use ref() instead",
        "Every model in the project, at every layer",
        "Only the marts layer",
        "Only intermediate models"
      ],
      "answer": 0,
      "explain": "The lesson states staging models are the only layer that should query source() directly; everything downstream uses ref()."
    }
  ]
}
