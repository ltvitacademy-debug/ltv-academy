[
  {
    "type": "title",
    "eyebrow": "Applied Machine Learning · Chapter 1",
    "titlePlain": "Estimators, Transformers & ",
    "titleEmphasis": "Predictors",
    "titleSuffix": "",
    "subtitle": "Lesson 2 · The scikit-learn Workflow"
  },
  {
    "type": "steps",
    "eyebrow": "Three roles in scikit-learn",
    "titlePlain": "Who does ",
    "titleEmphasis": "what",
    "titleSuffix": "",
    "steps": [
      {
        "label": "ESTIMATOR",
        "sub": "learns from data with fit"
      },
      {
        "label": "TRANSFORM",
        "sub": "transformer: returns a table"
      },
      {
        "label": "PREDICTOR",
        "sub": "predictor: returns answers"
      },
      {
        "label": "TARGET",
        "sub": "only predictors need y"
      }
    ]
  },
  {
    "type": "code",
    "eyebrow": "A transformer learns, then applies",
    "code": "imp = SimpleImputer(strategy=\"median\")\nimp.fit(X_train)\nprint(imp.statistics_)\n# [44.  36.  76.26  1.]\nX_train_i = imp.transform(X_train)\nprint(X_train_i.shape)\n# (750, 4)",
    "caption": "One median per column, stored in statistics_."
  },
  {
    "type": "code",
    "eyebrow": "Fit on train, transform both",
    "code": "scaler = StandardScaler()\nX_train_s = scaler.fit_transform(\n    X_train_i)\nX_test_s = scaler.transform(\n    imp.transform(X_test))\nprint(X_train_s.mean(axis=0).round(2))\n# [ 0. -0.  0. -0.]\nprint(X_test_s.mean(axis=0).round(2))\n# [ 0.12  0.02 -0.05  0.02]",
    "caption": "Test columns need not average zero, and should not."
  },
  {
    "type": "code",
    "eyebrow": "Settings as building blocks",
    "code": "clf.get_params()[\"C\"]      # 1.0\nclf.set_params(C=0.1)\nfresh = clone(clf)\nfresh.C                   # 0.1\nhasattr(fresh, \"coef_\")    # False\nhasattr(clf, \"coef_\")      # True",
    "caption": "clone: same settings, no learned state."
  },
  {
    "type": "steps",
    "eyebrow": "The habit that prevents leakage",
    "titlePlain": "Fit on train, ",
    "titleEmphasis": "transform the rest",
    "titleSuffix": "",
    "steps": [
      {
        "label": "1. FIT",
        "sub": "on training rows only"
      },
      {
        "label": "2. TRANSFORM TRAIN",
        "sub": "with the fitted object"
      },
      {
        "label": "3. TRANSFORM TEST",
        "sub": "same object, no refit"
      },
      {
        "label": "4. NEVER REFIT",
        "sub": "on test or future data"
      }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 3",
    "titlePlain": "Preprocessing With ",
    "titleEmphasis": "ColumnTransformer",
    "titleSuffix": "",
    "subtitle": "Different treatments for different columns."
  }
]