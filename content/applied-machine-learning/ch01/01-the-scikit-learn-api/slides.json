[
  {
    "type": "title",
    "eyebrow": "Applied Machine Learning · Chapter 1",
    "titlePlain": "The scikit-learn ",
    "titleEmphasis": "API",
    "titleSuffix": "",
    "subtitle": "Lesson 1 · The scikit-learn Workflow"
  },
  {
    "type": "steps",
    "eyebrow": "One interface for every model",
    "titlePlain": "Four verbs, ",
    "titleEmphasis": "every model",
    "titleSuffix": "",
    "steps": [
      {
        "label": "CREATE",
        "sub": "settings go in the constructor"
      },
      {
        "label": "FIT",
        "sub": "learn from training data"
      },
      {
        "label": "PREDICT",
        "sub": "classes or probabilities"
      },
      {
        "label": "SCORE",
        "sub": "grade on held-out data"
      }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Features, label, split · illustrative data",
    "code": "df = make_customers()\nX = df[[\"tenure_months\",\n        \"support_calls\"]]\ny = df[\"churned\"]\nsplits = train_test_split(\n    X, y, test_size=0.25,\n    stratify=y, random_state=0)\nX_train, X_test, y_train, y_test = splits",
    "caption": "X is 2-D (rows x columns); y is 1-D. 750 train, 250 test."
  },
  {
    "type": "code",
    "eyebrow": "Fit, predict, score",
    "code": "model = LogisticRegression()\nmodel.fit(X_train, y_train)\nmodel.predict(X_test.head(3))\n# [1 0 0]\nmodel.predict_proba(X_test.head(3))\n# [[0.448 0.552] ...]\nmodel.score(X_test, y_test)\n# 0.768",
    "caption": "predict_proba columns follow model.classes_ = [0 1]."
  },
  {
    "type": "code",
    "eyebrow": "Swap the algorithm, keep the code",
    "code": "for m in [\n    DummyClassifier(\n        strategy=\"most_frequent\"),\n    LogisticRegression(),\n    RandomForestClassifier(\n        random_state=0)]:\n    m.fit(X_train, y_train)\n    print(m.score(X_test, y_test))\n# 0.764 / 0.768 / 0.728",
    "caption": "Dummy baseline 0.764: a score means little without one."
  },
  {
    "type": "steps",
    "eyebrow": "Conventions to memorize",
    "titlePlain": "Read the ",
    "titleEmphasis": "signals",
    "titleSuffix": "",
    "steps": [
      {
        "label": "CONSTRUCTOR ARGS",
        "sub": "settings, chosen before fit"
      },
      {
        "label": "TRAILING UNDERSCORE",
        "sub": "learned during fit: coef_"
      },
      {
        "label": "X IS 2-D",
        "sub": "a table of feature columns"
      },
      {
        "label": "Y IS 1-D",
        "sub": "the answer for each row"
      }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 2",
    "titlePlain": "Estimators, Transformers & ",
    "titleEmphasis": "Predictors",
    "titleSuffix": "",
    "subtitle": "The three roles objects play in scikit-learn."
  }
]