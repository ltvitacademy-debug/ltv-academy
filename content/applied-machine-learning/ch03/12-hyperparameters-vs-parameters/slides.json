[
  {
    "type": "title",
    "eyebrow": "Applied Machine Learning · Chapter 3",
    "titlePlain": "Hyperparameters vs. ",
    "titleEmphasis": "Parameters",
    "titleSuffix": "",
    "subtitle": "Lesson 12 · Tuning Models"
  },
  {
    "type": "steps",
    "eyebrow": "Two kinds of numbers",
    "titlePlain": "Learned vs. ",
    "titleEmphasis": "chosen",
    "titleSuffix": "",
    "steps": [
      {
        "label": "PARAMETERS",
        "sub": "Learned from data during fit"
      },
      {
        "label": "HYPERPARAMETERS",
        "sub": "Chosen by you before fit"
      },
      {
        "label": "TUNING",
        "sub": "Search hyperparameters with validation"
      }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Parameters end in an underscore",
    "code": "lr = LogisticRegression(C=1.0)\nlr.fit(X_tr, y_tr)\nprint(lr.coef_.round(2))\nprint(lr.intercept_.round(2))\n# [[-0.38  0.91  0.19 -0.03  0.6\n#   -0.08 -0.72 -0.65]]  (wrapped)\n# [0.19]",
    "caption": "Illustrative churn data. These exist only after fit."
  },
  {
    "type": "code",
    "eyebrow": "Hyperparameters: get_params / set_params",
    "code": "lr.get_params()[\"C\"]      # 1.0\nlr.set_params(C=0.01)\n\npipe = Pipeline([\n    (\"scale\", StandardScaler()),\n    (\"clf\", LogisticRegression())])\npipe.set_params(clf__C=0.1)\npipe.get_params()[\"clf__C\"]  # 0.1",
    "caption": "Inside a pipeline, the name is step__parameter."
  },
  {
    "type": "code",
    "eyebrow": "A hyperparameter changes what is learned",
    "code": "for C in [0.001, 0.1, 10]:\n    m = LogisticRegression(C=C)\n    m.fit(X_tr, y_tr)\n    print(C, np.abs(m.coef_).sum())\n# 0.001  0.62   test acc 0.725\n# 0.1    3.23   test acc 0.810\n# 10     3.61   test acc 0.805",
    "caption": "Small C shrinks the learned coefficients."
  },
  {
    "type": "code",
    "eyebrow": "Choose with validation, not training",
    "code": "for d in [2, 4, 8, None]:\n    t = DecisionTreeClassifier(\n        max_depth=d, random_state=42)\n    s = cross_val_score(t, X_tr, y_tr,\n                        cv=cv)\n# depth  cv mean   cv std   train\n#   2     0.885     0.019   0.894\n#   8     0.916     0.019   0.998\n#  None   0.919     0.024   1.000",
    "caption": "Training accuracy always rewards the most complex model."
  },
  {
    "type": "steps",
    "eyebrow": "The tuning habit",
    "titlePlain": "Search, ",
    "titleEmphasis": "score, keep",
    "titleSuffix": "",
    "steps": [
      {
        "label": "1. Candidates",
        "sub": "List values worth trying"
      },
      {
        "label": "2. Cross-validate",
        "sub": "Score each on held-out folds"
      },
      {
        "label": "3. Keep the best",
        "sub": "Watch the spread, not just the mean"
      },
      {
        "label": "4. Test once",
        "sub": "Final check on the untouched test set"
      }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 13",
    "titlePlain": "Grid ",
    "titleEmphasis": "Search",
    "titleSuffix": "",
    "subtitle": "Trying every combination of hyperparameters, automatically."
  }
]
