[
  {
    "type": "title",
    "eyebrow": "Azure Databricks & Delta Lake · Chapter Two",
    "titlePlain": "Schema ",
    "titleEmphasis": "Evolution",
    "titleSuffix": "",
    "subtitle": "Lesson 20 · Delta Lake"
  },
  {
    "type": "code",
    "eyebrow": "The escape hatch: mergeSchema",
    "code": "new_data.write.format(\"delta\").mode(\"append\") \\\n  .option(\"mergeSchema\", \"true\") \\\n  .save(\"/data/nyc_taxi/delta/trips\")\n\n# Existing rows get the new column filled with NULL.",
    "caption": "Last lesson's rejected write, now succeeding -- deliberately, not accidentally."
  },
  {
    "type": "code",
    "eyebrow": "What it permits, and what it still refuses",
    "code": "# ALLOWED: a genuinely new column appearing (additive)\n#\n# STILL REJECTED: a lossy type change --\n#   e.g. double column suddenly getting string values.\n#   That's still a real error, on purpose.",
    "caption": "Schema evolution grows a schema safely -- it doesn't disable enforcement."
  },
  {
    "type": "code",
    "eyebrow": "The explicit SQL alternative",
    "code": "ALTER TABLE trips ADD COLUMN payment_type STRING;\n\n# A deliberate, planned change -- its own step,\n# independent of any particular write.",
    "caption": "Often preferred in production: a reviewed migration, not a write-time side effect."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 21",
    "titlePlain": "Time ",
    "titleEmphasis": "Travel",
    "titleSuffix": "",
    "subtitle": "Querying historical versions of a table."
  }
]
