{
  "lesson": "reading-and-writing-delta-tables",
  "questions": [
    {
      "q": "What does spark.table(\"trips\") do?",
      "options": [
        "Reads a registered Delta table by name, with no path or .format(\"delta\") needed",
        "Creates a brand-new empty table named trips",
        "Only works for temporary views, never persistent tables",
        "Deletes the table named trips"
      ],
      "answer": 0,
      "explain": "Spark already knows what the table is from the metastore, since it was registered in Lesson 15."
    },
    {
      "q": "How do mode(\"overwrite\") and mode(\"append\") behave on a Delta table compared to plain Parquet?",
      "options": [
        "The same way they did for plain Parquet — this is Foundations Lesson 60's mode() pattern, unchanged",
        "They behave completely differently and require new syntax",
        "mode(\"append\") is not supported on Delta tables",
        "Delta requires mode(\"merge\") instead of overwrite or append"
      ],
      "answer": 0,
      "explain": "Delta's real advantages for updating existing rows show up in Lesson 22's MERGE, not in overwrite/append."
    },
    {
      "q": "What does saveAsTable(\"trips\") do that a plain .save(path) write doesn't?",
      "options": [
        "It writes the data as Delta AND registers it under that name in the metastore immediately, in one call",
        "It only writes the data without ever making it queryable",
        "It converts the table to CSV format automatically",
        "There's no real difference between the two"
      ],
      "answer": 0,
      "explain": "This combines what Lesson 15's DataFrame write plus a separate SQL CREATE TABLE did in two steps into one."
    },
    {
      "q": "Once a Delta table is loaded as a DataFrame, which Foundations Chapter 4 methods still work on it?",
      "options": [
        "All of them — select, filter, groupBy, joins, window functions apply exactly as before",
        "Only select() and filter(); groupBy and joins require special Delta-specific syntax",
        "None of them; Delta tables require an entirely separate API",
        "Only SQL queries work, not DataFrame methods"
      ],
      "answer": 0,
      "explain": "Delta changes how data got there and its safety at rest — it doesn't change the DataFrame API itself."
    },
    {
      "q": "What's the DataFrame-API equivalent of SELECT * FROM trips for a registered Delta table?",
      "options": [
        "spark.table(\"trips\")",
        "spark.read.csv(\"trips\")",
        "dbutils.widgets.get(\"trips\")",
        "trips.createOrReplaceTempView()"
      ],
      "answer": 0,
      "explain": "Both spark.table() and spark.sql(\"SELECT * FROM trips\") return the same ordinary DataFrame."
    }
  ]
}
