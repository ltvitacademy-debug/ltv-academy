[
  {"slide": "s1", "text": "Now let's cover the everyday operations — reading and writing Delta tables."},
  {"slide": "s2", "text": "Spark dot table reads a registered table by name, no path or format delta needed — Spark already knows what it is from the metastore. All three ways here return an ordinary DataFrame, and everything from Foundations' Chapter 4 works on it immediately."},
  {"slide": "s3", "text": "Overwrite and append are exactly Foundations Lesson 60's mode pattern, unchanged — they behave the same way they did for plain Parquet. Delta's real edge shows up later, in Lesson 22's merge, which updates and inserts in one atomic operation."},
  {"slide": "s4", "text": "And save as table does two things at once: writes the data as Delta, and registers it under that name in the metastore immediately — the shortest real path from having a DataFrame to having a named table."},
  {"slide": "s5", "text": "Read by path or name, write with overwrite or append, and save as table skips a step. Next lesson: the Delta transaction log, opened up directly."}
]
