[
  {
    "type": "title",
    "eyebrow": "Azure Databricks & Delta Lake · Chapter Two",
    "titlePlain": "Reading and Writing Delta ",
    "titleEmphasis": "Tables",
    "titleSuffix": "",
    "subtitle": "Lesson 16 · Delta Lake"
  },
  {
    "type": "code",
    "eyebrow": "Reading — path or name",
    "code": "# By path\ndf = spark.read.format(\"delta\").load(\"/data/nyc_taxi/delta/trips\")\n\n# By name (registered, Lesson 15)\ndf = spark.table(\"trips\")\n# or: spark.sql(\"SELECT * FROM trips\")",
    "caption": "All three return an ordinary DataFrame -- Foundations' Chapter 4 works on it immediately."
  },
  {
    "type": "code",
    "eyebrow": "Write modes that matter here",
    "code": "trips.write.format(\"delta\").mode(\"overwrite\").save(path)  # replace\ntrips.write.format(\"delta\").mode(\"append\").save(path)     # add rows\n\n# Delta's real edge: Lesson 22's MERGE -- update + insert, atomically",
    "caption": "Exactly Foundations Lesson 60's mode() pattern, unchanged."
  },
  {
    "type": "code",
    "eyebrow": "saveAsTable() — skip the separate step",
    "code": "trips.write.format(\"delta\").saveAsTable(\"trips\")\n\n# writes AND registers, in one call --\n# the shortest path from DataFrame to named table.",
    "caption": "What Lesson 15's write + CREATE TABLE did in two steps, done in one."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 17",
    "titlePlain": "The Delta Transaction ",
    "titleEmphasis": "Log",
    "titleSuffix": "",
    "subtitle": "Opening _delta_log up directly."
  }
]
