{
  "lesson": "creating-a-delta-table",
  "questions": [
    {
      "q": "What does CREATE TABLE trips USING DELTA LOCATION '...' actually do?",
      "options": [
        "Registers \"trips\" as a real, persistent name in the metastore, pointing at that storage path",
        "Creates a temporary view that disappears when the session ends",
        "Deletes the existing data at that location before creating an empty table",
        "Converts the table to CSV format"
      ],
      "answer": 0,
      "explain": "Unlike Foundations' temp views, a registered table persists for good, independent of any notebook or session."
    },
    {
      "q": "Why is a named table preferred for most real production tables over a bare path?",
      "options": [
        "Chapter 4's access control grants and revokes permissions on named catalog objects, not on bare storage paths",
        "Bare paths cannot be read by Spark at all",
        "Named tables use a completely different underlying file format",
        "There is no real difference; the choice is purely aesthetic"
      ],
      "answer": 0,
      "explain": "You can't grant someone access to \"a path\" — only to a real catalog object."
    },
    {
      "q": "What does CONVERT TO DELTA do to an existing Parquet folder?",
      "options": [
        "Adds a _delta_log/ in place, without rewriting the actual Parquet data files",
        "Rewrites every data file into a completely new binary format",
        "Deletes the Parquet files and requires re-uploading the data",
        "Only works if the folder is empty"
      ],
      "answer": 0,
      "explain": "This is the real migration path from \"we already have Parquet\" to \"now it's Delta\" with no full data rewrite."
    },
    {
      "q": "How does reading from a registered Delta table differ from Foundations' temp views (Lesson 58)?",
      "options": [
        "A registered table persists indefinitely; a temp view existed only for the life of the SparkSession that created it",
        "They are functionally identical in every respect",
        "Temp views can be queried with SQL, but registered tables cannot",
        "Registered tables require re-reading the DataFrame every time, unlike temp views"
      ],
      "answer": 0,
      "explain": "SELECT * FROM trips works directly for a registered table, with no spark.read call needed first, in any future session."
    },
    {
      "q": "What's the simplest way to create a Delta table directly from a DataFrame?",
      "options": [
        "trips.write.format(\"delta\").save(path) — the same write pattern from Lesson 14, addressed by storage path",
        "Only through the CREATE TABLE SQL syntax; there's no DataFrame-based way",
        "By calling trips.toDelta() directly",
        "Delta tables cannot be created from a DataFrame at all"
      ],
      "answer": 0,
      "explain": "This works from any notebook that knows the path, with no metastore registration needed."
    }
  ]
}
