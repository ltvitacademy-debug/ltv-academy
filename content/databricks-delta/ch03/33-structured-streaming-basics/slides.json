[
  {
    "type": "title",
    "eyebrow": "Azure Databricks & Delta Lake · Chapter Three",
    "titlePlain": "Structured Streaming ",
    "titleEmphasis": "Basics",
    "titleSuffix": "",
    "subtitle": "Lesson 33 · Medallion Architecture"
  },
  {
    "type": "code",
    "eyebrow": "The core idea: an unbounded table",
    "code": "# A continuously arriving source, treated as a table\n# that just keeps growing -- one MICRO-BATCH at a time.\n#\n# Not a different API. The SAME DataFrame concept,\n# fed continuously instead of once.",
    "caption": "Foundations' Chapter 4 concepts, continuous instead of one-shot."
  },
  {
    "type": "code",
    "eyebrow": "readStream / writeStream — same DataFrame API",
    "code": "stream_df = spark.readStream.format(\"delta\").table(\"bronze.trips\")\n\nfiltered = stream_df.filter(col(\"fare_amount\") > 0)  # unchanged!\n\nfiltered.writeStream.option(\"checkpointLocation\", \"/checkpoints/silver_stream\") \\\n  .trigger(processingTime=\"1 minute\").toTable(\"silver.trips\")",
    "caption": "spark.readStream instead of spark.read -- that's structurally almost the whole difference."
  },
  {
    "type": "code",
    "eyebrow": "Triggers — how often the stream checks",
    "code": ".trigger(processingTime=\"1 minute\")  # forever, on an interval\n.trigger(availableNow=True)           # what's there now, once, then stop\n.trigger(once=True)                   # older syntax, same idea",
    "caption": "availableNow is what makes streaming syntax fit a scheduled job (Lesson 11)."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 34",
    "titlePlain": "Streaming from Bronze to ",
    "titleEmphasis": "Silver",
    "titleSuffix": "",
    "subtitle": "Putting this to real use."
  }
]
