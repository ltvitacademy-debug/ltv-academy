[
  {
    "type": "title",
    "eyebrow": "Azure Databricks & Delta Lake · Chapter Three",
    "titlePlain": "The Silver ",
    "titleEmphasis": "Layer",
    "titleSuffix": "",
    "subtitle": "Lesson 27 · Medallion Architecture"
  },
  {
    "type": "code",
    "eyebrow": "Silver is Foundations' Chapter 4, applied",
    "code": "bronze_trips = spark.table(\"bronze.trips\")\n\nsilver_trips = (\n  bronze_trips\n  .withColumn(\"fare_amount\", col(\"fare_amount\").cast(\"double\"))\n  .dropDuplicates([\"VendorID\", \"tpep_pickup_datetime\"])\n  .na.drop(subset=[\"fare_amount\"])\n)\n\nsilver_trips.write.format(\"delta\").mode(\"overwrite\").saveAsTable(\"silver.trips\")",
    "caption": "No new syntax -- casting, dedup, null handling, exactly as in Foundations."
  },
  {
    "type": "code",
    "eyebrow": "Order matters",
    "code": "# Cast FIRST -- so later steps see real types, not strings.\n#\n# dropDuplicates on string \"14.50\" vs double 14.5 --\n# different types, treated as different values.",
    "caption": "Cast, then dedupe, then handle nulls -- in that order, every time."
  },
  {
    "type": "code",
    "eyebrow": "\"Conformed\" — beyond just clean",
    "code": "# Source A: fare_amt\n# Source B: fare_amount\n#\n# Silver reconciles these into ONE consistent name --\n# gold never has to know two sources were involved.",
    "caption": "Consistent naming, units, and categorical values -- across sources."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 28",
    "titlePlain": "The Gold ",
    "titleEmphasis": "Layer",
    "titleSuffix": "",
    "subtitle": "Business-level aggregates."
  }
]
