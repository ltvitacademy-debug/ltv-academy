[
  {
    "type": "title",
    "eyebrow": "Azure Databricks & Delta Lake · Chapter Three",
    "titlePlain": "Designing a Silver-to-Gold ",
    "titleEmphasis": "Pipeline",
    "titleSuffix": "",
    "subtitle": "Lesson 30 · Medallion Architecture"
  },
  {
    "type": "code",
    "eyebrow": "Simpler than you might expect",
    "code": "# Bronze -> silver: real messiness -- casting, dedup, nulls.\n#\n# Silver -> gold: data is ALREADY clean by now.\n# Its job is aggregation, not cleaning.\n# Fewer steps here is expected, not a red flag.",
    "caption": "groupBy()/.agg() (Lesson 28), applied to already-trustworthy data."
  },
  {
    "type": "code",
    "eyebrow": "overwrite — the right default for gold",
    "code": "gold_daily_revenue = spark.table(\"silver.trips\").groupBy(\n  \"VendorID\", to_date(\"tpep_pickup_datetime\").alias(\"trip_date\")\n).agg(sum(\"fare_amount\").alias(\"total_revenue\"))\n\ngold_daily_revenue.write.format(\"delta\").mode(\"overwrite\").saveAsTable(\"gold.daily_revenue\")",
    "caption": "Small enough to fully recompute each run -- simpler than MERGE, just as correct."
  },
  {
    "type": "code",
    "eyebrow": "Chaining both into one job",
    "code": "Job: daily_medallion_pipeline\n  Task 1: bronze_to_silver  (Lesson 29)\n  Task 2: silver_to_gold    (this lesson) -- depends on Task 1",
    "caption": "Task 2 never starts until Task 1 finishes -- gold never reads silver mid-update."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 31",
    "titlePlain": "Incremental Processing ",
    "titleEmphasis": "Patterns",
    "titleSuffix": "",
    "subtitle": "Beyond reprocessing everything, every time."
  }
]
