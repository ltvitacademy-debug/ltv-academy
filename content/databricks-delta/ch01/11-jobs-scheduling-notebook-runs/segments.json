[
  {"slide": "s1", "text": "Every run so far has needed a person clicking Run — let's fix that. Jobs, for scheduling notebook runs."},
  {"slide": "s2", "text": "Creating a job is four steps: create the job, add a task — a notebook plus a cluster, usually a fresh job cluster — set a schedule, and save. From there it runs itself, on schedule, with nobody watching."},
  {"slide": "s3", "text": "And a job isn't limited to one notebook. Multiple tasks can chain in sequence, each depending on the one before succeeding — that's the real mechanism behind a bronze, silver, gold pipeline, exactly what this course's Chapter 3 builds."},
  {"slide": "s4", "text": "Remember Lesson 9's widgets — a job's task can supply a value for that exact widget at run time. The same notebook, unattended, processes a different month every time, with the job filling in what a human would otherwise type."},
  {"slide": "s5", "text": "And every run shows up in the job's run history — succeeded, failed, or still running, with the real output and any error. That's the first place to check after a scheduled run. Next lesson: the CLI and REST API, controlling all of this without touching the UI at all."}
]
