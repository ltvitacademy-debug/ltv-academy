{
  "lesson": "expectations-declarative-data-quality",
  "questions": [
    {
      "q": "What does @dlt.expect(\"valid_fare\", \"fare_amount > 0\") declare?",
      "options": [
        "A named data quality rule and a SQL condition rows must satisfy, with violations logged but rows kept by default",
        "A rule that immediately deletes any row failing the condition",
        "A rule that stops the entire pipeline on any violation",
        "A rule with no actual enforcement, purely decorative"
      ],
      "answer": 0,
      "explain": "The default expect behavior is WARN — keeps the row, just logs the violation count."
    },
    {
      "q": "How does @dlt.expect_or_drop relate to Lesson 36's quarantine pattern?",
      "options": [
        "It's Lesson 36's quarantine behavior directly — bad rows never reach the table, just declared in one line instead of manual filter() calls",
        "It has no relationship to quarantine at all",
        "It only warns about bad rows, never actually removing them",
        "It requires a separate quarantine table to be created manually first"
      ],
      "answer": 0,
      "explain": "This replaces the manual valid_rows/quarantined_rows split from Lesson 36 with one decorator."
    },
    {
      "q": "What real difference in outcome separates expect_or_drop from expect_or_fail?",
      "options": [
        "expect_or_drop filters out only the violating rows; expect_or_fail stops the entire pipeline run on a single violation",
        "They behave identically in every situation",
        "expect_or_fail only logs a warning, same as plain expect",
        "expect_or_drop stops the pipeline, while expect_or_fail just drops rows"
      ],
      "answer": 0,
      "explain": "expect_or_fail is for a condition so fundamental that continuing at all would be worse than stopping."
    },
    {
      "q": "How does using @dlt.expect* decorators compare to Lesson 36's manual approach, in terms of code required?",
      "options": [
        "Genuinely less code — three decorators replace manual filter() calls, a separate quarantine table write, and a separate quality-metrics query",
        "It requires writing more code overall to achieve the same outcome",
        "It requires exactly the same amount of code, just reorganized",
        "Expectations cannot replace any of Lesson 36's manual logic"
      ],
      "answer": 0,
      "explain": "The framework tracks pass/fail counts and handles drop/fail behavior automatically, from one-line declarations."
    },
    {
      "q": "Where do expectation pass/fail counts appear once a pipeline runs?",
      "options": [
        "Automatically in Lakeflow's pipeline UI, with zero custom query required",
        "Only if a separate gold.data_quality_metrics table is manually built, exactly like Lesson 36",
        "They are never surfaced anywhere; expectations are silent",
        "Only in the Databricks CLI, never the UI"
      ],
      "answer": 0,
      "explain": "This is the same kind of metric Lesson 36 tracked by hand, now built into the platform itself."
    }
  ]
}
