{
  "lesson": "monitoring-pipeline-runs",
  "questions": [
    {
      "q": "What does the pipeline graph view show?",
      "options": [
        "Each @dlt.table/@dlt.view as a node, with arrows for the dependencies inferred from dlt.read()/dlt.read_stream() calls, updated live per run",
        "Only the final gold table, with no upstream detail",
        "A static diagram that never updates after the pipeline is first defined",
        "Only error messages, with no structural information"
      ],
      "answer": 0,
      "explain": "This is the exact bronze->silver->gold structure from Lesson 44's lineage diagram, scoped to one pipeline's execution."
    },
    {
      "q": "What do per-table metrics in the pipeline UI combine?",
      "options": [
        "Rows processed this run, plus expectation pass/fail counts (Lesson 52) for that table, shown together in one place",
        "Only row counts, with no relationship to expectations",
        "Only Unity Catalog access grants for that table",
        "Only the table's storage size on disk"
      ],
      "answer": 0,
      "explain": "This combines what Lesson 36's separate quality table and Lesson 11's job run history tracked separately, now together."
    },
    {
      "q": "Why is the event log described as \"nothing exotic\" to query?",
      "options": [
        "It's a real, queryable Delta table — exactly Chapter 2's material — so it can be queried with plain SQL like any other table",
        "It requires a completely new, specialized query language",
        "It can only be viewed through the UI, never queried directly",
        "It's stored in a format unrelated to Delta Lake"
      ],
      "answer": 0,
      "explain": "This turns \"did something break, and where\" into a real SQL query, filterable and joinable like any other table."
    },
    {
      "q": "What does the event log record?",
      "options": [
        "Every event across every run — which tables updated, which expectations failed, which run raised an error, and when",
        "Only successful runs, never failures",
        "Only the pipeline's initial creation, not ongoing runs",
        "Only Unity Catalog grant/revoke actions"
      ],
      "answer": 0,
      "explain": "This is a real, queryable Delta table, not just a UI-only log."
    },
    {
      "q": "What's the recommended order for investigating a pipeline failure?",
      "options": [
        "Graph view first (which table failed), then per-table metrics (did expectations catch it?), then the event log (the actual error message)",
        "Always start by querying the event log with no other context",
        "Only check Unity Catalog permissions, regardless of the failure type",
        "There's no recommended order; any starting point works equally well"
      ],
      "answer": 0,
      "explain": "This ordered approach is the practical answer to \"what broke and where\" after a failure has already occurred."
    }
  ]
}
