{
  "lesson": "installing-the-tools",
  "questions": [
    {
      "q": "How does Terraform get installed on your machine?",
      "options": [
        "As a single standalone binary, installable via a package manager like winget, Homebrew, or apt",
        "It comes pre-installed inside every version of the Azure CLI",
        "It requires a dedicated Java runtime to be installed first",
        "It can only be built from source code"
      ],
      "answer": 0,
      "explain": "Terraform ships as one executable with no separate runtime dependency, though package managers make installing and updating it easier."
    },
    {
      "q": "How is Bicep installed?",
      "options": [
        "As a component of the Azure CLI, via `az bicep install`",
        "As a standalone binary with no other dependencies",
        "It must be compiled manually from the ARM template schema",
        "Through the same installer as Terraform"
      ],
      "answer": 0,
      "explain": "Bicep isn't a separate download — it installs as an Azure CLI component, which is why the Azure CLI has to be installed first."
    },
    {
      "q": "What command verifies the Bicep CLI's own version, separate from the Azure CLI's version?",
      "options": [
        "az bicep version",
        "terraform version",
        "az login",
        "bicep --check"
      ],
      "answer": 0,
      "explain": "az bicep version checks Bicep specifically, since it updates on its own release cadence independent of the Azure CLI."
    },
    {
      "q": "Why do both Terraform and Bicep depend on `az login`?",
      "options": [
        "Both tools ultimately deploy into Azure, and authentication against your Azure account goes through the Azure CLI",
        "Terraform requires az login but Bicep does not",
        "az login is only needed for the Bicep CLI's own installation step, never afterward",
        "Neither tool actually needs any authentication to create real resources"
      ],
      "answer": 0,
      "explain": "Whichever tool issues the API calls, both need an authenticated session against your Azure subscription, which az login provides."
    },
    {
      "q": "Why should you run `az account show` before provisioning anything?",
      "options": [
        "To confirm which Azure subscription is currently active, so you don't provision a resource into the wrong one (e.g. production instead of dev)",
        "Because it installs the Bicep CLI as a side effect",
        "Because it is required before installing Terraform",
        "Because it permanently deletes unused subscriptions"
      ],
      "answer": 0,
      "explain": "az account show reports the currently active subscription — a quick check that prevents provisioning into the wrong environment."
    }
  ]
}
