{
  "lesson": "providers",
  "questions": [
    {
      "q": "What is a Terraform provider?",
      "options": [
        "The plugin that translates generic Terraform resource blocks into real API calls against a specific platform",
        "A separate CLI tool that replaces Terraform entirely",
        "A file that stores Terraform's state",
        "A cloud region setting with no other function"
      ],
      "answer": 0,
      "explain": "Terraform's core engine is platform-agnostic; a provider like azurerm is what actually knows how to call a given platform's API."
    },
    {
      "q": "Which provider does this course use to provision Azure resources?",
      "options": [
        "azurerm",
        "aws",
        "bicep",
        "google"
      ],
      "answer": 0,
      "explain": "azurerm is the Terraform provider for Azure, maintained by HashiCorp in partnership with Microsoft."
    },
    {
      "q": "What does the version constraint `~> 3.80` allow?",
      "options": [
        "Any 3.80.x or later 3.x release, but not the next breaking major version like 4.0",
        "Only the exact version 3.80.0 and nothing else",
        "Any version at all, including future major releases",
        "Only versions older than 3.80"
      ],
      "answer": 0,
      "explain": "The ~> operator allows minor and patch updates within the specified major version while blocking the next major, breaking release."
    },
    {
      "q": "What does `terraform init` do with a `required_providers` block?",
      "options": [
        "Downloads a provider version matching the constraint and records the exact resolved version in a lock file",
        "Deletes the provider block from the configuration",
        "Immediately provisions all resources in the configuration",
        "Deploys the configuration directly to Bicep instead"
      ],
      "answer": 0,
      "explain": "terraform init resolves and installs the provider, then writes the exact version to .terraform.lock.hcl so it's reproducible."
    },
    {
      "q": "Why should `.terraform.lock.hcl` be committed to Git?",
      "options": [
        "So every machine that applies the configuration -- yours, a teammate's, CI -- resolves to the identical provider version",
        "Because it contains the Azure subscription's password",
        "Because Terraform refuses to run without it being committed",
        "It should never be committed, only kept locally"
      ],
      "answer": 0,
      "explain": "Committing the lock file prevents a teammate's unconstrained init from silently resolving to a different, possibly incompatible provider version."
    }
  ]
}
