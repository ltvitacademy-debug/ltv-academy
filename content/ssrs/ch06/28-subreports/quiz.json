{
  "lesson": "subreports",
  "questions": [
    {
      "q": "What is a subreport?",
      "options": [
        "A reference to another report, embedded inside a main report so the main report acts as a container",
        "A toggle that expands or collapses detail rows within the same report",
        "A separate report opened only when a user clicks a link",
        "A saved snapshot of a report's data taken on a schedule"
      ],
      "answer": 0,
      "explain": "A subreport embeds an existing report inside a main report, which then acts as a container for multiple related reports."
    },
    {
      "q": "When does a subreport's data actually get retrieved?",
      "options": [
        "At the same time as the main report's data, whether or not anyone looks at it closely",
        "Only when a user clicks the subreport area",
        "Only after the main report has been exported to PDF",
        "Never — subreports reuse the main report's own dataset"
      ],
      "answer": 0,
      "explain": "Unlike a drillthrough report, a subreport's data is retrieved at the same time as the main report's, regardless of whether the user ever pays attention to it."
    },
    {
      "q": "On the Subreport Properties dialog's Parameters tab, what must the Name in each row match?",
      "options": [
        "A report parameter defined in the subreport (not a query parameter)",
        "A column name in the main report's dataset",
        "The subreport's file name",
        "A query parameter used inside the subreport's dataset query"
      ],
      "answer": 0,
      "explain": "The Name must match a report parameter in the subreport, not a query parameter — Value can then be a static value or an expression from the main report's fields."
    },
    {
      "q": "In Report Designer (SSDT), what happens if a subreport's required parameter is missing from the Parameters list?",
      "options": [
        "The subreport isn't displayed correctly in the main report",
        "Report Designer automatically supplies a default value with no issue",
        "The main report fails to compile entirely, even before running",
        "It has no effect, since Report Designer ignores subreport parameters"
      ],
      "answer": 0,
      "explain": "In Report Designer, all parameters required by the subreport must be included in the Parameters list. A missing required parameter means the subreport won't display correctly — unlike Report Builder, where a default value covers the gap."
    },
    {
      "q": "What should you consider before adding a subreport to a dynamic row or column of a table or matrix?",
      "options": [
        "Whether a nested data region could achieve the same result without a separate report execution per row",
        "Whether the subreport supports being exported to CSV",
        "Whether the main report has an even number of columns",
        "Whether the subreport's RDL file is smaller than 1 MB"
      ],
      "answer": 0,
      "explain": "A subreport in a dynamic row runs once per row, which gets expensive on tables with many rows. A nested data region can often achieve the same layout without that per-row report execution cost."
    }
  ]
}
