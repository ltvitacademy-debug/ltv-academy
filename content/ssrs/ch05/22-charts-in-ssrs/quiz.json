{
  "lesson": "charts-in-ssrs",
  "questions": [
    {
      "q": "What are the three drop zones in the Chart Data pane?",
      "options": [
        "Category Groups, Series Groups, and Values",
        "Rows, Columns, and Filters",
        "Title, Legend, and Axis",
        "Dataset, Parameter, and Expression"
      ],
      "answer": 0,
      "explain": "Once a Chart data region is selected, the Chart Data pane shows Category Groups, Series Groups, and Values — dragging dataset fields into these areas is what actually builds the chart."
    },
    {
      "q": "What happens if the Series Groups area is left empty?",
      "options": [
        "The chart shows exactly one series, fixed at design time",
        "The chart fails to render",
        "The chart automatically creates one series per category",
        "The Values area is disabled"
      ],
      "answer": 0,
      "explain": "With Series Groups empty, the number of series is fixed at design time — there's only ever one series until you add a field there."
    },
    {
      "q": "How is a Chart data region organized, conceptually?",
      "options": [
        "Like a matrix — Category Groups maps to columns, Series Groups to rows, Values to the data area",
        "Like a list, with no grouping concept at all",
        "Like a stored procedure, executing top to bottom",
        "It has no analog to any other data region"
      ],
      "answer": 0,
      "explain": "A chart is organized like a matrix underneath: Category Groups is like the matrix's Columns group, Series Groups is like its Rows group, and Values is like its Data area."
    },
    {
      "q": "What does dropping a numeric field into the Values area do by default?",
      "options": [
        "Report Builder aggregates it using the SUM function",
        "The field is displayed unaggregated, row by row",
        "It's converted to a string automatically",
        "It's ignored until a Category Group is added"
      ],
      "answer": 0,
      "explain": "By default, a numeric field dropped into Values is aggregated with SUM (non-numeric fields default to COUNT instead, since there's nothing to sum)."
    },
    {
      "q": "Which chart types does the New Chart Wizard offer?",
      "options": [
        "Column, line, pie, bar, and area",
        "Only column and pie",
        "Every chart type and sub-type Reporting Services supports",
        "Scatter, polar, and stock charts only"
      ],
      "answer": 0,
      "explain": "The Chart Wizard covers the five most common chart types — column, line, pie, bar, and area. For anything else, such as scatter or polar charts, you add a chart manually and choose from the full Select Chart Type dialog."
    }
  ]
}
