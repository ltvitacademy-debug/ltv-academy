{
  "lesson": "report-parameters",
  "questions": [
    {
      "q": "What is a report parameter?",
      "options": [
        "A named input a report reader supplies to control what the report shows, appearing on the report viewer toolbar",
        "A column in the underlying database table",
        "A setting that only exists on the report server, never in the report itself",
        "A type of chart used to visualize filtered data"
      ],
      "answer": 0,
      "explain": "A report parameter is the reader-facing input — a store, date range, or region — that shows up as a prompt on the report viewer toolbar."
    },
    {
      "q": "What happens automatically when you add a query variable like @StoreID to a dataset's query text?",
      "options": [
        "Report Builder creates a matching dataset parameter and report parameter with the same name",
        "Nothing — you must manually create both by hand every time",
        "The report deletes all existing parameters",
        "The query stops running until you publish the report"
      ],
      "answer": 0,
      "explain": "A query variable inside a dataset's query text automatically creates a matching dataset parameter and a report parameter to go with it — you don't have to build either by hand."
    },
    {
      "q": "Where do all of a report's parameters live in the design environment?",
      "options": [
        "Under the Parameters node in the Report Data pane",
        "Inside the Data Sources dialog only",
        "In a separate file outside the report entirely",
        "Only visible after the report is published to a Report Server"
      ],
      "answer": 0,
      "explain": "Every report parameter — automatic or manually added — appears under the Parameters node in the Report Data pane, alongside Built-in Fields, Data Sources, and Datasets."
    },
    {
      "q": "A query variable auto-creates a report parameter with data type Text, but the underlying column is really numeric. What's the typical fix?",
      "options": [
        "Open Report Parameter Properties and change the Data type to Integer (or the appropriate numeric type)",
        "Delete the parameter and rebuild the entire dataset from scratch",
        "Leave it as Text — data type never affects report behavior",
        "Rename the query variable to fix the data type automatically"
      ],
      "answer": 0,
      "explain": "Changing the Data type on the General tab of Report Parameter Properties is one of the most common first edits after Report Builder auto-creates a parameter as Text."
    },
    {
      "q": "Why would you set a parameter to Hidden instead of Visible?",
      "options": [
        "When its value is supplied by something else, like a drillthrough from another report, rather than typed by a person",
        "Hidden parameters run faster than visible ones",
        "Hidden is required for every parameter with a Boolean data type",
        "Visible parameters cannot have default values"
      ],
      "answer": 0,
      "explain": "Hidden parameters are common when a value arrives from elsewhere — like a drillthrough action passing a value — so there's no need to prompt a person for it on the toolbar."
    }
  ]
}
