[
  {
    "type": "title",
    "eyebrow": "Advanced Databricks Specialization · Chapter Three",
    "titlePlain": "Declarative Pipeline ",
    "titleEmphasis": "Syntax",
    "titleSuffix": "",
    "subtitle": "Lesson 13 · Lakeflow & Declarative Pipelines"
  },
  {
    "type": "code",
    "eyebrow": "A streaming table, in SQL",
    "code": "CREATE OR REFRESH STREAMING TABLE bronze_trips\nAS SELECT * FROM STREAM(\n  cloud_files('/Volumes/nyc_taxi/bronze/raw_files/', 'csv')\n);",
    "caption": "Incremental, row-by-row processing — the SQL analog of Auto Loader."
  },
  {
    "type": "code",
    "eyebrow": "A materialized view + its inferred dependency",
    "code": "CREATE MATERIALIZED VIEW gold_daily_revenue\nAS SELECT VendorID, sum(fare_amount) AS total_revenue\nFROM bronze_trips\nGROUP BY VendorID;",
    "caption": "\"FROM bronze_trips\" is the entire dependency declaration."
  },
  {
    "type": "code",
    "eyebrow": "The same two tables, in Python",
    "code": "from pyspark import pipelines as dp\n\n@dp.table()\ndef bronze_trips():\n    return spark.readStream.format(\"cloudFiles\")...\n\n@dp.materialized_view()\ndef gold_daily_revenue():\n    return spark.read.table(\"bronze_trips\").groupBy(\"VendorID\")...",
    "caption": "spark.read.table() — no special dlt.read() call needed anymore."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 14",
    "titlePlain": "Lakeflow vs. Traditional ",
    "titleEmphasis": "Notebooks",
    "titleSuffix": "",
    "subtitle": "The honest trade-off, both directions."
  }
]
