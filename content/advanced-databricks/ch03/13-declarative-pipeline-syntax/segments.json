[
  {"slide": "s1", "text": "Lesson 12 named streaming tables and materialized views. Here's what they actually look like in real SQL and Python syntax, and how their dependencies get inferred without anyone declaring them."},
  {"slide": "s2", "text": "A streaming table processes each row exactly once, incrementally, as it arrives — the SQL analog of Auto Loader. CREATE OR REFRESH STREAMING TABLE, reading from a cloud_files source, is the real syntax for it."},
  {"slide": "s3", "text": "A materialized view recomputes its full result on refresh. Its FROM bronze_trips reference is the entire dependency declaration — Lakeflow builds the execution graph from exactly that."},
  {"slide": "s4", "text": "pyspark.pipelines mirrors this exactly — spark.read.table and spark.readStream.table are all it takes to reference another pipeline table, no special dlt.read() call required anymore."},
  {"slide": "s5", "text": "Same engine, two authoring surfaces — SQL and Python, both inferring the same dependency graph. Next up: the honest trade-off against a traditional notebook pipeline."}
]
