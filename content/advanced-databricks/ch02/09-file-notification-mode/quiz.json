{
  "lesson": "file-notification-mode",
  "questions": [
    {
      "q": "What mode were all of this course's earlier Auto Loader examples actually using, without naming it?",
      "options": [
        "Directory listing mode — periodically listing the directory and diffing against its own state",
        "File notification mode using Event Grid and a queue",
        "A manual watermark-based batch pattern",
        "A mode that requires no state tracking at all"
      ],
      "answer": 0,
      "explain": "Every earlier example ran in directory listing mode by default, without ever needing to name it at modest scale."
    },
    {
      "q": "Why does directory listing's cost keep growing over time even when new data per run stays small?",
      "options": [
        "Listing cost scales with how many objects and prefixes exist under the path, not just how many are new",
        "Listing mode charges a flat fee regardless of directory size",
        "Directory listing mode is not affected by directory size at all",
        "Listing cost only depends on file notification settings"
      ],
      "answer": 0,
      "explain": "A deep, continuously growing tree means every trigger re-lists a mostly-unchanged structure, which is a real, scaling API cost."
    },
    {
      "q": "What does cloudFiles.useNotifications set to true actually change?",
      "options": [
        "Auto Loader reads new-file notices from a cloud-native event queue instead of periodically listing the directory",
        "It disables schema evolution entirely",
        "It converts the stream from streaming to batch processing",
        "It removes the need for a checkpointLocation"
      ],
      "answer": 0,
      "explain": "File notification mode relies on cloud storage events (e.g. Event Grid + a queue) pushing notices, instead of Auto Loader listing."
    },
    {
      "q": "According to this lesson, when is file notification mode's setup cost actually worth paying?",
      "options": [
        "For very large or deep directory trees, high-frequency file arrival, or low-latency requirements",
        "It's always worth it, even for a directory with ten files",
        "Only when running on a single-node cluster",
        "Never — directory listing mode is always the better choice"
      ],
      "answer": 0,
      "explain": "Notification mode trades setup cost for eliminating re-listing cost, which pays off specifically at large scale or high arrival rate."
    },
    {
      "q": "What one-time setup does file notification mode require that directory listing mode never needed?",
      "options": [
        "Permission to create a cloud event subscription and a queue (e.g. Event Grid + Queue Storage on Azure)",
        "A dedicated GPU-enabled cluster",
        "A separate Unity Catalog metastore",
        "Manually writing the entire table schema by hand"
      ],
      "answer": 0,
      "explain": "Notification mode needs infrastructure permissions to create the event subscription and queue that listing mode doesn't require."
    }
  ]
}
