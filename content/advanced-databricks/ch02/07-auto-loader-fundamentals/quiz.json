{
  "lesson": "auto-loader-fundamentals",
  "questions": [
    {
      "q": "What actually backs Auto Loader's record of 'already processed' files at scale?",
      "options": [
        "A real embedded key-value store (RocksDB) inside the checkpoint, not a flat list",
        "A single text file with one filename per line",
        "A separate SQL database the user must create manually",
        "Nothing is tracked — every run rescans the entire directory"
      ],
      "answer": 0,
      "explain": "A flat list of every filename ever seen would itself become slow to scan; Auto Loader uses a real state store to stay fast at scale."
    },
    {
      "q": "What do maxFilesPerTrigger and maxBytesPerTrigger control?",
      "options": [
        "How much a single micro-batch takes on, useful for controlling backlog catch-up",
        "The maximum number of columns a schema can have",
        "How often the checkpoint location is backed up",
        "The total number of streams that can run on a cluster"
      ],
      "answer": 0,
      "explain": "These options cap the size of an individual micro-batch, especially important when a directory has a large existing backlog."
    },
    {
      "q": "What does cloudFiles.includeExistingFiles control?",
      "options": [
        "Whether a newly started stream processes a directory's existing files or skips straight to only new ones",
        "Whether Auto Loader deletes files after processing them",
        "Whether schema inference runs at all",
        "Whether the checkpoint location is stored in a volume"
      ],
      "answer": 0,
      "explain": "Setting it to false is the real switch for skipping a directory's historical backlog when starting a brand-new stream."
    },
    {
      "q": "According to this lesson, what did Databricks & Delta Lake Lesson 32 already cover?",
      "options": [
        "The core cloudFiles syntax and checkpointLocation, at the level a small example needed",
        "The RocksDB-backed internal state store mechanism",
        "maxFilesPerTrigger and maxBytesPerTrigger throughput controls",
        "cloudFiles.includeExistingFiles for backfilling"
      ],
      "answer": 0,
      "explain": "This lesson recaps Lesson 32's core syntax before covering the internals and options that example never needed to mention."
    },
    {
      "q": "Why would a naive flat list of processed filenames eventually cause a performance problem?",
      "options": [
        "The list itself grows large enough that scanning it becomes slow, defeating the purpose of avoiding a full directory rescan",
        "Flat lists are not supported by any file system",
        "It would cause Auto Loader to delete files by accident",
        "It has no real downside at any scale"
      ],
      "answer": 0,
      "explain": "At millions of files, checking a flat list for membership becomes its own bottleneck — a real key-value store avoids that."
    }
  ]
}
