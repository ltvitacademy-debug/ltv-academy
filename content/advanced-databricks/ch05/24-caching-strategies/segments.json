[
  {"slide": "s1", "text": "If you've seen caching as a system-design topic before — result sets, materialized views, invalidation — this is one level down. These are the two caching mechanisms the Databricks engine itself gives you underneath any of that."},
  {"slide": "s2", "text": "The disk cache is automatic and transparent. The first time a cluster reads a file from cloud storage, it stores a local copy on that worker's SSD, keyed by file — every later read of that same file comes from local disk instead."},
  {"slide": "s3", "text": ".cache() is explicit and keyed by DataFrame, not by file. It materializes the result of a specific computation in cluster memory for the life of that session — nobody does this for you, and nobody releases it for you either."},
  {"slide": "s4", "text": ".cache() wastes real memory when the DataFrame is read only once, when the underlying data changes and the cache goes stale, or when it's a job cluster that terminates right after this one run."},
  {"slide": "s5", "text": "The question every time: will this exact result get read again before something invalidates it? If not, caching it is pure overhead. Next up: sizing a cluster for the workload's actual shape."}
]
