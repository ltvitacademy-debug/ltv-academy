{
  "lesson": "row-and-column-level-security",
  "questions": [
    {
      "q": "What did Databricks & Delta Lake Lesson 43 already cover, which this lesson builds on rather than repeats?",
      "options": [
        "Column masks and row filters, with one worked example each on a single table",
        "Attribute-based access control policies applied by tag across an entire catalog",
        "The difference between is_member() and is_account_group_member()",
        "Delta Sharing's open protocol for external recipients"
      ],
      "answer": 0,
      "explain": "Lesson 43 covered the mechanism itself with one function and one ALTER TABLE; this lesson covers what changes once that scales past one table."
    },
    {
      "q": "Why does is_account_group_member() behave more reliably than is_member() when a catalog is attached to multiple workspaces?",
      "options": [
        "is_account_group_member() checks account-level group membership, consistent across every attached workspace, while is_member() checks a workspace-local group",
        "is_member() only works with row filters, never with column masks",
        "is_account_group_member() is faster to execute at query time",
        "There is no real difference between the two functions"
      ],
      "answer": 0,
      "explain": "A workspace-local group check can resolve differently depending on which attached workspace the query runs from — an account-level check does not."
    },
    {
      "q": "What real production problem does writing a separate ALTER TABLE ... SET MASK for every PII column create at scale?",
      "options": [
        "Masks quietly drift out of sync — some tables get updated when policy changes, others are forgotten",
        "It is technically impossible to apply more than one mask per catalog",
        "It automatically applies the mask to every future table without any further action",
        "It has no downside at any scale"
      ],
      "answer": 0,
      "explain": "Manually updating dozens of tables one statement at a time is exactly how protection coverage becomes inconsistent over time."
    },
    {
      "q": "What does an attribute-based access control (ABAC) policy tie protection to, instead of a specific column's name?",
      "options": [
        "A tag or attribute the column carries — so the policy applies to every column with that tag, present or future",
        "The physical cloud region where the table's files are stored",
        "The specific user who created the table",
        "The number of rows currently in the table"
      ],
      "answer": 0,
      "explain": "A tag-based masking policy automatically covers any column carrying the tag, including ones added after the policy was written."
    },
    {
      "q": "Why is a tag-based masking policy considered a genuine improvement over Lesson 43's single-table pattern, rather than just a restatement of it?",
      "options": [
        "It protects every column classified with a given tag across the whole catalog, instead of requiring a manually repeated statement per table",
        "It removes the need for any function to be evaluated at query time",
        "It only works for row filters, not column masks",
        "It requires disabling Unity Catalog entirely"
      ],
      "answer": 0,
      "explain": "The tag-based approach shifts protection from a per-object list someone maintains by hand to a property of the data's own classification."
    }
  ]
}
