{
  "lesson": "catalogs-schemas-external-locations",
  "questions": [
    {
      "q": "What is an external location, as its own governed object?",
      "options": [
        "A specific cloud path bound to a storage credential, with its own GRANT/REVOKE permissions",
        "A synonym for a catalog's MANAGED LOCATION",
        "A folder inside DBFS root with no governance at all",
        "A type of cluster used only for external data sources"
      ],
      "answer": 0,
      "explain": "An external location binds a cloud path to a credential and is itself a securable object, granted and revoked exactly like a table."
    },
    {
      "q": "What does a storage credential actually wrap?",
      "options": [
        "A real cloud identity, such as a managed identity or service principal",
        "A user's personal Databricks login password",
        "A cached copy of table data for faster reads",
        "The schema of every table in a catalog"
      ],
      "answer": 0,
      "explain": "A storage credential wraps a real cloud identity so Unity Catalog can authenticate to storage without exposing keys to users."
    },
    {
      "q": "Why do users get GRANTs on the external location instead of the storage credential?",
      "options": [
        "The credential is infrastructure — users should never see or touch access keys directly",
        "Storage credentials cannot be granted to anyone under any circumstances",
        "External locations and storage credentials are actually the same object",
        "Because catalogs no longer support any form of permission"
      ],
      "answer": 0,
      "explain": "Credentials are infrastructure; day-to-day permissions belong on the external location, keeping keys out of users' hands entirely."
    },
    {
      "q": "According to this lesson, what did Databricks & Delta Lake Lessons 39-40 already cover in full?",
      "options": [
        "The catalog.schema.table namespace and a catalog's own MANAGED LOCATION",
        "External locations and storage credentials",
        "Auto Loader's file notification mode",
        "Row-level and column-level security"
      ],
      "answer": 0,
      "explain": "This lesson explicitly recaps that material rather than re-teaching it, then covers what those lessons left out."
    },
    {
      "q": "Can an external table or external volume point at any cloud path, with no other setup?",
      "options": [
        "No — the path must already have an external location registered over it",
        "Yes — external tables can point at any unregistered path freely",
        "Only if the path is inside the catalog's own managed location",
        "Only for tables, never for volumes"
      ],
      "answer": 0,
      "explain": "Unity Catalog requires the path to sit under a registered external location before an external table or volume can reference it."
    }
  ]
}
