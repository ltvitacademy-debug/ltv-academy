{
  "lesson": "unit-testing-pyspark-transformations",
  "questions": [
    {
      "q": "Why is clean_trips(df) easier to unit test than process_trips(), which reads a file and transforms it in one function?",
      "options": [
        "A function taking a DataFrame in and returning one out can be handed exactly the test data you want, without needing a real file to read",
        "clean_trips(df) doesn't actually perform any real transformation logic",
        "process_trips() is impossible to write in PySpark",
        "There's no real testing difference between the two approaches"
      ],
      "answer": 0,
      "explain": "You can't easily unit-test \"reads a file and transforms it\" without a real file, but you can easily hand a function a specific DataFrame."
    },
    {
      "q": "Why use master(\"local[1]\") when creating a SparkSession for tests?",
      "options": [
        "It runs Spark on a single local thread with no Fabric workspace, real cluster, or network call — fast enough to run dozens of times per commit",
        "It's required for any PySpark code to run at all",
        "It connects directly to the production KQL Database",
        "It disables all DataFrame transformations for safety"
      ],
      "answer": 0,
      "explain": "This speed is what makes unit tests practical as part of Lesson 45's CI/CD Test stage."
    },
    {
      "q": "Why can naively comparing two DataFrames row-by-row via collect() produce a flaky test?",
      "options": [
        "Spark distributes work across partitions with no guaranteed row order, so identical data can come back in a different order each time",
        "collect() always returns an empty result",
        "DataFrames can never be compared in any way",
        "Flakiness in this context is always a sign of a real bug, not a test problem"
      ],
      "answer": 0,
      "explain": "Sorting both results by a stable key before comparing avoids this false-failure problem entirely."
    },
    {
      "q": "What does a unit test on a single transformation catch that a full pipeline test (Lesson 48) wouldn't pinpoint as easily?",
      "options": [
        "Exactly which specific transformation broke, without needing to debug an entire pipeline run to find it",
        "Nothing — full pipeline tests and unit tests catch exactly the same things",
        "Only schema drift, never a genuine logic bug",
        "Only row-count problems, never logic errors"
      ],
      "answer": 0,
      "explain": "A full pipeline test tells you the whole thing worked or didn't; a unit test isolates exactly where a failure occurred."
    },
    {
      "q": "What DataFrame operations from DE Foundations Lessons 44-45 does this lesson's testable transformation shape build on?",
      "options": [
        "select() and filter()/where()",
        "join() and groupBy() only",
        "read.csv() and write.parquet()",
        "createDataFrame() and toPandas()"
      ],
      "answer": 0,
      "explain": "The testable version keeps the exact same DataFrame-in, DataFrame-out shape those lessons taught, just isolated from file I/O."
    }
  ]
}
