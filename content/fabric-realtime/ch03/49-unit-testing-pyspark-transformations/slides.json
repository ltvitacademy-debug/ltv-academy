[
  {
    "type": "title",
    "eyebrow": "Microsoft Fabric & Real-Time Analytics · Chapter Three",
    "titlePlain": "Unit Testing PySpark ",
    "titleEmphasis": "Transformations",
    "titleSuffix": "",
    "subtitle": "Lesson 49 · Production Data Engineering"
  },
  {
    "type": "code",
    "eyebrow": "Separating logic from I/O",
    "code": "# Hard to test: reads AND transforms\ndef process_trips():\n    df = spark.read.csv(\"abfss://.../trips.csv\")\n    return df.filter(df.FareAmount > 0).select(\"TripId\", \"FareAmount\")\n\n# Testable: DataFrame in, DataFrame out\ndef clean_trips(df):\n    return df.filter(df.FareAmount > 0).select(\"TripId\", \"FareAmount\")",
    "caption": "You can hand clean_trips() exactly the test data you want."
  },
  {
    "type": "code",
    "eyebrow": "A local SparkSession fixture",
    "code": "@pytest.fixture(scope=\"session\")\ndef spark():\n    return SparkSession.builder.master(\"local[1]\").getOrCreate()\n\ndef test_clean_trips_drops_negative_fares(spark):\n    input_df = spark.createDataFrame([(1, 10.0), (2, -5.0)], [\"TripId\", \"FareAmount\"])\n    assert clean_trips(input_df).count() == 1",
    "caption": "No cluster, no Fabric workspace -- fast enough for every commit."
  },
  {
    "type": "steps",
    "eyebrow": "What this catches that a full run wouldn't",
    "titlePlain": "Isolate the ",
    "titleEmphasis": "one",
    "titleSuffix": " function that broke",
    "items": [
      {"label": "Full pipeline test", "sub": "Tells you the whole thing worked or didn't -- slow, not precise"},
      {"label": "Unit test", "sub": "Isolates one function -- a failure tells you exactly where the bug is"}
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 50",
    "titlePlain": "Data ",
    "titleEmphasis": "Contracts",
    "titleSuffix": "",
    "subtitle": "A promise about shape, enforced automatically."
  }
]
