{
  "lesson": "semantic-models-in-fabric",
  "questions": [
    {
      "q": "What does a semantic model add on top of raw tables like trips and zones?",
      "options": [
        "Relationships between tables, measures (business calculations), and friendly names, so a report author queries concepts rather than raw columns",
        "A physical copy of every table, duplicated for reporting",
        "Row-level security masks only, nothing else",
        "A completely new storage format replacing Delta"
      ],
      "answer": 0,
      "explain": "Without this layer, there's no agreed-on business definition — what counts as \"revenue,\" for instance."
    },
    {
      "q": "When is a default semantic model created?",
      "options": [
        "Automatically, the moment a Lakehouse exists — no separate setup step required",
        "Only after a user manually configures one from scratch",
        "Only after a report is published",
        "Semantic models must be created before a Lakehouse can exist"
      ],
      "answer": 0,
      "explain": "This is similar in spirit to how a workspace already has somewhere for data to live before you ask for it."
    },
    {
      "q": "How does a relationship in a semantic model compare to a PySpark join()?",
      "options": [
        "It connects two tables the same way a join does, except defined once in the model rather than repeated in every query",
        "Relationships and joins are completely unrelated concepts",
        "A relationship can only be defined inside a notebook, never in a semantic model",
        "Relationships replace the need for any join ever again"
      ],
      "answer": 0,
      "explain": "This directly parallels Databricks & Delta Lake Lesson 55's join() connecting the same two tables, but defined once instead of repeatedly."
    },
    {
      "q": "Why does defining a measure once matter, rather than each report author writing their own calculation?",
      "options": [
        "It avoids the risk of two different report authors defining the same business concept (like \"Total Revenue\") differently",
        "Measures can only be defined once per Fabric tenant, technically",
        "It has no real benefit; it's purely a stylistic convention",
        "Measures are automatically generated with no user input needed"
      ],
      "answer": 0,
      "explain": "A named, reusable calculation avoids that exact kind of definitional drift across reports."
    },
    {
      "q": "Where does Direct Lake mode (from Lesson 12) actually live?",
      "options": [
        "It's a property of the semantic model itself, not a report-level setting",
        "It's a setting only configurable per individual report",
        "It's a property of the Lakehouse's storage account",
        "Direct Lake mode has no connection to semantic models at all"
      ],
      "answer": 0,
      "explain": "A model built in Direct Lake mode is what lets reports built against it read Delta's Parquet files directly."
    }
  ]
}
