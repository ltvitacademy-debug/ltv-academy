[
  {"slide": "s1", "text": "Eventstream transformations — the rest of the canvas's nodes, beyond window."},
  {"slide": "s2", "text": "Filter is functionally identical to a KQL where clause, but runs on the stream itself. Manage fields is the visual equivalent of project and extend — keep, rename, or cast fields before anything lands."},
  {"slide": "s3", "text": "Group by computes an aggregate directly on the canvas, the same idea as summarize by. Union merges multiple streams into one. Join combines two streams on a matching key, within a time window — all before landing."},
  {"slide": "s4", "text": "Filtering noisy events out in-stream means they're never written at all — no storage, no downstream compute wasted. Filtering after landing still pays to store and scan the noise first."},
  {"slide": "s5", "text": "Shape it early, and every query written afterward gets simpler. Next up: routing events to multiple destinations — one stream, several places to go."}
]
