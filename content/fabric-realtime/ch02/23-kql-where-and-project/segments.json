[
  {"slide": "s1", "text": "Let's actually write real KQL queries — where and project."},
  {"slide": "s2", "text": "Where filters rows exactly like Foundations' filter, or T-SQL's where clause. And and or combine conditions, and multiple where steps in sequence behave like one combined and."},
  {"slide": "s3", "text": "Project combines select and alias into one step — name the columns to keep, optionally rename one inline. Anything not listed simply gets dropped."},
  {"slide": "s4", "text": "And extend is the real equivalent of with column — it adds a new computed column while keeping every existing one, unlike project, which keeps only what's named."},
  {"slide": "s5", "text": "Filter, then compute, then select down — the same shape as a PySpark method chain, just written with pipes. Next lesson: summarize and aggregations, KQL's group by, in full."}
]
