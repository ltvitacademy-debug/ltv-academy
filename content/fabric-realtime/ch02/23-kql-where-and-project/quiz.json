{
  "lesson": "kql-where-and-project",
  "questions": [
    {
      "q": "What does where do in a KQL query?",
      "options": [
        "Filters rows, exactly like Foundations' .filter() or T-SQL's WHERE clause",
        "Selects which columns to keep",
        "Adds a new computed column",
        "Limits the number of rows returned"
      ],
      "answer": 0,
      "explain": "and/or combine conditions, and multiple where steps in sequence behave like one combined and condition."
    },
    {
      "q": "What does project combine into one operator?",
      "options": [
        "select() and alias() — naming columns to keep, with optional inline renaming",
        "where and take",
        "extend and summarize",
        "join and union"
      ],
      "answer": 0,
      "explain": "Columns not listed are dropped, same as a Foundations select() naming only some columns."
    },
    {
      "q": "How does extend differ from project?",
      "options": [
        "extend adds a new computed column while keeping every existing column; project keeps only what's explicitly named",
        "They do exactly the same thing",
        "extend removes columns; project adds them",
        "extend can only be used with the where operator"
      ],
      "answer": 0,
      "explain": "extend is the real KQL equivalent of Foundations Lesson 46's withColumn()."
    },
    {
      "q": "Why would you use extend before project rather than the reverse, when both a new column and a narrower column list are needed?",
      "options": [
        "extend needs the original columns present to compute the new one; project would have already dropped anything not explicitly kept",
        "Order never matters in KQL, since it works like T-SQL",
        "project must always come first in every KQL query",
        "extend cannot be used together with project in the same query"
      ],
      "answer": 0,
      "explain": "This is a direct consequence of KQL's pipe-order semantics from Lesson 22."
    },
    {
      "q": "What does a chain like where -> extend -> project -> take accomplish?",
      "options": [
        "Real filtering, a real computed column, a real column selection, and a real row limit — the same shape as chaining PySpark's .filter().withColumn().select().limit()",
        "It's invalid KQL syntax; only one operator can be used per query",
        "It only performs filtering, ignoring the other three operators",
        "It requires a separate query for each operator"
      ],
      "answer": 0,
      "explain": "This mirrors a familiar PySpark transformation chain, just written with pipes instead of method dots."
    }
  ]
}
