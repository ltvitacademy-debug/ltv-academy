{
  "lesson": "building-a-pivottable",
  "questions": [
    {
      "q": "Why does building a PivotTable from a real Excel Table beat building it from a raw range?",
      "options": [
        "The PivotTable automatically expands to include new rows as the Table grows, with no source reference to manually fix",
        "PivotTables can only technically be created from Tables, never from raw ranges",
        "Tables make every PivotTable calculation faster, with no other benefit",
        "There is no real difference between the two approaches"
      ],
      "answer": 0,
      "explain": "A Table-based PivotTable's Table/Range box references the Table by name, so newly added rows are automatically included after a refresh — a raw range stays fixed at whatever address was originally selected."
    },
    {
      "q": "What does Excel show when you go to Insert > PivotTable in current versions?",
      "options": [
        "An Insert PivotTable pane that proposes a recommended layout preview based on the source data's shape",
        "A blank worksheet with no guidance of any kind",
        "A mandatory wizard that cannot be skipped or adjusted afterward",
        "Nothing — Insert > PivotTable only works after a layout is manually coded"
      ],
      "answer": 0,
      "explain": "The Insert PivotTable pane shows a recommended layout preview (like 'Sales by Product and Year') as a helpful starting point — accepting it still produces a fully editable PivotTable."
    },
    {
      "q": "What are the four drop zones in the PivotTable Fields pane?",
      "options": [
        "Filters, Rows, Columns, and Values",
        "Headers, Footers, Filters, and Totals",
        "Rows, Columns, Formulas, and Charts",
        "Source, Filters, Groups, and Totals"
      ],
      "answer": 0,
      "explain": "Every PivotTable is structured from these four zones: Filters narrows the whole report, Rows and Columns group the data, and Values aggregates it."
    },
    {
      "q": "What happens by default when a numeric field is dragged into the Values zone?",
      "options": [
        "It is aggregated, defaulting to Sum for numbers (Count for text fields)",
        "It is displayed as raw, unaggregated individual values",
        "It is automatically converted into a calculated field",
        "It is moved into the Rows zone instead"
      ],
      "answer": 0,
      "explain": "Values doesn't just display a field — it aggregates it, defaulting to Sum for numeric data and Count for text, which is the foundation Lesson 14's calculated fields build on."
    },
    {
      "q": "What is a common PivotTable bug that a Table-based source avoids?",
      "options": [
        "A source range that quietly stops short of the real data because rows were inserted below the original selection",
        "PivotTables built from Tables cannot use the Values zone",
        "PivotTables built from ranges always calculate incorrect totals",
        "Raw ranges prevent the Filters zone from working"
      ],
      "answer": 0,
      "explain": "A fixed raw-range reference doesn't grow when new rows are added below it, silently excluding new data — a Table reference expands automatically, avoiding that exact bug."
    }
  ]
}
