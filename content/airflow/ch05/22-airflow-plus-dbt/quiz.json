{
  "lesson": "airflow-plus-dbt",
  "questions": [
    {
      "q": "According to this lesson, what is the key relationship between dbt's dependency graph and Airflow's DAG?",
      "options": [
        "They are two different graphs — dbt's is built from ref() and orders models internally, while Airflow's DAG decides when the whole dbt run happens and what surrounds it",
        "They are the exact same graph, just displayed in two different tools",
        "Airflow's DAG replaces dbt's ref()-based ordering entirely",
        "dbt has no dependency graph of its own"
      ],
      "answer": 0,
      "explain": "dbt already orders its own models correctly via ref(); Airflow's DAG is the outer layer controlling scheduling, what runs before/after, and failure handling."
    },
    {
      "q": "When a BashOperator runs `dbt build` as a single Airflow task, what does Airflow's Graph view show if one dbt model fails out of forty?",
      "options": [
        "Just one red box for the whole task — you'd need to read the log to find which model failed",
        "Forty separate boxes, one per model, with the failed one in red",
        "Nothing — Airflow can't detect dbt failures at all",
        "A pop-up naming the exact failed model"
      ],
      "answer": 0,
      "explain": "To Airflow, the whole dbt build is one task/one box; it has no visibility inside that task without a tool like Cosmos."
    },
    {
      "q": "What does `astronomer-cosmos`'s `DbtDag` actually do?",
      "options": [
        "Reads a dbt project's manifest and generates one Airflow task per dbt model, mirroring dbt's own ref() dependencies",
        "Replaces dbt entirely with native Airflow SQL tasks",
        "Only runs dbt tests, never dbt models",
        "Converts Airflow DAGs into dbt models"
      ],
      "answer": 0,
      "explain": "Cosmos turns each dbt model into its own real Airflow task, wired with the same dependency order dbt's ref() graph already defines."
    },
    {
      "q": "Per the lesson, when is a plain BashOperator running `dbt build` the right choice over Cosmos?",
      "options": [
        "When a team just needs to know whether the whole dbt run succeeded or failed, without per-model visibility inside Airflow",
        "Never — Cosmos is always strictly better",
        "Only when the dbt project has zero models",
        "Only in local development, never in production"
      ],
      "answer": 0,
      "explain": "The lesson frames BashOperator as a valid, lower-maintenance default; Cosmos earns its complexity when per-model visibility/retries inside Airflow specifically matter."
    },
    {
      "q": "In the `DbtDag` code example, what does `ProjectConfig` point to?",
      "options": [
        "The dbt project directory Cosmos should read to build the DAG",
        "The Airflow scheduler's configuration file",
        "A Snowflake connection string",
        "The Airflow web server's port"
      ],
      "answer": 0,
      "explain": "`ProjectConfig` tells Cosmos which dbt project's manifest to parse into Airflow tasks."
    }
  ]
}
