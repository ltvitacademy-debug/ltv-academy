[
  {
    "type": "title",
    "eyebrow": "Airflow · Chapter 5, Lesson 22",
    "titlePlain": "Airflow + ",
    "titleEmphasis": "dbt",
    "titleSuffix": "",
    "subtitle": "Two DAGs, doing two different jobs"
  },
  {
    "type": "steps",
    "eyebrow": "dbt's DAG vs. Airflow's DAG",
    "titlePlain": "Not the ",
    "titleEmphasis": "same graph",
    "titleSuffix": "",
    "steps": [
      { "label": "dbt's DAG", "sub": "Built from ref() — model order, inside dbt" },
      { "label": "Airflow's DAG", "sub": "Decides when the whole dbt run kicks off" },
      { "label": "This lesson", "sub": "How Airflow actually triggers it" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "Option 1: BashOperator",
    "code": "trigger_dbt_build = BashOperator(\n    task_id=\"trigger_dbt_build\",\n    bash_command=(\n        \"cd /opt/dbt/sales_project && \"\n        \"dbt build\"\n    ),\n)",
    "caption": "One Airflow task. dbt's entire model graph runs invisibly inside it."
  },
  {
    "type": "code",
    "eyebrow": "Option 2: astronomer-cosmos",
    "code": "sales_dbt_dag = DbtDag(\n    project_config=ProjectConfig(\n        \"/opt/dbt/sales_project\"\n    ),\n    profile_config=profile_config,\n    dag_id=\"sales_dbt_models\",\n)",
    "caption": "One Airflow task per dbt model — dbt's lineage becomes Airflow's dependency chain."
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "Airflow + Snowflake ",
    "titleEmphasis": "COPY INTO",
    "titleSuffix": "",
    "subtitle": "The load step that feeds this whole trigger"
  }
]
