[
  {
    "type": "title",
    "eyebrow": "Airflow · Chapter 4, Lesson 17",
    "titlePlain": "",
    "titleEmphasis": "Sensors",
    "titleSuffix": "",
    "subtitle": "A task that waits, instead of running once and finishing"
  },
  {
    "type": "code",
    "eyebrow": "poke_interval, timeout, mode",
    "code": "FileSensor(\n    task_id=\"wait_for_extract\",\n    filepath=\"/data/incoming/orders_extract.csv\",\n    poke_interval=60,\n    timeout=60 * 60 * 6,\n    mode=\"reschedule\",\n)",
    "caption": "reschedule frees the worker slot between checks — right for long waits."
  },
  {
    "type": "code",
    "eyebrow": "Waiting on another DAG",
    "code": "ExternalTaskSensor(\n    task_id=\"wait_for_upstream_dag\",\n    external_dag_id=\"daily_orders_refresh\",\n    external_task_id=\"refresh_orders_summary\",\n    allowed_states=[\"success\"],\n)",
    "caption": "Chains two independently-scheduled DAGs without merging them into one."
  },
  {
    "type": "steps",
    "eyebrow": "Sensors vs. regular tasks",
    "titlePlain": "Still just a ",
    "titleEmphasis": "task",
    "titleSuffix": "",
    "steps": [
      { "label": "Same Grid and Graph views", "sub": "No special UI of its own" },
      { "label": "Same dependencies, retries, trigger rules", "sub": "Nothing changes" },
      { "label": "execute() loops and checks", "sub": "Instead of doing the work directly" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Next lesson",
    "titlePlain": "",
    "titleEmphasis": "Branching",
    "titleSuffix": "",
    "subtitle": "Sending a DAG down one of several possible paths"
  }
]
