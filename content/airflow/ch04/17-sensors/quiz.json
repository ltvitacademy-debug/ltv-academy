{
  "lesson": "sensors",
  "questions": [
    {
      "q": "How does a Sensor differ from a regular Operator task?",
      "options": [
        "It checks a condition repeatedly on an interval, succeeding only once that condition becomes true",
        "It runs faster than any other task type",
        "It cannot fail under any circumstances",
        "It skips the scheduler entirely"
      ],
      "answer": 0,
      "explain": "A Sensor waits and repeatedly checks a condition, unlike a normal Operator that runs once and finishes."
    },
    {
      "q": "What does `mode=\"reschedule\"` do for a Sensor?",
      "options": [
        "Frees the worker slot between checks, instead of holding it for the entire wait",
        "Makes the Sensor check twice as often",
        "Disables the timeout entirely",
        "Converts the Sensor into a regular Operator"
      ],
      "answer": 0,
      "explain": "reschedule mode releases the worker between checks — much better than poke mode for long waits, which would otherwise tie up a worker doing nothing."
    },
    {
      "q": "What does `ExternalTaskSensor` wait for?",
      "options": [
        "A specific task (or DAG) in a different DAG to reach a given state",
        "A file to appear in a local directory only",
        "The Airflow scheduler to restart",
        "A fixed amount of wall-clock time with no condition check"
      ],
      "answer": 0,
      "explain": "ExternalTaskSensor is the standard way to chain two independently-scheduled DAGs by waiting on a task/DAG in another DAG."
    },
    {
      "q": "According to this lesson, how does a Sensor appear in the Grid and Graph views?",
      "options": [
        "Like any other task — it participates in dependencies, retries, and trigger rules the same way",
        "It's invisible until it succeeds",
        "It appears in a completely separate, dedicated view",
        "It cannot be viewed in the UI at all"
      ],
      "answer": 0,
      "explain": "A Sensor is still just a task under the hood — the only real difference is what its execute() method does internally."
    },
    {
      "q": "What does `timeout` control on a Sensor?",
      "options": [
        "How long the Sensor keeps checking before failing the task",
        "How many retries the Sensor gets after succeeding",
        "The DAG's overall schedule interval",
        "How many workers are assigned to the Sensor"
      ],
      "answer": 0,
      "explain": "timeout sets the maximum time the Sensor will keep checking its condition before giving up and failing."
    }
  ]
}
