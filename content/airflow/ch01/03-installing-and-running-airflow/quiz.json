{
  "lesson": "installing-and-running-airflow",
  "questions": [
    {
      "q": "Why does Airflow's official install command pin a constraints file, not just the Airflow version?",
      "options": [
        "It locks every dependency to a version actually tested together, so pip doesn't silently pull in untested combinations",
        "It makes the install run faster",
        "It's a legacy requirement with no current purpose",
        "It only affects which Python version you can use, nothing else"
      ],
      "answer": 0,
      "explain": "Airflow has a large dependency tree; the constraints file pins every dependency to versions that were actually tested against that Airflow release."
    },
    {
      "q": "What does the single command `airflow standalone` actually do?",
      "options": [
        "Initializes the metadata database, creates an admin user, and starts the scheduler, webserver, and triggerer all at once",
        "Only starts the webserver, nothing else",
        "Installs Airflow from scratch",
        "Deletes all existing DAGs"
      ],
      "answer": 0,
      "explain": "It's a one-command bootstrap for a complete local Airflow instance — no separate manual setup of each component."
    },
    {
      "q": "Where do you find Airflow's admin password after running `airflow standalone`?",
      "options": [
        "In a generated file inside $AIRFLOW_HOME, such as simple_auth_manager_passwords.json.generated",
        "It's always printed directly to the terminal with no exceptions",
        "It's the same for every Airflow installation everywhere",
        "You must set it manually before starting Airflow"
      ],
      "answer": 0,
      "explain": "In current Airflow versions the password isn't always shown in the terminal — it's saved to a generated file under $AIRFLOW_HOME."
    },
    {
      "q": "What is $AIRFLOW_HOME?",
      "options": [
        "The folder (default ~/airflow) holding Airflow's config file, metadata database, and generated credentials",
        "A Python package you must import in every DAG",
        "The URL where the Airflow UI is hosted",
        "A required environment variable that has no default and must always be set manually"
      ],
      "answer": 0,
      "explain": "$AIRFLOW_HOME defaults to ~/airflow and is where Airflow keeps its configuration and local state."
    },
    {
      "q": "What is an \"extra\" like the `[celery]` in `apache-airflow[celery]==3.3.1`?",
      "options": [
        "An optional install component in brackets that pulls in additional functionality at install time",
        "A required flag with no effect on what gets installed",
        "A typo that pip ignores",
        "A separate, unrelated Python package"
      ],
      "answer": 0,
      "explain": "Extras in brackets opt into additional functionality (like specific integrations) at install time, on top of the base Airflow install."
    }
  ]
}
