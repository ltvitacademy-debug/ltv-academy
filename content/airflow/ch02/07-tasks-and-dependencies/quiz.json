{
  "lesson": "tasks-and-dependencies",
  "questions": [
    {
      "q": "What does `extract >> load` mean?",
      "options": [
        "extract must finish before load starts",
        "extract and load run at the exact same time",
        "load must finish before extract starts",
        "extract and load are unrelated"
      ],
      "answer": 0,
      "explain": "The >> operator sets a downstream dependency: the left task must finish before the right one begins."
    },
    {
      "q": "In `extract >> [transform_orders, transform_customers] >> load`, when can load start?",
      "options": [
        "Only after both transform_orders and transform_customers finish",
        "As soon as extract finishes, regardless of the transform tasks",
        "Only after transform_orders finishes, transform_customers is ignored",
        "Immediately, in parallel with everything else"
      ],
      "answer": 0,
      "explain": "This is a fan-in: load depends on both fanned-out tasks completing before it can start."
    },
    {
      "q": "What is `set_downstream` in relation to `>>`?",
      "options": [
        "The method call that `>>` is shorthand for — `a >> b` is the same as `a.set_downstream(b)`",
        "A deprecated method that no longer works",
        "A method that only works with BashOperator",
        "The opposite of `>>`"
      ],
      "answer": 0,
      "explain": "The bitshift operators are convenience syntax for the set_downstream/set_upstream method calls."
    },
    {
      "q": "In the real run shown in this lesson's screenshot, why were downstream tasks marked `upstream_failed` instead of `failed`?",
      "options": [
        "They never actually ran because a task they depended on failed — Airflow distinguishes their own failure from a failure upstream",
        "upstream_failed and failed mean exactly the same thing",
        "Those tasks succeeded but were mislabeled by a bug",
        "upstream_failed means the whole Airflow instance crashed"
      ],
      "answer": 0,
      "explain": "upstream_failed tells you the task itself never ran — the real problem is further up the dependency chain."
    },
    {
      "q": "What's the practical benefit of Airflow distinguishing `failed` from `upstream_failed` when debugging?",
      "options": [
        "You can go straight to the one task that actually failed instead of investigating every downstream task individually",
        "There is no practical benefit, it's purely cosmetic",
        "It means you never need to check logs",
        "It automatically fixes the failing task"
      ],
      "answer": 0,
      "explain": "The distinction points you directly at the root cause rather than making you chase symptoms across every dependent task."
    }
  ]
}
