{
  "lesson": "pythonoperator",
  "questions": [
    {
      "q": "What does PythonOperator's `python_callable` parameter do?",
      "options": [
        "Points to the ordinary Python function that should run when the task executes",
        "Sets the number of retries for the task",
        "Defines the DAG's overall schedule",
        "Names the Airflow connection to use"
      ],
      "answer": 0,
      "explain": "python_callable tells PythonOperator which function to call when the task runs — the function itself doesn't need to change."
    },
    {
      "q": "What does `op_kwargs` do?",
      "options": [
        "Passes a dict of keyword arguments into the callable, exactly like calling the function directly",
        "Sets the task's priority weight",
        "Deletes the task after it runs once",
        "Configures email alerting for the task"
      ],
      "answer": 0,
      "explain": "op_kwargs supplies keyword arguments to the wrapped function at run time, the same way you'd call it directly."
    },
    {
      "q": "What is the TaskFlow API's @task decorator?",
      "options": [
        "A lighter-weight alternative to PythonOperator that turns a decorated function into a task automatically",
        "A way to delete tasks from a DAG",
        "A special type of Sensor",
        "A deprecated feature no longer supported in Airflow"
      ],
      "answer": 0,
      "explain": "@task does the same job as PythonOperator with less setup — no task_id or python_callable to wire up manually."
    },
    {
      "q": "According to this lesson, when would you still need to read or write classic PythonOperator code?",
      "options": [
        "In existing pre-TaskFlow DAGs, cases needing dynamic behavior TaskFlow doesn't cover as cleanly, and older documentation",
        "Never — TaskFlow has completely replaced PythonOperator in all Airflow versions",
        "Only when using BashOperator instead",
        "Only in DAGs that don't use Python at all"
      ],
      "answer": 0,
      "explain": "Classic PythonOperator still shows up constantly in real codebases and documentation, so recognizing it matters even if @task is more ergonomic for new code."
    },
    {
      "q": "What is the alternative to op_kwargs for passing arguments positionally as a list?",
      "options": [
        "op_args",
        "op_positional",
        "python_args",
        "task_kwargs"
      ],
      "answer": 0,
      "explain": "op_args passes arguments positionally, as a list, as an alternative style to the keyword-based op_kwargs."
    }
  ]
}
