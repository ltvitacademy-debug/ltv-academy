{
  "lesson": "bashoperator",
  "questions": [
    {
      "q": "What does BashOperator's `bash_command` actually run?",
      "options": [
        "A real shell command, exactly as if typed at a terminal",
        "A Python function only",
        "A SQL query directly against a database",
        "Nothing — it only logs the command without executing it"
      ],
      "answer": 0,
      "explain": "bash_command runs in an actual shell, so anything you could type at a terminal — piping, chaining, calling CLI tools — works."
    },
    {
      "q": "What does `{{ ds }}` resolve to inside a bash_command string?",
      "options": [
        "The DAG run's logical date, formatted as YYYY-MM-DD",
        "The name of the DAG itself",
        "A random unique identifier",
        "The current server timestamp down to the millisecond"
      ],
      "answer": 0,
      "explain": "ds is a built-in Airflow template variable resolving to the DAG run's logical date in YYYY-MM-DD format."
    },
    {
      "q": "What determines whether a BashOperator task succeeds or fails?",
      "options": [
        "The shell command's exit code — 0 succeeds, anything else fails",
        "Whether the command printed any output at all",
        "A separate success/failure flag you must set manually",
        "The task always succeeds regardless of the command's outcome"
      ],
      "answer": 0,
      "explain": "Task success or failure maps directly to the shell command's exit code, exactly as it would at a real terminal."
    },
    {
      "q": "What does @task.bash do?",
      "options": [
        "Wraps BashOperator in the TaskFlow style — a decorated function's return value becomes the bash command to run",
        "Converts a bash command into a PythonOperator task",
        "Deletes a bash task from the DAG",
        "Only works for SQL commands, not shell commands"
      ],
      "answer": 0,
      "explain": "@task.bash mirrors @task's pattern — the decorated function returns a string, and that string becomes the command executed."
    },
    {
      "q": "According to this lesson, when should you reach for BashOperator instead of PythonOperator?",
      "options": [
        "When calling an existing CLI tool or script that already does the job",
        "Always, regardless of what the task does",
        "Only when the task involves no external tools at all",
        "Never — PythonOperator should always be preferred"
      ],
      "answer": 0,
      "explain": "BashOperator is the natural fit when an existing command-line tool already handles the work — no Python wrapper needed."
    }
  ]
}
