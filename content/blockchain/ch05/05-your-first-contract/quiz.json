{
  "lesson": "your-first-contract",
  "questions": [
    {
      "q": "What file extension does every Solidity file need to end in?",
      "options": [
        ".sol",
        ".solidity",
        ".eth",
        ".contract"
      ],
      "answer": 0,
      "explain": "Every Solidity file must end in .sol — that's how Remix (and the compiler) knows it's a Solidity file."
    },
    {
      "q": "What real rule of thumb did the instructor give for choosing a pragma version?",
      "options": [
        "Use the latest version available, not the oldest, since older versions carry bugs that were later fixed",
        "Always use the very first version, 0.4",
        "The version number doesn't matter at all",
        "Use a random version each time"
      ],
      "answer": 0,
      "explain": "The instructor explicitly recommended using the latest available compiler version, since older versions have known bugs that later releases fixed."
    },
    {
      "q": "What happens if you leave off the semicolon after the pragma line in Remix?",
      "options": [
        "Remix shows a real compiler error",
        "Nothing — semicolons are always optional in Solidity",
        "The contract deploys anyway with a warning",
        "It automatically adds one for you silently"
      ],
      "answer": 0,
      "explain": "The instructor demonstrated live that removing the semicolon produces a visible error in Remix — it's a required part of the line."
    },
    {
      "q": "In `string message = \"Hello class\";`, what does the word `string` represent?",
      "options": [
        "The data type — indicating the variable holds a word or collection of letters",
        "The variable's actual value",
        "The name of the contract",
        "A comment"
      ],
      "answer": 0,
      "explain": "`string` is the data type declaration. `message` is the variable's name, and `\"Hello class\"` is its actual value."
    },
    {
      "q": "Which of these is a valid single-line comment in Solidity, per this lesson?",
      "options": [
        "// this is my comment",
        "# this is my comment",
        "<!-- this is my comment -->",
        "** this is my comment **"
      ],
      "answer": 0,
      "explain": "Solidity uses two forward slashes (//) for a single-line comment, and /* ... */ for a multi-line comment — both are ignored by the compiler."
    }
  ]
}
