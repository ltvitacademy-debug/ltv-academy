{
  "lesson": "solidity-data-types",
  "questions": [
    {
      "q": "What is the real, defining difference between `uint` and `int` in Solidity?",
      "options": [
        "uint can never be negative; int can be positive, negative, or zero",
        "int can never be negative; uint can be positive, negative, or zero",
        "They are exactly identical with no real difference",
        "uint is only for strings, int is only for numbers"
      ],
      "answer": 0,
      "explain": "uint (unsigned integer) is restricted to zero or positive whole numbers, while int (signed integer) can also represent negative values."
    },
    {
      "q": "Why would a developer choose uint8 instead of uint256 for a small value, per this lesson?",
      "options": [
        "Smaller uint sizes cost less gas to store than larger ones",
        "uint8 can hold larger numbers than uint256",
        "uint256 doesn't actually exist in Solidity",
        "There's no real reason — they cost the same"
      ],
      "answer": 0,
      "explain": "The larger the uint size, the more gas it costs to store — so using a smaller size like uint8 for values that will always stay small genuinely saves gas."
    },
    {
      "q": "What is the real default value of an unsigned integer that's declared without being given an initial value?",
      "options": [
        "Zero",
        "Negative one",
        "It causes a compile error",
        "It stays undefined forever"
      ],
      "answer": 0,
      "explain": "The instructor demonstrated live that a uint left without an initial value defaults to zero when you call its getter function."
    },
    {
      "q": "What does msg.sender actually refer to?",
      "options": [
        "The address of the account or contract that called the current function",
        "The contract's own deployed address",
        "The Ethereum Foundation's address",
        "A random address generated on each transaction"
      ],
      "answer": 0,
      "explain": "msg.sender is a global variable that always holds the address of whoever (or whatever) is calling the current function — demonstrated live by switching accounts and redeploying."
    },
    {
      "q": "How many bytes does a Solidity `address` value actually hold?",
      "options": [
        "20 bytes (40 hex characters)",
        "8 bytes",
        "256 bytes",
        "It varies depending on the network"
      ],
      "answer": 0,
      "explain": "Every Ethereum address is a fixed 20 bytes, represented as 40 hexadecimal characters — the same size for every address on the network."
    }
  ]
}
