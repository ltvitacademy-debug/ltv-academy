{
  "lesson": "fixing-reentrancy",
  "questions": [
    {
      "q": "What single real change turns the vulnerable withdraw function into the fixed version?",
      "options": [
        "Resetting the balance to zero before making the external call, instead of after",
        "Removing the require statement entirely",
        "Adding a pragma version bump",
        "Renaming the function"
      ],
      "answer": 0,
      "explain": "The only real change is moving the balance reset earlier — from after the .call to before it — following the checks-effects-interactions order."
    },
    {
      "q": "What is the real, correct order of the checks-effects-interactions pattern?",
      "options": [
        "Checks, then effects (state changes), then interactions (external calls)",
        "Interactions, then checks, then effects",
        "Effects, then interactions, then checks",
        "There is no real fixed order — any order works equally well"
      ],
      "answer": 0,
      "explain": "First verify conditions (checks), then update your own state (effects), and only then make external calls (interactions) — this exact order is what prevents reentrancy."
    },
    {
      "q": "How does the real `noReentrant` modifier actually stop a reentrancy attack?",
      "options": [
        "It sets a locked boolean to true while the function runs, so a nested re-entry call fails the require(!locked) check",
        "It deletes the function entirely",
        "It requires a password to call the function",
        "It only works if the attacker uses a different account"
      ],
      "answer": 0,
      "explain": "The modifier locks the contract for the duration of the function call — if the attacker's fallback tries to re-enter, locked is already true, and the check fails."
    },
    {
      "q": "Per the instructor's real, honest advice, what do most production contracts actually use for reentrancy protection?",
      "options": [
        "OpenZeppelin's audited ReentrancyGuard, rather than a hand-written version",
        "No protection at all — reentrancy is considered unavoidable",
        "Only the checks-effects-interactions pattern, never a guard modifier",
        "A completely different, undisclosed technique"
      ],
      "answer": 0,
      "explain": "The instructor noted that popular real projects overwhelmingly use OpenZeppelin's own ReentrancyGuard rather than writing their own from scratch."
    },
    {
      "q": "Why are checks-effects-interactions and the reentrancy guard described as independent defenses?",
      "options": [
        "A contract could use either one alone and still be protected from the same real exploit built in the previous lesson",
        "They must always be used together or neither works",
        "The guard modifier replaces the need for a withdraw function entirely",
        "Checks-effects-interactions only works on testnets"
      ],
      "answer": 0,
      "explain": "Either fix on its own neutralizes the specific reentrancy exploit demonstrated earlier — correct state ordering, or a lock preventing re-entry, both independently close the same door."
    }
  ]
}
