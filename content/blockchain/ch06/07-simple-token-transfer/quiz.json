{
  "lesson": "simple-token-transfer",
  "questions": [
    {
      "q": "Why does the `require(balances[msg.sender] >= amount, ...)` check need to run before any balance is changed?",
      "options": [
        "So the entire transaction reverts immediately if funds are insufficient, before any state change happens",
        "It doesn't actually matter what order it runs in",
        "require only works if it's the last line of the function",
        "To make the function cost less gas overall"
      ],
      "answer": 0,
      "explain": "Checking first means an insufficient-funds transaction reverts before any balances are touched — protecting against ending up in an inconsistent, partially-updated state."
    },
    {
      "q": "In the real transfer function, what happens to the sender's and receiver's balances?",
      "options": [
        "The sender's balance decreases by amount, and the receiver's increases by amount",
        "Both balances increase by amount",
        "Only the receiver's balance changes",
        "Balances are unaffected — only the event fires"
      ],
      "answer": 0,
      "explain": "balances[msg.sender] -= amount removes the amount from the sender, and balances[to] += amount adds it to the receiver."
    },
    {
      "q": "Why are both `from` and `to` marked `indexed` in the Transfer event?",
      "options": [
        "So the event can be searched either by sender or by receiver",
        "Indexed parameters are required for the event to compile at all",
        "It makes the event emit for free",
        "Only one address can ever be indexed at a time"
      ],
      "answer": 0,
      "explain": "Indexing both from and to lets a block explorer or front end filter transfers by either the sending address or the receiving address."
    },
    {
      "q": "What real, standard order of operations does this function follow?",
      "options": [
        "require, then mutate state, then emit",
        "emit, then mutate state, then require",
        "mutate state, then require, then emit",
        "There is no consistent order — it's arbitrary"
      ],
      "answer": 0,
      "explain": "The function checks its condition first (require), only then changes balances (mutate), and finally announces the change (emit) — the standard real order."
    },
    {
      "q": "Per this lesson, why is this simple transfer function described as \"the core of what an ERC-20 token contract does\"?",
      "options": [
        "It combines a balances mapping, a require guard, state-changing arithmetic, and an event — the same real building blocks a full token contract extends",
        "It has no real connection to actual token standards",
        "ERC-20 tokens never use mappings or events",
        "This function is only used for testing and never appears in production"
      ],
      "answer": 0,
      "explain": "Every concept from this chapter — mappings, require guards, mapping arithmetic, and events — appears together in this one function, which is genuinely the same shape real ERC-20 contracts build on."
    }
  ]
}
