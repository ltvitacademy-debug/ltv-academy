{
  "lesson": "erc20-openzeppelin",
  "questions": [
    {
      "q": "What does `contract ERC20Test is ERC20 { }` actually do?",
      "options": [
        "Uses real Solidity inheritance so ERC20Test automatically gets every function OpenZeppelin's ERC20 defines",
        "Creates a completely empty contract with no functions at all",
        "Deletes OpenZeppelin's ERC20 contract",
        "Only imports the ERC20 contract's variable names, not its functions"
      ],
      "answer": 0,
      "explain": "The `is ERC20` syntax is real inheritance — the new contract automatically inherits transfer, approve, transferFrom, and everything else ERC20 implements."
    },
    {
      "q": "Where did the instructor find the real ERC20 contract to import?",
      "options": [
        "OpenZeppelin's own GitHub repository, under contracts/token/ERC20",
        "A random pastebin link",
        "Ethereum's own official website",
        "It was already built into Remix by default"
      ],
      "answer": 0,
      "explain": "The real, audited ERC20.sol lives in OpenZeppelin's GitHub repo, under contracts/token/ERC20 — the instructor navigated there directly and copied the file's URL."
    },
    {
      "q": "Why do real production tokens generally prefer OpenZeppelin's ERC20 over a hand-written version?",
      "options": [
        "OpenZeppelin's version has already been reviewed by security researchers, reducing real risk",
        "Hand-written versions are always faster to deploy",
        "OpenZeppelin's contracts cannot be imported into Remix",
        "There's no real difference in practice"
      ],
      "answer": 0,
      "explain": "Reusing an already-audited, widely-used implementation avoids the real risk of reintroducing bugs that a from-scratch implementation might contain."
    },
    {
      "q": "What real value does writing your own ERC-20 by hand (from the previous lessons) still provide, per this lesson?",
      "options": [
        "It builds a genuine understanding of how the mechanics actually work, even if you use OpenZeppelin's version in production",
        "It has no value at all once OpenZeppelin exists",
        "It's required before OpenZeppelin's contracts can even compile",
        "It replaces the need to ever read documentation"
      ],
      "answer": 0,
      "explain": "Understanding the real mechanics — balances, allowances, events — makes it possible to actually reason about and extend a production-grade implementation later."
    },
    {
      "q": "What does importing a contract directly by its GitHub URL, as shown in this lesson, actually require?",
      "options": [
        "Pasting the file's real raw GitHub URL inside the import statement's quotes",
        "Manually retyping the entire contract by hand",
        "A special paid Remix plugin",
        "Downloading and re-uploading the file first"
      ],
      "answer": 0,
      "explain": "Just like the earlier OpenZeppelin lesson in Chapter 7, pasting a file's GitHub URL directly into an import statement pulls the real, live source in without retyping it."
    }
  ]
}
