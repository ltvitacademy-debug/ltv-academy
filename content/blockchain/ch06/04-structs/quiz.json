{
  "lesson": "structs",
  "questions": [
    {
      "q": "What real analogy did the instructor use to explain what a struct is?",
      "options": [
        "A paper form at the doctor's office, grouping name, age, address, and phone number together",
        "A vending machine",
        "A stack of plates",
        "A traffic light"
      ],
      "answer": 0,
      "explain": "The paper-form analogy captures it exactly: all those fields belong together and describe one person — just like a struct groups related fields into one unit."
    },
    {
      "q": "In `person1 = Person(\"Alice\", 30, msg.sender);`, what does calling `getName()` return afterward?",
      "options": [
        "\"Alice\"",
        "30",
        "The deployer's address",
        "Nothing — structs can't be read after being set"
      ],
      "answer": 0,
      "explain": "getName() returns person1.name, which was just set to \"Alice\" — confirmed live in the demo."
    },
    {
      "q": "What real guarantee does `mapping(address => Person) public directory;` combined with `msg.sender` as the key provide?",
      "options": [
        "Each account automatically gets its own separate record that only it can read or overwrite via that key",
        "All accounts share one single Person record",
        "Only the contract's original deployer can ever call addPerson",
        "It guarantees the struct can never be read back"
      ],
      "answer": 0,
      "explain": "Using msg.sender as the mapping key means every calling account is automatically keyed to its own record, separate from everyone else's."
    },
    {
      "q": "In the live demo, what happened when two different accounts each called addPerson with different data?",
      "options": [
        "Each account's data was stored separately, and getMyInfo() returned only that account's own record",
        "The second call overwrote the first account's data entirely",
        "Only the first account's call was ever accepted",
        "Both accounts ended up sharing identical data"
      ],
      "answer": 0,
      "explain": "One account stored \"Carol\", 56 and a different account stored \"Alice\", 57 — each account's getMyInfo() call returned only its own separately stored data."
    },
    {
      "q": "Why does this lesson describe the struct-inside-a-mapping pattern as foundational for token standards?",
      "options": [
        "It's the same real shape used later for tracking things like NFT metadata or a staking position per account",
        "It has no real connection to tokens at all",
        "Token standards never use structs or mappings",
        "It's only useful for storing text messages"
      ],
      "answer": 0,
      "explain": "One struct describing a record, stored per-address in a mapping, is exactly the pattern more advanced token features (like per-account NFT metadata or staking) build on."
    }
  ]
}
