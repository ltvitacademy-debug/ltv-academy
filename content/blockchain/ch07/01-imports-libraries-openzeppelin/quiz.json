{
  "lesson": "imports-libraries-openzeppelin",
  "questions": [
    {
      "q": "What is OpenZeppelin, as described in this lesson?",
      "options": [
        "A real library of audited, pre-written, vetted Solidity building blocks",
        "A Solidity compiler version",
        "A blockchain competing with Ethereum",
        "A Remix-specific plugin with no real code in it"
      ],
      "answer": 0,
      "explain": "OpenZeppelin provides audited building blocks for Solidity code — contracts that are already safe, efficient, and vetted by other developers."
    },
    {
      "q": "What is the real, key difference between a library and a contract in Solidity?",
      "options": [
        "Libraries can't hold state variables or support inheritance; contracts can do both",
        "Libraries can send and receive Ether, but contracts cannot",
        "Contracts can only have pure functions; libraries can have any kind",
        "There is no real difference between them"
      ],
      "answer": 0,
      "explain": "Libraries are stateless and don't support inheritance — contracts can hold state and support inheritance, among other differences."
    },
    {
      "q": "What does `using MathLib for uint;` actually let you do?",
      "options": [
        "Call MathLib's functions directly on a uint value, like val.double()",
        "Automatically convert every uint into a string",
        "Delete the MathLib library after one use",
        "Give every uint variable a starting value of zero"
      ],
      "answer": 0,
      "explain": "The using keyword attaches a library's functions to a type, so you can call them with dot syntax directly on a value of that type."
    },
    {
      "q": "What's the real, practical difference between an embedded and a deployed library?",
      "options": [
        "Embedded (internal-only) libraries get copied into your contract at compile time; deployed libraries (with public/external functions) are deployed and linked separately, at more gas cost",
        "Deployed libraries are always cheaper than embedded ones",
        "Embedded libraries can never be reused across contracts",
        "There is no real cost difference between the two"
      ],
      "answer": 0,
      "explain": "Keeping library functions internal lets Solidity embed the code directly at compile time (cheaper); public/external functions require separate deployment and linking (more expensive)."
    },
    {
      "q": "What real, practical way did the instructor demonstrate for importing a file directly from GitHub?",
      "options": [
        "Pasting the file's raw GitHub URL directly inside the import statement's quotes",
        "Downloading the file manually and re-uploading it to Remix",
        "It's not actually possible to import directly from GitHub",
        "Only OpenZeppelin's paid tier supports GitHub imports"
      ],
      "answer": 0,
      "explain": "The instructor imported OpenZeppelin's strings.sol directly by pasting its GitHub URL inside the import statement — no manual copy-paste of the file's contents required."
    }
  ]
}
