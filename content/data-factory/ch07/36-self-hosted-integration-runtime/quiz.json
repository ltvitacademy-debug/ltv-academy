{
  "lesson": "self-hosted-integration-runtime",
  "questions": [
    {
      "q": "What problem does the self-hosted integration runtime actually solve?",
      "options": [
        "Reaching data sources inside a private network that the Azure IR can't connect to",
        "Running SSIS packages, which the Azure IR can't do",
        "Making Copy activities run faster than the Azure IR",
        "Replacing the Azure IR entirely in every data factory"
      ],
      "answer": 0,
      "explain": "The Azure IR lives in Azure's own network and has no path into an on-premises or private network — the self-hosted IR is software you install inside that network specifically to bridge the gap."
    },
    {
      "q": "What does a self-hosted integration runtime actually consist of?",
      "options": [
        "The Microsoft Integration Runtime Configuration Manager, installed on a machine you control",
        "A managed Azure VM that Microsoft provisions for you automatically",
        "A browser extension installed on the developer's laptop",
        "A SQL Server stored procedure"
      ],
      "answer": 0,
      "explain": "It's real software — the Configuration Manager — installed and registered on a Windows machine inside your own network, not a managed Azure resource."
    },
    {
      "q": "What does a self-hosted IR use to register itself against a specific data factory?",
      "options": [
        "An authentication key generated when the IR is created",
        "The data factory's admin password",
        "A self-signed SSL certificate you create manually",
        "It registers automatically with no credential at all"
      ],
      "answer": 0,
      "explain": "Data Factory generates a primary and secondary authentication key when you create the self-hosted IR; pasting one into the Configuration Manager registers that node."
    },
    {
      "q": "Why would a self-hosted integration runtime have more than one node?",
      "options": [
        "For high availability and greater scalability for data movement",
        "Nodes have nothing to do with availability — they're purely cosmetic",
        "Because a single node can only run one pipeline at a time",
        "Each node must belong to a different data factory"
      ],
      "answer": 0,
      "explain": "Multiple nodes (up to four) share one logical self-hosted IR, keeping data movement running if one node goes down and adding throughput — Microsoft recommends at least two for production."
    },
    {
      "q": "In the self-hosted IR architecture, which side initiates the network connection?",
      "options": [
        "The self-hosted IR initiates an outbound connection to Azure",
        "Azure Data Factory initiates an inbound connection into the corporate network",
        "Neither side initiates — it's a peer-to-peer connection",
        "The on-premises data source connects directly to Azure, bypassing the IR"
      ],
      "answer": 0,
      "explain": "The self-hosted IR always initiates outbound, so no inbound port has to be opened into the corporate network — Lesson 37 covers exactly what that requires from a firewall."
    }
  ]
}
