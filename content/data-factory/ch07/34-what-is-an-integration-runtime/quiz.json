{
  "lesson": "what-is-an-integration-runtime",
  "questions": [
    {
      "q": "What is an integration runtime?",
      "options": [
        "The compute infrastructure that bridges an activity and a linked service",
        "A type of dataset",
        "A control activity for branching logic",
        "A pipeline trigger type"
      ],
      "answer": 0,
      "explain": "An activity defines the action, a linked service defines the target — the integration runtime is the actual compute that bridges the two and does the work."
    },
    {
      "q": "What are the three integration runtime types?",
      "options": [
        "Azure, Self-hosted, and Azure-SSIS",
        "Manual, Scheduled, and Event-based",
        "Source, Sink, and Transform",
        "Public, Private, and Hybrid"
      ],
      "answer": 0,
      "explain": "Azure IR handles cloud-to-cloud work, self-hosted IR bridges to on-premises data, and Azure-SSIS IR runs existing SSIS packages on a managed VM cluster."
    },
    {
      "q": "Can an integration runtime's location differ from the data factory's own region?",
      "options": [
        "Yes — an IR's location is independent of where the data factory's metadata lives",
        "No — an IR must always be in the same region as its data factory",
        "Only self-hosted IRs can be in a different region",
        "Only Azure-SSIS IRs support a different region"
      ],
      "answer": 0,
      "explain": "A data factory's metadata lives in one fixed region, but you can create an IR in a different region entirely — useful for keeping data movement inside a specific region for compliance."
    },
    {
      "q": "When both a self-hosted IR and an Azure IR could apply to an activity, which one takes precedence?",
      "options": [
        "The self-hosted IR",
        "The Azure IR always wins",
        "Whichever was created first",
        "Data Factory refuses to run the activity"
      ],
      "answer": 0,
      "explain": "Self-hosted IR takes precedence over an Azure IR in a managed virtual network, which in turn takes precedence over the global, autoresolve Azure IR."
    },
    {
      "q": "For a Copy activity, what happens if the source uses a self-hosted IR but the sink doesn't specify one?",
      "options": [
        "The entire copy runs on the self-hosted IR — there's no mixing between source and sink runtimes",
        "The sink automatically uses the Azure IR instead",
        "The copy activity fails immediately",
        "Data Factory picks whichever IR is geographically closer"
      ],
      "answer": 0,
      "explain": "If either the source or sink linked service points to a self-hosted IR, the whole Copy activity runs on that self-hosted IR."
    }
  ]
}
