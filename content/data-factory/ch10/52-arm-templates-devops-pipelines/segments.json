[
  {"slide": "s1", "text": "Lesson 51's diagram showed a release pipeline pulling the publish branch as its artifact. This lesson genuinely builds that release pipeline, piece by piece."},
  {"slide": "s2", "text": "Concretely, that means pointing the release at your factory's Git repository, with the publish branch, adf_publish, set as the default branch — not the collaboration branch itself, where development actually happens."},
  {"slide": "s3", "text": "Inside that branch sit two real files, generated automatically every time someone publishes: ARMTemplateForFactory.json, the actual template, and ARMTemplateParametersForFactory.json, every parameterized value the template expects. An ARM Template Deployment task points directly at both. Two settings genuinely matter: Create or update resource group, since you're deploying into an existing environment, and Incremental deployment mode — not Complete, which deletes any resource absent from the new template."},
  {"slide": "s4", "text": "Here's a real, sharp edge: deployment can fail outright if you try to update a trigger that's currently active. The fix is a PowerShell script, run before deployment, that stops every trigger, followed by a second script after deployment that starts them again. Microsoft ships a ready-made version of this exact script that's smart enough to only touch triggers that actually changed."},
  {"slide": "s5", "text": "Two real ways to keep secrets out of the parameters file itself. Reference Key Vault directly in the parameters file, using a reference block Azure Resource Manager resolves automatically at deployment time. Or add a dedicated Key Vault task before the deployment task, fetching secrets into pipeline variables instead. Either way, the actual secret value never sits in plain text anywhere in your release definition."},
  {"slide": "s6", "text": "That completes Chapter Ten: managed identities and Key Vault, RBAC, Git integration, environments, and now the actual release pipeline tying it all together. Next, Chapter Eleven: the Capstone Project — everything from this entire course, built into one real, finished pipeline from scratch."}
]
