{
  "lesson": "copy-activity",
  "questions": [
    {
      "q": "What does the Copy activity do?",
      "options": [
        "Copies data from a source data store to a sink data store",
        "Only deletes data from a data store",
        "Only validates that a dataset exists",
        "Sends email notifications about pipeline runs"
      ],
      "answer": 0,
      "explain": "The Copy activity moves data between a source and a sink — on-premises and cloud stores alike."
    },
    {
      "q": "What are the three steps the Copy activity performs, regardless of source or sink?",
      "options": [
        "Read from source, perform serialization/mapping, write to sink",
        "Validate, publish, monitor",
        "Extract, transform, load — always in a separate engine",
        "Connect, authenticate, disconnect"
      ],
      "answer": 0,
      "explain": "Every Copy activity run reads the source data, performs serialization/deserialization, compression/decompression, and column mapping, then writes to the sink."
    },
    {
      "q": "Can a single Copy activity use two different self-hosted integration runtimes, one for source and one for sink?",
      "options": [
        "No — if a self-hosted IR is needed, both source and sink must be reachable from the same one",
        "Yes, any two self-hosted IRs can be mixed freely",
        "Only if both IRs are on the same physical machine",
        "The Copy activity never uses integration runtimes at all"
      ],
      "answer": 0,
      "explain": "You can't use more than one self-hosted integration runtime within the same Copy activity — source and sink must connect through the same one."
    },
    {
      "q": "What does \"Auto create table\" do when copying into a SQL destination?",
      "options": [
        "Automatically generates the destination table's schema based on the source data, if the table doesn't exist yet",
        "Deletes the existing destination table before copying",
        "Only works when copying into Azure Blob Storage",
        "Requires manually writing the CREATE TABLE statement first"
      ],
      "answer": 0,
      "explain": "Auto create table is a fast way to start loading and evaluating data — review the generated schema afterward, since auto-detected types don't always match what you'd design by hand."
    },
    {
      "q": "Why is it a common early mistake to try to force heavy transformation logic into a Copy activity's mapping tab?",
      "options": [
        "The Copy activity is deliberately limited on transformation — real transformation belongs to mapping data flows or an external compute engine",
        "The Copy activity's mapping tab doesn't actually exist",
        "Copy activities can't move data between different data store types",
        "Mapping data flows and Copy activities are the exact same feature"
      ],
      "answer": 0,
      "explain": "The Copy activity handles column mapping and basic type conversion, but real transformation is the job of mapping data flows (Chapter 5) or a dedicated compute engine."
    }
  ]
}
