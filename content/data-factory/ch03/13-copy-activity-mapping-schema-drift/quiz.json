{
  "lesson": "copy-activity-mapping-schema-drift",
  "questions": [
    {
      "q": "With no mapping configured, how does the Copy activity map source data to sink?",
      "options": [
        "By column name, case-sensitively",
        "By column position (ordinal) only",
        "It refuses to run without explicit mapping",
        "By data type, ignoring column names entirely"
      ],
      "answer": 0,
      "explain": "Default mapping matches source and sink columns by name. If the sink doesn't exist yet, the source's names simply become the sink's."
    },
    {
      "q": "What does \"schema drift\" mean in this context?",
      "options": [
        "Source columns changing between pipeline runs, which default mapping can absorb automatically",
        "A pipeline gradually running slower over time",
        "A dataset losing its linked service reference",
        "A sink table being deleted unexpectedly"
      ],
      "answer": 0,
      "explain": "Schema drift describes source columns changing from run to run — a new column added, say. Default, by-name mapping picks up whatever the source returns each time, with no pipeline change required."
    },
    {
      "q": "When would you reach for explicit mapping instead of default mapping?",
      "options": [
        "To copy only part of the source data, rename columns, or reshape data between structures",
        "Only when copying between two identical schemas",
        "Explicit mapping is never actually necessary",
        "Only for Copy activities using a self-hosted integration runtime"
      ],
      "answer": 0,
      "explain": "Explicit mapping handles subsetting columns, renaming them, and reshaping hierarchical data into a tabular sink — none of which default mapping can do."
    },
    {
      "q": "How do you map columns from a source file that has no header row?",
      "options": [
        "By ordinal (column position), since there are no column names to match against",
        "By column name, exactly like any other source",
        "It's impossible to copy a file with no header row",
        "The Copy activity generates random column names automatically"
      ],
      "answer": 0,
      "explain": "With no header row, there's nothing for default by-name mapping to match against — explicit mapping by ordinal (position) is required instead."
    },
    {
      "q": "What does Collection reference do when mapping a hierarchical source to a tabular sink?",
      "options": [
        "Selects the array to iterate over, cross-applying it so one nested object becomes multiple flat rows",
        "Deletes the array field entirely from the output",
        "Only applies to SQL Server sources, never to JSON or MongoDB",
        "Converts the entire source into a single JSON blob column"
      ],
      "answer": 0,
      "explain": "Collection reference tells the mapping which array to flatten — the service cross-applies it against the object's other fields to produce multiple tabular rows."
    }
  ]
}
