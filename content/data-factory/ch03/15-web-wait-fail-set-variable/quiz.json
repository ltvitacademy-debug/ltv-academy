{
  "lesson": "web-wait-fail-set-variable",
  "questions": [
    {
      "q": "What does the Web activity do?",
      "options": [
        "Calls a custom REST endpoint from inside a pipeline",
        "Pauses the pipeline for a specified duration",
        "Deliberately fails the pipeline with a custom message",
        "Sets the value of a pipeline variable"
      ],
      "answer": 0,
      "explain": "Web activity can call GET, POST, PUT, PATCH, or DELETE against a custom REST endpoint, optionally passing datasets and linked services along with the request."
    },
    {
      "q": "What does the Wait activity do?",
      "options": [
        "Pauses the pipeline for a specified number of seconds before continuing",
        "Retries a failed activity automatically",
        "Waits for user approval before running",
        "Delays only the very first activity in a pipeline"
      ],
      "answer": 0,
      "explain": "Wait does exactly one thing: pause execution for a set number of seconds, useful for waiting out a known external delay or spacing out retries deliberately."
    },
    {
      "q": "Why would a pipeline builder deliberately add a Fail activity?",
      "options": [
        "To stop the pipeline loudly, with a clear custom message, when a condition that should be true isn't",
        "Fail activities are only added by accident and should always be removed",
        "To make a pipeline run faster",
        "To automatically retry a failed Copy activity"
      ],
      "answer": 0,
      "explain": "Paired with an If Condition checking something that should be true (like a row count above zero), Fail lets a pipeline stop with a clear explanation rather than silently \"succeeding\" on bad data."
    },
    {
      "q": "What does the Set Variable activity do?",
      "options": [
        "Sets the value of an already-defined pipeline variable",
        "Creates a brand-new linked service",
        "Defines a new dataset's schema",
        "Deletes a pipeline parameter"
      ],
      "answer": 0,
      "explain": "Set Variable writes a value into a pipeline variable that's already been defined at the pipeline level, so a later activity can read it back — Chapter 4 covers variables in full depth."
    },
    {
      "q": "What authentication options does the Web activity support?",
      "options": [
        "Basic, Client Certificate, Service Principal, and Managed Identity",
        "Only anonymous access, with no authentication options at all",
        "Only a shared access signature",
        "Only account key authentication"
      ],
      "answer": 0,
      "explain": "Web activity supports the same broad range of identity options as many connectors — Basic, Client Certificate, Service Principal, and Managed Identity."
    }
  ]
}
