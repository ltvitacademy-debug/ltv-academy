{
  "lesson": "control-flow-error-handling",
  "questions": [
    {
      "q": "What happens in Lesson 54's original pipeline when the on-premises SQL Server is briefly unreachable?",
      "options": [
        "The activity simply fails, with no retry, notification, or record of what happened",
        "The pipeline automatically retries every five minutes forever",
        "An email is sent immediately to the on-call engineer",
        "The pipeline silently skips that night's data with no failure logged at all"
      ],
      "answer": 0,
      "explain": "Without any error handling added, the Copy activity just fails outright — nothing retries, nothing logs it, nothing notifies anyone."
    },
    {
      "q": "What does the retry policy layer specifically handle?",
      "options": [
        "Brief, transient failures like a momentary network blip",
        "A SQL Server that's genuinely down for the entire night",
        "Notifying a human immediately on any failure",
        "Permanently fixing any error without further handling needed"
      ],
      "answer": 0,
      "explain": "The retry policy absorbs short-lived, self-resolving issues automatically — three retries a minute apart handles most transient 3 AM problems with zero human involvement."
    },
    {
      "q": "Why use Try-Catch-Proceed rather than Do-If-Else for logging a sustained failure?",
      "options": [
        "Try-Catch lets the pipeline complete without erroring the whole run, while still leaving an audit trail",
        "Do-If-Else is functionally identical, so it doesn't matter which is used",
        "Try-Catch always causes the pipeline to fail loudly",
        "Do-If-Else can't call a stored procedure at all"
      ],
      "answer": 0,
      "explain": "Try-Catch means a handled failure still lets the pipeline show Success while logging what happened — Do-If-Else would make the same handled failure show as an overall pipeline Failure."
    },
    {
      "q": "Why is the alert scoped specifically to the CopySalesToRawZone activity rather than left unscoped?",
      "options": [
        "So only failures in that specific activity trigger a notification, avoiding noisy, ignorable alerts",
        "Scoping has no real effect on which failures trigger the alert",
        "Unscoped alerts are technically impossible to configure",
        "It's required syntax with no functional purpose"
      ],
      "answer": 0,
      "explain": "Dimension-filtering the alert to one specific activity keeps it meaningful — an unscoped alert firing on any failure anywhere in the factory would quickly become noise people learn to ignore."
    },
    {
      "q": "Why does the pipeline use three separate layers instead of just one alert on any failure?",
      "options": [
        "Each layer only escalates to the next when the previous one genuinely couldn't handle the problem",
        "More layers are always better regardless of what they actually do",
        "The three layers all do exactly the same thing for redundancy",
        "Only the alert layer actually matters; the other two are decorative"
      ],
      "answer": 0,
      "explain": "Retries absorb transient blips silently, Try-Catch-Proceed logs sustained-but-survivable failures without paging anyone, and the alert is the deliberate last resort — escalating only when needed."
    }
  ]
}
