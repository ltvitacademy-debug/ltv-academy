{
  "lesson": "parameters-vs-variables",
  "questions": [
    {
      "q": "Can a pipeline parameter be modified while the pipeline is running?",
      "options": [
        "No — parameters are read-only, set once from outside when the pipeline starts",
        "Yes, using a Set Variable activity",
        "Yes, but only inside a ForEach loop",
        "Parameters don't exist at the pipeline level"
      ],
      "answer": 0,
      "explain": "Parameters control pipeline behavior from outside — supplied as a default or explicit value when the run starts — and can't change during that run."
    },
    {
      "q": "What activity changes a variable's value while a pipeline is running?",
      "options": [
        "Set Variable",
        "Lookup",
        "If Condition",
        "Web"
      ],
      "answer": 0,
      "explain": "Set Variable (and Append Variable, for arrays) is how a variable's value actually changes mid-run — that's the whole point of a variable versus a parameter."
    },
    {
      "q": "What expression references a parameter's value inside a pipeline?",
      "options": [
        "@pipeline().parameters.<name>",
        "@variables('<name>')",
        "@item()",
        "@trigger().startTime"
      ],
      "answer": 0,
      "explain": "Parameters are referenced with @pipeline().parameters.<name>. Variables use the separate @variables('<name>') expression."
    },
    {
      "q": "Which data types are available for a pipeline variable?",
      "options": [
        "String, Bool, or Array only",
        "String, Int, Float, Bool, Array, Object, and SecureString",
        "Only Int and Float",
        "Variables have no type at all"
      ],
      "answer": 0,
      "explain": "Variables support fewer types than parameters — String, Bool, or Array. Parameters additionally support Int, Float, Object, and SecureString."
    },
    {
      "q": "Why is setting a variable inside a parallel ForEach loop risky?",
      "options": [
        "Variables are scoped to the whole pipeline, not thread-safe — parallel iterations can race to read and write the same variable",
        "Variables can't be used inside any ForEach loop at all, sequential or parallel",
        "It's only risky if the variable is a String type",
        "ForEach activities delete all variables automatically when they complete"
      ],
      "answer": 0,
      "explain": "Because a variable belongs to the whole pipeline rather than to one iteration, parallel iterations setting the same variable can produce unpredictable results — pass per-iteration values as parameters to an Execute Pipeline activity instead."
    }
  ]
}
