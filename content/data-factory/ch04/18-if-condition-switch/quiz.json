{
  "lesson": "if-condition-switch",
  "questions": [
    {
      "q": "What does the If Condition activity require its expression to evaluate to?",
      "options": [
        "A boolean — true or false",
        "A string matching one of several cases",
        "An array of activities",
        "A pipeline parameter name"
      ],
      "answer": 0,
      "explain": "If Condition evaluates one expression to true or false, then runs the corresponding True or False set of activities."
    },
    {
      "q": "What does the Switch activity's expression need to evaluate to?",
      "options": [
        "A string value, matched against defined cases",
        "A boolean true or false",
        "An integer between 1 and 25",
        "A pipeline variable's default value"
      ],
      "answer": 0,
      "explain": "Switch evaluates an expression to a string, then runs whichever case's activities match that value, or the default activities if nothing matches."
    },
    {
      "q": "When would you reach for Switch instead of nesting multiple If Conditions?",
      "options": [
        "When there are three or more distinct outcomes from one value — nested If Conditions get hard to read",
        "Switch and If Condition are functionally identical in every scenario",
        "Only when there are exactly two possible outcomes",
        "Switch activities can't have a default case, so use nested Ifs instead"
      ],
      "answer": 0,
      "explain": "Switch expresses many outcomes from one expression flatter and more legibly than nesting an If Condition inside another If Condition's false branch, repeatedly."
    },
    {
      "q": "What is the maximum number of cases a Switch activity supports?",
      "options": [
        "25",
        "5",
        "There is no limit",
        "100"
      ],
      "answer": 0,
      "explain": "A Switch activity supports a maximum of 25 cases. Needing more than that is usually a sign the routing logic belongs in a Lookup-driven table instead."
    },
    {
      "q": "What runs in a Switch activity when the expression doesn't match any defined case?",
      "options": [
        "The Default activities",
        "The pipeline fails immediately with no explanation",
        "The first case's activities, as a fallback",
        "Nothing — Switch requires an exact match or it throws a compile error"
      ],
      "answer": 0,
      "explain": "Default activities handle anything that doesn't match any of the defined cases, ensuring the Switch always has somewhere to go."
    }
  ]
}
