{
  "lesson": "datasets-structure-schema",
  "questions": [
    {
      "q": "What does a dataset actually represent?",
      "options": [
        "A named view pointing to specific data — a table, file, or folder — inside a linked service",
        "The connection information needed to reach a data store",
        "A scheduled time for a pipeline to run",
        "A visual chart built from pipeline monitoring data"
      ],
      "answer": 0,
      "explain": "A dataset points to specific data reachable through a linked service. The linked service is the connection; the dataset is what inside that connection you actually mean."
    },
    {
      "q": "Where do you go in Data Factory Studio to create a new dataset?",
      "options": [
        "The Author hub, select the + icon, then Dataset",
        "The Monitor hub",
        "The Home page's Configure SSIS tile",
        "Datasets can only be created via PowerShell, not the Studio UI"
      ],
      "answer": 0,
      "explain": "Author hub → + icon → Dataset opens the same connector gallery used for linked services, then walks through format and connection selection."
    },
    {
      "q": "What does a dataset's \"format\" setting actually specify?",
      "options": [
        "How the underlying data is structured — DelimitedText, JSON, Parquet, Binary, and so on",
        "Which Azure region the dataset's data lives in",
        "The color scheme used to display the dataset in Monitor",
        "Whether the dataset is public or private"
      ],
      "answer": 0,
      "explain": "Format describes the physical shape of the data itself — a CSV needs DelimitedText, a columnar file needs Parquet, and so on."
    },
    {
      "q": "What does Import Schema do?",
      "options": [
        "Reads the actual column names and types directly from the live source",
        "Deletes the dataset's existing schema permanently",
        "Converts the dataset to a different file format automatically",
        "Publishes the dataset to the Power BI service"
      ],
      "answer": 0,
      "explain": "Import Schema reads the real shape of the data straight from the source — the right default almost every time, versus basing it on a local file when no live source is available yet."
    },
    {
      "q": "Can a single linked service support more than one dataset?",
      "options": [
        "Yes — one linked service can back many datasets, each pointing at a different location within it",
        "No — each dataset requires its own separate linked service",
        "Only if both datasets use the exact same file format",
        "Only in Fabric Data Factory, never in Azure Data Factory"
      ],
      "answer": 0,
      "explain": "Since a linked service only defines the connection, many datasets can reuse the same one, each pointing at a different container, folder, file, or table."
    }
  ]
}
