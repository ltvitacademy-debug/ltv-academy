{
  "lesson": "why-analysts-need-the-data-model",
  "questions": [
    {
      "q": "Per this lesson, what's the key difference between a syntax error and a data-model error in a query?",
      "options": [
        "A syntax error fails loudly and is noticed immediately; a data-model error runs fine and returns a real but wrong-meaning result",
        "There is no real difference between them",
        "A data-model error always crashes the Salesforce org",
        "A syntax error is more dangerous because it corrupts data"
      ],
      "answer": 0,
      "explain": "A data-model error is more dangerous precisely because it doesn't announce itself — the query succeeds and looks plausible."
    },
    {
      "q": "In the double-counting example, why does joining Account to Opportunity through Contact produce a wrong pipeline total?",
      "options": [
        "Because an Opportunity gets counted once for every Contact linked to that Account, inflating the total",
        "Because Contacts and Opportunities are actually the same object",
        "Because Opportunities are deleted when Contacts are added",
        "Because Accounts can never have more than one Contact"
      ],
      "answer": 0,
      "explain": "Joining through Contact multiplies each Opportunity by however many Contacts exist on that Account, rather than counting it once."
    },
    {
      "q": "What would the correct relationship path be for a \"total pipeline value per Account\" query, per this lesson?",
      "options": [
        "Account joined directly to Opportunity, without routing through Contact",
        "Contact joined directly to Opportunity, without involving Account at all",
        "Opportunity joined to itself",
        "There is no correct path; the number can't be calculated"
      ],
      "answer": 0,
      "explain": "The lesson states the correct path is Account to Opportunity directly, not through Contact."
    },
    {
      "q": "Why does this course teach Chapters 2 through 6 on objects and relationships before teaching SOQL syntax in depth?",
      "options": [
        "Because understanding objects and how they connect lets an analyst catch bad joins that query syntax alone can't prevent",
        "Because SOQL syntax is too difficult to teach to beginners",
        "Because Salesforce doesn't actually support a query language",
        "There's no stated reason; it's an arbitrary ordering"
      ],
      "answer": 0,
      "explain": "The lesson explicitly frames data-model understanding as a prerequisite that prevents silent, syntax-valid errors like double-counting."
    },
    {
      "q": "What does this lesson mean by a query \"failing silently\"?",
      "options": [
        "The query runs successfully and returns a real, correctly formatted number that doesn't mean what the analyst assumed",
        "The query throws a visible error message that halts execution",
        "The query returns no rows at all",
        "The Salesforce org crashes without any error message"
      ],
      "answer": 0,
      "explain": "A silent failure looks like success — a real result, formatted correctly — while still being conceptually wrong."
    }
  ]
}
