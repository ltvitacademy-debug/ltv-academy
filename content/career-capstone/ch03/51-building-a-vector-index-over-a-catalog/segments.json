[
  {"slide": "s1", "text": "Lesson 50 named the four RAG stages conceptually. This lesson makes the first two concrete, using a data catalog's table and column descriptions as the actual content to embed and index."},
  {"slide": "s2", "text": "A chunk is the unit of content you embed and later retrieve. For a catalog, the natural chunk is one table's full profile — name, description, and columns together — not one chunk per column."},
  {"slide": "s3", "text": "Each chunk's text goes through an embedding model and comes out as a fixed-length vector. Every chunk and every question must be embedded with the same model, or the vectors aren't comparable."},
  {"slide": "s4", "text": "A vector index isn't just a list of vectors — each entry also stores the original chunk text and metadata like table name and workspace, so a match can be traced back to where it came from."},
  {"slide": "s5", "text": "That metadata is what lets the next lesson's pipeline return which table an answer came from, so it can be verified. Next up: wiring embed, index, retrieve, and generate into a working pipeline over Fabric's metadata."}
]
