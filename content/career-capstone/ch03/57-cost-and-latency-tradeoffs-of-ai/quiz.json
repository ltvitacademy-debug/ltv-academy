{
  "lesson": "cost-and-latency-tradeoffs-of-ai",
  "questions": [
    {
      "q": "How does an LLM call's cost and latency compare to a deterministic transformation like a CASE statement or regex?",
      "options": [
        "An LLM call costs real tokens and takes hundreds of milliseconds to seconds, while a deterministic rule costs microseconds and effectively nothing",
        "They are functionally identical in cost and latency",
        "Deterministic rules always cost more than LLM calls",
        "LLM calls have no latency because they run instantly"
      ],
      "answer": 0,
      "explain": "Both cost and latency are real per-call numbers for an LLM, unlike a deterministic rule that runs inline for nearly free."
    },
    {
      "q": "What happens when a pipeline calls an LLM once per row at production data volumes?",
      "options": [
        "The per-call cost and latency multiply into real daily dollar costs and a latency bottleneck that can violate SLAs",
        "The cost and latency stay exactly the same as a 50-row demo",
        "LLM calls automatically batch themselves at no extra cost",
        "Per-row LLM calls always run faster at higher volume"
      ],
      "answer": 0,
      "explain": "A demo that looks fine with 50 rows can turn into thousands of dollars a day and a real SLA problem at a million rows a day."
    },
    {
      "q": "When does a deterministic rule (lookup table, regex, small rules engine) beat an LLM call inside a pipeline?",
      "options": [
        "When the logic is enumerable, cases are a lookup, and volume is high",
        "Only when the input is unstructured text",
        "Only when the pipeline runs offline",
        "Never — LLMs always outperform deterministic rules"
      ],
      "answer": 0,
      "explain": "Classifying a transaction from a country code or normalizing a phone number is a rule, not a prompt, and wins on cost, latency, and predictability."
    },
    {
      "q": "According to this lesson, where does an LLM call's cost still pay off inside a pipeline context?",
      "options": [
        "A one-time batch job or offline pass, like classifying a backlog or generating documentation, that never touches the live latency budget",
        "Any per-row call inside a real-time streaming path",
        "Anywhere volume is extremely high and the case is enumerable",
        "Nowhere — this lesson concludes LLMs should never run in a pipeline"
      ],
      "answer": 0,
      "explain": "The trade-off is about where the call runs, not whether to ever use one — offline or one-time work pays the cost once."
    },
    {
      "q": "What is the core trade-off principle this lesson establishes for AI-augmented pipelines?",
      "options": [
        "Never put an LLM call where a cheaper rule would do, and never put one in a path that has to hit a real-time SLA",
        "Always replace deterministic rules with LLM calls for consistency",
        "LLM calls should only ever be used per-row for maximum coverage",
        "Cost and latency don't matter as long as the output sounds correct"
      ],
      "answer": 0,
      "explain": "This is the lesson's explicit conclusion: choose the cheaper, faster tool for enumerable, high-volume, or SLA-bound work."
    }
  ]
}
