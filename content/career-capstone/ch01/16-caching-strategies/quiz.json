{
  "lesson": "caching-strategies",
  "questions": [
    {
      "q": "What does a cache actually store, in an analytics system?",
      "options": [
        "The result of expensive work, somewhere fast to read, so a repeat request doesn't redo that work",
        "A permanent, unmodifiable copy of the raw bronze layer",
        "Only the schema of a table, not its data",
        "A backup used exclusively for disaster recovery"
      ],
      "answer": 0,
      "explain": "Fabric's Import mode is exactly this — a scheduled copy of gold, not recomputed on every report open."
    },
    {
      "q": "In what sense is a gold table itself a caching strategy?",
      "options": [
        "The expensive aggregation runs once, on a schedule, and every read afterward is cheap — pre-aggregation, functionally the same as caching",
        "Gold tables never actually store any precomputed results",
        "Gold tables are only used for raw, unmodified data",
        "Caching and pre-aggregation are unrelated concepts with no overlap"
      ],
      "answer": 0,
      "explain": "Databricks & Delta Lake Lesson 30's overwrite pattern is, functionally, pre-aggregation as caching."
    },
    {
      "q": "What's the real distinction between pre-aggregation and result-set caching?",
      "options": [
        "Pre-aggregation computes a known, recurring question ahead of time; result-set caching stores whatever query just ran, for reuse if it repeats",
        "They are two names for exactly the same technique",
        "Result-set caching only works on OLTP systems",
        "Pre-aggregation can only be used for streaming data, never batch"
      ],
      "answer": 0,
      "explain": "Pre-aggregation only helps if the question was known in advance; result-set caching helps ad hoc queries only if the same one repeats."
    },
    {
      "q": "Why is invalidation described as the genuinely hard part of caching, not storage?",
      "options": [
        "A cache that's never invalidated serves confidently wrong numbers forever, while invalidating too aggressively defeats the cache's own purpose",
        "Invalidation is actually simpler than storing the cached result in the first place",
        "Invalidation only matters for OLTP databases, never analytics caches",
        "Modern caching systems have fully automated invalidation with no design decisions required"
      ],
      "answer": 0,
      "explain": "Knowing when a cached answer has gone stale is where real caching designs actually fail — storing the result is the easy half."
    },
    {
      "q": "Why are caching strategy and freshness requirements described as 'the same design decision viewed from two different layers'?",
      "options": [
        "Every caching decision is a bet on how much staleness a consumer will tolerate — the same bet Lesson 18's freshness SLAs make explicit",
        "Caching and freshness SLAs have no real relationship to each other",
        "Freshness SLAs are decided entirely independently of any caching strategy",
        "Only streaming systems need to consider both caching and freshness together"
      ],
      "answer": 0,
      "explain": "Getting one wrong without checking the other is how a dashboard ends up confidently showing yesterday's numbers as today's."
    }
  ]
}
