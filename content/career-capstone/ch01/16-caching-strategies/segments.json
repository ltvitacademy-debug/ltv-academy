[
  {"slide": "s1", "text": "A cache stores the result of expensive work somewhere fast to read, so a repeat request doesn't redo it. Fabric's Import mode is exactly this — a scheduled copy of gold, not recomputed on every open."},
  {"slide": "s2", "text": "A gold table is, functionally, a caching strategy — the expensive aggregation runs once, on a schedule, and every read afterward is cheap. Databricks Lesson 30's overwrite pattern is pre-aggregation as caching."},
  {"slide": "s3", "text": "Pre-aggregation only helps if you knew the question in advance, built into gold itself. Result-set caching helps with ad hoc queries, but only pays off if the same query actually repeats."},
  {"slide": "s4", "text": "Storing a cached result is the easy half. Knowing when it's gone stale is where real designs fail — never invalidating serves wrong numbers forever; invalidating too aggressively defeats the whole point."},
  {"slide": "s5", "text": "Caching strategy and freshness requirements are the same decision, viewed from two layers. Next up: designing for multi-tenancy — the next real constraint a system design has to absorb."}
]
