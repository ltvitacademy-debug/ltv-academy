[
  {"slide": "s1", "text": "Production-ready doesn't mean \"runs once successfully\" — it means surviving the failure modes that eventually show up: malformed events, transient errors, a briefly unavailable table."},
  {"slide": "s2", "text": "A malformed event gets rejected to a dead-letter table. A duplicate redelivery gets absorbed silently by the idempotent merge already built into ingestion. A missing export file alerts."},
  {"slide": "s3", "text": "Idempotent MERGE is what makes \"just retry it\" a safe recovery strategy instead of a data-corruption risk — a retry produces the same result either way."},
  {"slide": "s4", "text": "Logs, metrics, and traces map onto this pipeline directly. The alert threshold fires well before the SLO would actually breach — an early warning, not a postmortem."},
  {"slide": "s5", "text": "A pipeline that survives its own failure modes and tells you before it breaks an SLA. Next up: wrapping up Project 1 with an honest retrospective."}
]
