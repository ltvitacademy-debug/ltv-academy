{
  "lesson": "monitoring-kafka",
  "questions": [
    {
      "q": "What is described as the single most important Kafka metric to watch?",
      "options": [
        "Consumer lag — the gap between the latest offset written and what a consumer group has actually committed",
        "The number of topics in the cluster",
        "The Kafka version number currently installed",
        "The number of producers connected at any given time"
      ],
      "answer": 0,
      "explain": "Climbing lag directly means a consumer is falling behind the rate events are arriving — the clearest sign real-time processing is breaking down."
    },
    {
      "q": "What does an under-replicated partition indicate?",
      "options": [
        "One or more replicas has fallen behind or is unreachable, thinning the safety margin replication is supposed to provide",
        "The partition has too many replicas and needs fewer",
        "The topic has been deleted",
        "The consumer group has too many members"
      ],
      "answer": 0,
      "explain": "It's an early-warning signal that precedes a real outage, not necessarily an outage itself, tying directly back to Lesson 11's replication coverage."
    },
    {
      "q": "Why does broker disk usage matter as an ongoing concern rather than a one-time decision?",
      "options": [
        "Retention deliberately holds data for a configured window, so disk usage keeps accumulating as long as that window is active",
        "Disk usage is irrelevant once a broker is first provisioned",
        "Kafka automatically deletes all data every hour regardless of settings",
        "Disk usage only matters for consumers, never for brokers"
      ],
      "answer": 0,
      "explain": "Because retention (Lesson 13) keeps data around for a set period, disk usage is a continuous concern tied to that setting, not a one-time sizing exercise."
    },
    {
      "q": "How does consumer lag compare to the watermark/late-data concepts from Fabric & Real-Time Analytics, per this lesson?",
      "options": [
        "Both answer the same underlying question — how far behind is processing relative to when things actually happened — just with different vocabulary",
        "They are completely unrelated concepts with no similarity",
        "Watermarks replace the need for consumer lag entirely",
        "Consumer lag only applies to batch processing, not streaming"
      ],
      "answer": 0,
      "explain": "Kafka expresses this as an offset or time gap; Fabric expresses it against event time and a watermark threshold — same underlying concern, different vocabulary."
    },
    {
      "q": "What does the cluster detail header screenshot in this lesson actually show?",
      "options": [
        "A real Confluent Cloud cluster's settings and monitoring header — cluster ID, type, and status for an actual provisioned cluster",
        "A conceptual diagram with no relationship to a real deployment",
        "The Azure Portal's billing summary page",
        "Java source code for a Kafka Streams application"
      ],
      "answer": 0,
      "explain": "It's a genuine screenshot from Confluent's own documentation, the entry point to the throughput and partition-health metrics this lesson covers."
    }
  ]
}
