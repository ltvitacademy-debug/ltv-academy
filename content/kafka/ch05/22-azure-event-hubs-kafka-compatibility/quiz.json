{
  "lesson": "azure-event-hubs-kafka-compatibility",
  "questions": [
    {
      "q": "What is Azure Event Hubs' Kafka-compatible endpoint, per this lesson?",
      "options": [
        "A protocol-level bridge that lets real Kafka client code connect to Event Hubs, often with just a connection-string and port change",
        "A full open-source reimplementation of every Kafka feature including Connect and Streams",
        "A tool that converts Kafka topics into Azure SQL tables automatically",
        "A separate product entirely unrelated to Azure Event Hubs"
      ],
      "answer": 0,
      "explain": "It's a compatibility layer on top of Event Hubs' native API, not a separate Kafka distribution."
    },
    {
      "q": "What actually has to change in an existing Kafka producer/consumer application to point it at Event Hubs instead of a real broker?",
      "options": [
        "Configuration only — the broker address and authentication mechanism — not the producer/consumer application logic itself",
        "The entire application must be rewritten from scratch in a different language",
        "Nothing at all needs to change, including configuration",
        "The Kafka client library must be replaced with an Azure-specific SDK"
      ],
      "answer": 0,
      "explain": "The same KafkaProducer/KafkaConsumer classes and send()/poll() calls are used — only bootstrap.servers and the SASL_SSL auth config change."
    },
    {
      "q": "According to this lesson, which part of Kafka compatibility is only partially supported by Event Hubs?",
      "options": [
        "The Kafka Admin API — some administrative operations instead go through Azure's own control plane",
        "The basic producer send() call",
        "The basic consumer poll() call",
        "TCP networking itself"
      ],
      "answer": 0,
      "explain": "Not every Admin API call available against a self-hosted cluster or Confluent Cloud has a direct Event Hubs equivalent."
    },
    {
      "q": "Why isn't it guaranteed that Kafka Connect, Kafka Streams, or ksqlDB will work the same way against Event Hubs as against real Kafka?",
      "options": [
        "Those tools are built expecting a real Kafka-protocol-speaking cluster underneath, and Event Hubs' compatibility layer doesn't guarantee full support across that whole ecosystem",
        "Kafka Connect, Streams, and ksqlDB don't exist as concepts",
        "Event Hubs runs a full copy of Kafka Connect automatically for every namespace",
        "This limitation only affects Confluent Cloud, not Event Hubs"
      ],
      "answer": 0,
      "explain": "Some connectors work against the endpoint, but it isn't a blanket guarantee the way it is on real Kafka or Confluent Cloud."
    },
    {
      "q": "How does this lesson connect to Terraform & Bicep Lesson 10?",
      "options": [
        "Lesson 10 covers actually provisioning the Event Hub resource with infrastructure as code; this lesson covers what you can point existing Kafka client code at once that resource exists",
        "Terraform & Bicep Lesson 10 is about deploying Kafka Streams applications",
        "There is no connection between the two lessons",
        "Lesson 10 replaces the need to learn Kafka's Admin API"
      ],
      "answer": 0,
      "explain": "The provisioning step and the client-compatibility step are two different, complementary parts of using Event Hubs with Kafka tooling."
    }
  ]
}
