[
  {"slide": "s1", "text": "Every consumer with the same group.id is a member of the same consumer group. Kafka's real guarantee: within one group, each partition is assigned to exactly one member at a time."},
  {"slide": "s2", "text": "That's what turns many independent consumer processes into one logical, horizontally scaled consumer. Two consumers in the same group never both process the same partition at the same time."},
  {"slide": "s3", "text": "A rebalance is the group coordinator re-running that assignment — triggered by a consumer joining, one leaving or crashing, or the partition count changing. Partitions briefly stop being consumed while it happens."},
  {"slide": "s4", "text": "Kafka never assigns more than one consumer per partition within a group, so a partition is the smallest unit of parallelism a group can use. Extra consumers beyond the partition count just sit idle."},
  {"slide": "s5", "text": "Parallelism in a consumer group is capped by partition count, full stop. Next up: at-most, at-least, and exactly-once delivery."}
]
