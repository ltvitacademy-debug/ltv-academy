[
  {
    "type": "title",
    "eyebrow": "Kafka & Event Streaming · Chapter Four",
    "titlePlain": "Kafka Streams ",
    "titleEmphasis": "Basics",
    "titleSuffix": "",
    "subtitle": "Lesson 18 · Kafka Connect & Stream Processing"
  },
  {
    "type": "code",
    "eyebrow": "The topology pattern",
    "code": "StreamsBuilder builder = new StreamsBuilder();\nKStream<String, Order> orders = builder.stream(\"orders\");\n\norders\n    .filter((key, order) -> order.getAmount() > 100)\n    .mapValues(order -> order.withStatus(\"high-value\"))\n    .to(\"high-value-orders\");",
    "caption": "Source topic in, transform, output topic out — a continuous pipeline."
  },
  {
    "type": "code",
    "eyebrow": "Stateless vs. stateful",
    "code": "// Stateless -- no memory of prior events\norders.filter((key, order) -> order.getAmount() > 100);\n\n// Stateful -- backed by a local state store + changelog topic\nKTable<String, Long> countsByCustomer = orders\n    .groupBy((key, order) -> order.getCustomerId())\n    .count();",
    "caption": "State stores are replicated to Kafka so they survive a failure."
  },
  {
    "type": "steps",
    "eyebrow": "Kafka's stream-processing layer",
    "titlePlain": "Three ways to ",
    "titleEmphasis": "process",
    "titleSuffix": " a topic",
    "steps": [
      {"label": "Kafka Connect", "sub": "Move data in/out of topics, no code"},
      {"label": "Kafka Streams", "sub": "Java library, full application code, own scaling"},
      {"label": "ksqlDB", "sub": "Same engine, SQL syntax instead of Java"}
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 19",
    "titlePlain": "ksqlDB ",
    "titleEmphasis": "Basics",
    "titleSuffix": "",
    "subtitle": "The SQL layer over the same idea."
  }
]
