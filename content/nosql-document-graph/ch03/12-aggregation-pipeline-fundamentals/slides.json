[
  {
    "type": "title",
    "eyebrow": "NoSQL, Document & Graph Databases · Chapter Three",
    "titlePlain": "Aggregation Pipeline ",
    "titleEmphasis": "Fundamentals",
    "titleSuffix": "",
    "subtitle": "Lesson 12 · MongoDB Querying & Aggregation"
  },
  {
    "type": "code",
    "eyebrow": "A pipeline is an array of stages",
    "code": "db.orders.aggregate([\n  { $match: { status: \"shipped\" } },\n  { $sort: { total: -1 } },\n  { $limit: 10 }\n])",
    "caption": "Each stage's output becomes the next stage's input — read left to right."
  },
  {
    "type": "code",
    "eyebrow": "$match: filter early, same operators as find()",
    "code": "{\n  $match: {\n    status: \"shipped\",\n    total: { $gt: 100 }\n  }\n}",
    "caption": "An early $match can use an index, just like a WHERE clause in SQL Server."
  },
  {
    "type": "code",
    "eyebrow": "$project: reshape and compute mid-pipeline",
    "code": "{\n  $project: {\n    customerName: 1,\n    total: 1,\n    isLargeOrder: { $gt: [\"$total\", 500] },\n    _id: 0\n  }\n}",
    "caption": "Like a computed column in a SELECT list, not just a pass-through field."
  },
  {
    "type": "steps",
    "eyebrow": "Reading a pipeline left to right",
    "titlePlain": "Match, Sort, ",
    "titleEmphasis": "Limit",
    "titleSuffix": "",
    "steps": [
      { "label": "$match", "sub": "filter down, same as WHERE" },
      { "label": "$sort", "sub": "order the results" },
      { "label": "$limit", "sub": "cap the output" }
    ]
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 13",
    "titlePlain": "Advanced Aggregation: $lookup, ",
    "titleEmphasis": "$group & $facet",
    "titleSuffix": "",
    "subtitle": "Joins, grouping, and parallel pipelines on the same input."
  }
]
