{
  "lesson": "the-mongodb-query-language-filters-and-projections",
  "questions": [
    {
      "q": "What does `db.orders.find({ total: { $gt: 100 } })` return?",
      "options": [
        "Orders where total is greater than 100",
        "Orders where total equals 100",
        "Orders where total is greater than or equal to 100",
        "All orders, sorted by total descending"
      ],
      "answer": 0,
      "explain": "$gt is the strict greater-than comparison operator, equivalent to SQL's > ."
    },
    {
      "q": "In `db.orders.find({ status: \"shipped\", total: { $gt: 100 } })`, how are the two conditions combined?",
      "options": [
        "With an implicit AND — listing multiple fields in a filter document ANDs them together with no operator needed",
        "With an implicit OR",
        "This is invalid syntax and would throw an error",
        "Only the second condition (total) is actually applied"
      ],
      "answer": 0,
      "explain": "MongoDB ANDs multiple fields in a filter document together by default, the same as comma-separated WHERE conditions joined with AND in SQL."
    },
    {
      "q": "What does the projection `{ customerName: 1, total: 1, _id: 0 }` do?",
      "options": [
        "Returns only the customerName and total fields, and explicitly excludes _id (which would otherwise return by default)",
        "Returns every field except customerName and total",
        "Returns only the _id field",
        "Sorts results by customerName and total"
      ],
      "answer": 0,
      "explain": "1 includes a field, 0 excludes it; _id is included by default so it must be explicitly set to 0 to leave it out."
    },
    {
      "q": "Which rule is true about mixing inclusion and exclusion in a single projection document?",
      "options": [
        "You can't mix 1 and 0 for other fields in the same projection — except _id, which can always be excluded alongside an inclusion projection",
        "You can freely mix as many included and excluded fields as you want",
        "Projections only support exclusion, never inclusion",
        "Projections require listing every field in the collection's schema"
      ],
      "answer": 0,
      "explain": "Outside of the _id special case, a projection must be either all-inclusion or all-exclusion, not a mix of both."
    },
    {
      "q": "What is the SQL equivalent of MongoDB's $in operator?",
      "options": [
        "IN, for matching a field against a list of possible values",
        "JOIN, for combining two tables",
        "LIKE, for pattern matching",
        "GROUP BY, for aggregating rows"
      ],
      "answer": 0,
      "explain": "$in matches when a field's value is any of the values in the given array, the same as SQL's IN (...) list."
    }
  ]
}
