{
  "lesson": "neo4j-authentication-and-role-based-access-control",
  "questions": [
    {
      "q": "What state is Neo4j's default neo4j account created in, and what does that state actually mean?",
      "options": [
        "password_change_required — the server rejects every query except changing the password",
        "fully_privileged — it has admin access from the start with no restrictions",
        "disabled — it cannot be used to log in at all",
        "read_only — it can query but never write"
      ],
      "answer": 0,
      "explain": "Neo4j refuses to let the default account do anything useful until its password has been changed, similar to the instinct behind disabling SQL Server's sa account."
    },
    {
      "q": "What are Neo4j's built-in roles, from least to most privileged?",
      "options": [
        "PUBLIC, reader, editor, publisher, architect, admin",
        "guest, user, moderator, superuser",
        "read, write, admin — only three roles exist",
        "There are no built-in roles in Neo4j"
      ],
      "answer": 0,
      "explain": "Each tier adds a coherent slice of capability on top of the last, similar in spirit to SQL Server's fixed database roles."
    },
    {
      "q": "What does the DENY privilege do in Neo4j's fine-grained access control?",
      "options": [
        "It overrides any GRANT for that same privilege — the same instinct as an explicit DENY overriding a GRANT in SQL Server",
        "DENY has no real effect and is purely documentation",
        "DENY only works for the admin role",
        "DENY permanently deletes the affected data"
      ],
      "answer": 0,
      "explain": "DENY in Neo4j's privilege system overrides a conflicting GRANT, mirroring SQL Server's DENY-wins behavior."
    },
    {
      "q": "What is the real Community vs. Enterprise limitation this lesson flags for Neo4j RBAC?",
      "options": [
        "Custom roles and fine-grained privilege grants are an Enterprise Edition feature — Community only supports basic username/password authentication with no role-based access control",
        "Community Edition has no authentication at all",
        "Enterprise Edition removes role-based access control entirely",
        "There is no licensing difference between Community and Enterprise for security features"
      ],
      "answer": 0,
      "explain": "This is a genuinely different licensing line than SQL Server, where basic role-based security is available in every edition — worth knowing before promising fine-grained access control on Community."
    },
    {
      "q": "What does the TRAVERSE privilege control in Neo4j, as distinct from READ?",
      "options": [
        "Whether a user can even see that a node exists, while READ controls which properties they can see on nodes they can traverse to",
        "TRAVERSE and READ are identical and interchangeable",
        "TRAVERSE only applies to relationships, never nodes",
        "TRAVERSE controls write access, and READ controls read access"
      ],
      "answer": 0,
      "explain": "TRAVERSE gates visibility of the node itself; READ gates visibility of specific properties on nodes already visible via TRAVERSE."
    }
  ]
}
