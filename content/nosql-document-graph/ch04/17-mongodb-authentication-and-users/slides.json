[
  {
    "type": "title",
    "eyebrow": "NoSQL, Document & Graph Databases · Chapter Four",
    "titlePlain": "MongoDB Authentication ",
    "titleEmphasis": "& Users",
    "titleSuffix": "",
    "subtitle": "Lesson 17 · MongoDB Security & Administration"
  },
  {
    "type": "code",
    "eyebrow": "The default-off gotcha",
    "code": "mongod\n// no --auth flag\n\nmongosh\n// connects instantly — full read/write\n// no username, no password",
    "caption": "A fresh install has authentication disabled by default. Turn it on yourself."
  },
  {
    "type": "code",
    "eyebrow": "Creating the first user, then enabling auth",
    "code": "use admin\ndb.createUser({\n  user: \"dbAdmin\",\n  pwd: passwordPrompt(),\n  roles: [{ role: \"userAdminAnyDatabase\", db: \"admin\" }]\n})\n\n// mongod.conf:\n// security:\n//   authorization: enabled",
    "caption": "Create the admin user while auth is still off, then flip authorization: enabled."
  },
  {
    "type": "code",
    "eyebrow": "Connecting once auth is on",
    "code": "mongosh -u dbAdmin -p \\\n  --authenticationDatabase admin\n\n// SCRAM-SHA-256 by default since v4.0",
    "caption": "SCRAM authenticates without ever sending the password itself over the wire."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 18",
    "titlePlain": "Role-Based Access ",
    "titleEmphasis": "Control",
    "titleSuffix": "",
    "subtitle": "Scoping exactly what each authenticated user can do."
  }
]
