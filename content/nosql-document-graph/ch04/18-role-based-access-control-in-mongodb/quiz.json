{
  "lesson": "role-based-access-control-in-mongodb",
  "questions": [
    {
      "q": "What does the built-in readWrite role grant?",
      "options": [
        "Read and write access to a database's data, without administrative privileges",
        "Full superuser access to the entire deployment",
        "Only the ability to create and drop indexes",
        "Only the ability to manage users, not touch data"
      ],
      "answer": 0,
      "explain": "readWrite grants read and write access to a database's data but stops short of admin tasks like user management."
    },
    {
      "q": "What does the dbOwner role combine?",
      "options": [
        "readWrite, dbAdmin, and userAdmin on a single database — effectively full control of that database",
        "read access across every database on the deployment",
        "Only userAdmin, with no data access at all",
        "root access across the entire cluster"
      ],
      "answer": 0,
      "explain": "dbOwner bundles readWrite, dbAdmin, and userAdmin into full control scoped to one database."
    },
    {
      "q": "What does db.createRole() let you do that built-in roles can't?",
      "options": [
        "Grant an exactly-scoped set of actions on an exact resource, like only find on one specific collection",
        "Create a new database automatically",
        "Bypass authentication entirely for that role",
        "Assign a role without ever needing db.grantRolesToUser()"
      ],
      "answer": 0,
      "explain": "Custom roles let you define privileges down to specific actions on specific resources, narrower than any built-in role."
    },
    {
      "q": "Can a single MongoDB user hold multiple roles scoped to different databases?",
      "options": [
        "Yes — db.grantRolesToUser() accepts an array of {role, db} pairs",
        "No, a user can only ever have one role total",
        "Only if all the roles are custom roles",
        "Only the root role can span multiple databases"
      ],
      "answer": 0,
      "explain": "grantRolesToUser takes an array, so a user can hold different roles scoped to different databases simultaneously."
    },
    {
      "q": "Per this lesson, what is the recommended practice for an application's database connection user?",
      "options": [
        "Grant exactly the roles its workload needs, typically scoped readWrite on its own database — not root or dbOwner",
        "Always use the root role so the application never hits a permissions error",
        "Applications should never authenticate at all",
        "Give every application user the dbOwner role on every database"
      ],
      "answer": 0,
      "explain": "The lesson reinforces least privilege: broad roles like root or dbOwner should be reserved for actual administrators, not embedded in application connections."
    }
  ]
}
