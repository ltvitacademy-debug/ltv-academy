{
  "lesson": "mongodb-shell-mongosh-and-crud-operations",
  "questions": [
    {
      "q": "What is mongosh, and what did it replace?",
      "options": [
        "MongoDB's current, actively-developed command-line shell, which replaced the legacy `mongo` shell",
        "A GUI tool that replaced MongoDB Compass",
        "A cloud-only shell with no local connection support",
        "A deprecated tool being replaced by the legacy `mongo` shell"
      ],
      "answer": 0,
      "explain": "mongosh is the current shell; the legacy `mongo` shell is deprecated and no longer bundled as the default starting with MongoDB 6.0."
    },
    {
      "q": "What happens if you call `db.users.insertOne({...})` on a collection that doesn't exist yet?",
      "options": [
        "MongoDB creates the collection automatically on the first insert",
        "The command fails with an error requiring you to create the collection first",
        "MongoDB silently discards the insert",
        "It inserts into a default fallback collection instead"
      ],
      "answer": 0,
      "explain": "Consistent with MongoDB's schema-less-by-default model, collections are created automatically on first write."
    },
    {
      "q": "What does `db.users.find({ loyaltyTier: \"gold\" })` return?",
      "options": [
        "A cursor over every document in the users collection where loyaltyTier equals \"gold\"",
        "Only the first matching document",
        "A count of matching documents",
        "Every document in the collection, ignoring the filter"
      ],
      "answer": 0,
      "explain": "find() takes a filter document and returns a cursor over all matches; findOne() is the one that returns just the first match."
    },
    {
      "q": "What does the `$set` operator do in an update command like `updateOne({filter}, { $set: { loyaltyTier: \"platinum\" } })`?",
      "options": [
        "Updates only the named field(s), leaving the rest of the document untouched",
        "Deletes every field not explicitly listed",
        "Replaces the entire document with only the listed fields",
        "Creates a new document instead of updating the existing one"
      ],
      "answer": 0,
      "explain": "$set is scoped to the fields you name — it does not touch or remove any other field in the document."
    },
    {
      "q": "Why is `db.users.deleteMany({})` considered genuinely destructive, per this lesson?",
      "options": [
        "An empty filter matches every document in the collection, so it removes all of them",
        "It only works on empty collections, so it's harmless",
        "deleteMany requires a non-empty filter and would simply error out",
        "It deletes the entire database, not just the collection"
      ],
      "answer": 0,
      "explain": "The filter document decides what's affected — an empty filter matches everything, so deleteMany({}) clears the whole collection."
    }
  ]
}
