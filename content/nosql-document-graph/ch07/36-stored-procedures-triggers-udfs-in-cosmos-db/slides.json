[
  {
    "type": "title",
    "eyebrow": "NoSQL, Document & Graph Databases · Chapter Seven",
    "titlePlain": "Stored Procedures, Triggers ",
    "titleEmphasis": "& UDFs",
    "titleSuffix": "",
    "subtitle": "Lesson 36 · Cosmos DB Querying & Application Integration"
  },
  {
    "type": "code",
    "eyebrow": "Stored procedures: JavaScript, one partition",
    "code": "function createOrder(orderDoc) {\n  var container = getContext().getCollection();\n  container.createDocument(\n    container.getSelfLink(), orderDoc,\n    function (err, created) {\n      if (err) throw new Error(err.message);\n    }\n  );\n}",
    "caption": "Multi-statement atomicity — scoped to one logical partition, not the whole database."
  },
  {
    "type": "steps",
    "eyebrow": "Triggers: nothing fires automatically",
    "titlePlain": "Pre-trigger vs. ",
    "titleEmphasis": "post-trigger",
    "titleSuffix": "",
    "steps": [
      { "label": "Client names the trigger", "sub": "on the create/replace/delete call" },
      { "label": "Pre-trigger", "sub": "runs before — validate or modify" },
      { "label": "Post-trigger", "sub": "runs after — audit or update related docs" }
    ]
  },
  {
    "type": "code",
    "eyebrow": "UDFs: scalar functions inside queries",
    "code": "// registered as \"toFahrenheit\"\nfunction toFahrenheit(c) {\n  return c * 9 / 5 + 32;\n}\n\nSELECT c.city, udf.toFahrenheit(c.tempCelsius) AS tempF\nFROM c",
    "caption": "Called with a udf. prefix, computed per document."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 37",
    "titlePlain": "Change Feed ",
    "titleEmphasis": "in Cosmos DB",
    "titleSuffix": "",
    "subtitle": "An ordered, persistent log of every change."
  }
]
