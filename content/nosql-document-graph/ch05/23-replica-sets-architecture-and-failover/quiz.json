{
  "lesson": "replica-sets-architecture-and-failover",
  "questions": [
    {
      "q": "In a MongoDB replica set, which member accepts writes?",
      "options": [
        "Only the primary",
        "Any secondary, at any time",
        "Only the arbiter",
        "All members accept writes simultaneously"
      ],
      "answer": 0,
      "explain": "Exactly one member, the primary, accepts writes at any given moment; secondaries replicate from it."
    },
    {
      "q": "What is the oplog, and why is it described as a genuine analog to a SQL Server transaction log?",
      "options": [
        "A capped collection recording every write on the primary in order, which secondaries tail and replay — both are ordered, replayable records of change, not just similarly named",
        "A log of failed queries only, unrelated to replication",
        "A configuration file listing replica set members",
        "A backup file created once per day"
      ],
      "answer": 0,
      "explain": "The oplog is what replication actually is in MongoDB — secondaries replay it to stay current, just as a transaction log drives log shipping."
    },
    {
      "q": "What happens to a secondary that falls behind and drops outside the oplog's capped window?",
      "options": [
        "It requires a full resync",
        "It automatically becomes the new primary",
        "Nothing — it catches up instantly regardless of how far behind it is",
        "The primary shuts down"
      ],
      "answer": 0,
      "explain": "Once a secondary is too far behind for the capped oplog to cover the gap, replaying is no longer possible and a full resync is needed."
    },
    {
      "q": "What role does an arbiter play in a replica set?",
      "options": [
        "It votes in elections but holds no data itself",
        "It holds a full copy of the data and can become primary",
        "It performs backups automatically",
        "It replaces the need for an oplog"
      ],
      "answer": 0,
      "explain": "An arbiter exists purely to vote, useful for keeping the voting member count odd without the cost of an extra data-bearing node."
    },
    {
      "q": "What is the real tradeoff during a replica set election, per this lesson?",
      "options": [
        "There is briefly no primary, and writes that hadn't yet replicated to the newly elected primary can be rolled back",
        "There is no tradeoff — failover is instantaneous and lossless",
        "All secondaries are permanently promoted to primary",
        "The oplog is deleted during every election"
      ],
      "answer": 0,
      "explain": "The lesson names this honestly: during the election window the set can't accept writes, and unreplicated data can be lost via rollback."
    }
  ]
}
