{
  "lesson": "sharding-fundamentals-and-choosing-a-shard-key",
  "questions": [
    {
      "q": "What problem does sharding solve that a replica set alone does not?",
      "options": [
        "Write scaling and data sets larger than one server can hold",
        "Automatic failover",
        "Read-only reporting queries",
        "Encryption at rest"
      ],
      "answer": 0,
      "explain": "A replica set gives availability and read scaling, but every member still holds the full data set — sharding partitions the data horizontally to scale writes and total capacity."
    },
    {
      "q": "What does mongos do in a sharded cluster?",
      "options": [
        "Acts as the query router applications connect to, routing operations to the correct shard(s)",
        "Stores the actual document data",
        "Holds the cluster's metadata about key ranges",
        "Performs elections for replica set primaries"
      ],
      "answer": 0,
      "explain": "Applications never talk to shards directly — they connect to mongos, which consults config servers and routes each operation."
    },
    {
      "q": "What do config servers store?",
      "options": [
        "The cluster's metadata — which shard key ranges live on which shard",
        "A full copy of every document in the cluster",
        "Only backup files",
        "User authentication credentials exclusively"
      ],
      "answer": 0,
      "explain": "Config servers are a small replica set holding routing metadata that every mongos routing decision depends on."
    },
    {
      "q": "Why does a low-cardinality shard key (e.g. a status field with 3 values) cause problems?",
      "options": [
        "It can only ever be split into a few chunks, capping how far data can spread across shards",
        "It causes immediate data loss",
        "It prevents mongos from starting",
        "It has no effect on chunk distribution"
      ],
      "answer": 0,
      "explain": "With only a few distinct values, the data can't be divided into enough chunks to spread meaningfully across many shards."
    },
    {
      "q": "Why does a monotonically increasing shard key like an auto-incrementing _id create a hotspot, and what's the relational parallel?",
      "options": [
        "Every new document's key is higher than the last, so all new writes land on the same shard — the same failure mode as a monotonically increasing partition key creating a hot partition",
        "It causes the config servers to crash",
        "It has no relational parallel — this is a MongoDB-only problem",
        "It only affects read queries, not writes"
      ],
      "answer": 0,
      "explain": "New writes concentrate on whichever shard owns the highest key range, mirroring the classic hot-partition problem from bad relational partition key choices."
    }
  ]
}
