{
  "lesson": "neo4j-architecture-and-installation",
  "questions": [
    {
      "q": "What does it mean that Neo4j uses \"native graph storage\"?",
      "options": [
        "Nodes and relationships are physically persisted on disk as graph structures, not simulated on top of a relational or key-value engine",
        "Neo4j stores all data as JSON documents internally",
        "Neo4j only runs in memory and never persists to disk",
        "Native graph storage refers to using only the command line, never a GUI"
      ],
      "answer": 0,
      "explain": "The graph is the storage model itself, with relationships stored as direct references between node records — the foundation for index-free adjacency."
    },
    {
      "q": "What port does the Bolt protocol use by default, and what is it used for?",
      "options": [
        "Port 7687, used by drivers and Cypher clients to query the database",
        "Port 7474, used exclusively for backups",
        "Port 1433, the same port SQL Server uses",
        "Port 27017, the default MongoDB port"
      ],
      "answer": 0,
      "explain": "Bolt (7687) is Neo4j's efficient binary protocol for drivers and Cypher clients; HTTP (7474) serves Neo4j Browser."
    },
    {
      "q": "Which installation option is described as the natural choice for local learning and development, used for this chapter's lab work?",
      "options": [
        "Neo4j Desktop",
        "Neo4j Aura only",
        "Compiling Neo4j from source",
        "SQL Server Management Studio"
      ],
      "answer": 0,
      "explain": "Neo4j Desktop manages local database instances and opens Neo4j Browser against them, making it the practical choice for local development."
    },
    {
      "q": "What is Neo4j Aura, per this lesson?",
      "options": [
        "Neo4j's fully managed, cloud-hosted database service, with a free tier for learning",
        "A command-line only version of Neo4j with no GUI",
        "A deprecated legacy version of Neo4j",
        "A third-party open-source fork of Neo4j"
      ],
      "answer": 0,
      "explain": "Aura is described as conceptually the Neo4j equivalent of choosing Cosmos DB over self-hosting SQL Server on a VM — a managed cloud option."
    },
    {
      "q": "When running the official Docker neo4j image, what must be mapped for the database to be reachable and to persist data across restarts?",
      "options": [
        "Both ports 7474 and 7687, plus a mounted volume for /data",
        "Only port 1433",
        "No configuration is needed at all",
        "Only a volume for /logs, ports are irrelevant"
      ],
      "answer": 0,
      "explain": "Both the HTTP and Bolt ports need to be published, and a /data volume mount is required for the database to survive a container restart."
    }
  ]
}
