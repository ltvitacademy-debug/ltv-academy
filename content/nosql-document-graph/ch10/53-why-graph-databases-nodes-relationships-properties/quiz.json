{
  "lesson": "why-graph-databases-nodes-relationships-properties",
  "questions": [
    {
      "q": "What does a Neo4j label represent, and what is it roughly analogous to in the relational world?",
      "options": [
        "A category attached to a node, roughly analogous to a table name",
        "A unique identifier for each node, like a primary key value",
        "A type of relationship between two nodes",
        "A stored procedure attached to a node"
      ],
      "answer": 0,
      "explain": "Labels categorize nodes (e.g. Customer, Employee), and a single node can carry more than one label at once."
    },
    {
      "q": "In Neo4j, what are true statements about every relationship between two nodes?",
      "options": [
        "It is always directed (has a start and end node) and always typed (has exactly one relationship type)",
        "Relationships have no direction and no type",
        "A relationship can have multiple types simultaneously",
        "Relationships cannot carry any properties"
      ],
      "answer": 0,
      "explain": "Every Neo4j relationship is directed and has exactly one type, written in ALL_CAPS by convention, such as PURCHASED or REPORTS_TO."
    },
    {
      "q": "What genuinely surprises relational-trained DBAs about properties in Neo4j, per this lesson?",
      "options": [
        "Properties can live on both nodes and relationships, not just on entities",
        "Properties can only exist on nodes, never on relationships",
        "Properties must always be integers",
        "Only one property is allowed per node"
      ],
      "answer": 0,
      "explain": "A relationship like PURCHASED can itself carry properties such as date and quantity, data that relationally would require a separate junction table."
    },
    {
      "q": "Why doesn't a graph traversal get proportionally slower as the total database grows, according to this lesson's explanation of index-free adjacency?",
      "options": [
        "Relationships are stored as direct physical pointers between nodes, so traversal cost depends on the pattern matched, not total database size",
        "Neo4j caches the entire database in memory at all times",
        "Graph databases don't actually store relationships at all",
        "Traversal speed is unrelated to how relationships are stored"
      ],
      "answer": 0,
      "explain": "Because relationships are stored objects with direct pointers rather than resolved via a JOIN, traversing to a neighbor is a constant-time lookup regardless of overall graph size."
    },
    {
      "q": "Per this lesson's honest framing, which of the following is still typically better handled by a relational or document database than a graph database?",
      "options": [
        "Simple lookups by a single known key and heavy aggregate reporting across large flat datasets",
        "Multi-hop fraud detection through chains of accounts",
        "Org-chart traversal of direct and indirect reports",
        "Product recommendations based on what similar customers bought"
      ],
      "answer": 0,
      "explain": "The lesson is explicit that graphs aren't the better tool for everything — simple key lookups and flat aggregate reporting are still typically simpler or better relationally."
    }
  ]
}
