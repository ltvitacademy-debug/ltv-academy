[
  {
    "type": "title",
    "eyebrow": "Data Visualization & EDA · Chapter Six",
    "titlePlain": "Capstone: Build ",
    "titleEmphasis": "It",
    "titleSuffix": "",
    "subtitle": "Lesson 24 · Capstone"
  },
  {
    "type": "code",
    "eyebrow": "Step 1: quality (12 missing values)",
    "code": "m = df.monthly_spend.isna()\nprint(m.sum(), round(df[m].churned.mean(), 3),\n      round(df[~m].churned.mean(), 3))\n# 12 0.417 0.193\nmed = (df.groupby(\"plan\").monthly_spend\n         .transform(\"median\"))\ndf[\"monthly_spend\"] = (\n    df.monthly_spend.fillna(med))",
    "caption": "Only 12 rows, so 41.7% proves nothing. Fill and move on."
  },
  {
    "type": "code",
    "eyebrow": "Step 2: churn versus each variable",
    "code": "tix = df.support_tickets.clip(upper=3)\ndf[\"tenure_band\"] = pd.cut(df.tenure_months,\n    [0, 12, 24, 36, 60],\n    labels=[\"0-12\", \"13-24\", \"25-36\", \"37-60\"])\nfor c in [tix, \"tenure_band\", \"plan\", \"region\"]:\n    print(df.groupby(c).churned.mean().round(3))",
    "caption": "Baseline churn is 19.6%."
  },
  {
    "type": "steps",
    "eyebrow": "What the rates say",
    "titlePlain": "Two strong signals, two ",
    "titleEmphasis": "weak",
    "titleSuffix": "",
    "steps": [
      {"label": "Tickets", "sub": "11.3% (0) up to 44.7% (3+)"},
      {"label": "Tenure", "sub": "30.9% at 0-12, 10.6% at 37-60"},
      {"label": "Plan", "sub": "Basic 22.0% vs Premium 13.6%"},
      {"label": "Region", "sub": "East 26.4% vs South 15.4%"}
    ]
  },
  {
    "type": "code",
    "eyebrow": "Step 3: is the region gap real?",
    "code": "from scipy.stats import chi2_contingency\n\nct = pd.crosstab(df.region, df.churned)\np = chi2_contingency(ct)[1]\nprint(round(p, 3))\n# 0.037",
    "caption": "Barely under 0.05, after scanning 4 variables. Weak."
  },
  {
    "type": "code",
    "eyebrow": "Step 4 and 5: the figure",
    "code": "fig, axes = plt.subplots(1, 2, figsize=(9, 3.5))\nby_tix = df.groupby(tix).churned.mean()*100\nby_ten = df.groupby(\"tenure_band\").churned.mean()*100\nby_tix.plot(kind=\"bar\", ax=axes[0], rot=0)\nby_ten.plot(kind=\"bar\", ax=axes[1], rot=0)\naxes[0].set_title(\"Churn % by support tickets\")\naxes[1].set_title(\"Churn % by tenure (months)\")\nplt.tight_layout()\nplt.savefig(\"findings.png\")",
    "caption": "In the left panel, 3 means 3 or more tickets."
  },
  {
    "type": "screenshot",
    "file": "findings.png",
    "eyebrow": "Output of the code above",
    "caption": "Illustrative data. Tickets climb; tenure falls."
  },
  {
    "type": "outro",
    "eyebrow": "Up next · Lesson 25",
    "titlePlain": "Capstone: Wrap-Up & Portfolio ",
    "titleEmphasis": "Presentation",
    "titleSuffix": "",
    "subtitle": "Present the findings and package the project."
  }
]
