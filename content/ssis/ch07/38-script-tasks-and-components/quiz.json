{
  "lesson": "script-tasks-and-components",
  "questions": [
    {
      "q": "When should you reach for a Script Task or Script Component instead of a built-in task or transformation?",
      "options": [
        "When the job isn't covered by anything in the Toolbox — like checking whether a file exists, parsing an unusual format, or calling an API with no SSIS connector",
        "For every single task in a package, as a general replacement for built-in components",
        "Only when the package needs to run faster",
        "Never — Script Tasks were removed from modern versions of SSIS"
      ],
      "answer": 0,
      "explain": "Script Tasks and Script Components exist for the jobs that don't map to a common, well-defined built-in task or transformation."
    },
    {
      "q": "What is the key difference between a Script Task and a Script Component?",
      "options": [
        "A Script Task lives on the Control Flow and runs once per execution; a Script Component lives on the Data Flow and runs once per row",
        "A Script Task only supports VB, while a Script Component only supports C#",
        "A Script Component can only be used as a destination, never a source or transform",
        "There is no difference — they are two names for the same object"
      ],
      "answer": 0,
      "explain": "Script Task is a control-flow task (runs once per execution/iteration); Script Component is a data-flow component that runs once per row and can act as a source, transformation, or destination."
    },
    {
      "q": "What development environment do both Script Tasks and Script Components use to write and edit code?",
      "options": [
        "VSTA (Visual Studio Tools for Applications)",
        "Visual Studio Code",
        "SQL Server Management Studio's Query Editor",
        "Notepad, launched directly from the Toolbox"
      ],
      "answer": 0,
      "explain": "Both open into VSTA, the Visual Studio-based code editor built into SSIS Designer for writing Script Task/Component code."
    },
    {
      "q": "In the example Script Task, what does setting `Dts.TaskResult = (int)ScriptResults.Failure;` actually do?",
      "options": [
        "Reports failure back to the control flow, so the package can react (for example, stop or take a different path) based on the script's outcome",
        "Deletes the file referenced by the FilePath variable",
        "Immediately closes SSIS Designer",
        "Has no effect unless the package is running in debug mode"
      ],
      "answer": 0,
      "explain": "TaskResult is how a Script Task reports Success or Failure back to the control flow that contains it, just like any other task."
    },
    {
      "q": "Before a Script Task's code can read a package variable through `Dts.Variables`, what must you configure first?",
      "options": [
        "List the variable in the Script Task Editor's ReadOnlyVariables (or ReadWriteVariables) property",
        "Nothing — every package variable is automatically available to every script with no configuration",
        "Convert the project to the project deployment model",
        "Rename the variable to start with 'Dts.'"
      ],
      "answer": 0,
      "explain": "A variable has to be explicitly listed in ReadOnlyVariables or ReadWriteVariables on the Script page before the script can access it through Dts.Variables."
    }
  ]
}
