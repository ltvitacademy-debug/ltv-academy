{
  "lesson": "ole-db-source-and-destination",
  "questions": [
    {
      "q": "Which OLE DB Source data access mode lets you write a parameterized SQL query directly?",
      "options": [
        "SQL command",
        "Table or view",
        "Table name or view name variable",
        "Fast load"
      ],
      "answer": 0,
      "explain": "SQL command lets you type or build a SQL query, which can include ? parameter placeholders mapped to variables. Fast load is a destination-only concept."
    },
    {
      "q": "What does the OLE DB Destination's \"fast load\" access mode actually do?",
      "options": [
        "Uses SQL Server's bulk-insert mechanism instead of row-by-row INSERT statements",
        "Skips the Mappings page entirely",
        "Automatically creates indexes on the destination table",
        "Disables the error output for that destination"
      ],
      "answer": 0,
      "explain": "Fast load access modes use bulk insert, which is dramatically faster for large loads than issuing individual INSERT statements per row."
    },
    {
      "q": "What happens if a non-nullable destination column is left unmapped on the Mappings page?",
      "options": [
        "The load fails at run time, because that column requires a value",
        "SSIS silently inserts a zero or empty string",
        "The destination column is automatically dropped",
        "Nothing — mapping is purely cosmetic"
      ],
      "answer": 0,
      "explain": "You don't have to map every destination column, but if an unmapped column doesn't allow nulls, the load fails at run time."
    },
    {
      "q": "Why does the OLE DB Destination not support the \"SQL command from variable\" access mode that the OLE DB Source has?",
      "options": [
        "The destination's access modes are built around loading data efficiently (table/fast-load/SQL command), not the source's broader set of selection options",
        "It does support it, under a different name",
        "SSIS never allows variables on the destination side at all",
        "Because destinations never use connection managers"
      ],
      "answer": 0,
      "explain": "The OLE DB Destination offers five modes total (Table or view, Table or view - fast load, Table name/view name variable, that variable's fast-load counterpart, and SQL command) — there's no SQL-command-from-variable mode, and no parameter support at all."
    },
    {
      "q": "What can you do on the Connection Manager page of the OLE DB Source Editor before finishing configuration?",
      "options": [
        "Click Preview to see up to 200 rows of the extracted data",
        "Directly edit the destination table's schema",
        "Attach a Data Viewer to the source's output",
        "Run the entire package"
      ],
      "answer": 0,
      "explain": "Preview is available right on the Connection Manager page and shows up to 200 rows — always worth checking before moving on."
    }
  ]
}
