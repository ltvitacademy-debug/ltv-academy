{
  "lesson": "conditional-split",
  "questions": [
    {
      "q": "What programming structure is the Conditional Split transformation's behavior most similar to?",
      "options": [
        "A CASE decision structure",
        "A FOR loop",
        "A try/catch error handler",
        "A recursive function call"
      ],
      "answer": 0,
      "explain": "Microsoft's own documentation describes the Conditional Split transformation as similar to a CASE decision structure — it evaluates expressions in order and routes each row to the first matching output."
    },
    {
      "q": "In a Conditional Split with multiple conditions, which output does a row go to?",
      "options": [
        "The output for the first condition, in listed order, that evaluates to true",
        "Every output whose condition evaluates to true",
        "Whichever output happens to run fastest",
        "The default output, always, regardless of the conditions"
      ],
      "answer": 0,
      "explain": "Order is significant: the transformation checks conditions top to bottom and sends the row to the output for the first one that evaluates true, ignoring the rest."
    },
    {
      "q": "What happens to a row that doesn't match any of the defined conditions in a Conditional Split?",
      "options": [
        "It's routed to the required default output",
        "It's dropped from the data flow silently",
        "It causes the package to fail immediately",
        "It gets sent to every output at once"
      ],
      "answer": 0,
      "explain": "A Conditional Split transformation requires a default output, so any row that matches no condition still has somewhere defined to go."
    },
    {
      "q": "If you need to evaluate several conditions independently — not just the first match — for the same row, what should you do?",
      "options": [
        "Chain multiple Conditional Split transformations in sequence",
        "Increase the number of default outputs",
        "Reverse the order of the existing conditions",
        "Use a Lookup transformation instead"
      ],
      "answer": 0,
      "explain": "A single Conditional Split only ever sends a row to one output — the first matching case. Evaluating several conditions independently requires concatenating multiple Conditional Split transformations."
    },
    {
      "q": "What language are Conditional Split conditions written in?",
      "options": [
        "Integration Services (SSIS) Expressions",
        "T-SQL",
        "C# script blocks",
        "Regular expressions only"
      ],
      "answer": 0,
      "explain": "Conditions are boolean expressions written in the SSIS expression language, built from columns, variables, functions, and operators — the same language formalized in the Expressions lesson."
    }
  ]
}
