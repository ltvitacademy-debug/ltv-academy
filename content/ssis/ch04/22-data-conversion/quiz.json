{
  "lesson": "data-conversion",
  "questions": [
    {
      "q": "What does the Data Conversion transformation do?",
      "options": [
        "Converts an input column's data type and copies the result into a new output column",
        "Sorts input rows and removes duplicates",
        "Joins a data flow against a reference dataset",
        "Splits rows across multiple outputs based on an expression"
      ],
      "answer": 0,
      "explain": "Data Conversion changes the data type of an input column — for example, string to integer — and writes the converted value to a new output column, leaving the original column untouched."
    },
    {
      "q": "Which four attributes can you configure for each column in the Data Conversion Transformation Editor?",
      "options": [
        "Data Type, Length, Precision, and Scale",
        "Match output, No Match output, Error output, and Default output",
        "Sort Type, Sort Order, Comparison Flags, and Passthrough",
        "Join type, Swap Inputs, Input, and Output Alias only"
      ],
      "answer": 0,
      "explain": "Data Conversion lets you set the target Data Type, the column Length for strings, and Precision and Scale for numeric data, plus an Output Alias name."
    },
    {
      "q": "What must be true when copying data between two columns that both have a string data type?",
      "options": [
        "Both columns must use the same code page",
        "Both columns must have identical lengths",
        "The source column must be uppercase",
        "The transformation must run in full cache mode"
      ],
      "answer": 0,
      "explain": "When copying between string-typed columns, the two columns must use the same code page — the Data Conversion editor lets you set this explicitly for DT_STR output columns."
    },
    {
      "q": "What happens if a Data Conversion output column's string length is shorter than its input column's length?",
      "options": [
        "The output data is truncated, which SSIS treats as a row-level error you can handle via Configure Error Output",
        "The package automatically widens the output column to match",
        "SSIS pads the shorter output with null characters silently",
        "The row is automatically routed to the Match output"
      ],
      "answer": 0,
      "explain": "If the output column length is shorter than the input, the data is truncated — this is a row-level error condition, configurable through the transformation's error output."
    },
    {
      "q": "How does Data Conversion differ from Derived Column?",
      "options": [
        "Data Conversion exists to fix a type mismatch; Derived Column reshapes values with a general expression",
        "They are functionally identical and interchangeable",
        "Data Conversion can only run on numeric columns, while Derived Column only runs on strings",
        "Derived Column requires a reference dataset, but Data Conversion does not"
      ],
      "answer": 0,
      "explain": "Data Conversion is specifically for changing a column's data type (e.g., text to integer). Derived Column is a more general tool for computing new values with expressions — it can also change type, but that's not its primary purpose."
    }
  ]
}
