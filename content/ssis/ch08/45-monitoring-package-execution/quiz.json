{
  "lesson": "monitoring-package-execution",
  "questions": [
    {
      "q": "Where does every deployment, validation, and package execution against the SSIS Catalog get logged?",
      "options": [
        "The SSISDB database",
        "A separate text log file per package, stored on the file system",
        "The Windows Event Log exclusively",
        "It isn't logged anywhere unless a custom log provider is added"
      ],
      "answer": 0,
      "explain": "SSISDB is the single source of truth — every catalog operation's history writes there, which is exactly what makes the Active Operations dialog and standard reports possible."
    },
    {
      "q": "What does the Active Operations dialog show?",
      "options": [
        "Every currently-running operation on the server, with the ability to stop one",
        "Only operations that failed in the last 24 hours",
        "A historical archive of every execution ever run",
        "The list of deployed projects only, not executions"
      ],
      "answer": 0,
      "explain": "Active Operations is the real-time view — deployments, validations, and executions in progress right now — distinct from the historical standard reports."
    },
    {
      "q": "Which standard report lets you view executions across a custom date range, including the exact parameter values used per run?",
      "options": [
        "All Executions",
        "Integration Services Dashboard",
        "Active Operations",
        "All Connections"
      ],
      "answer": 0,
      "explain": "Unlike the Dashboard, which is locked to the last 24 hours, All Executions covers any date range you configure and shows the parameter values used for each specific run."
    },
    {
      "q": "What must be true for the Execution Performance report's per-component Active Time/Total Time detail to actually appear?",
      "options": [
        "The logging level for that run must have been set to Performance or Verbose",
        "The package must have run at least 10 times total",
        "The report only works for packages deployed within the last 24 hours",
        "The job step's Run as account must be a Windows administrator"
      ],
      "answer": 0,
      "explain": "That per-component detail is only captured when logging level was Performance or Verbose — at the default Basic level, it's simply never recorded, and no report can show data that was never logged."
    },
    {
      "q": "Which report shows connections that have failed across executions, along with the connection string and failure count?",
      "options": [
        "All Connections",
        "All Validations",
        "Integration Services Dashboard only",
        "Active Operations"
      ],
      "answer": 0,
      "explain": "All Connections is specifically scoped to connection failures across executions — the connection string, how many times it failed, and when it last failed."
    }
  ]
}
