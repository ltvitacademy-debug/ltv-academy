{
  "lesson": "execute-sql-task",
  "questions": [
    {
      "q": "What does the Execute SQL Task's SQLSourceType property control?",
      "options": [
        "Where the SQL statement the task runs comes from — direct input, a file connection, or a variable",
        "Which SQL Server instance the task connects to",
        "Whether the task runs before or after the Data Flow Task",
        "The number of rows the task is allowed to return"
      ],
      "answer": 0,
      "explain": "SQLSourceType has three options — Direct input, File connection, and Variable — each pointing the task at a different source for its SQL statement."
    },
    {
      "q": "An OLE DB connection's Execute SQL Task uses which parameter marker in a parameterized statement?",
      "options": [
        "A question mark (?), with parameters named by ordinal position (0, 1, 2…)",
        "A named marker like @ProductID",
        "A pound sign (#) followed by a number",
        "OLE DB connections don't support parameters at all"
      ],
      "answer": 0,
      "explain": "OLE DB, EXCEL, and ODBC connections all use ? as the parameter marker, with parameters mapped by their ordinal position — 0, 1, 2 and so on (ODBC starts at 1)."
    },
    {
      "q": "Which ResultSet type would you choose for a SELECT statement that returns many rows, bound to an Object variable?",
      "options": [
        "Full result set",
        "Single row",
        "None",
        "XML"
      ],
      "answer": 0,
      "explain": "Full result set is used when a query returns multiple rows; it must be mapped to a variable with the Object data type."
    },
    {
      "q": "What kinds of parameters does the Execute SQL Task support?",
      "options": [
        "Input, Output, and ReturnValue",
        "Only Input parameters",
        "Only stored procedure parameters, not raw SQL statement parameters",
        "GET and POST parameters"
      ],
      "answer": 0,
      "explain": "The task supports Input parameters (for filtering/values going in), Output parameters (values coming back), and ReturnValue parameters (a stored procedure's integer return code)."
    },
    {
      "q": "The Execute SQL Task is frequently combined with which other control flow elements to run the same statement repeatedly?",
      "options": [
        "The For Loop and Foreach Loop containers",
        "The Sequence Container only",
        "Event handlers only",
        "It can never be reused inside a loop"
      ],
      "answer": 0,
      "explain": "Because For Loop and Foreach Loop containers implement repeating control flows, they can run an Execute SQL Task repeatedly — for example, once per file enumerated by a Foreach Loop."
    }
  ]
}
