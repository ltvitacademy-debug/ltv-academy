[
  {
    "n": 1,
    "text": "The Execute SQL Task is one of the tasks you'll reach for in nearly every package you build. Let's look at what it actually runs, and how to configure it correctly."
  },
  {
    "n": 2,
    "text": "At its core, the Execute SQL Task runs SQL statements or stored procedures straight from your control flow — no Data Flow Task required. You'll use it to truncate a table before a load, create or drop objects, re-create fact and dimension tables, run a stored procedure, or save a query's result into a variable for later tasks to use. Configuring it comes down to four things. First, Connection — which connection manager type it uses: OLE DB, ODBC, ADO, ADO.NET, or SQLMOBILE. Second, SQLSourceType — where the statement itself comes from: typed directly in, read from a file, or pulled from a variable. Third, Parameter Mapping — binding variables to the statement's Input, Output, and ReturnValue parameters. And fourth, ResultSet — what kind of result comes back: None, Single row, Full result set, or XML."
  },
  {
    "n": 3,
    "text": "Here's where most Execute SQL Task mistakes actually happen — parameter markers. OLE DB, Excel, and ODBC connections all use a question mark as the marker, with parameters mapped by their ordinal position: zero, one, two, and so on — ODBC starts at one instead of zero. ADO.NET connections are different — they use named markers like \"at-parameter-name\" directly in the statement. Get the wrong marker style for your connection type, and the task fails before it even runs your SQL."
  },
  {
    "n": 4,
    "text": "Combine the Execute SQL Task with the looping containers from Lesson 10, and you can run the same parameterized statement once per file, or once per row — a pattern you'll use constantly. Next lesson, we look at the Execute Package Task — how one package calls another as part of its own workflow."
  }
]
