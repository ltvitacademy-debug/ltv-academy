{
  "lesson": "execute-package-task",
  "questions": [
    {
      "q": "What does the Execute Package Task let a package do?",
      "options": [
        "Run another SSIS package (a child package) as part of its own workflow",
        "Run a SQL Server Agent job",
        "Restart the SSIS service",
        "Convert a package to the project deployment model"
      ],
      "answer": 0,
      "explain": "The Execute Package Task extends a package by letting it run another package — the child package — as a step in its own control flow."
    },
    {
      "q": "What does setting ReferenceType to Project Reference require?",
      "options": [
        "The child package must be in the same project as the parent, using the project deployment model",
        "The child package must be stored in the msdb database",
        "The parent and child packages must share the same connection managers",
        "Nothing extra — Project Reference works with any package location"
      ],
      "answer": 0,
      "explain": "Project Reference only works when the child package lives in the same project as the parent and that project uses the project deployment model; otherwise ReferenceType is locked to External Reference."
    },
    {
      "q": "What happens when ExecuteOutOfProcess is set to True?",
      "options": [
        "The child package runs in its own process, adding memory overhead but isolating failures from the parent",
        "The child package runs faster because it skips validation",
        "The parent package can no longer pass parameters to the child",
        "The child package is prevented from writing to the log"
      ],
      "answer": 0,
      "explain": "True runs the child package out-of-process — more memory and a slower launch, but a crash in the child doesn't necessarily crash the parent's process."
    },
    {
      "q": "How does logging behave between a parent and child package?",
      "options": [
        "The child package always forwards its log details to the parent, regardless of the child's own logging configuration",
        "Logging never crosses between parent and child packages",
        "The parent's logging configuration overwrites the child's log entirely",
        "Only error-level log entries are forwarded"
      ],
      "answer": 0,
      "explain": "Child packages always forward their log details up to the parent; if the Execute Package Task itself is configured to use logging, it logs those forwarded details too."
    },
    {
      "q": "Which is NOT a reason given for splitting a package into parent and child packages?",
      "options": [
        "It makes the package run on a faster CPU automatically",
        "It breaks down complex workflow into readable, testable pieces",
        "It lets you reuse a shared package from multiple parents",
        "It lets you restrict package authors' access for security"
      ],
      "answer": 0,
      "explain": "Splitting packages doesn't change CPU allocation — the real reasons are simplifying workflow, reusing packages, grouping transactional work units, and controlling security access."
    }
  ]
}
