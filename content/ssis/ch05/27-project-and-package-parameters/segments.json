[
  {
    "n": 1,
    "text": "Last lesson, variables gave a package internal state. Now, parameters — how you feed a value into a package from the outside, at the moment it runs, without ever opening the package to edit it."
  },
  {
    "n": 2,
    "text": "Every package has its own Parameters tab in SSIS Designer, right next to Control Flow and Data Flow. A parameter you add here is a package parameter — scoped to just this one package. Click Add Parameter, set a Name, Data Type, and Value, and two properties matter beyond that: Sensitive, which encrypts the value once deployed so it reads back as NULL from T-SQL, good for passwords and API keys; and Required, which forces a real value to exist before the package is even allowed to run."
  },
  {
    "n": 3,
    "text": "If a value needs to be shared across every package in the project — a source folder path, an environment name — that's a project parameter instead, and it lives in exactly one place: Project.params, right here in Solution Explorer. Double-click it, add parameters the same way you would on a package's Parameters tab, and every package in the project can now read that same value."
  },
  {
    "n": 4,
    "text": "Once a project is deployed, a parameter's actual value can come from three layers, and only one wins per run. The design value is the default you set in SSDT — it just persists with the project. The server value is set after deployment and overrides that design default for every execution. And the execution value is set for one specific run only — through the Execute Package dialog or dtexec — and it overrides both of the others, but only for that one run."
  },
  {
    "n": 5,
    "text": "Parameters and variables both need a common language to actually get manipulated and combined — string concatenation, date math, casting one type to another. That language is expressions, and that's next."
  }
]
